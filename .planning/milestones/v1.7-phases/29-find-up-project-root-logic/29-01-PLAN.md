---
phase: 29-find-up-project-root-logic
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - plugins/claude-notify/tests/test_flags.py
autonomous: true
requirements: [PROJ-07]

must_haves:
  truths:
    - "Test class TestFindProjectRoot exists with 9 test methods covering all traversal scenarios"
    - "Test class TestGetProjectName exists with 4 test methods covering name extraction and fallback"
    - "All new tests FAIL (RED) because find_project_root and get_project_name do not exist yet"
  artifacts:
    - path: "plugins/claude-notify/tests/test_flags.py"
      provides: "TDD test cases for find_project_root() and get_project_name()"
      contains: "class TestFindProjectRoot"
  key_links:
    - from: "plugins/claude-notify/tests/test_flags.py"
      to: "flags.py"
      via: "from flags import find_project_root, get_project_name"
      pattern: "from flags import"
---

<objective>
Write TDD test cases for find_project_root() and get_project_name().

Purpose: RED phase -- all tests must FAIL because the functions do not exist yet. This defines the behavioral contract that the implementation must satisfy.

Output: test_flags.py with TestFindProjectRoot (9 tests) and TestGetProjectName (4 tests) classes appended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-find-up-project-root-logic/29-RESEARCH.md

<interfaces>
<!-- Existing flags.py exports that tests must import alongside new functions -->
From plugins/claude-notify/hooks/scripts/flags.py:
```python
from pathlib import Path
from typing import Dict, Optional

def check_notification_flags() -> Dict:
    # Already exported and tested
```

<!-- New functions to be imported (do not exist yet - will cause ImportError in RED phase) -->
```python
def find_project_root() -> Optional[Path]:
    """Returns Path to project root dir (has .git dir or CLAUDE.md file), or None."""

def get_project_name() -> str:
    """Returns project root dir name, or cwd basename as fallback."""
```

<!-- Existing mock pattern from test_flags.py (lines 48-68) -->
```python
@patch('flags.Path')
def test_example(self, mock_path_class):
    mock_cwd = MagicMock()
    mock_parent = MagicMock()
    mock_cwd.parent = mock_parent
    mock_parent.parent = mock_parent  # root stops traversal

    def cwd_div(self, key):
        m = MagicMock()
        m.is_file.return_value = False
        m.is_dir.return_value = False  # IMPORTANT: set BOTH to False by default
        return m

    mock_cwd.__truediv__ = cwd_div
    mock_parent.__truediv__ = parent_div
    mock_path_class.cwd.return_value = mock_cwd
```

<!-- IMPORTANT: Pitfall from research - must set BOTH is_dir and is_file on every mock path -->
<!-- Default MagicMock returns truthy MagicMock for unconfigured methods -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write TestFindProjectRoot and TestGetProjectName test classes (RED phase)</name>
  <files>plugins/claude-notify/tests/test_flags.py</files>
  <read_first>
    - plugins/claude-notify/tests/test_flags.py (existing test file to append to)
    - plugins/claude-notify/hooks/scripts/flags.py (understand the traversal pattern being tested)
    - .planning/phases/29-find-up-project-root-logic/29-RESEARCH.md (test scenarios and mock patterns)
  </read_first>
  <behavior>
    TestFindProjectRoot (9 tests):
    - test_git_in_cwd: CWD has .git directory -> returns CWD Path object
    - test_claude_md_in_cwd: CWD has CLAUDE.md file -> returns CWD Path object
    - test_git_in_parent: Parent has .git directory -> returns parent Path object
    - test_claude_md_in_parent: Parent has CLAUDE.md file -> returns parent Path object
    - test_git_priority_over_claude_md: CWD has both .git (is_dir=True) and CLAUDE.md (is_file=True) -> returns CWD (first check wins)
    - test_nested_project_returns_closest: .git at depth 2, another marker at depth 5 -> returns depth 2 directory
    - test_no_markers_returns_none: No .git or CLAUDE.md anywhere -> returns None
    - test_max_depth_limit: Marker only at depth 11 -> returns None (max_depth=10 stops traversal)
    - test_filesystem_root_stops: CWD is filesystem root (parent == self), no markers -> returns None

    TestGetProjectName (4 tests):
    - test_returns_dir_name: find_project_root returns mock with .name="my-project" -> returns "my-project"
    - test_fallback_to_cwd_basename: find_project_root returns None, cwd has .name="fallback-dir" -> returns "fallback-dir"
    - test_name_with_spaces: Project root .name="my project" -> returns "my project"
    - test_name_with_chinese: Project root .name="my-project" -> returns Chinese chars correctly
  </behavior>
  <action>
    Append two new test classes to the END of test_flags.py (before the `if __name__` block):

    1. Add imports at the top of the file: add `find_project_root` and `get_project_name` to the existing `from flags import check_notification_flags` line. The new import line should be:
       `from flags import check_notification_flags, find_project_root, get_project_name`

       NOTE: This will cause ImportError in RED phase because the functions don't exist yet. That is EXPECTED and correct for TDD RED phase.

    2. Add TestFindProjectRoot class with 9 test methods. Each test uses `@patch('flags.Path')` following the EXACT mock pattern from existing tests. Critical mock setup rules:
       - Every mock path returned by `__truediv__` MUST set BOTH `is_file.return_value = False` AND `is_dir.return_value = False` by default
       - Only override the specific method needed (e.g., for .git check, set `is_dir.return_value = True`)
       - For CLAUDE.md check, set `is_file.return_value = True`
       - Use `mock_parent.parent = mock_parent` to simulate filesystem root
       - Use chained `.parent` attributes to build multi-level hierarchies

    3. For test_nested_project_returns_closest: build a 6-level chain (depth 0-5), place .git at level 2, another marker at level 5. Assert the function returns level 2.

    4. For test_max_depth_limit: build a 12-level chain, place .git only at level 11 (depth 11). Assert returns None.

    5. Add TestGetProjectName class with 4 test methods:
       - test_returns_dir_name: Mock `flags.find_project_root` (not `flags.Path`) to return a MagicMock with `.name = "my-project"`. Assert "my-project".
       - test_fallback_to_cwd_basename: Mock `flags.find_project_root` to return None. Mock `flags.Path.cwd()` to return MagicMock with `.name = "fallback-dir"`. Assert "fallback-dir".
       - test_name_with_spaces: Same as test_returns_dir_name but `.name = "my project"`.
       - test_name_with_chinese: Same but `.name = "my-project"` (use ASCII for the mock, the test validates the name property is used correctly -- the Chinese char handling is inherent to Python str).

    6. Keep the existing `if __name__ == '__main__':` block at the very end unchanged.
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && python -m pytest plugins/claude-notify/tests/test_flags.py::TestFindProjectRoot -v --tb=short 2>&1 | head -30; echo "---"; python -m pytest plugins/claude-notify/tests/test_flags.py::TestGetProjectName -v --tb=short 2>&1 | head -30</automated>
  </verify>
  <done>
    - test_flags.py contains class TestFindProjectRoot with 9 test methods
    - test_flags.py contains class TestGetProjectName with 4 test methods
    - Import line includes find_project_root and get_project_name
    - All 13 new tests FAIL (RED phase) because functions don't exist yet
    - Existing 16 TestCheckNotificationFlags tests still pass (unaffected)
  </done>
  <acceptance_criteria>
    - test_flags.py contains string "class TestFindProjectRoot"
    - test_flags.py contains string "class TestGetProjectName"
    - test_flags.py contains string "from flags import check_notification_flags, find_project_root, get_project_name"
    - test_flags.py contains "def test_git_in_cwd"
    - test_flags.py contains "def test_claude_md_in_cwd"
    - test_flags.py contains "def test_git_in_parent"
    - test_flags.py contains "def test_claude_md_in_parent"
    - test_flags.py contains "def test_git_priority_over_claude_md"
    - test_flags.py contains "def test_nested_project_returns_closest"
    - test_flags.py contains "def test_no_markers_returns_none"
    - test_flags.py contains "def test_max_depth_limit"
    - test_flags.py contains "def test_filesystem_root_stops"
    - test_flags.py contains "def test_returns_dir_name"
    - test_flags.py contains "def test_fallback_to_cwd_basename"
    - test_flags.py contains "def test_name_with_spaces"
    - test_flags.py contains "def test_name_with_chinese"
    - Running pytest on TestFindProjectRoot shows ImportError or test collection error (RED phase expected)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All 13 new test method names appear in test_flags.py
- Import line updated to include find_project_root and get_project_name
- Tests are in RED state (cannot import non-existent functions)
- Existing 16 tests in TestCheckNotificationFlags still pass independently
</verification>

<success_criteria>
- test_flags.py has TestFindProjectRoot (9 tests) and TestGetProjectName (4 tests)
- New tests reference find_project_root() and get_project_name() which don't exist yet (RED)
- Existing 16 tests unaffected and still pass
- Total test file has 29 tests (16 existing + 13 new)
</success_criteria>

<output>
After completion, create `.planning/phases/29-find-up-project-root-logic/29-01-SUMMARY.md`
</output>
