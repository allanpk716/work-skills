---
phase: 24-cli-entry-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - installer/src/cli.js
  - installer/src/index.js
  - installer/src/i18n/en.json
  - installer/src/i18n/zh.json
  - installer/tests/cli.test.js
  - installer/tests/index.test.js
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - UX-04

must_haves:
  truths:
    - "User can pass --uninstall flag and the installer routes to uninstall detection instead of install flow"
    - "--help output shows --uninstall option with description"
    - "--version output remains unchanged"
    - "--uninstall takes priority over --verify when both are specified"
    - "All uninstall.* i18n keys exist in both en.json and zh.json with matching key names"
  artifacts:
    - path: "installer/src/cli.js"
      provides: "parseArgs() returns uninstallOnly boolean"
      contains: "uninstallOnly"
    - path: "installer/src/index.js"
      provides: "main() routes to uninstall flow before verify flow"
      contains: "uninstallOnly"
    - path: "installer/src/i18n/en.json"
      provides: "English uninstall.* translation keys"
      contains: "uninstall."
    - path: "installer/src/i18n/zh.json"
      provides: "Chinese uninstall.* translation keys"
      contains: "uninstall."
  key_links:
    - from: "installer/src/cli.js"
      to: "installer/src/index.js"
      via: "options.uninstallOnly return value"
      pattern: "uninstallOnly"
    - from: "installer/src/index.js"
      to: "installer/src/uninstall/index.js"
      via: "require('./uninstall/index.js')"
      pattern: "runUninstallDetection"
---

<objective>
Add --uninstall CLI entry point and i18n translation keys for the uninstall detection feature.

Purpose: Users need a way to trigger the uninstall flow via `npx @allanpk716/work-skills-setup --uninstall`. This plan wires the CLI flag to a routing branch in main(), adds the i18n keys for bilingual output, and ensures --version is unaffected.

Output: Modified cli.js (new --uninstall option), modified index.js (uninstall routing), modified en.json/zh.json (uninstall.* keys), updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cli-entry-detection/24-CONTEXT.md
@.planning/phases/24-cli-entry-detection/24-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor MUST replicate -->

From installer/src/cli.js (current parseArgs return):
```javascript
return {
  lang: options.lang,
  useColors: options.color !== false,
  verifyOnly: options.verify === true
};
```

From installer/src/index.js (current --verify routing):
```javascript
if (options.verifyOnly) {
  const result = await runVerification();
  process.exit(result.success ? 0 : 1);
}
```

From installer/src/i18n/en.json (existing key pattern):
All keys use dot-notation: "section.subsection.description", e.g. "verification.title", "verification.summary"

From installer/src/i18n/zh.json (existing key pattern):
Must have identical key names as en.json, with Chinese values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --uninstall CLI option and routing</name>
  <files>installer/src/cli.js, installer/src/index.js, installer/tests/cli.test.js, installer/tests/index.test.js</files>
  <read_first>
    - installer/src/cli.js (current parseArgs to see exact structure)
    - installer/src/index.js (current main() flow and --verify routing)
    - installer/tests/cli.test.js (existing test patterns)
    - installer/tests/index.test.js (existing test patterns)
  </read_first>
  <behavior>
    - Test: parseArgs(['node', 'setup.js', '--uninstall']) returns { uninstallOnly: true }
    - Test: parseArgs(['node', 'setup.js']) returns { uninstallOnly: false }
    - Test: parseArgs(['node', 'setup.js', '--uninstall', '--verify']) returns { uninstallOnly: true, verifyOnly: true } (Commander allows both, main() handles priority)
    - Test: --help output contains "--uninstall" string
    - Test: --version output is unchanged (regression check)
    - Test: index.js main() with uninstallOnly=true calls runUninstallDetection and exits
  </behavior>
  <action>
    Per D-03: Follow the existing --verify pattern exactly.

    **cli.js changes:**
    1. Add `.option('--uninstall', 'Run uninstall detection')` after the existing `.option('--verify', ...)` line (line 23).
    2. Add `uninstallOnly: options.uninstall === true` to the return object (after verifyOnly on line 38).

    **index.js changes:**
    1. Add a require for the uninstall module at the top: `const { runUninstallDetection } = require('./uninstall/index.js');`
    2. Add the --uninstall routing block BEFORE the existing --verify block (lines 23-27). Per D-03, --uninstall takes priority:
       ```javascript
       // Handle --uninstall flag (uninstallOnly takes priority over verifyOnly)
       if (options.uninstallOnly) {
         const result = await runUninstallDetection();
         process.exit(0);
       }
       ```
    3. Do NOT modify any other part of the flow.

    **cli.test.js additions:**
    Add these test cases inside the existing 'CLI Module' describe block:
    - `'parseArgs handles --uninstall'`: parseArgs with ['node', 'setup.js', '--uninstall'] returns uninstallOnly: true
    - `'parseArgs defaults uninstallOnly to false'`: parseArgs with ['node', 'setup.js'] returns uninstallOnly: false
    - `'parseArgs handles --uninstall with --verify'`: both flags set, returns uninstallOnly: true AND verifyOnly: true

    **index.test.js additions:**
    Add a test case inside the existing 'Main Entry Point' describe block:
    - `'main() routes to uninstall when --uninstall flag is set'`: Mock process.argv to include --uninstall, mock process.exit, verify that runUninstallDetection is called. Use jest.mock to mock the uninstall module.

    Note: The uninstall/index.js module does not exist yet (created in Plan 02). For the index.test.js test, mock the require: `jest.mock('../src/uninstall/index.js', () => ({ runUninstallDetection: jest.fn().mockResolvedValue({ hasAnyInstalled: false }) }))`.
  </action>
  <verify>
    <automated>cd installer && npx jest tests/cli.test.js tests/index.test.js -x</automated>
  </verify>
  <done>
    - cli.js parseArgs() returns uninstallOnly boolean
    - index.js main() routes to runUninstallDetection() before verifyOnly check
    - All existing tests still pass
    - New tests for --uninstall flag pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add uninstall.* i18n translation keys</name>
  <files>installer/src/i18n/en.json, installer/src/i18n/zh.json</files>
  <read_first>
    - installer/src/i18n/en.json (current key structure and ordering)
    - installer/src/i18n/zh.json (current key structure and ordering)
  </read_first>
  <action>
    Add the following keys to BOTH en.json and zh.json. Place them at the end of the file, before the closing brace. Ensure no trailing comma issues.

    **en.json additions:**
    ```json
    "uninstall.title": "Uninstall Detection",
    "uninstall.cliHelp": "Run uninstall detection and show what will be removed",
    "uninstall.category.plugins": "Plugins",
    "uninstall.category.hooks": "Hook Scripts",
    "uninstall.category.hookRegistration": "Hook Registration",
    "uninstall.category.commands": "Slash Commands",
    "uninstall.category.marketplace": "Marketplace Source",
    "uninstall.category.environment": "Environment Variables",
    "uninstall.status.installed": "Installed",
    "uninstall.status.notInstalled": "Not installed",
    "uninstall.summary": "Detection complete: {found}/{total} items installed",
    "uninstall.nothingFound": "Nothing installed. No uninstall needed.",
    "uninstall.item.pluginDir": "Plugin directory: {name}",
    "uninstall.item.hookScript": "Hook script: {name}",
    "uninstall.item.hookReg": "Hook registration in settings.json",
    "uninstall.item.command": "Slash command: {name}",
    "uninstall.item.marketplaceSource": "Marketplace source: work-skills",
    "uninstall.item.envVar": "Environment variable: {name}"
    ```

    **zh.json additions (same keys, Chinese values):**
    ```json
    "uninstall.title": "卸载检测",
    "uninstall.cliHelp": "运行卸载检测并显示将要移除的内容",
    "uninstall.category.plugins": "插件",
    "uninstall.category.hooks": "Hook 脚本",
    "uninstall.category.hookRegistration": "Hook 注册",
    "uninstall.category.commands": "斜杠命令",
    "uninstall.category.marketplace": "Marketplace 源",
    "uninstall.category.environment": "环境变量",
    "uninstall.status.installed": "已安装",
    "uninstall.status.notInstalled": "未安装",
    "uninstall.summary": "检测完成: {found}/{total} 项已安装",
    "uninstall.nothingFound": "未检测到安装内容,无需卸载。",
    "uninstall.item.pluginDir": "插件目录: {name}",
    "uninstall.item.hookScript": "Hook 脚本: {name}",
    "uninstall.item.hookReg": "settings.json 中的 Hook 注册",
    "uninstall.item.command": "斜杠命令: {name}",
    "uninstall.item.marketplaceSource": "Marketplace 源: work-skills",
    "uninstall.item.envVar": "环境变量: {name}"
    ```

    Also update the --uninstall option description in cli.js to use the i18n key. In cli.js, the `.option('--uninstall', ...)` description string should reference `t('uninstall.cliHelp')`. However, since Commander's `.option()` description is set at parse time before language detection, keep the description as a static English string for now (consistent with how --verify works). The i18n keys are for the detection output, not the help text.
  </action>
  <verify>
    <automated>cd installer && node -e "const en=require('./src/i18n/en.json'); const zh=require('./src/i18n/zh.json'); const enKeys=Object.keys(en).filter(k=>k.startsWith('uninstall.')); const zhKeys=Object.keys(zh).filter(k=>k.startsWith('uninstall.')); console.log('EN keys:', enKeys.length, 'ZH keys:', zhKeys.length); if(JSON.stringify(enKeys)!==JSON.stringify(zhKeys)){console.error('KEY MISMATCH');process.exit(1)} console.log('All uninstall keys match')"</automated>
  </verify>
  <done>
    - Both en.json and zh.json contain exactly the same uninstall.* key names
    - 18 uninstall.* keys added to each file
    - No JSON syntax errors
    - Existing keys untouched
  </done>
</task>

</tasks>

<verification>
1. CLI flag tests pass: `cd installer && npx jest tests/cli.test.js -x`
2. i18n key parity verified: EN and ZH have identical uninstall.* key sets
3. Existing test suite still passes: `cd installer && npm test`
</verification>

<success_criteria>
- `parseArgs(['node', 'setup.js', '--uninstall'])` returns `{ uninstallOnly: true }`
- `main()` with uninstallOnly routes to `runUninstallDetection()` before checking verifyOnly
- Both en.json and zh.json have 18 matching uninstall.* keys
- All existing tests pass (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/24-cli-entry-detection/24-01-SUMMARY.md`
</output>
