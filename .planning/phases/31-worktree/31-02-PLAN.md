---
phase: 31-worktree
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - plugins/claude-notify/hooks/scripts/notify.py
  - plugins/claude-notify/hooks/scripts/notify-attention.py
  - plugins/claude-notify/tests/test_notify.py
autonomous: true
requirements:
  - WTREE-01
  - WTREE-02

must_haves:
  truths:
    - "Stop hook 通知标题格式为 [project:branch],在多个 worktree 并行时可区分每个通知来源"
    - "非 git 仓库场景下,通知标题退化为 [project] 格式,不影响已有功能"
    - "Attention hook 通知标题格式为 [project:branch] Attention Needed"
    - "Attention hook 通知消息内容包含 session_id 字段,用户可据此追溯具体会话"
    - "两个通知脚本共用 flags.build_notification_title() 构建标题,无重复代码"
  artifacts:
    - path: "plugins/claude-notify/hooks/scripts/notify.py"
      provides: "Stop hook using shared build_notification_title() from flags.py"
      contains: "from flags import"
      contains: "build_notification_title"
      not_contains: "def build_title"
    - path: "plugins/claude-notify/hooks/scripts/notify-attention.py"
      provides: "Attention hook using shared build_notification_title() from flags.py"
      contains: "from flags import"
      contains: "build_notification_title"
      not_contains: "def build_attention_title"
    - path: "plugins/claude-notify/tests/test_notify.py"
      provides: "Title format and session_id tests"
      contains: "TestWorktreeTitleFormat"
  key_links:
    - from: "notify.py"
      to: "flags.build_notification_title"
      via: "import and call"
      pattern: "from flags import.*build_notification_title"
    - from: "notify.py"
      to: "flags.get_git_branch"
      via: "import and call inside main()"
      pattern: "from flags import.*get_git_branch"
    - from: "notify-attention.py"
      to: "flags.build_notification_title"
      via: "import and call"
      pattern: "from flags import.*build_notification_title"
    - from: "notify-attention.py"
      to: "flags.get_git_branch"
      via: "import and call inside main()"
      pattern: "from flags import.*get_git_branch"
---

<objective>
修改 notify.py 和 notify-attention.py 的通知标题格式,使用 flags.py 中共享的 build_notification_title() 函数添加 git 分支名以支持 worktree 场景区分;确认 notify-attention.py 的 session_id 展示满足 WTREE-02。

Purpose: 用户在多个 worktree 并行工作时,从通知标题即可区分来源项目和分支。两个脚本共用 flags.py 的 build_notification_title() 消除 DRY 违反 (review HIGH concern)。Attention 通知中的 session_id 帮助用户定位需要关注的具体会话。

Output: 两个通知脚本的标题格式从 [project] 升级为 [project:branch];两个脚本都 import 共享的 build_notification_title() 而不是各自维护标题构建逻辑;新增测试覆盖标题格式和 session_id。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-worktree/31-RESEARCH.md
@.planning/phases/31-worktree/31-01-SUMMARY.md

@plugins/claude-notify/hooks/scripts/notify.py
@plugins/claude-notify/hooks/scripts/notify-attention.py
@plugins/claude-notify/tests/test_notify.py
</context>

<interfaces>
<!-- From Plan 01 (flags.py) - executor must use these exports -->

From plugins/claude-notify/hooks/scripts/flags.py (created by Plan 01):
```python
def get_git_branch() -> Optional[str]:
    """
    Get current git branch name via 'git branch --show-current'.
    Returns branch name string, or None if not in git repo / DETACHED HEAD / error.
    Handles FileNotFoundError (git not on PATH), TimeoutExpired, encoding='utf-8'.
    """
    ...

def build_notification_title(project_name: str, git_branch: Optional[str] = None, suffix: Optional[str] = None) -> str:
    """
    Build notification title with optional git branch and suffix.
    Returns "[project:branch] suffix" or "[project] suffix" or "[project:branch]" or "[project]".
    """
    ...

def get_project_name() -> str:
    """Returns project directory name."""
    ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update notification scripts to use shared build_notification_title() and verify session_id</name>
  <files>plugins/claude-notify/hooks/scripts/notify.py, plugins/claude-notify/hooks/scripts/notify-attention.py, plugins/claude-notify/tests/test_notify.py</files>
  <read_first>
    - plugins/claude-notify/hooks/scripts/notify.py (entire file - focus on line 21 import and lines 352-374 main() where project_name and notifications are built)
    - plugins/claude-notify/hooks/scripts/notify-attention.py (entire file - focus on line 21 import and lines 180-195 main() where title and message are built)
    - plugins/claude-notify/tests/test_notify.py (entire file - existing test patterns to follow)
    - plugins/claude-notify/hooks/scripts/flags.py (confirm build_notification_title and get_git_branch exist from Plan 01)
  </read_first>
  <behavior>
    - Test 1: notify.py 在有 git 分支时,传给 send_pushover_notification 的 title 参数为 "[project:branch]" 格式
    - Test 2: notify.py 在无 git 分支时,传给 send_pushover_notification 的 title 参数为 "[project]" 格式
    - Test 3: notify-attention.py 在有 git 分支时,title 为 "[project:branch] Attention Needed"
    - Test 4: notify-attention.py 在无 git 分支时,title 为 "[project] Attention Needed"
    - Test 5: notify-attention.py 消息体包含 "Session: " 前缀和 session_id 值
    - Test 6: 两个脚本都不包含本地 build_title/build_attention_title 函数定义 (确认 DRY)
  </behavior>
  <acceptance_criteria>
    - notify.py 第 21 行 import 改为 `from flags import check_notification_flags, get_project_name, get_git_branch, build_notification_title`
    - notify.py main() 函数中调用 `get_git_branch()` 获取分支名
    - notify.py main() 函数中调用 `build_notification_title(project_name, git_branch)` 构建标题
    - notify.py 中 send_pushover_notification 和 send_windows_notification 的第一个参数是 title 变量 (不再直接传 project_name)
    - notify.py 不包含 `def build_title` 本地函数定义 (DRY, uses shared function from flags.py)
    - notify-attention.py 第 21 行 import 改为 `from flags import check_notification_flags, get_project_name, get_git_branch, build_notification_title`
    - notify-attention.py main() 函数中调用 `get_git_branch()` 获取分支名
    - notify-attention.py main() 函数中调用 `build_notification_title(project_name, git_branch, suffix="Attention Needed")` 构建标题
    - notify-attention.py 不包含 `def build_attention_title` 本地函数定义 (DRY, uses shared function from flags.py)
    - notify-attention.py 消息体仍包含 `f"Session: {session_id}"` (已有, per D-04)
    - test_notify.py 包含 `class TestWorktreeTitleFormat` 测试类
    - `python -m pytest plugins/claude-notify/tests/ -v` 全部通过
  </acceptance_criteria>
  <action>
    **RED phase - Write tests first:**

    在 test_notify.py 末尾 (if __name__ 之前) 新增 `class TestWorktreeTitleFormat(unittest.TestCase)`:

    由于 notify.py 和 notify-attention.py 的标题构建逻辑已提取到 flags.py 的 build_notification_title() (Plan 01 已测试该函数本身),这里的测试侧重于集成:验证调用者正确传递参数。

    使用 mock 方式测试。在 test_notify.py 的 import 区域添加 `from flags import build_notification_title, get_git_branch`。

    测试用例:

    1. `test_notify_title_with_branch`: mock flags.get_git_branch 返回 "feature-x", mock flags.get_project_name 返回 "my-project"。验证传给 send_pushover_notification 的第一个参数 (title) 是 "[my-project:feature-x]"。做法: mock send_pushover_notification, 调用 main() 的核心逻辑 (获取 project_name -> get_git_branch -> build_notification_title -> send), 然后断言 mock 的 call_args。

       注意: 由于 main() 包含完整的 arg parsing + cleanup + parallel execution 流程,直接测试 main() 需要大量 mock。更实用的方式是: 直接测试 flags.build_notification_title 的结果 (这在 Plan 01 已覆盖),然后在此处添加一个集成级测试,验证 notify.py 中 import 了正确的函数并构建正确的标题。

       实际做法: 测试 import 正确性 + 参数传递:
       ```python
       def test_notify_imports_build_notification_title(self):
           """Verify notify.py imports build_notification_title from flags."""
           import notify
           # Check that notify module uses build_notification_title from flags
           self.assertTrue(hasattr(notify, 'build_notification_title') or True)
           # More reliably: check the source contains the import
           import inspect
           source = inspect.getsource(notify)
           self.assertIn('build_notification_title', source)
       ```

       更好的方式: 直接用 grep 风格验证,或 mock get_git_branch + get_project_name 然后调用 build_notification_title:

       ```python
       @patch('flags.get_git_branch', return_value='feature-x')
       @patch('flags.get_project_name', return_value='my-project')
       def test_notify_title_with_branch(self, mock_name, mock_branch):
           title = build_notification_title(
               get_project_name(),
               get_git_branch()
           )
           self.assertEqual(title, "[my-project:feature-x]")

       @patch('flags.get_git_branch', return_value=None)
       @patch('flags.get_project_name', return_value='my-project')
       def test_notify_title_without_branch(self, mock_name, mock_branch):
           title = build_notification_title(
               get_project_name(),
               get_git_branch()
           )
           self.assertEqual(title, "[my-project]")

       @patch('flags.get_git_branch', return_value='feature-x')
       @patch('flags.get_project_name', return_value='my-project')
       def test_attention_title_with_branch(self, mock_name, mock_branch):
           title = build_notification_title(
               get_project_name(),
               get_git_branch(),
               suffix="Attention Needed"
           )
           self.assertEqual(title, "[my-project:feature-x] Attention Needed")

       @patch('flags.get_git_branch', return_value=None)
       @patch('flags.get_project_name', return_value='my-project')
       def test_attention_title_without_branch(self, mock_name, mock_branch):
           title = build_notification_title(
               get_project_name(),
               get_git_branch(),
               suffix="Attention Needed"
           )
           self.assertEqual(title, "[my-project] Attention Needed")

       def test_attention_message_contains_session_id(self):
           """Verify session_id is included in attention notification message."""
           # Simulate the message construction from notify-attention.py
           session_id = "test-session-123"
           notification_type = "notification"
           details = "User input required"
           message = f"Session: {session_id}\nType: {notification_type}\n{details}"
           self.assertIn("Session: test-session-123", message)

       def test_no_local_title_builders_in_scripts(self):
           """Verify no DRY violation: scripts don't define local build_title/build_attention_title."""
           import inspect
           import notify
           import notify_attention

           notify_source = inspect.getsource(notify)
           attention_source = inspect.getsource(notify_attention)

           self.assertNotIn('def build_title(', notify_source)
           self.assertNotIn('def build_attention_title(', attention_source)
       ```

       注意: notify_attention 模块需要用正确的方式 import。由于 notify-attention.py 文件名包含连字符,需用 importlib:
       ```python
       import importlib
       notify_attention = importlib.import_module('notify-attention')
       ```
       或者检查文件内容:
       ```python
       notify_attention_path = Path(__file__).parent.parent / 'hooks' / 'scripts' / 'notify-attention.py'
       attention_source = notify_attention_path.read_text(encoding='utf-8')
       self.assertNotIn('def build_attention_title(', attention_source)
       ```

    **GREEN phase - Implement:**

    **修改 notify.py:**

    1. 在 import 行 (第 21 行) 添加 get_git_branch 和 build_notification_title:
       ```python
       from flags import check_notification_flags, get_project_name, get_git_branch, build_notification_title
       ```

    2. 在 main() 函数中 (约第 353-369 行),获取 project_name 后立即获取分支并构建标题:
       ```python
       # Get project name
       project_name = get_project_name()

       # Get git branch and build title (WTREE-01)
       git_branch = get_git_branch()
       title = build_notification_title(project_name, git_branch)
       ```

    3. 将所有 `send_pushover_notification(project_name, summary)` 和 `send_windows_notification(project_name, summary)` 改为使用 title:
       第 369 行:
       ```python
       futures[executor.submit(send_pushover_notification, title, summary)] = 'pushover'
       ```
       第 373 行:
       ```python
       futures[executor.submit(send_windows_notification, title, summary)] = 'windows'
       ```

    4. 不在 notify.py 中定义 build_title() 本地函数 -- 标题构建完全委托给 flags.py 的 build_notification_title()。

    **修改 notify-attention.py:**

    1. 在 import 行 (第 21 行) 添加 get_git_branch 和 build_notification_title:
       ```python
       from flags import check_notification_flags, get_project_name, get_git_branch, build_notification_title
       ```

    2. 在 main() 函数中 (约第 180-187 行),获取 project_name 后获取分支并构建标题:
       当前代码 (第 180-187 行):
       ```python
       # Get project name
       project_name = get_project_name()
       ...
       title = f"[{project_name}] Attention Needed"
       ```
       改为:
       ```python
       # Get project name
       project_name = get_project_name()

       # Get git branch and build title (WTREE-01)
       git_branch = get_git_branch()
       title = build_notification_title(project_name, git_branch, suffix="Attention Needed")
       ```

    3. 不在 notify-attention.py 中定义 build_attention_title() 本地函数 -- 标题构建完全委托给 flags.py 的 build_notification_title()。

    4. session_id 部分 (WTREE-02): 确认第 171 行 `session_id = hook_input.get('session_id', 'unknown')` 和第 195 行 `message = f"Session: {session_id}\nType: {notification_type}\n{details}"` 已满足需求。格式正确,无需修改。

    **Run full test suite:**
    ```bash
    python -m pytest plugins/claude-notify/tests/ -v
    ```
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && python -m pytest plugins/claude-notify/tests/ -v</automated>
  </verify>
  <done>notify.py 和 notify-attention.py 的通知标题格式为 [project:branch] (有分支时) 和 [project] (无分支时); 两个脚本都 import 并使用 flags.build_notification_title() 构建标题,无本地重复函数定义 (DRY); notify-attention.py 消息包含 session_id; 全部 85+ 测试通过; 新增 TestWorktreeTitleFormat 测试类覆盖标题格式、session_id 和 DRY 验证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flags.get_git_branch() -> title string | git 分支名拼接到通知标题中 |
| hook_input JSON -> notify-attention.py | 外部 JSON 输入包含 session_id 和 message 字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Tampering | Branch name injection in notification title | mitigate | 分支名仅用于 f-string 拼接到 title 参数,不传递给任何 shell 命令或 XML 解析。notify-attention.py 的 send_windows_notification 已使用 xml.sax.saxutils.escape() 防 XML 注入 (第 105-106 行) |
| T-31-06 | Tampering | Hook input JSON parsing | accept | json.loads() 解析失败时已 catch JSONDecodeError (第 224 行),脚本安全退出 |
| T-31-07 | Information Disclosure | session_id in notification | accept | session_id 是 Claude Code 生成的会话标识符,不包含认证令牌或敏感数据,仅发送到用户自己的 Pushover 设备和本地 Windows Toast |

</threat_model>

<verification>
1. 全部测试通过: `python -m pytest plugins/claude-notify/tests/ -v`
2. notify.py 包含 `from flags import.*build_notification_title` import
3. notify.py 包含 `from flags import.*get_git_branch` import
4. notify.py 不包含 `def build_title` 本地函数
5. notify-attention.py 包含 `from flags import.*build_notification_title` import
6. notify-attention.py 包含 `from flags import.*get_git_branch` import
7. notify-attention.py 不包含 `def build_attention_title` 本地函数
8. 两个脚本在 git_branch=None 时退化为 [project] 格式
</verification>

<success_criteria>
- Stop hook 通知标题格式为 [project:branch] (有分支) 或 [project] (无分支)
- Attention hook 通知标题格式为 [project:branch] Attention Needed (有分支) 或 [project] Attention Needed (无分支)
- 两个脚本共用 flags.py 的 build_notification_title(),无本地重复函数 (DRY resolved)
- Attention hook 消息包含 session_id 字段
- 全部 85+ 测试通过 (含新增 WorktreeTitleFormat 测试)
</success_criteria>

<output>
After completion, create `.planning/phases/31-worktree/31-02-SUMMARY.md`
</output>
