---
phase: 34-single-lang-fix
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - plugins/codepoint/references/golang.md
  - plugins/codepoint/references/python.md
  - tmp/template-test/go/codepoint/codepoint.go
  - tmp/template-test/python/codepoint/__init__.py
autonomous: true
requirements:
  - FIX1-02
  - FIX1-03
user_setup: []
must_haves:
  truths:
    - "golang.md 在 Base Library 代码块之后包含 Quick Compile Check 段落"
    - "python.md 在 Base Library 代码块之后包含 Quick Import Check 段落"
    - "从 golang.md 提取的模板代码可以独立编译通过（go build ./... 零错误）"
    - "从 python.md 提取的模板代码可以独立导入通过（python -c 'import codepoint' 输出 OK）"
    - "go-calculator 全部测试通过（go test ./... -count=1）"
    - "python-calculator 全部测试通过（pytest tests/ -q）"
  artifacts:
    - path: "plugins/codepoint/references/golang.md"
      provides: "Go 探针模板（含编译检查说明）"
      contains: "Quick Compile Check"
    - path: "plugins/codepoint/references/python.md"
      provides: "Python 探针模板（含导入检查说明）"
      contains: "Quick Import Check"
    - path: "tmp/template-test/go/codepoint/codepoint.go"
      provides: "从 golang.md 提取的独立编译测试文件"
    - path: "tmp/template-test/python/codepoint/__init__.py"
      provides: "从 python.md 提取的独立导入测试文件"
  key_links:
    - from: "plugins/codepoint/references/golang.md"
      to: "tmp/template-test/go/codepoint/codepoint.go"
      via: "python 脚本提取 Base Library 代码块"
      pattern: "re.findall.*```go.*package codepoint"
    - from: "plugins/codepoint/references/python.md"
      to: "tmp/template-test/python/codepoint/__init__.py"
      via: "python 脚本提取 Base Library 代码块"
      pattern: "re.findall.*```python.*Code Point"
---

<objective>
在 golang.md 和 python.md 探针模板文档中添加编译/导入检查说明段落，然后验证两个模板代码可以独立编译/导入，最后确认两个测试项目的全部测试仍然通过。

Purpose: per D-02，用户复制模板代码后可以立即使用编译/导入命令验证代码正确性。这是防止 DEV-01 类问题再次发生的预防措施——通过文档化的检查步骤让用户在第一时间发现模板代码问题。

Output: 修改后的 golang.md 和 python.md（含 Quick Compile/Import Check 段落），验证通过的模板编译测试环境
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-single-lang-fix/34-CONTEXT.md
@.planning/phases/34-single-lang-fix/34-RESEARCH.md
@.planning/phases/34-single-lang-fix/34-DEFECTS.yaml
@plugins/codepoint/references/golang.md
@plugins/codepoint/references/python.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 golang.md 添加 Quick Compile Check 段落并验证模板编译</name>
  <files>plugins/codepoint/references/golang.md, tmp/template-test/go/codepoint/codepoint.go, tmp/template-test/go/go.mod</files>
  <read_first>
    - plugins/codepoint/references/golang.md（全文，找到 Base Library 代码块结束位置和 Placement Patterns 标题）
    - .planning/phases/34-single-lang-fix/34-RESEARCH.md（Code Examples 部分：编译检查说明的推荐内容和模板提取脚本）
  </read_first>
  <action>
    **Part A: 在 golang.md 中添加 Quick Compile Check 段落**

    在 golang.md 中找到 `## Placement Patterns` 标题所在行，在其**正前方**插入以下内容（即 Base Library 代码块之后、Placement Patterns 之前）：

    ```markdown
    ## Quick Compile Check

    After copying the base library to your project, verify it compiles:

    \`\`\`bash
    cd your-project
    go build ./codepoint/...
    go vet ./codepoint/...
    \`\`\`

    Expected: zero errors.

    This catches type mismatches (e.g., `Frame` not being package-level) before you start adding probes.
    ```

    注意：使用 Edit 工具在 `## Placement Patterns` 行之前插入，不要重写整个文件。

    **Part B: 从 golang.md 提取模板代码并独立编译验证**

    1. 创建测试环境目录：
       ```bash
       mkdir -p tmp/template-test/go/codepoint
       ```
    2. 使用 Python 提取 golang.md 中 Base Library 的 go 代码块（包含 `package codepoint` 和 `func init()` 的代码块）：
       ```bash
       python -c "
       import re
       content = open('plugins/codepoint/references/golang.md', 'r', encoding='utf-8').read()
       blocks = re.findall(r'\x60\x60\x60go\n(.*?)\x60\x60\x60', content, re.DOTALL)
       for b in blocks:
           if 'package codepoint' in b and 'func init()' in b:
               open('tmp/template-test/go/codepoint/codepoint.go', 'w', encoding='utf-8').write(b.strip())
               break
       "
       ```
       注意：上面使用了 \x60 代替反引号，实际命令中使用反引号。
    3. 创建 go.mod：
       ```bash
       echo 'module template-test
       go 1.22' > tmp/template-test/go/go.mod
       ```
    4. 编译验证：
       ```bash
       cd tmp/template-test/go && go build ./...
       cd tmp/template-test/go && go vet ./...
       ```
       预期结果：零错误
  </action>
  <verify>
    <automated>grep -c "Quick Compile Check" plugins/codepoint/references/golang.md && cd "C:/WorkSpace/agent/work-skills/tmp/template-test/go" && go build ./... 2>&1 && go vet ./... 2>&1 && echo "PASS"</automated>
  </verify>
  <done>
    golang.md 包含 "Quick Compile Check" 段落，位于 Base Library 代码块和 Placement Patterns 之间。从 golang.md 提取的模板代码在 tmp/template-test/go/ 中独立编译通过，go build 和 go vet 零错误。
  </done>
</task>

<task type="auto">
  <name>Task 2: 为 python.md 添加 Quick Import Check 段落并验证模板导入</name>
  <files>plugins/codepoint/references/python.md, tmp/template-test/python/codepoint/__init__.py</files>
  <read_first>
    - plugins/codepoint/references/python.md（全文，找到 Base Library 代码块结束位置和 FastAPI/Flask Patterns 标题）
    - .planning/phases/34-single-lang-fix/34-RESEARCH.md（Code Examples 部分：导入检查说明的推荐内容和模板提取脚本）
  </read_first>
  <action>
    **Part A: 在 python.md 中添加 Quick Import Check 段落**

    在 python.md 中找到 `## FastAPI / Flask Patterns` 标题所在行，在其**正前方**插入以下内容（即 Base Library 代码块之后、FastAPI Patterns 之前）：

    ```markdown
    ## Quick Import Check

    After copying the base library to your project, verify it imports:

    \`\`\`bash
    cd your-project
    python -c "import codepoint; print('OK')"
    \`\`\`

    Expected: `OK`.

    This catches syntax errors and missing dependencies before you start adding probes.
    ```

    注意：使用 Edit 工具在 `## FastAPI / Flask Patterns` 行之前插入，不要重写整个文件。

    **Part B: 从 python.md 提取模板代码并独立导入验证**

    1. 创建测试环境目录：
       ```bash
       mkdir -p tmp/template-test/python/codepoint
       ```
    2. 使用 Python 提取 python.md 中 Base Library 的 python 代码块（包含 `Code Point: lightweight` 和 `def _write` 的代码块）：
       ```bash
       python -c "
       import re
       content = open('plugins/codepoint/references/python.md', 'r', encoding='utf-8').read()
       blocks = re.findall(r'\x60\x60\x60python\n(.*?)\x60\x60\x60', content, re.DOTALL)
       for b in blocks:
           if 'Code Point: lightweight' in b and len(b.strip().split(chr(10))) > 50:
               open('tmp/template-test/python/codepoint/__init__.py', 'w', encoding='utf-8').write(b.strip())
               break
       "
       ```
       注意：上面使用了 \x60 代替反引号，实际命令中使用反引号。
    3. 导入验证：
       ```bash
       cd tmp/template-test/python && python -c "import codepoint; print('OK')"
       ```
       预期结果：OK
  </action>
  <verify>
    <automated>grep -c "Quick Import Check" plugins/codepoint/references/python.md && cd "C:/WorkSpace/agent/work-skills/tmp/template-test/python" && python -c "import codepoint; print('OK')" && echo "PASS"</automated>
  </verify>
  <done>
    python.md 包含 "Quick Import Check" 段落，位于 Base Library 代码块和 FastAPI/Flask Patterns 之间。从 python.md 提取的模板代码在 tmp/template-test/python/ 中独立导入通过，输出 OK。
  </done>
</task>

<task type="auto">
  <name>Task 3: 回归验证两个测试项目的全部测试通过</name>
  <files></files>
  <read_first>
    - 无需读取文件，纯验证任务
  </read_first>
  <action>
    运行 Go 和 Python 测试项目的完整测试套件，确认模板文档修改不影响测试项目：

    1. Go 计算器全量测试：
       ```bash
       cd C:/WorkSpace/agent/work-skills/tmp/go-calculator && go test ./... -count=1
       ```
       预期结果：5 个包全部 PASS（api, batch, calculator, history, tests）

    2. Python 计算器全量测试：
       ```bash
       cd C:/WorkSpace/agent/work-skills/tmp/python-calculator && python -m pytest tests/ -q
       ```
       预期结果：57 passed
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/work-skills/tmp/go-calculator" && go test ./... -count=1 2>&1 && cd "C:/WorkSpace/agent/work-skills/tmp/python-calculator" && python -m pytest tests/ -q 2>&1 && echo "ALL PASS"</automated>
  </verify>
  <done>
    go-calculator: go test ./... -count=1 全部 PASS（5 包）。python-calculator: pytest tests/ -q 全部 PASS（57 tests）。确认模板文档修改未引入任何回归。
  </done>
</task>

</tasks>

<verification>
1. golang.md 包含 "Quick Compile Check" 段落，位于 Base Library 和 Placement Patterns 之间
2. python.md 包含 "Quick Import Check" 段落，位于 Base Library 和 FastAPI/Flask Patterns 之间
3. golang.md 模板代码在 tmp/template-test/go/ 中独立编译通过（go build + go vet 零错误）
4. python.md 模板代码在 tmp/template-test/python/ 中独立导入通过（python -c "import codepoint" 输出 OK）
5. go-calculator 全量测试通过（5 包）
6. python-calculator 全量测试通过（57 tests）
</verification>

<success_criteria>
- 两个模板文档都包含对应的编译/导入检查说明
- 模板代码可从 markdown 独立提取并编译/导入成功
- 两个测试项目的所有测试保持绿色，无回归
</success_criteria>

<output>
After completion, create `.planning/phases/34-single-lang-fix/34-02-SUMMARY.md`
</output>
