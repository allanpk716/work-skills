---
phase: 38-e2e-test-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/codepoint-v2/go-calculator/
  - tests/e2e/codepoint-v2/python-calculator/
  - tests/e2e/codepoint-v2/gojs-calculator/
  - tests/e2e/codepoint-v2/pyts-calculator/
  - tests/e2e/codepoint-v2/template-test/
  - tests/e2e/codepoint-v2/.gitignore
  - tests/e2e/.gitkeep
autonomous: true
requirements:
  - R1
  - R3

must_haves:
  truths:
    - "tests/e2e/codepoint-v2/ 目录存在且包含 5 个测试项目"
    - "gojs-calculator 和 pyts-calculator 的 git 历史通过 git mv 保留"
    - "go-calculator, python-calculator, template-test 已添加到 git"
    - "迁移后的项目不包含 node_modules/, __pycache__/, .exe, .playwright-cli/ 构建产物"
    - "gojs-calculator/frontend/dist/ 目录被保留且非空（Go embed 指令需要）"
    - "Go 项目迁移后 go build ./... 成功（在项目目录内执行）"
    - "tests/e2e/codepoint-v2/.gitignore 阻止未来构建产物被提交"
  artifacts:
    - path: "tests/e2e/codepoint-v2/go-calculator/"
      provides: "Go 单语言计算器测试项目"
      contains: "go.mod"
      contains: "main.go"
    - path: "tests/e2e/codepoint-v2/python-calculator/"
      provides: "Python 单语言计算器测试项目"
      contains: "main.py"
      contains: "calculator/core.py"
    - path: "tests/e2e/codepoint-v2/gojs-calculator/"
      provides: "Go+JS 全栈跨语言测试项目"
      contains: "go.mod"
      contains: "frontend/package.json"
    - path: "tests/e2e/codepoint-v2/pyts-calculator/"
      provides: "Python+TS 全栈跨语言测试项目"
      contains: "main.py"
      contains: "frontend/package.json"
    - path: "tests/e2e/codepoint-v2/template-test/"
      provides: "探针模板测试项目"
      contains: "go/go.mod"
      contains: "python/codepoint/__init__.py"
    - path: "tests/e2e/codepoint-v2/.gitignore"
      provides: "防止构建产物被意外提交"
      contains: "node_modules/"
      contains: "__pycache__/"
      contains: "*.exe"
  key_links:
    - from: "tmp/gojs-calculator/"
      to: "tests/e2e/codepoint-v2/gojs-calculator/"
      via: "git mv"
      pattern: "git mv tmp/gojs-calculator"
    - from: "tmp/pyts-calculator/"
      to: "tests/e2e/codepoint-v2/pyts-calculator/"
      via: "git mv"
      pattern: "git mv tmp/pyts-calculator"
    - from: "tmp/go-calculator/"
      to: "tests/e2e/codepoint-v2/go-calculator/"
      via: "cp + git add"
      pattern: "git add tests/e2e/codepoint-v2/go-calculator"
---

<objective>
将 tmp/ 下 5 个 E2E 测试项目迁移到 tests/e2e/codepoint-v2/，使用正确的 git 策略（tracked 用 git mv，untracked 用 cp + git add），排除构建产物，添加 .gitignore 防止未来构建产物被提交。

Purpose: tmp/ 是临时目录，不应保留持久性测试资产。E2E 测试项目是 Codepoint V2 的验证资产，需要长期存档用于回归测试和后续改进。

Output: tests/e2e/codepoint-v2/ 目录包含 5 个完整的测试项目、一个 .gitignore 文件，无构建产物，git 跟踪状态正确。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-e2e-test-migration/38-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create target directory, add .gitignore, and migrate tracked projects via git mv</name>
  <files>tests/e2e/codepoint-v2/.gitignore, tests/e2e/codepoint-v2/gojs-calculator/, tests/e2e/codepoint-v2/pyts-calculator/, tests/e2e/.gitkeep</files>
  <read_first>
    - .planning/phases/38-e2e-test-migration/38-RESEARCH.md (Section "Detailed File Inventory" for tracked vs untracked classification, Section "Pattern 1: Git-aware Migration" for commands, Section "Pitfall 3" for frontend/dist/ tracking details)
  </read_first>
  <acceptance_criteria>
    - `tests/e2e/codepoint-v2/` 目录存在
    - `tests/e2e/codepoint-v2/.gitignore` 存在且包含 `node_modules/`、`__pycache__/`、`*.exe`、`.playwright-cli/` 四个模式
    - `tests/e2e/.gitkeep` 存在
    - `tests/e2e/codepoint-v2/gojs-calculator/main.go` 存在
    - `tests/e2e/codepoint-v2/gojs-calculator/go.mod` 存在
    - `tests/e2e/codepoint-v2/gojs-calculator/frontend/dist/index.html` 存在（Go embed 必需）
    - `tests/e2e/codepoint-v2/gojs-calculator/frontend/dist/assets/` 目录存在且非空
    - `tests/e2e/codepoint-v2/pyts-calculator/main.py` 存在
    - `tests/e2e/codepoint-v2/pyts-calculator/frontend/package.json` 存在
    - `git ls-files -- tests/e2e/codepoint-v2/gojs-calculator/` 返回 35 个文件
    - `git ls-files -- tests/e2e/codepoint-v2/pyts-calculator/` 返回 30 个文件
    - `git status` 显示这两个目录处于 renamed 状态（from tmp/）
    - `tests/e2e/codepoint-v2/gojs-calculator/frontend/node_modules/` 不存在
    - `tests/e2e/codepoint-v2/gojs-calculator/gojs-calculator.exe` 不存在
    - `tests/e2e/codepoint-v2/gojs-calculator/.playwright-cli/` 不存在
  </acceptance_criteria>
  <action>
    **Step 1: Create target directory structure**
    ```bash
    mkdir -p tests/e2e/codepoint-v2
    ```

    **Step 2: Create .gitignore for tests/e2e/codepoint-v2/**

    Addresses review concern: prevent future build artifacts from being accidentally committed.

    Create `tests/e2e/codepoint-v2/.gitignore` with content:
    ```
    node_modules/
    __pycache__/
    *.pyc
    *.exe
    .playwright-cli/
    ```

    **Step 3: Add .gitkeep to tests/e2e/ root**
    ```bash
    touch tests/e2e/.gitkeep
    ```

    **Step 4: Clean build artifacts from gojs-calculator BEFORE git mv**

    CRITICAL: Do NOT delete `frontend/dist/` from gojs-calculator. It is git-tracked (5 files including index.html, favicon.svg, icons.svg, 2 JS/CSS assets) and REQUIRED for Go's `//go:embed` directive. Deleting it will break `go build`.

    ```bash
    # Remove UNTRACKED artifacts only
    rm -rf tmp/gojs-calculator/frontend/node_modules/
    rm -f tmp/gojs-calculator/gojs-calculator.exe
    rm -rf tmp/gojs-calculator/.playwright-cli/
    ```

    **Step 5: Clean build artifacts from pyts-calculator BEFORE git mv**

    Note: `pyts-calculator/frontend/dist/` is NOT tracked by git (confirmed: `git ls-files -- tmp/pyts-calculator/frontend/dist/` returns 0 files). It is safe to delete.

    ```bash
    rm -rf tmp/pyts-calculator/frontend/node_modules/
    rm -rf tmp/pyts-calculator/frontend/dist/
    find tmp/pyts-calculator/ -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
    ```

    **Step 6: git mv tracked projects**
    ```bash
    git mv tmp/gojs-calculator tests/e2e/codepoint-v2/gojs-calculator
    git mv tmp/pyts-calculator tests/e2e/codepoint-v2/pyts-calculator
    ```

    **Step 7: Verify tracked files arrived correctly AND gojs dist/ preserved**

    Addresses review concern: explicit assertion that gojs-calculator/frontend/dist/ is preserved after migration.

    ```bash
    # Verify file counts
    git ls-files -- tests/e2e/codepoint-v2/gojs-calculator/ | wc -l   # Expect 35
    git ls-files -- tests/e2e/codepoint-v2/pyts-calculator/ | wc -l   # Expect 30

    # ASSERTION: gojs-calculator frontend/dist/ must exist and be non-empty
    # This is CRITICAL for Go's //go:embed directive to work
    if [ -z "$(ls -A tests/e2e/codepoint-v2/gojs-calculator/frontend/dist/ 2>/dev/null)" ]; then
      echo "FATAL: gojs-calculator/frontend/dist/ is empty or missing!"
      echo "Go //go:embed directive requires this directory. Cannot proceed."
      exit 1
    fi
    echo "PASS: gojs-calculator/frontend/dist/ preserved ($(ls tests/e2e/codepoint-v2/gojs-calculator/frontend/dist/ | wc -l) files)"

    # Verify gojs dist contains expected files
    test -f tests/e2e/codepoint-v2/gojs-calculator/frontend/dist/index.html || { echo "FATAL: index.html missing from dist/"; exit 1; }
    ```

    **Step 8: Stage new files**
    ```bash
    git add tests/e2e/codepoint-v2/.gitignore
    git add tests/e2e/.gitkeep
    ```

    Do NOT commit yet -- wait until all 5 projects are migrated and verified in Task 2.
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && test -d tests/e2e/codepoint-v2 && test -f tests/e2e/codepoint-v2/.gitignore && grep -q "node_modules/" tests/e2e/codepoint-v2/.gitignore && grep -q "__pycache__/" tests/e2e/codepoint-v2/.gitignore && grep -q "*.exe" tests/e2e/codepoint-v2/.gitignore && test -f tests/e2e/.gitkeep && test -f tests/e2e/codepoint-v2/gojs-calculator/main.go && test -f tests/e2e/codepoint-v2/pyts-calculator/main.py && test -f tests/e2e/codepoint-v2/gojs-calculator/frontend/dist/index.html && test -d tests/e2e/codepoint-v2/gojs-calculator/frontend/dist/assets && ! test -d tests/e2e/codepoint-v2/gojs-calculator/frontend/node_modules && ! test -f tests/e2e/codepoint-v2/gojs-calculator/gojs-calculator.exe && ! test -d tests/e2e/codepoint-v2/gojs-calculator/.playwright-cli && echo "PASS: Tracked projects migrated, gojs dist/ preserved, .gitignore created"</automated>
  </verify>
  <done>gojs-calculator 和 pyts-calculator 通过 git mv 迁移到 tests/e2e/codepoint-v2/，构建产物已清除，gojs-calculator/frontend/dist/ 明确验证保留（含 index.html 和 assets/），.gitignore 和 .gitkeep 已创建</done>
</task>

<task type="auto">
  <name>Task 2: Copy untracked projects, add to git, and verify builds</name>
  <files>tests/e2e/codepoint-v2/go-calculator/, tests/e2e/codepoint-v2/python-calculator/, tests/e2e/codepoint-v2/template-test/</files>
  <read_first>
    - .planning/phases/38-e2e-test-migration/38-RESEARCH.md (Section "Detailed File Inventory" for untracked projects file lists, Section "Pitfall 2" for Go module path integrity)
  </read_first>
  <acceptance_criteria>
    - `tests/e2e/codepoint-v2/go-calculator/main.go` 存在
    - `tests/e2e/codepoint-v2/go-calculator/go.mod` 存在
    - `tests/e2e/codepoint-v2/go-calculator/.codepoints/index.json` 存在
    - `tests/e2e/codepoint-v2/python-calculator/main.py` 存在
    - `tests/e2e/codepoint-v2/python-calculator/calculator/core.py` 存在
    - `tests/e2e/codepoint-v2/template-test/go/go.mod` 存在
    - `tests/e2e/codepoint-v2/template-test/python/codepoint/__init__.py` 存在
    - `find tests/e2e/codepoint-v2/ -type d -name "__pycache__"` 返回空
    - `git status` 显示 tests/e2e/codepoint-v2/go-calculator/, python-calculator/, template-test/ 为 new file
    - `cd C:/WorkSpace/agent/work-skills/tests/e2e/codepoint-v2/go-calculator && go build ./...` 成功（exit 0）
    - `cd C:/WorkSpace/agent/work-skills/tests/e2e/codepoint-v2/gojs-calculator && go build ./...` 成功（exit 0）
  </acceptance_criteria>
  <action>
    **Step 1: Copy untracked projects**
    ```bash
    cp -r tmp/go-calculator tests/e2e/codepoint-v2/go-calculator
    cp -r tmp/python-calculator tests/e2e/codepoint-v2/python-calculator
    cp -r tmp/template-test tests/e2e/codepoint-v2/template-test
    ```

    **Step 2: Clean build artifacts from copied directories**
    ```bash
    # Remove __pycache__ from all copied projects
    find tests/e2e/codepoint-v2/ -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
    # Remove .pyc files
    find tests/e2e/codepoint-v2/ -type f -name "*.pyc" -delete 2>/dev/null
    # Remove any .exe that might have been copied
    find tests/e2e/codepoint-v2/ -type f -name "*.exe" -delete 2>/dev/null
    ```

    **Step 3: Add untracked projects to git**
    ```bash
    git add tests/e2e/codepoint-v2/go-calculator/
    git add tests/e2e/codepoint-v2/python-calculator/
    git add tests/e2e/codepoint-v2/template-test/
    ```

    **Step 4: Verify Go projects build after migration**

    Addresses review concern: Go build must run from within the module directory. Using absolute path with `cd` to ensure correct working directory.

    ```bash
    # Verify go-calculator builds (MUST run from within the module directory)
    cd C:/WorkSpace/agent/work-skills/tests/e2e/codepoint-v2/go-calculator && go build ./... && cd C:/WorkSpace/agent/work-skills

    # Verify gojs-calculator builds (MUST run from within the module directory)
    cd C:/WorkSpace/agent/work-skills/tests/e2e/codepoint-v2/gojs-calculator && go build ./... && cd C:/WorkSpace/agent/work-skills
    ```

    If Go builds fail with module path errors, check RESEARCH.md Pitfall 2: Go modules use module name from go.mod, not filesystem path. The build should succeed without any changes.

    **Step 5: Verify complete directory structure**
    ```bash
    ls tests/e2e/codepoint-v2/
    # Expected: go-calculator  gojs-calculator  python-calculator  pyts-calculator  template-test
    # Also should show .gitignore file
    ```

    **Step 6: Commit all migration work**

    Addresses review concern: commit checkpoint after Plan 01 provides a recovery point before irreversible deletion in Plan 02.

    ```bash
    git add tests/e2e/codepoint-v2/
    git commit -m "$(cat <<'EOF'
feat(38): migrate E2E test projects from tmp/ to tests/e2e/codepoint-v2/

Migrate 5 test projects to permanent location:
- gojs-calculator, pyts-calculator: git mv (history preserved)
- go-calculator, python-calculator, template-test: cp + git add

Build artifacts cleaned (node_modules, __pycache__, .exe, .playwright-cli).
gojs-calculator/frontend/dist/ preserved for Go //go:embed.
Added .gitignore to prevent future artifact commits.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
    )"
    ```
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && test -f tests/e2e/codepoint-v2/go-calculator/main.go && test -f tests/e2e/codepoint-v2/python-calculator/main.py && test -f tests/e2e/codepoint-v2/template-test/go/go.mod && test -f tests/e2e/codepoint-v2/template-test/python/codepoint/__init__.py && test -f tests/e2e/codepoint-v2/.gitignore && test -f tests/e2e/.gitkeep && find tests/e2e/codepoint-v2/ -type d -name "__pycache__" | wc -l | grep -q "^0$" && cd tests/e2e/codepoint-v2/go-calculator && go build ./... && cd C:/WorkSpace/agent/work-skills && cd tests/e2e/codepoint-v2/gojs-calculator && go build ./... && cd C:/WorkSpace/agent/work-skills && git log --oneline -1 | grep -q "migrate E2E" && echo "PASS: All projects migrated, builds verified, committed"</automated>
  </verify>
  <done>go-calculator, python-calculator, template-test 复制到 tests/e2e/codepoint-v2/ 并 git add；__pycache__ 和 .exe 已清除；.gitignore 防止未来构建产物；Go 项目 go build 验证通过（从项目目录执行）；所有变更已提交为恢复点</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this phase -- pure file migration with no code changes, no network operations, no security-relevant operations.

## STRIDE Threat Register

No threats identified. This phase moves existing test project files from one directory to another. No new code is written, no credentials are handled, no network endpoints are exposed, no authentication is involved.
</threat_model>

<verification>
1. tests/e2e/codepoint-v2/ 包含 5 个目录: go-calculator, python-calculator, gojs-calculator, pyts-calculator, template-test
2. tests/e2e/codepoint-v2/.gitignore 存在且包含 node_modules/, __pycache__/, *.exe, .playwright-cli/
3. 无构建产物: `find tests/e2e/codepoint-v2/ -type d -name "__pycache__" | wc -l` 返回 0
4. 无 node_modules: `find tests/e2e/codepoint-v2/ -type d -name "node_modules" | wc -l` 返回 0
5. 无 .exe 文件: `find tests/e2e/codepoint-v2/ -name "*.exe" | wc -l` 返回 0
6. Go 项目可构建: `cd tests/e2e/codepoint-v2/go-calculator && go build ./...` 成功
7. Go 项目可构建: `cd tests/e2e/codepoint-v2/gojs-calculator && go build ./...` 成功
8. gojs-calculator/frontend/dist/ 保留: `ls tests/e2e/codepoint-v2/gojs-calculator/frontend/dist/index.html` 成功
9. git 跟踪状态正确: gojs-calculator (35 files tracked), pyts-calculator (30 files tracked), go-calculator/python-calculator/template-test (newly added)
10. 变更已提交: `git log --oneline -1` 显示 migration commit
</verification>

<success_criteria>
- tests/e2e/codepoint-v2/ 包含 5 个测试项目，结构完整
- 每个 Go 项目 go build ./... 成功（从项目目录内执行）
- 无构建产物（node_modules, __pycache__, .exe, .playwright-cli）
- gojs-calculator/frontend/dist/ 保留且包含 index.html + assets/（Go embed 需要）
- tests/e2e/codepoint-v2/.gitignore 包含 4 个排除模式
- tests/e2e/.gitkeep 存在
- 变更已提交（作为 Plan 02 删除 tmp/ 前的恢复点）
</success_criteria>

<output>
After completion, create `.planning/phases/38-e2e-test-migration/38-01-SUMMARY.md`
</output>
