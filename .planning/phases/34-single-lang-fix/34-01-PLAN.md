---
phase: 34-single-lang-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-single-lang-fix/34-DEFECTS.yaml
autonomous: true
requirements:
  - FIX1-01
user_setup: []
must_haves:
  truths:
    - "34-DEFECTS.yaml 存在且包含 4 个缺陷条目（DEV-01 到 DEV-04）"
    - "每个缺陷条目包含 id, description, category, steps_to_reproduce, expected, actual, status, fixed_in, verification 字段"
    - "YAML 文件可以被 Python yaml.safe_load 正确解析"
  artifacts:
    - path: ".planning/phases/34-single-lang-fix/34-DEFECTS.yaml"
      provides: "Phase 32/33 所有已知缺陷的结构化记录"
      min_lines: 50
  key_links: []
---

<objective>
创建 34-DEFECTS.yaml 结构化缺陷记录文件，将 Phase 32 中发现的 4 个偏差（DEV-01 到 DEV-04）正式记录为缺陷，每个条目包含完整的问题描述、复现步骤、预期/实际行为、修复状态和验证证据。

Purpose: per D-04，使用 YAML 格式记录所有已知缺陷，便于机器读取和自动化验证。这是 Phase 34 的基础产出，后续计划的验证工作依赖此文件中的缺陷信息。

Output: 34-DEFECTS.yaml（4 个缺陷条目，所有条目 status: fixed，包含 fixed_in 和 verification 字段）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-single-lang-fix/34-CONTEXT.md
@.planning/phases/34-single-lang-fix/34-RESEARCH.md
@.planning/phases/32-go-single-lang-calc/32-01-SUMMARY.md
@.planning/phases/32-go-single-lang-calc/32-05-SUMMARY.md
@.planning/phases/33-python-single-lang-calc/33-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 34-DEFECTS.yaml 结构化缺陷记录文件</name>
  <files>.planning/phases/34-single-lang-fix/34-DEFECTS.yaml</files>
  <read_first>
    - .planning/phases/32-go-single-lang-calc/32-01-SUMMARY.md（DEV-01 到 DEV-04 的原始记录和修复过程）
    - .planning/phases/34-single-lang-fix/34-RESEARCH.md（Pattern 1: 结构化缺陷文件 YAML 示例格式）
    - .planning/phases/34-single-lang-fix/34-CONTEXT.md（D-04 缺陷文档格式要求）
  </read_first>
  <action>
    创建 `.planning/phases/34-single-lang-fix/34-DEFECTS.yaml`，结构如下：

    ```yaml
    # Phase 34: Single-Language Defects Record
    # Generated: 2026-04-18
    # Source: Phase 32 (Go) E2E test deviations
    # Phase 33 (Python): 0 defects found — not included

    phase: "34-single-lang-fix"
    source_phases:
      - "32-go-single-lang-calc"
      - "33-python-single-lang-calc"

    defects:
      - id: DEV-01
        category: template
        severity: high
        template: golang.md
        discovered_in: "Phase 32, Plan 01"
        description: |
          golang.md 模板中 parseGoStack 返回匿名 struct，与 PointJSON 内部 Frame 类型不兼容。
          Frame 类型定义在 PointJSON 函数内部作为局部类型，而 parseGoStack 返回的是独立的匿名 struct。
          这导致模板代码无法编译：parseGoStack 的返回值无法赋值给 Entry.Frames ([]Frame)。
        steps_to_reproduce: |
          1. 从 golang.md 复制 Base Library 代码到新项目 codepoint/codepoint.go
          2. 创建 go.mod（module template-test）
          3. 运行 go build ./codepoint/...
          4. 编译失败：Frame 类型未定义（undefined: Frame）
        expected: "Frame 为 package-level 类型，parseGoStack 返回 []Frame，整个 Base Library 可独立编译"
        actual: "Frame 定义在 PointJSON 内部，parseGoStack 返回匿名 struct，编译失败"
        status: fixed
        fixed_in: "commit 088f086"
        fixed_by: "Phase 32 Plan 01"
        verification: |
          golang.md 模板独立编译通过：
          cd tmp/template-test/go && go build ./...
          Expected: zero errors
          [VERIFIED: 2026-04-18]

      - id: DEV-02
        category: test_project
        severity: low
        template: null
        discovered_in: "Phase 32, Plan 01"
        description: |
          go-calculator/internal/history/store.go 包含未使用的 fmt import。
          该文件在实现过程中引入了 fmt 包但未实际使用，导致 go vet 报告 unused import 错误。
        steps_to_reproduce: |
          1. 进入 go-calculator 项目
          2. 运行 go vet ./internal/history/...
          3. 报错：imported and not used: "fmt"
        expected: "所有 import 均被使用，go vet 零警告"
        actual: "store.go 包含未使用的 fmt import"
        status: fixed
        fixed_in: "Phase 32 Plan 01 (代码修复)"
        fixed_by: "Phase 32 Plan 01"
        verification: |
          cd tmp/go-calculator && go vet ./... 
          Expected: zero warnings
          [VERIFIED: 2026-04-18 — go vet 无输出]

      - id: DEV-03
        category: test_project
        severity: medium
        template: null
        discovered_in: "Phase 32, Plan 01"
        description: |
          go-calculator 测试中 (2+3)*4-10/2 的期望值计算错误。
          正确答案为 15.0（(2+3)*4=20, 20-10/2=20-5=15），但测试期望值为 10.0。
          这是一个测试断言错误，非业务逻辑缺陷。
        steps_to_reproduce: |
          1. 进入 go-calculator 项目
          2. 运行 go test ./internal/calculator/... -run TestEvaluate -v
          3. 观察期望值与实际值的差异
        expected: "测试期望 (2+3)*4-10/2 = 15.0，与 calculator.Evaluate 的实际计算结果一致"
        actual: "测试期望值为 10.0，导致测试失败（实际计算结果为 15.0）"
        status: fixed
        fixed_in: "Phase 32 Plan 01 (测试修复)"
        fixed_by: "Phase 32 Plan 01"
        verification: |
          cd tmp/go-calculator && go test ./internal/calculator/... -run TestEvaluate -v
          Expected: PASS
          [VERIFIED: 2026-04-18 — 31 tests PASS]

      - id: DEV-04
        category: test_project
        severity: medium
        template: null
        discovered_in: "Phase 32, Plan 01"
        description: |
          go-calculator History 测试直接调用 handler 函数，绕过了 HTTP mux 的 PathValue 提取。
          使用 http.Request 的直接调用不会经过 http.NewServeMux 的路由匹配，导致
          r.PathValue("id") 返回空字符串，无法正确测试历史记录查询功能。
        steps_to_reproduce: |
          1. 进入 go-calculator 项目
          2. 运行 go test ./internal/api/... -run TestHistory -v
          3. 观察：测试直接调用 HandleHistoryGet，r.PathValue("id") 返回 ""
        expected: "测试通过 mux 路由（srv.ServeHTTP），正确提取 URL 路径参数"
        actual: "测试直接调用 handler 函数，PathValue 无法提取参数"
        status: fixed
        fixed_in: "Phase 32 Plan 01 (测试修复)"
        fixed_by: "Phase 32 Plan 01"
        verification: |
          cd tmp/go-calculator && go test ./internal/api/... -run TestHistory -v
          Expected: PASS — 测试通过 srv.ServeHTTP 路由
          [VERIFIED: 2026-04-18 — API tests 全部 PASS]

    summary:
      total_defects: 4
      template_defects: 1
      test_project_defects: 3
      all_fixed: true
      python_defects: 0
      notes: |
        Phase 33 (Python) E2E 测试未发现任何缺陷。
        所有 4 个 Go 缺陷在 Phase 32 执行过程中已修复并通过验证。
        Phase 34 的核心工作是正式化这些修复记录并进行模板编译验证。
    ```

    注意事项：
    - 不要使用中文以外的语言编写 YAML 内容中的 description 和 steps_to_reproduce
    - 确保 YAML 格式正确，可被 Python yaml.safe_load 解析
    - 每个 defect 条目的字段必须与 RESEARCH.md Pattern 1 示例和 D-04 要求一致
  </action>
  <verify>
    <automated>python -c "import yaml; d = yaml.safe_load(open('.planning/phases/34-single-lang-fix/34-DEFECTS.yaml', encoding='utf-8')); assert len(d['defects']) == 4; ids = [x['id'] for x in d['defects']]; assert ids == ['DEV-01', 'DEV-02', 'DEV-03', 'DEV-04']; print('PASS: 4 defects found'); [assert all(k in d for k in ['id','description','category','steps_to_reproduce','expected','actual','status','fixed_in','verification']) for d in d['defects']]; print('PASS: all required fields present')"</automated>
  </verify>
  <done>
    34-DEFECTS.yaml 存在于 .planning/phases/34-single-lang-fix/ 目录，包含 4 个缺陷条目（DEV-01 到 DEV-04），YAML 可被 Python 正确解析，所有条目 status 为 fixed 且包含 fixed_in 和 verification 字段。
  </done>
</task>

</tasks>

<verification>
1. 34-DEFECTS.yaml 存在且可被 Python yaml.safe_load 正确解析
2. 包含 4 个 defect 条目，id 分别为 DEV-01, DEV-02, DEV-03, DEV-04
3. 每个条目包含所有必需字段：id, description, category, steps_to_reproduce, expected, actual, status, fixed_in, verification
4. 所有条目 status 为 "fixed"
5. summary 部分包含 total_defects, template_defects, test_project_defects, all_fixed, python_defects 字段
</verification>

<success_criteria>
- 34-DEFECTS.yaml 可被 Python yaml.safe_load 解析，零错误
- 4 个缺陷条目完整记录，包含问题描述、复现步骤、预期/实际行为
- 文件格式规范，便于后续自动化工具读取
</success_criteria>

<output>
After completion, create `.planning/phases/34-single-lang-fix/34-01-SUMMARY.md`
</output>
