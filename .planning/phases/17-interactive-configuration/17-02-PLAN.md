---
phase: 17-interactive-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [installer/src/configurators/git-ssh.js, installer/src/configurators/git-user.js, installer/src/configurators/index.js, installer/src/i18n/en.json, installer/src/i18n/zh.json]
autonomous: true
requirements: [CONF-05, CONF-06, CONF-07]
user_setup: []

must_haves:
  truths:
    - "Installer detects Git SSH configuration (core.sshCommand)"
    - "User receives guidance for Git SSH configuration if not set"
    - "User can skip Git SSH configuration (optional feature)"
    - "Installer detects Git user.name and user.email settings"
    - "User can input Git user info through interactive prompts if not set"
    - "Git user info is configured globally"
    - "Git user info is required - user cannot skip"
  artifacts:
    - path: "installer/src/configurators/git-ssh.js"
      provides: "Git SSH detection and guidance"
      exports: ["detectGitSSH", "configureGitSSH"]
    - path: "installer/src/configurators/git-user.js"
      provides: "Git user.name and user.email configuration"
      exports: ["detectGitUser", "configureGitUser"]
    - path: "installer/src/configurators/index.js"
      provides: "Updated orchestrator with Git configurators"
      contains: "configureGitSSH, configureGitUser"
    - path: "installer/src/i18n/en.json"
      provides: "English translations for Git config messages"
      contains: "gitSSH.*, gitUser.*"
    - path: "installer/src/i18n/zh.json"
      provides: "Chinese translations for Git config messages"
      contains: "gitSSH.*, gitUser.*"
  key_links:
    - from: "installer/src/configurators/git-ssh.js"
      to: "git config"
      via: "execa('git', ['config', '--get', 'core.sshCommand'])"
      pattern: "git.*config.*--get.*core\\.sshCommand"
    - from: "installer/src/configurators/git-user.js"
      to: "git config"
      via: "execa('git', ['config', '--global', '--get', 'user.name'])"
      pattern: "git.*config.*--global.*user\\.(name|email)"
    - from: "installer/src/configurators/git-user.js"
      to: "git config --global"
      via: "execa('git', ['config', '--global', 'user.name', value])"
      pattern: "git.*config.*--global.*user\\.(name|email).*value"
---

<objective>
Implement Git SSH and Git user configuration detection and interactive guidance.

Purpose: Detect and guide users through Git SSH setup, and ensure Git user.name/email are configured for commits.
Output: Working Git configurator modules integrated into installer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-interactive-configuration/17-CONTEXT.md
@.planning/phases/17-interactive-configuration/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From installer/src/configurators/pushover.js (Plan 01):
```javascript
// Pattern: Configurator returns {status: 'configured'|'skipped'|'failed', details: string}
async function configurePushover(maxRetries = 3) {
  // ...
  return { status: 'configured', details: 'validated and saved' };
}
```

From installer/src/detectors/git.js:
```javascript
// Pattern: Use execa with reject: false for optional commands
const { stdout } = await execa('git', ['--version'], { reject: false });
```

Git config patterns:
```javascript
// Get config value
await execa('git', ['config', '--global', '--get', 'user.name'], { reject: false });
// Set config value
await execa('git', ['config', '--global', 'user.name', userName]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Git SSH configurator module</name>
  <files>installer/src/configurators/git-ssh.js</files>
  <read_first>
    - installer/src/installers/pip-installer.js (execa error handling pattern)
    - installer/src/configurators/pushover.js (configurator pattern)
    - installer/src/i18n/index.js (t() function for i18n)
  </read_first>
  <behavior>
    - Test 1: detectGitSSH() returns {configured: true, command: '...'} when core.sshCommand is set
    - Test 2: detectGitSSH() returns {configured: false, command: null} when core.sshCommand is not set
    - Test 3: configureGitSSH() returns {status: 'configured'} when already set
    - Test 4: configureGitSSH() returns {status: 'skipped'} when user chooses to skip
  </behavior>
  <action>
Create installer/src/configurators/git-ssh.js with the following exports:

1. **detectGitSSH()** - Async function:
   - Use `execa('git', ['config', '--get', 'core.sshCommand'], { reject: false })`
   - If stdout exists and is non-empty: return `{configured: true, command: stdout.trim()}`
   - Else: return `{configured: false, command: null}`

2. **configureGitSSH()** - Async function:
   - Call detectGitSSH() to check current state
   - If configured:
     - Show "Git SSH is configured" (green)
     - Show the current command (gray)
     - Return `{status: 'configured', details: command}`
   - Show "Git SSH is not configured" (yellow)
   - Show guidance text (use t() for i18n):
     ```
     Git SSH is not configured. SSH is recommended for Git remote operations.

     Configuration steps:
     1. Install TortoiseGit (includes Pageant)
     2. Generate SSH key (e.g., using PuTTYgen)
     3. Add public key to remote repository (GitHub/GitLab)
     4. Configure Git to use TortoiseGit SSH:
        git config --global core.sshCommand "C:/Program Files/TortoiseGit/bin/TortoisePlink.exe"

     Documentation: https://work-skills.example.com/docs/git-ssh-setup
     ```
   - Prompt "Skip Git SSH configuration? (You can use HTTPS instead)" (Confirm, initial: true)
   - If Yes (skip): show "Git SSH configuration skipped" (yellow), return `{status: 'skipped', details: 'user skipped'}`
   - If No: return `{status: 'configured', details: 'guidance shown'}` (user will configure manually)

Use require statements:
```javascript
'use strict';
const { Confirm } = require('enquirer');
const execa = require('execa');
const chalk = require('chalk');
const { t } = require('../i18n/index.js');
```
  </action>
  <verify>
    <automated>node -e "const {detectGitSSH, configureGitSSH} = require('./installer/src/configurators/git-ssh.js'); console.log('Git SSH exports OK:', typeof detectGitSSH, typeof configureGitSSH);"</automated>
  </verify>
  <done>
    - installer/src/configurators/git-ssh.js exists with 2 exported functions
    - detectGitSSH() uses git config --get core.sshCommand
    - configureGitSSH() shows guidance and allows skip
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Git user configurator module</name>
  <files>installer/src/configurators/git-user.js</files>
  <read_first>
    - installer/src/configurators/pushover.js (Input prompt pattern, configurator return format)
    - installer/src/i18n/index.js (t() function for i18n)
  </read_first>
  <behavior>
    - Test 1: detectGitUser() returns {name: 'Alice', email: 'alice@example.com'} when both are set
    - Test 2: detectGitUser() returns {name: null, email: null} when neither is set
    - Test 3: detectGitUser() returns {name: 'Alice', email: null} when only name is set
    - Test 4: configureGitUser() returns {status: 'configured', name: 'Alice', email: 'a@b.c'} on success
    - Test 5: configureGitUser() returns {status: 'failed'} on git config error
  </behavior>
  <action>
Create installer/src/configurators/git-user.js with the following exports:

1. **detectGitUser()** - Async function:
   - Use `execa('git', ['config', '--global', '--get', 'user.name'], { reject: false })`
   - Use `execa('git', ['config', '--global', '--get', 'user.email'], { reject: false })`
   - Return `{name: stdout1 || null, email: stdout2 || null}`

2. **configureGitUser()** - Async function:
   - Call detectGitUser() to check current state
   - If both name AND email are set:
     - Show "Git user information already configured" (green)
     - Show current values (gray)
     - Return `{status: 'configured', name: currentName, email: currentEmail}`
   - Show "Git user information is required for commits" (yellow)
   - Prompt for user.name: "Enter your Git username (for commits):" (Input)
   - Prompt for user.email: "Enter your Git email address:" (Input)
   - Set config using:
     - `await execa('git', ['config', '--global', 'user.name', userName])`
     - `await execa('git', ['config', '--global', 'user.email', userEmail])`
   - Wrap in try-catch:
     - On success: show "Git user information configured" (green), return `{status: 'configured', name: userName, email: userEmail}`
     - On error: show "Failed to configure Git user information" (red) + error message (gray), return `{status: 'failed'}`

Use require statements:
```javascript
'use strict';
const { Input } = require('enquirer');
const execa = require('execa');
const chalk = require('chalk');
const { t } = require('../i18n/index.js');
```
  </action>
  <verify>
    <automated>node -e "const {detectGitUser, configureGitUser} = require('./installer/src/configurators/git-user.js'); console.log('Git User exports OK:', typeof detectGitUser, typeof configureGitUser);"</automated>
  </verify>
  <done>
    - installer/src/configurators/git-user.js exists with 2 exported functions
    - detectGitUser() uses git config --global --get for user.name and user.email
    - configureGitUser() prompts for input and sets config globally
    - Git user info is required (no skip option)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add i18n translations for Git configuration</name>
  <files>installer/src/i18n/en.json, installer/src/i18n/zh.json</files>
  <read_first>
    - installer/src/i18n/en.json (existing structure, find where to add keys)
    - installer/src/i18n/zh.json (existing structure)
    - installer/src/configurators/git-ssh.js (to see which t() keys are used)
    - installer/src/configurators/git-user.js (to see which t() keys are used)
  </read_first>
  <action>
Add the following keys to installer/src/i18n/en.json:

```json
  "gitSSH.configured": "Git SSH is configured",
  "gitSSH.notConfigured": "Git SSH is not configured",
  "gitSSH.recommended": "SSH is recommended for Git remote operations",
  "gitSSH.guidance": "To configure Git SSH:",
  "gitSSH.step1": "1. Install TortoiseGit (includes Pageant)",
  "gitSSH.step2": "2. Generate SSH key (e.g., using PuTTYgen)",
  "gitSSH.step3": "3. Add public key to remote repository (GitHub/GitLab)",
  "gitSSH.step4": "4. Configure Git to use TortoiseGit SSH:",
  "gitSSH.command": "   git config --global core.sshCommand \"C:/Program Files/TortoiseGit/bin/TortoisePlink.exe\"",
  "gitSSH.docs": "Documentation: https://work-skills.example.com/docs/git-ssh-setup",
  "gitSSH.promptSkip": "Skip Git SSH configuration? (You can use HTTPS instead)",
  "gitSSH.skipped": "Git SSH configuration skipped",
  "gitUser.alreadyConfigured": "Git user information already configured",
  "gitUser.required": "Git user information is required for commits",
  "gitUser.promptName": "Enter your Git username (for commits):",
  "gitUser.promptEmail": "Enter your Git email address:",
  "gitUser.configured": "Git user information configured",
  "gitUser.failed": "Failed to configure Git user information"
```

Add the corresponding Chinese translations to installer/src/i18n/zh.json:

```json
  "gitSSH.configured": "Git SSH ",
  "gitSSH.notConfigured": "Git SSH ",
  "gitSSH.recommended": " SSH  Git ",
  "gitSSH.guidance": " Git SSH :",
  "gitSSH.step1": "1.  TortoiseGit ( Pageant)",
  "gitSSH.step2": "2.  SSH  (  PuTTYgen)",
  "gitSSH.step3": "3.     (GitHub/GitLab)",
  "gitSSH.step4": "4.  Git  TortoiseGit  SSH:",
  "gitSSH.command": "   git config --global core.sshCommand \"C:/Program Files/TortoiseGit/bin/TortoisePlink.exe\"",
  "gitSSH.docs": ": https://work-skills.example.com/docs/git-ssh-setup",
  "gitSSH.promptSkip": " Git SSH ? ( HTTPS)",
  "gitSSH.skipped": "Git SSH ",
  "gitUser.alreadyConfigured": "Git ",
  "gitUser.required": "Git ",
  "gitUser.promptName": " Git :",
  "gitUser.promptEmail": " Git :",
  "gitUser.configured": "Git ",
  "gitUser.failed": "Git "
```
  </action>
  <verify>
    <automated>node -e "const en = require('./installer/src/i18n/en.json'); const zh = require('./installer/src/i18n/zh.json'); const keys = ['gitSSH.configured', 'gitSSH.notConfigured', 'gitSSH.promptSkip', 'gitUser.alreadyConfigured', 'gitUser.promptName', 'gitUser.promptEmail']; keys.forEach(k => { if (!en[k] || !zh[k]) throw new Error('Missing key: ' + k); }); console.log('Git i18n keys verified:', keys.length);"</automated>
  </verify>
  <done>
    - en.json contains all gitSSH.* and gitUser.* keys
    - zh.json contains matching keys with Chinese translations
    - No JSON syntax errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Update configurator index to include Git configurators</name>
  <files>installer/src/configurators/index.js</files>
  <read_first>
    - installer/src/configurators/index.js (current state from Plan 01)
    - installer/src/configurators/git-ssh.js (configureGitSSH function)
    - installer/src/configurators/git-user.js (configureGitUser function)
  </read_first>
  <action>
Update installer/src/configurators/index.js:

1. Add imports:
```javascript
const { configureGitSSH } = require('./git-ssh.js');
const { configureGitUser } = require('./git-user.js');
```

2. Update runAllConfigurators() to include Git steps after Pushover:

```javascript
async function runAllConfigurators() {
  const results = [];

  // 1. Pushover (optional)
  console.log(chalk.bold('\n' + t('config.section.pushover')));
  const pushoverResult = await configurePushover();
  results.push({ name: 'Pushover', status: pushoverResult.status, details: pushoverResult.details });

  // 2. Git SSH (optional)
  console.log(chalk.bold('\n' + t('config.section.gitSSH')));
  const sshResult = await configureGitSSH();
  results.push({ name: 'Git SSH', status: sshResult.status, details: sshResult.details });

  // 3. Git user.info (required)
  console.log(chalk.bold('\n' + t('config.section.gitUser')));
  const userResult = await configureGitUser();
  results.push(
    { name: 'Git user.name', status: userResult.status, details: userResult.name },
    { name: 'Git user.email', status: userResult.status, details: userResult.email }
  );

  // Display summary
  displayConfigSummary(results);
}
```
  </action>
  <verify>
    <automated>node -e "const {runAllConfigurators} = require('./installer/src/configurators/index.js'); console.log('Updated configurator exports OK:', typeof runAllConfigurators);"</automated>
  </verify>
  <done>
    - installer/src/configurators/index.js imports and uses configureGitSSH and configureGitUser
    - runAllConfigurators() runs all 3 configurators in sequence
    - Summary displays all configuration items
  </done>
</task>

</tasks>

<verification>
Run the installer manually to verify Git configuration flow:
1. `cd installer && npm start`
2. Verify Git SSH section appears after Pushover
3. Verify Git SSH shows guidance if not configured
4. Verify user can skip Git SSH configuration
5. Verify Git user section appears after Git SSH
6. Verify Git user prompts for name and email if not configured
7. Verify summary table shows all configuration items
</verification>

<success_criteria>
- User sees Git SSH configuration section after Pushover
- User sees guidance text if Git SSH is not configured
- User can skip Git SSH configuration
- User sees Git user configuration section
- User is prompted for name and email if not configured
- Git user configuration cannot be skipped (required)
- Configuration summary shows all items with correct status
</success_criteria>

<output>
After completion, create `.planning/phases/17-interactive-configuration/17-02-SUMMARY.md`
</output>
