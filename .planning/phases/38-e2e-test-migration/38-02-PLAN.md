---
phase: 38-e2e-test-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - R1
  - R3

must_haves:
  truths:
    - "tmp/ 目录中 5 个测试项目已删除，tmp/ 目录保留为空（含 .gitkeep）"
    - "tests/e2e/codepoint-v2/ 项目在删除后仍可正常构建"
    - ".planning/PROJECT.md 中 R1 状态更新为已完成"
    - ".planning/STATE.md 中 tmp/ 目录迁移条目更新"
    - ".planning/REQUIREMENTS.md 中 R1 checklist items 已勾选"
    - ".planning/ROADMAP.md Phase 38 已更新 plan 数量和状态"
    - "活跃 .planning/ 文档中无过时 tmp/ 引用（排除 milestones/）"
    - "活跃 .planning/ 文档中包含 tests/e2e/codepoint-v2/ 新路径引用"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated project status with migration complete"
      contains: "[x] R1:"
    - path: ".planning/STATE.md"
      provides: "Updated state with migration complete"
      contains: "迁移已完成"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements checklist"
      contains: "- [x]"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "tests/e2e/codepoint-v2/"
      via: "path reference update"
      pattern: "tests/e2e/codepoint-v2/"
---

<objective>
验证 tests/e2e/codepoint-v2/ 项目功能正常后清理 tmp/ 目录，全面搜索并更新所有活跃文档中对 tmp/ 路径的引用，标记 R1/R3 需求为完成。

Purpose: 迁移完成后需要验证项目完整性，然后清理源目录并更新文档，使项目状态与实际文件结构一致。归档文档（.planning/milestones/）不更新（保持历史准确性）。

Output: tmp/ 目录中测试项目已删除（tmp/ 保留为空目录），tests/e2e/ 项目重新验证构建成功，PROJECT.md / STATE.md / REQUIREMENTS.md / ROADMAP.md 更新完成，新路径引用已验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-e2e-test-migration/38-RESEARCH.md
@.planning/phases/38-e2e-test-migration/38-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify migrated projects build, then clean tmp/ directory</name>
  <files>tmp/go-calculator/, tmp/gojs-calculator/, tmp/python-calculator/, tmp/pyts-calculator/, tmp/template-test/</files>
  <read_first>
    - .planning/phases/38-e2e-test-migration/38-01-SUMMARY.md (confirm Plan 01 completed successfully before cleaning)
  </read_first>
  <acceptance_criteria>
    - `cd C:/WorkSpace/agent/work-skills/tests/e2e/codepoint-v2/go-calculator && go build ./...` 成功（exit 0）
    - `cd C:/WorkSpace/agent/work-skills/tests/e2e/codepoint-v2/gojs-calculator && go build ./...` 成功（exit 0）
    - `ls tests/e2e/codepoint-v2/` 返回 5 个项目目录
    - `test -d tmp/go-calculator` 返回 false
    - `test -d tmp/gojs-calculator` 返回 false
    - `test -d tmp/python-calculator` 返回 false
    - `test -d tmp/pyts-calculator` 返回 false
    - `test -d tmp/template-test` 返回 false
    - `test -d tmp` 返回 true（tmp/ 目录本身保留）
  </acceptance_criteria>
  <action>
    **Step 1: Verify Plan 01 completed successfully**

    Addresses review concern (LOW: partial failure guard): Read 38-01-SUMMARY.md and check for any indication of partial failure. Proceed ONLY if the summary confirms full success (all 5 projects migrated, builds verified, committed). If the summary indicates any failure or partial completion, STOP and report -- do not delete tmp/.

    Then verify on disk:
    ```bash
    ls tests/e2e/codepoint-v2/
    # Must show: go-calculator  gojs-calculator  python-calculator  pyts-calculator  template-test
    ```

    If any project is missing, STOP and report -- do not delete tmp/.

    **Step 2: Re-verify builds BEFORE deleting tmp/**

    Addresses review concern: verify projects still function correctly in their new location before irreversible deletion. Using absolute paths with cd to ensure correct working directory.

    ```bash
    # Verify go-calculator builds
    cd C:/WorkSpace/agent/work-skills/tests/e2e/codepoint-v2/go-calculator && go build ./...
    if [ $? -ne 0 ]; then echo "FATAL: go-calculator build failed after migration"; exit 1; fi
    cd C:/WorkSpace/agent/work-skills

    # Verify gojs-calculator builds (includes frontend/dist/ embed check)
    cd C:/WorkSpace/agent/work-skills/tests/e2e/codepoint-v2/gojs-calculator && go build ./...
    if [ $? -ne 0 ]; then echo "FATAL: gojs-calculator build failed after migration"; exit 1; fi
    cd C:/WorkSpace/agent/work-skills

    echo "PASS: Both Go projects build successfully in new location"
    ```

    **Step 3: Pre-deletion listing -- confirm tmp/ contents**

    Addresses review concern (MEDIUM: no pre-deletion listing): Before deleting anything, list tmp/ contents to confirm the 5 expected directories exist and identify any unexpected content that should NOT be deleted.

    ```bash
    ls tmp/
    # Expected: go-calculator  gojs-calculator  python-calculator  pyts-calculator  template-test
    # May also contain: other files/dirs NOT part of this migration
    ```

    Verify all 5 project names match. If any expected directory is missing (already deleted or never existed), investigate before proceeding.

    **Step 4: Clean tmp/ directory**
    Remove only the 5 migrated test project directories (not any other tmp/ content):
    ```bash
    rm -rf tmp/go-calculator
    rm -rf tmp/gojs-calculator
    rm -rf tmp/python-calculator
    rm -rf tmp/pyts-calculator
    rm -rf tmp/template-test
    ```

    Note: Use targeted `rm -rf` for each project, NOT `rm -rf tmp/*`. The tmp/ directory may contain other files not part of this migration.

    **Step 5: Verify tmp/ final state**

    Addresses review concern (MEDIUM: tmp/ final state): After removing the 5 directories, verify what remains. The tmp/ directory itself is kept (CLAUDE.md designates it as the project's temporary directory). If tmp/ is now empty, that is expected and correct.

    ```bash
    ls tmp/ 2>/dev/null
    # Should show no test project directories
    # tmp/ directory itself should still exist (it is the designated temp dir per CLAUDE.md)
    # If tmp/ is empty, that is correct -- no action needed
    ```

    **Step 6: Commit cleanup**
    ```bash
    git add -A tmp/
    git commit -m "$(cat <<'EOF'
chore(38): clean tmp/ after E2E test project migration

Remove migrated test projects from tmp/:
- go-calculator, python-calculator, template-test (were untracked)
- gojs-calculator, pyts-calculator (were tracked, now at new location)

Projects verified building in tests/e2e/codepoint-v2/ before deletion.
tmp/ directory retained (empty) as designated temp directory per CLAUDE.md.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
    )"
    ```
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && ! test -d tmp/go-calculator && ! test -d tmp/gojs-calculator && ! test -d tmp/python-calculator && ! test -d tmp/pyts-calculator && ! test -d tmp/template-test && test -d tmp && ls tests/e2e/codepoint-v2/ | grep -q go-calculator && ls tests/e2e/codepoint-v2/ | grep -q gojs-calculator && cd tests/e2e/codepoint-v2/go-calculator && go build ./... && cd C:/WorkSpace/agent/work-skills && cd tests/e2e/codepoint-v2/gojs-calculator && go build ./... && cd C:/WorkSpace/agent/work-skills && echo "PASS: tmp/ cleaned (5 dirs removed, tmp/ retained), projects verified in new location"</automated>
  </verify>
  <done>Go 项目构建重新验证通过（go-calculator, gojs-calculator）；tmp/ 删除前内容已列出确认；5 个测试项目目录已从 tmp/ 删除；tmp/ 目录本身保留（空）；变更已提交</done>
</task>

<task type="auto">
  <name>Task 2: Search all active .planning/ files for tmp/ references, then update documentation</name>
  <files>.planning/PROJECT.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/PROJECT.md (entire file -- contains tmp/ references at lines 113, 140)
    - .planning/STATE.md (entire file -- contains tmp/ reference at line 66)
    - .planning/REQUIREMENTS.md (entire file -- contains tmp/ references at multiple lines)
    - .planning/ROADMAP.md (entire file -- Phase 38 entry needs plan count update)
  </read_first>
  <acceptance_criteria>
    - `grep -rn "tmp/" .planning/ --include="*.md" | grep -v milestones/ | grep -v "38-RESEARCH.md" | grep -v "38-REVIEWS.md" | grep -v "38-01-PLAN.md" | grep -v "38-02-PLAN.md" | grep -v "historical" | grep -c "tmp/"` returns 0 (no stale tmp/ references in active docs)
    - `grep -rn "tests/e2e/codepoint-v2/" .planning/ --include="*.md" | grep -v milestones/ | grep -v "38-RESEARCH.md" | grep -v "38-REVIEWS.md" | grep -v "38-01-PLAN.md" | grep -v "38-02-PLAN.md" | grep -c "tests/e2e/codepoint-v2/"` returns at least 3 matches (positive assertion: new path references exist in updated docs)
    - `.planning/PROJECT.md` line 113: `- [x] R1: E2E 测试项目已从 tmp/ 迁移到 tests/e2e/codepoint-v2/ (Phase 38)`（checked, past tense）
    - `.planning/PROJECT.md` line 140: text updated to reflect migration is complete (not "待从")
    - `.planning/STATE.md` line 66: `- tmp/ 目录迁移已完成` (replacing "迁移完成后清空")
    - `.planning/REQUIREMENTS.md`: all R1 checklist items marked `[x]`
    - `.planning/REQUIREMENTS.md`: R3 checklist items marked `[x]`
    - `.planning/ROADMAP.md`: Phase 38 entry updated with plan details
    - `grep -c "待从 tmp/" .planning/PROJECT.md` returns 0
    - `grep -c "迁移完成后清空" .planning/STATE.md` returns 0
  </acceptance_criteria>
  <action>
    **IMPORTANT: Only update ACTIVE docs. Do NOT touch .planning/milestones/ (926 tmp/ references are historical records).**

    **Step 1: Comprehensive search for tmp/ references**

    Addresses review concern: search ALL active .planning/ files (excluding milestones/) before updating, not just the 4 known files. This grep pre-step ensures full scope coverage before any edits are made. The 4 target files are straightforward known-quantity edits with specific line numbers, so a single task is appropriate -- the grep is a safety net, not a complex discovery process.

    ```bash
    grep -rn "tmp/" .planning/ --include="*.md" | grep -v milestones/ | grep -v "38-RESEARCH.md" | grep -v "38-REVIEWS.md" | grep -v "38-01-PLAN.md" | grep -v "38-02-PLAN.md"
    ```

    Review every match. Expected files with tmp/ references:
    - `.planning/PROJECT.md` (lines 113, 140)
    - `.planning/STATE.md` (line 66)
    - `.planning/REQUIREMENTS.md` (lines 16, 18, 24, 25, 59, 61)

    If any OTHER active files contain tmp/ references (e.g., in `phases/` subdirectories other than milestones/), update those too.

    **Step 2: Update .planning/PROJECT.md**
    - Line 113: Change `- [ ] R1: E2E 测试项目从 \`tmp/\` 迁移到 \`tests/e2e/codepoint-v2/\` (Phase 38)` to `- [x] R1: E2E 测试项目已从 \`tmp/\` 迁移到 \`tests/e2e/codepoint-v2/\` (Phase 38)`
    - Line 115: Change `- [ ] R3: 目录结构标准化 (Phase 38-39)` to `- [x] R3: 目录结构标准化 (Phase 38)` (R3 is complete after migration)
    - Line 140: Change `- E2E 测试项目待从 tmp/ 迁移到 tests/e2e/codepoint-v2/` to `- E2E 测试项目已迁移到 tests/e2e/codepoint-v2/（5 个项目：go-calculator, python-calculator, gojs-calculator, pyts-calculator, template-test）`
    - Line 138: Update test count line: change `E2E 验证项目` to mention the new location

    **Step 3: Update .planning/STATE.md**
    - Line 66: Change `- tmp/ 目录迁移完成后清空` to `- tmp/ 目录迁移已完成（Phase 38）`
    - Line 33: Change `Status: Milestone planned, awaiting /gsd-plan-phase 38` to appropriate status
    - Line 34: Remove or update "Last activity" if needed

    **Step 4: Update .planning/REQUIREMENTS.md**
    Mark R1 items as complete:
    - Line 18: Change `- [ ] 将 \`tmp/\` 下 5 个测试项目迁移到 \`tests/e2e/codepoint-v2/\`:` to `- [x]`
    - Lines 19-23: Change each `- [ ]` to `- [x]` for the 5 project items
    - Line 24: Change `- [ ] 清理 tmp/ 目录（确认迁移完成后清空）` to `- [x]`
    - Line 25: Change `- [ ] 更新项目中任何引用 tmp/ 路径的文档或配置` to `- [x]`

    Mark R3 items as complete:
    - Line 43: Change `- [ ] \`tests/e2e/codepoint-v2/\` 目录命名与版本对应` to `- [x]`
    - Line 44: Change `- [ ] 每个测试项目保持独立完整性（可独立运行）` to `- [x]`
    - Line 45: Change `- [ ] \`docs/research/codepoint/\` 目录以日期命名追加文档` to leave as `[ ]` (this is Phase 39 scope)

    **Step 5: Update .planning/ROADMAP.md**
    - Phase 38 line: Update to show plan details:
      Change `- [ ] Phase 38: E2E 测试项目迁移 (2 plans)` to:
      ```
      - [ ] Phase 38: E2E 测试项目迁移 (2 plans)
        - [ ] 38-01-PLAN.md -- 迁移 5 个测试项目到 tests/e2e/codepoint-v2/
        - [ ] 38-02-PLAN.md -- 清理 tmp/ 并更新文档引用
      ```

    **Step 6: Final verification -- no stale tmp/ references AND positive assertion for new paths**

    Addresses review concern (LOW: positive assertion for new paths): In addition to checking that no stale references remain, verify that the new path `tests/e2e/codepoint-v2/` IS present in updated docs.

    ```bash
    # Negative check: no stale "tmp/ is current location" references
    grep -rn "tmp/" .planning/ --include="*.md" | grep -v milestones/ | grep -v "38-RESEARCH.md" | grep -v "38-REVIEWS.md" | grep -v "38-01-PLAN.md" | grep -v "38-02-PLAN.md"

    # Positive check: new path references exist
    grep -rn "tests/e2e/codepoint-v2/" .planning/ --include="*.md" | grep -v milestones/ | grep -v "38-RESEARCH.md" | grep -v "38-REVIEWS.md" | grep -v "38-01-PLAN.md" | grep -v "38-02-PLAN.md"
    ```

    Remaining tmp/ references should ONLY be:
    - Historical/rationale text (e.g., "从 tmp/ 迁移" -- this is correct past tense, not a stale reference)
    - .planning/PROJECT.md line 113: `[x] R1: ...已从 tmp/ 迁移...` (completed status referencing old location)
    - .planning/REQUIREMENTS.md: rationale text explaining why tmp/ was temporary (lines 16, 59, 61)

    These are acceptable -- they describe the migration that happened, not a stale "tmp/ is the current location" reference.

    Positive check should show at least 3 matches in PROJECT.md, REQUIREMENTS.md, and ROADMAP.md confirming `tests/e2e/codepoint-v2/` is referenced.

    **Step 7: Commit documentation updates**
    ```bash
    git add .planning/PROJECT.md .planning/STATE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md
    git commit -m "$(cat <<'EOF'
docs(38): update active documentation for E2E test migration

- Mark R1/R3 requirements as complete in PROJECT.md and REQUIREMENTS.md
- Update STATE.md migration status
- Add plan details to ROADMAP.md Phase 38 entry
- All active .planning/ docs audited for stale tmp/ references
- Positive verification: tests/e2e/codepoint-v2/ references confirmed

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
    )"
    ```
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && grep -q "\- \[x\] R1:" .planning/PROJECT.md && grep -q "已迁移到 tests/e2e" .planning/PROJECT.md && grep -q "迁移已完成" .planning/STATE.md && grep -q "\- \[x\] 将" .planning/REQUIREMENTS.md && grep -q "38-01-PLAN.md" .planning/ROADMAP.md && grep -rn "tests/e2e/codepoint-v2/" .planning/ --include="*.md" | grep -v milestones/ | grep -v "38-RESEARCH.md" | grep -v "38-REVIEWS.md" | grep -v "38-01-PLAN.md" | grep -v "38-02-PLAN.md" | wc -l | grep -qv "^0$" && echo "PASS: All docs updated, no stale tmp/ references, new path references confirmed"</automated>
  </verify>
  <done>PROJECT.md R1/R3 标记完成，路径引用更新；STATE.md 迁移状态更新；REQUIREMENTS.md R1/R3 checklist items 勾选；ROADMAP.md Phase 38 计划详情添加；全 .planning/ 活跃文档搜索确认无遗漏引用；tests/e2e/codepoint-v2/ 新路径引用正面断言通过；变更已提交</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- documentation updates only.

## STRIDE Threat Register

No threats identified. This task updates planning documents and cleans temporary directories. No code changes, no network operations, no security-relevant operations.
</threat_model>

<verification>
1. `ls tmp/` 不包含 5 个测试项目目录
2. `test -d tmp` 返回 true（tmp/ 目录保留）
3. `ls tests/e2e/codepoint-v2/` 包含 5 个项目
4. `cd tests/e2e/codepoint-v2/go-calculator && go build ./...` 成功（删除后重新验证）
5. `cd tests/e2e/codepoint-v2/gojs-calculator && go build ./...` 成功（删除后重新验证）
6. `grep -c "待从 tmp/" .planning/PROJECT.md` 返回 0
7. `grep -c "\[x\]" .planning/REQUIREMENTS.md` 包含 R1 和 R3 的完成标记
8. `.planning/ROADMAP.md` Phase 38 包含计划详情
9. `grep -rn "tmp/" .planning/ --include="*.md" | grep -v milestones/ | grep -v "38-RESEARCH.md" | grep -v "38-REVIEWS.md" | grep -v "38-01-PLAN.md" | grep -v "38-02-PLAN.md"` 无过时引用（仅保留历史描述性文字）
10. `grep -rn "tests/e2e/codepoint-v2/" .planning/ --include="*.md" | grep -v milestones/ | grep -v "38-RESEARCH.md" | grep -v "38-REVIEWS.md" | grep -v "38-01-PLAN.md" | grep -v "38-02-PLAN.md" | wc -l` 返回至少 3（正面断言：新路径引用存在）
</verification>

<success_criteria>
- Go 项目在 tmp/ 删除后构建验证通过（go-calculator, gojs-calculator）
- tmp/ 中 5 个测试项目目录已删除
- tmp/ 目录本身保留（空目录，按 CLAUDE.md 为指定临时目录）
- PROJECT.md R1 标记为 [x]，tmp/ 引用更新为迁移完成状态
- STATE.md 迁移状态更新
- REQUIREMENTS.md R1/R3 checklist 全部勾选
- ROADMAP.md Phase 38 包含 2 个计划的详情
- 全 .planning/ 活跃文档搜索确认无遗漏 tmp/ 引用
- tests/e2e/codepoint-v2/ 新路径在活跃文档中有正面断言（至少 3 处引用）
- .planning/milestones/ 中文档未被修改（历史记录保持不变）
- 所有变更已提交（3 个 commit: migration + cleanup + docs）
</success_criteria>

<output>
After completion, create `.planning/phases/38-e2e-test-migration/38-02-SUMMARY.md`
</output>
