---
phase: 36-pyts-fullstack-calc
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - tmp/pyts-calculator/tests/test_integration.py
autonomous: true
requirements: [FULL-06, FULL-07]

must_haves:
  truths:
    - "collector 端点正确收集前端 TS 探针数据，写入 cp-ts-*.log"
    - "Python 后端探针写入 cp-python-*.log，包含 flow_id"
    - "同一跨语言代码点在不同业务流程下输出不同的堆栈和 flow_id"
    - "Toggle 四组合独立控制：两个都开/只Python/只TS/两个都关，日志输出正确"
    - "Toggle 切换后重启服务立即生效"
  artifacts:
    - path: "tmp/pyts-calculator/tests/test_integration.py"
      provides: "跨语言联动集成测试 + Toggle 验证逻辑"
    - path: "~/.codepoint/pyts-calculator/cp-python-*.log"
      provides: "Python 后端探针日志（三业务流）"
    - path: "~/.codepoint/pyts-calculator/cp-ts-*.log"
      provides: "前端 TS 探针日志（通过 collector）"
  key_links:
    - from: "Browser probe (codepoint.ts)"
      to: "/__codepoint__"
      via: "fetch POST"
      pattern: "sendToCollector"
    - from: "/__codepoint__"
      to: "cp-ts-*.log"
      via: "collector.receive()"
      pattern: "receive(entry)"
    - from: "api/server.py routes"
      to: "cp-python-*.log"
      via: "point_json()"
      pattern: "point_json"
    - from: "Toggle .codepoint-python"
      to: "cp-python-*.log output"
      via: "codepoint/__init__.py _enabled"
      pattern: "_enabled: bool"
    - from: "Toggle .codepoint-ts"
      to: "cp-ts-*.log output"
      via: "collector.py _ts_enabled"
      pattern: "_ts_enabled: bool"
---

<objective>
验证跨语言探针联动（前端 TS 探针通过 Python collector 收集）和 Toggle 四组合独立控制。

Purpose: 确认 codepoint 技能在 Python+TS 全栈环境下的跨语言联动正确性，验证 Toggle 机制前后端独立控制（per D-05）。

Output: 集成测试通过，三业务流的跨语言日志确认 flow_id 关联，Toggle 四组合验证完成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-pyts-fullstack-calc/36-CONTEXT.md
@.planning/phases/36-pyts-fullstack-calc/36-RESEARCH.md
@.planning/phases/36-pyts-fullstack-calc/36-01-SUMMARY.md

<interfaces>
<!-- From Task 36-01 outputs -->

From tmp/pyts-calculator/api/server.py:
```python
# FastAPI app with routes:
# POST /__codepoint__ -- collector endpoint
# POST /api/calculate -- flow_id="flow-api-calculate"
# GET /api/history -- list all
# GET /api/history/{id} -- flow_id="flow-history-query", recomputes
# POST /api/batch -- flow_id="flow-batch-process"
# StaticFiles mount at / (last)
```

From tmp/pyts-calculator/codepoint/__init__.py:
```python
_toggle_path: Path = Path.home() / ".codepoint" / ".codepoint-python"
_enabled: bool = _toggle_path.exists()
def point_json(name: str, meta: Optional[dict[str, Any]] = None) -> None:
```

From tmp/pyts-calculator/codepoint/collector.py:
```python
_ts_toggle: Path = Path.home() / ".codepoint" / ".codepoint-ts"
_ts_enabled: bool = _ts_toggle.exists()
def is_ts_enabled() -> bool:
def receive(entry: dict[str, Any]) -> bool:
def close_ts_collector() -> None:
```

From tmp/pyts-calculator/calculator/core.py:
```python
def evaluate(expr: str, flow_id: str = "") -> Tuple[str, str]:
    # 4 probe points: cp-calc-parse, cp-calc-validate, cp-calc-compute, cp-calc-format
```

Toggle file paths:
- .codepoint-python -> controls cp-python-*.log output
- .codepoint-ts -> controls cp-ts-*.log output (via collector)
- Both in ~/.codepoint/ directory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 跨语言联动验证（collector + 多流程堆栈 + flow_id 关联）</name>
  <files>
    tmp/pyts-calculator/tests/test_integration.py
  </files>
  <read_first>
    tmp/pyts-calculator/api/server.py
    tmp/pyts-calculator/codepoint/__init__.py
    tmp/pyts-calculator/codepoint/collector.py
    tmp/pyts-calculator/calculator/core.py
    tmp/pyts-calculator/history/store.py
    tmp/pyts-calculator/main.py
    tmp/pyts-calculator/frontend/src/lib/codepoint.ts
    .planning/phases/35-gojs-fullstack-calc/35-04-SUMMARY.md
  </read_first>
  <action>
    This task verifies FULL-06 (scan/跨语言联动/多流程堆栈验证). Execute these steps in order:

    **Step 1: Ensure both toggle files exist**
    ```bash
    mkdir -p ~/.codepoint
    touch ~/.codepoint/.codepoint-python
    touch ~/.codepoint/.codepoint-ts
    ```

    **Step 2: Clean previous log files**
    Delete any existing cp-python-*.log and cp-ts-*.log in ~/.codepoint/pyts-calculator/

    **Step 3: Start FastAPI backend**
    ```bash
    cd tmp/pyts-calculator && python main.py &
    ```
    Wait for "Uvicorn running on http://0.0.0.0:18091" in stderr output.

    **Step 4: Test API calculate endpoint (flow-api-calculate)**
    ```bash
    curl -s -X POST http://localhost:18091/api/calculate -H "Content-Type: application/json" -d '{"expression":"2+3"}'
    ```
    Verify response contains `"result": "5.0"`.

    **Step 5: Test API history detail (flow-history-query)**
    ```bash
    curl -s http://localhost:18091/api/history/1
    ```
    Verify response contains `"recomputed": "5.0"`.

    **Step 6: Test API batch endpoint (flow-batch-process)**
    ```bash
    curl -s -X POST http://localhost:18091/api/batch -H "Content-Type: application/json" -d '{"expressions":["1+1","2*3","10/2"]}'
    ```
    Verify response contains 3 results.

    **Step 7: Test collector endpoint with simulated frontend probe**
    ```bash
    curl -s -X POST http://localhost:18091/__codepoint__ -H "Content-Type: application/json" -d '{"name":"cp-fe-calc-submit","stack":"Error\n  at Calculator.tsx:15\n  at React.Component.render","meta":{"point_id":"cp-fe-calc-submit","flow_id":"flow-api-calculate"}}'
    ```
    Verify returns 204 (no content).

    **Step 8: Verify cp-python-*.log exists and contains expected data**
    - File exists in ~/.codepoint/pyts-calculator/
    - Contains "cp-calc-parse" with flow_id "flow-api-calculate"
    - Contains "cp-calc-parse" with flow_id "flow-history-query"
    - Contains "cp-calc-parse" with flow_id "flow-batch-process"
    - Same code point (cp-calc-compute) appears with all 3 different flow_ids

    **Step 9: Verify cp-ts-*.log exists and contains collector data**
    - File exists in ~/.codepoint/pyts-calculator/
    - Contains "cp-fe-calc-submit" from the simulated frontend probe

    **Step 10: Verify multi-flow stack differentiation**
    For each flow (api-calculate, history-query, batch-process), the cp-python log must show:
    - Same code points (cp-calc-parse, cp-calc-validate, cp-calc-compute, cp-calc-format)
    - Different flow_id values
    - Different call stacks (one comes from handle_calculate, one from handle_history_get, one from handle_batch)

    **Step 11: Kill FastAPI server**
    Kill the uvicorn process.

    **Step 12: Create test_integration.py**
    Document the verification steps as a pytest integration test. Include:
    - test_collector_receives_probe: POST to /__codepoint__ returns 204 when enabled
    - test_collector_disabled_returns_404: POST to /__codepoint__ returns 404 when toggle absent (requires separate process, document as manual)
    - test_multi_flow_stack_differentiation: verify cp-python log has same code points with different flow_ids
    - test_flow_id_correlation: verify cp-ts log contains the frontend probe with matching flow_id

    Note: The integration tests require a running server. Write them as functions that make HTTP requests to localhost:18091. Mark with `@pytest.mark.integration` so they can be run separately.
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/work-skills/tmp/pyts-calculator" && python -m pytest tests/test_integration.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - cp-python-*.log 包含三业务流（flow-api-calculate, flow-history-query, flow-batch-process）的探针数据
    - cp-ts-*.log 包含通过 collector 收集的前端探针数据
    - 同一代码点 cp-calc-compute 在三个不同 flow_id 下有不同的调用栈
    - test_integration.py 通过
  </done>
  <acceptance_criteria>
    - ~/.codepoint/pyts-calculator/cp-python-*.log exists and contains "flow-api-calculate" and "flow-history-query" and "flow-batch-process"
    - ~/.codepoint/pyts-calculator/cp-python-*.log contains "cp-calc-compute" with at least 2 different flow_id values
    - ~/.codepoint/pyts-calculator/cp-ts-*.log exists and contains "[CODEPOINT] cp-fe-calc-submit"
    - tmp/pyts-calculator/tests/test_integration.py contains "test_collector_receives_probe"
    - tmp/pyts-calculator/tests/test_integration.py contains "test_multi_flow_stack_differentiation"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Toggle 四组合独立控制验证</name>
  <files>
    tmp/pyts-calculator/tests/test_integration.py
  </files>
  <read_first>
    tmp/pyts-calculator/codepoint/__init__.py
    tmp/pyts-calculator/codepoint/collector.py
    tmp/pyts-calculator/api/server.py
    tmp/pyts-calculator/main.py
    .planning/phases/36-pyts-fullstack-calc/36-CONTEXT.md (D-05)
    .planning/phases/36-pyts-fullstack-calc/36-RESEARCH.md (Pattern 3, Pitfall 2)
  </read_first>
  <action>
    This task verifies FULL-07 (Toggle 四组合独立控制). Execute the following verification matrix:

    CRITICAL: Toggle 切换后必须重启 FastAPI 服务（per RESEARCH Pitfall 2 -- Python codepoint 模块在 import 时检查 toggle）。

    **Combination 1: Both enabled (.codepoint-python + .codepoint-ts)**
    1. `touch ~/.codepoint/.codepoint-python && touch ~/.codepoint/.codepoint-ts`
    2. Clean log files in ~/.codepoint/pyts-calculator/
    3. Start FastAPI: `cd tmp/pyts-calculator && python main.py`
    4. Run all 3 API flows: calculate("2+3"), history/1, batch(["1+1"])
    5. POST simulated frontend probe to /__codepoint__
    6. Stop server
    7. Verify: cp-python-*.log exists AND cp-ts-*.log exists

    **Combination 2: Only Python enabled (.codepoint-python, no .codepoint-ts)**
    1. `touch ~/.codepoint/.codepoint-python && rm -f ~/.codepoint/.codepoint-ts`
    2. Clean log files
    3. Start FastAPI
    4. Run calculate("3+4")
    5. POST to /__codepoint__ -- should return 404 (collector disabled)
    6. Stop server
    7. Verify: cp-python-*.log exists, cp-ts-*.log does NOT exist

    **Combination 3: Only TS enabled (.codepoint-ts, no .codepoint-python)**
    1. `rm -f ~/.codepoint/.codepoint-python && touch ~/.codepoint/.codepoint-ts`
    2. Clean log files
    3. Start FastAPI
    4. Run calculate("5+5") -- Python probe should NOT output (disabled)
    5. POST to /__codepoint__ -- should return 204 (collector enabled)
    6. Stop server
    7. Verify: cp-python-*.log does NOT exist, cp-ts-*.log exists

    **Combination 4: Both disabled (neither toggle file)**
    1. `rm -f ~/.codepoint/.codepoint-python && rm -f ~/.codepoint/.codepoint-ts`
    2. Clean log files
    3. Start FastAPI
    4. Run calculate("7+8")
    5. POST to /__codepoint__ -- should return 404
    6. Stop server
    7. Verify: cp-python-*.log does NOT exist, cp-ts-*.log does NOT exist

    After all 4 combinations, restore both toggles:
    ```bash
    touch ~/.codepoint/.codepoint-python ~/.codepoint/.codepoint-ts
    ```

    Append toggle test results to test_integration.py:
    - Add a `test_toggle_combinations` function that documents the expected behavior for each combination
    - This is a documentation test since toggle verification requires process restart
    - Include clear comments explaining WHY restart is needed (module-level toggle check at import time)
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/work-skills/tmp/pyts-calculator" && grep -c "test_toggle" tests/test_integration.py && echo "Toggle tests documented" && ls ~/.codepoint/.codepoint-python ~/.codepoint/.codepoint-ts 2>&1</automated>
  </verify>
  <done>
    - 四组合全部验证通过：
      - Both on: cp-python + cp-ts 都有输出
      - Only Python: 只有 cp-python，无 cp-ts
      - Only TS: 只有 cp-ts，无 cp-python
      - Both off: 无任何日志输出
    - 两个 toggle 文件互不干扰
    - 切换后重启服务立即生效
    - 验证完成后恢复两个 toggle 文件
  </done>
  <acceptance_criteria>
    - tmp/pyts-calculator/tests/test_integration.py contains "test_toggle_combinations"
    - tmp/pyts-calculator/tests/test_integration.py contains all 4 combination descriptions
    - ~/.codepoint/.codepoint-python exists (restored after test)
    - ~/.codepoint/.codepoint-ts exists (restored after test)
    - Each combination's expected log file presence/absence is documented in the test file
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toggle filesystem -> Python process | File existence check at module load time |
| Toggle filesystem -> Collector | File existence check at module load time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | Toggle file manipulation | accept | Test project, local filesystem only |
</threat_model>

<verification>
1. test_integration.py: collector 接收前端探针并写入 cp-ts-*.log
2. test_integration.py: 多流程堆栈验证 -- cp-calc-compute 在 3 个 flow_id 下有不同调用栈
3. Toggle 四组合全部验证通过
4. 所有 toggle 文件恢复到 "both on" 状态
</verification>

<success_criteria>
- 跨语言联动：cp-python-*.log 和 cp-ts-*.log 都存在且包含正确的 flow_id
- 多流程堆栈：同一代码点 cp-calc-compute 在三个不同 flow_id 的调用中可区分
- Toggle 四组合：每种组合的日志输出完全符合预期（有/无）
- flow_id 关联：前端设置的 flow_id 在后端日志中可见
</success_criteria>

<output>
After completion, create `.planning/phases/36-pyts-fullstack-calc/36-02-SUMMARY.md`
</output>
