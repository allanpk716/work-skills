---
phase: 55-release-v3-metadata-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.zh.md
  - CHANGELOG.md
  - package.json
  - installer/package.json
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04]
user_setup: []

must_haves:
  truths:
    # SC1 (REL-01)
    - "根 README.md 与 README.zh.md 的 Quick Start 命令为 `npx skills add allanpk716/work-skills/claude-notify`（单技能路径），无 `--all` flag"
    - "README.md 与 README.zh.md 全文无 active windows-git-commit / codepoint 引用，无多技能 / skills collection 措辞"
    # SC2 (REL-02)
    - "CHANGELOG.md 顶部（## [1.6.0] 之前）存在 `## [3.0.0] - 2026-06-26` 条目，含 ### Removed 与 ### Changed 分节"
    - "CHANGELOG.md 中 1.6.0 及更早的历史条目完整保留"
    # SC3 (REL-03)
    - "根 package.json 与 installer/package.json 的 version 字段均为 3.0.0（两者同步）"
    - "存在本地 git tag v3.0（tag 未推送）"
    # SC4 (REL-04)
    - "python -m pytest claude-notify/tests/ -q 退出码 0（claude-notify 回归全绿）"
  artifacts:
    - README.md
    - README.zh.md
    - CHANGELOG.md
    - package.json
    - installer/package.json
    - git tag v3.0 (local)
  key_links:
    - "installer/welcome.js (line 14) + cli.js (line 12) 读 installer/package.json 的 version → 故 root 与 installer 两份 package.json 必须 version 同步"
    - "CLAUDE.md 发布规范: version 必须与最新 git tag 一致（tag v3.0 ↔ version 3.0.0）"
---

<objective>
完成 v3.0 里程碑的发版元数据收尾：根项目 README/CHANGELOG 完整反映单一技能 (claude-notify) 形态，两个 package.json 版本同步升至 3.0.0 并与本地 git tag `v3.0` 一致，claude-notify 全量 Python 测试回归通过证明瘦身未破坏其功能。纯元数据/发版阶段，不修改任何业务代码（claude-notify 代码不动、installer/src 已于 Phase 54 裁剪完毕）。

Purpose: 关闭 v3.0 里程碑 — 让仓库对外的元数据（README/CHANGELOG/版本号/tag）与 Phase 53-54 实际执行的"删除两个技能 + 裁剪 installer"瘦身结果完全一致，并以 claude-notify 回归测试为瘦身的安全性提供证据。
Output: 更新的 README.md / README.zh.md / CHANGELOG.md / package.json / installer/package.json + 本地 git tag v3.0。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-release-v3-metadata-regression/55-CONTEXT.md

# Prior phase context (瘦身结果，用于 CHANGELOG 与 README 复核的事实来源)
@.planning/phases/53-remove-deprecated-skills/53-01-SUMMARY.md
@.planning/phases/54-trim-installer-notify-only/54-01-SUMMARY.md

# CLAUDE.md 发布规范（权威：version 与最新 tag 一致；installer/package.json 也需同步）
@CLAUDE.md
</context>

## Artifacts this phase produces

- **git tag `v3.0`**（本地创建，不推送）— v3.0 里程碑的版本锚点
- **CHANGELOG.md `## [3.0.0] - 2026-06-26` 条目**（顶部新增，历史保留）— 记录 v3.0 破坏性变更范围
- **package.json + installer/package.json 版本同步 1.9.0 → 3.0.0**
- **README.md / README.zh.md Quick Start 收窄**为单技能路径（去除 `--all` 残留）
- **claude-notify 回归测试全绿证据**（pytest 退出码 0）

## Scope Fences (HARD)

- ❌ **不执行** `npm publish` / `npm deprecate`（用户手动发布动作，需 .npm-token 与发布时机）
- ❌ **不执行** `git push`（用户决定何时推送 commits + tag）
- ❌ **不修改** `claude-notify/` 任何代码（本阶段为回归测试对象，只读）
- ❌ **不修改** `installer/src/`（Phase 54 已完成裁剪；本阶段仅动 `installer/package.json` 的 version 字段）
- ❌ **不重写** CHANGELOG 历史条目（1.6.0 及更早全部保留，仅顶部插入 v3.0.0）

<tasks>

<task type="auto">
  <name>Task 1: README Quick Start 收窄为单技能路径 (REL-01)</name>
  <files>README.md, README.zh.md</files>
  <read_first>
    - README.md（全文 34 行 — 已于 Phase 53 收窄技能表/结构/description，本 Task 仅复核 Quick Start 与 Credits）
    - README.zh.md（全文 35 行）
    - 55-CONTEXT.md REL-01 段（明确 Quick Start 指向 `npx skills add allanpk716/work-skills/claude-notify`）
  </read_first>
  <action>
Phase 53 已将技能表从 3 行收为 1 行、项目结构收窄、description 改为 notifications-only。本 Task 处理 Phase 53 遗留的 Quick Start `--all` flag 残留。

1. **README.md** 第 10 行：将
   `npx skills add allanpk716/work-skills --all`
   改为
   `npx skills add allanpk716/work-skills/claude-notify`
   （单技能路径，去掉 `--all` 多技能 flag；命令指向 claude-notify 子路径）。

2. **README.zh.md** 第 10 行：同样将
   `npx skills add allanpk716/work-skills --all`
   改为
   `npx skills add allanpk716/work-skills/claude-notify`。

3. **复核 Credits 段**（README.md 第 33 行 / README.zh.md 第 33 行）：当前为 "inspired by baoyu-skills by Jim Liu"。这是项目结构致谢，**不涉及已删技能，保留不动**（Credits 提及的是第三方项目 baoyu-skills，非本仓库的 windows-git-commit/codepoint）。仅当发现 Credits 明确列出已删的本仓库技能时才清理 — 当前内容无需改动。

4. 全文扫描确认无其他多技能残留（`--all`、`skills collection`、`技能集合`、`多个技能`、`多个插件`）。
  </action>
  <verify>
    <automated>cd /c/WorkSpace/agent/work-skills && grep -c -- '--all' README.md README.zh.md | grep -v ':0' && echo "FAIL: --all still present" || echo "PASS: no --all flag"; grep -i -c 'skills collection\|技能集合\|多个技能\|多个插件' README.md README.zh.md | grep -v ':0' && echo "FAIL: multi-skill framing" || echo "PASS: no multi-skill framing"; grep -c 'npx skills add allanpk716/work-skills/claude-notify' README.md README.zh.md</automated>
  </verify>
  <done>
    - README.md 与 README.zh.md 的 Quick Start 命令均为 `npx skills add allanpk716/work-skills/claude-notify`（无 `--all`）
    - 两文件无 windows-git-commit / codepoint 的 active 引用，无多技能 / skills collection 措辞
    - 两个 grep 计数：`--all` 出现 0 次，单技能 Quick Start 路径各出现 ≥1 次
  </done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG 新增 v3.0.0 条目 (REL-02)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md（全文 106 行 — 确认顶部为 `## [1.6.0] - 2026-04-01`，历史条目顺序与格式）
    - 53-01-SUMMARY.md（删除事实来源：windows-git-commit、codepoint 8 子技能、docs/codepoint、docs/windows-git-commit）
    - 54-01-SUMMARY.md（裁剪事实来源：4 个 git/ssh 模块、marketplace 目录 4 文件、uninstall 裁剪、i18n/welcome 收窄）
    - 55-CONTEXT.md specifics 段（v3.0.0 条目结构建议）
  </read_first>
  <action>
在 CHANGELOG.md 中，于 `## [1.6.0] - 2026-04-01`（第 8 行）**之前**插入新的 v3.0.0 条目。遵循现有 Keep a Changelog 格式（`## [version] - date` + `### Section` + 中文无序列表项，与 1.6.0 风格一致）。

插入内容（精确文本）：

```
## [3.0.0] - 2026-06-26

### Removed
- **windows-git-commit 技能** — 移除 deprecated 的 Windows Git 工作流技能（scanner/hooks/references/tests/README/plugin.json 全部删除）
- **codepoint 技能链** — 移除 deprecated 的代码可观测性技能（根 SKILL.md + 8 个子技能 + templates/references）
- **docs/codepoint 与 docs/windows-git-commit 文档工作区** — 删除已弃用技能的调研/修复文档
- **installer git/ssh 检测器与配置器** — 移除 detectors/git.js、detectors/ssh-tools.js、configurators/git-ssh.js、configurators/git-user.js（仅服务已删技能）
- **installer marketplace 集成** — 移除整个 marketplace/ 目录（4 文件：index/config-manager/plugin-discovery/plugin-installer）
- **uninstall 中 marketplace 组件** — 卸载流程裁剪为仅清理 claude-notify 通知组件（5 类 / 5 步），移除 marketplace source 检测与移除类别

### Changed
- **installer 收窄为仅服务 claude-notify** — 单技能安装器形态，主流程去掉 marketplace 步骤与 git/ssh 配置步骤
- **i18n / welcome 横幅收窄** — en.json / zh.json 删除约 50 个 git/marketplace/多技能键，welcome 仅展示 claude-notify 通知特性
- **项目回归单一通知技能形态** — 根 README / package.json 元数据仅宣传 claude-notify
- **版本号 1.9.0 → 3.0.0** — v3.0 为破坏性变更（2/3 技能下线），主版本号升级

```

同时在文件底部链接引用区（第 98 行 `[1.6.0]: ...` 之前）新增一行：
`[3.0.0]: https://github.com/allanpk716/work-skills/releases/tag/v3.0`

**关键约束：**
- `## [3.0.0]` 必须在 `## [1.6.0]` 之前（最新在上）。
- **保留所有历史条目**（1.6.0 及更早，包括其中对 windows-git-commit / codepoint 的历史描述 — 那是历史事实，不是 active 引用，SC4 的"无 active 引用"针对 README/package.json/installer/src，不针对 CHANGELOG 历史）。
- 不修改底部 `[1.6.0]` ... `[0.1.0]` 已有的链接行，仅在它们之前插入 `[3.0.0]` 一行。
  </action>
  <verify>
    <automated>cd /c/WorkSpace/agent/work-skills && grep -n '^## \[3.0.0\] - 2026-06-26' CHANGELOG.md && grep -n '^## \[1.6.0\] - 2026-04-01' CHANGELOG.md && awk '/^## \[3.0.0\]/{p3=NR} /^## \[1.6.0\]/{p1=NR} END{if(p3<p1 && p3>0 && p1>0){print "PASS: v3.0.0 before 1.6.0"}else{print "FAIL: order"; exit 1}}' CHANGELOG.md && grep -c '^### Removed$\|^### Changed$' CHANGELOG.md && grep -n '^\[3.0.0\]:' CHANGELOG.md && grep -c '^## \[1.6.0\]\|^## \[1.5.0\]\|^## \[0.1.0\]' CHANGELOG.md</automated>
  </verify>
  <done>
    - `## [3.0.0] - 2026-06-26` 存在于 CHANGELOG.md，且行号小于 `## [1.6.0]` 的行号（v3.0.0 在最顶部）
    - v3.0.0 条目含 `### Removed` 与 `### Changed` 两个分节
    - 底部新增 `[3.0.0]: https://github.com/allanpk716/work-skills/releases/tag/v3.0` 链接行
    - 历史条目 1.6.0 / 1.5.0 / 0.1.0 均仍存在（计数 ≥ 3）
  </done>
</task>

<task type="auto">
  <name>Task 3: 双 package.json 版本同步升至 3.0.0 + 创建本地 git tag v3.0 (REL-03)</name>
  <files>package.json, installer/package.json</files>
  <read_first>
    - package.json（根 — 当前 version 第 3 行 "1.9.0"；description 第 4 行已于 Phase 53 收窄为通知语义；keywords 第 15-20 行已于 Phase 53 删除 windows-git-commit/codepoint）
    - installer/package.json（当前 version 第 3 行 "1.9.0"）
    - CLAUDE.md 发布规范段（version 必须与最新 git tag 一致；installer welcome.js/cli.js 读 installer/package.json 故两者须同步）
    - 55-CONTEXT.md REL-03 段（tag 名 v3.0，遵循 ROADMAP SC3）
  </read_first>
  <action>
1. **根 package.json**：将第 3 行
   `"version": "1.9.0",`
   改为
   `"version": "3.0.0",`
   （仅改 version 字段。description 已是 "Personal skill for Claude Code — task-completion notifications (Pushover + Windows Toast)" — Phase 53 已收窄，复核即可，不改。keywords 已是 `claude-code / skills / agentskills / claude-notify` — Phase 53 已删 windows-git-commit/codepoint，复核即可，不改。）

2. **installer/package.json**：将第 3 行
   `"version": "1.9.0",`
   改为
   `"version": "3.0.0",`
   （仅改 version 字段。CLAUDE.md 发布规范明确：installer 的 welcome.js line 14 `require('../package.json').version` + cli.js line 12 `require('../package.json').version` 读取 installer/package.json，故必须与根同步。）

3. **复核 package.json 合法性**：`node -e "JSON.parse(require('fs').readFileSync('package.json'))"` 与 `node -e "JSON.parse(require('fs').readFileSync('installer/package.json'))"` 均不抛异常。

4. **创建本地 git tag `v3.0`**（不推送）：
   `git tag v3.0`
   （不带 -a/-m 的轻量 tag 即可；tag 名严格用 `v3.0` 以匹配 ROADMAP SC3，即便历史有 `vX.Y.Z` 形式也遵循 ROADMAP 指定。**不要** `git push origin v3.0` — 用户决定何时推送。）

**禁止：** 不执行 `npm publish` / `npm deprecate`（用户手动发布动作）；不执行 `git push`（用户决定推送时机）。
  </action>
  <verify>
    <automated>cd /c/WorkSpace/agent/work-skills && node -e "const a=JSON.parse(require('fs').readFileSync('package.json','utf8'));const b=JSON.parse(require('fs').readFileSync('installer/package.json','utf8'));if(a.version!=='3.0.0'){console.error('FAIL root:',a.version);process.exit(1)}if(b.version!=='3.0.0'){console.error('FAIL installer:',b.version);process.exit(1)}console.log('PASS both package.json at 3.0.0')" && git tag -l v3.0 | grep -q '^v3.0$' && echo "PASS: tag v3.0 exists" || echo "FAIL: tag v3.0 missing"</automated>
  </verify>
  <done>
    - 根 package.json version === "3.0.0"
    - installer/package.json version === "3.0.0"（两者同步）
    - 两份 package.json 均为合法 JSON（node JSON.parse 不抛异常）
    - `git tag -l v3.0` 输出 `v3.0`（本地 tag 存在）
    - 未执行 npm publish / git push（scope fence 守住）
  </done>
</task>

<task type="auto">
  <name>Task 4: claude-notify 全量 Python 回归测试 (REL-04)</name>
  <files></files>
  <read_first>
    - claude-notify/tests/（test_notify.py / test_flags.py / test_diagnose.py — 确认测试目录存在，本 Task 只读不写）
  </read_first>
  <action>
运行 claude-notify 全量 pytest 作为瘦身后的回归验证。Phase 54 已确认 105 passed；本阶段再跑一次确认 Phase 53（删两个技能）+ Phase 54（裁剪 installer）+ 本阶段元数据变更均未破坏 claude-notify。

命令：
`python -m pytest claude-notify/tests/ -q`

claude-notify/ 代码本里程碑全程未动（Phase 53/54 scope fence + 本阶段 scope fence），故预期全部通过（退出码 0）。若出现失败，**不得修改 claude-notify 代码来"修"测试**（scope fence：claude-notify 代码只读）— 失败意味着瘦身有副作用，需上报而非自行修复。

**禁止：** 不修改 claude-notify/ 下任何文件（含 tests/）。本 Task 是纯执行验证，无文件产出。
  </action>
  <verify>
    <automated>cd /c/WorkSpace/agent/work-skills && python -m pytest claude-notify/tests/ -q; echo "PYTEST_EXIT=$?"</automated>
  </verify>
  <done>
    - `python -m pytest claude-notify/tests/ -q` 退出码 0（PYTEST_EXIT=0）
    - 全部测试通过（无 failed / error），证明瘦身未破坏 claude-notify
    - claude-notify/ 下文件未被本 Task 修改（git status 不显示 claude-notify/ 改动）
  </done>
</task>

</tasks>

<verification>
Phase-level 整体验证（全部 4 个 ROADMAP SC）：

1. **SC1 (REL-01 README 单技能)**：
   `grep -c -- '--all' README.md README.zh.md` → 两个文件均为 0
   `grep -c 'npx skills add allanpk716/work-skills/claude-notify' README.md README.zh.md` → 各 ≥1

2. **SC2 (REL-02 CHANGELOG v3.0.0)**：
   `grep -n '^## \[3.0.0\] - 2026-06-26' CHANGELOG.md` → 命中
   `grep -c '^## \[1.6.0\]\|^## \[0.1.0\]' CHANGELOG.md` → ≥2（历史保留）

3. **SC3 (REL-03 版本 + tag)**：
   根 + installer 两份 package.json version === "3.0.0"
   `git tag -l v3.0` → 输出 v3.0

4. **SC4 (REL-04 回归)**：
   `python -m pytest claude-notify/tests/ -q` 退出码 0

**Scope fence 终检：**
- `git status --short` 改动仅限：README.md / README.zh.md / CHANGELOG.md / package.json / installer/package.json（无 claude-notify/ 改动，无 installer/src/ 改动）
- 未执行 `npm publish` / `npm deprecate` / `git push`
</verification>

<success_criteria>
- 全部 4 个 ROADMAP SC 为 TRUE（见上方 verification）
- git tag v3.0 本地存在（未推送，等用户决定）
- 两份 package.json 同步在 3.0.0，与 tag 一致（CLAUDE.md 发布规范）
- CHANGELOG v3.0.0 条目在顶部，历史条目完整保留
- claude-notify 回归全绿（瘦身安全性证据）
- claude-notify/ 代码 0 改动（regression-only 守住）
- installer/src/ 0 改动（Phase 54 已完成，守住）
- 无 npm publish / git push 执行（用户手动动作，守住）
</success_criteria>

<output>
Create `.planning/phases/55-release-v3-metadata-regression/55-01-SUMMARY.md` when done
</output>
