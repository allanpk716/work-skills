---
phase: 16-python-dependencies
plan: 01
type: execute
wave: 1
depends_on: [16-00]
files_modified: [installer/package.json, installer/src/installers/pip-installer.js, installer/src/installers/index.js, installer/src/i18n/en.json, installer/src/i18n/zh.json, installer/src/index.js]
autonomous: true
requirements: [DEPS-01, DEPS-02, DEPS-03]
user_setup: []

must_haves:
  truths:
    - "User is prompted to install missing Python libraries when detected"
    - "Installer successfully runs pip install with --user flag"
    - "User sees helpful error message and resolution guidance if installation fails"
    - "Installation summary shows installed, failed, and skipped counts"
  artifacts:
    - path: "installer/src/installers/pip-installer.js"
      provides: "pip package installation with error handling"
      exports: ["installPipPackage", "getErrorGuidance"]
    - path: "installer/src/installers/index.js"
      provides: "installer module orchestration and user prompts"
      exports: ["runInstaller", "promptAndInstall"]
    - path: "installer/src/i18n/en.json"
      provides: "English translations for install messages"
      contains: "install.promptInstall"
    - path: "installer/src/i18n/zh.json"
      provides: "Chinese translations for install messages"
      contains: "install.promptInstall"
  key_links:
    - from: "installer/src/index.js"
      to: "installer/src/installers/index.js"
      via: "require('./installers/index.js')"
      pattern: "runInstaller\\(results\\)"
    - from: "installer/src/installers/index.js"
      to: "installer/src/installers/pip-installer.js"
      via: "require('./pip-installer.js')"
      pattern: "installPipPackage\\("
    - from: "installer/src/installers/pip-installer.js"
      to: "execa"
      via: "pip command execution"
      pattern: "execa\\(.*pip.*install"
---

<objective>
Implement Python dependency auto-installation with interactive prompts and comprehensive error handling.

Purpose: When Phase 15 detection finds missing Python libraries, provide users the option to automatically install them via pip, with clear error messages and resolution guidance if installation fails.

Output: Working Python dependency installer integrated into the main installer flow.
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-python-dependencies/16-RESEARCH.md

## Test Contracts (from Wave 0)

From installer/tests/installers/pip-installer.test.js:
- installPipPackage(packageName, pythonCmd) returns:
  - On success: { success: true, message: string, output: string }
  - On failure: { success: false, message: string, error: 'permission'|'network'|'pipNotFound'|'unknown', errorDetails: string }
- getErrorGuidance(errorType) returns i18n key string

From installer/tests/installers/index.test.js:
- promptAndInstall(packages, pythonCmd) returns { installed: [], failed: [], skipped: [] }
- runInstaller(detectionResults, pythonCmd) orchestrates installation flow

## Existing Code Patterns

From installer/src/detectors/index.js (Phase 15):
```javascript
async function runAllDetectors() {
  // Returns { results: Array, allPassed: boolean }
  // Each result: { name, installed, version, meetsMinimum, guidance }
}
```

From installer/src/i18n/index.js:
```javascript
function t(key, params = {}) {
  // Supports {param} replacement in translation strings
}
```

From installer/src/detectors/pip-package.js:
```javascript
const execa = require('execa');
// Pattern: execa(pythonCmd, ['-m', 'pip', 'show', packageName])
```

## Required i18n Keys (from RESEARCH.md)

English:
```json
{
  "install.missingFound": "Found {count} missing Python package(s):",
  "install.promptInstall": "Install {package}?",
  "install.installing": "Installing {package}...",
  "install.success": "{package} installed successfully",
  "install.failed": "Failed to install {package}",
  "install.summary": "Installation Summary:",
  "install.installed": "Installed",
  "install.skipped": "Skipped",
  "guidance.installPermission": "Run Command Prompt as Administrator, or use: pip install {package} --user",
  "guidance.installNetwork": "Check your internet connection and try again",
  "guidance.installPipNotFound": "pip not found. Reinstall Python with pip included",
  "guidance.installUnknown": "Manual install: pip install {package}"
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install enquirer and create pip-installer module (GREEN phase)</name>
  <files>installer/package.json, installer/src/installers/pip-installer.js, installer/src/i18n/en.json, installer/src/i18n/zh.json</files>
  <read_first>
    - installer/package.json (check current dependencies)
    - installer/src/detectors/pip-package.js (reference existing execa pattern)
    - installer/src/i18n/en.json (existing translation structure)
    - installer/src/i18n/zh.json (existing translation structure)
    - installer/tests/installers/pip-installer.test.js (test contracts from Wave 0)
  </read_first>
  <action>
1. Install enquirer dependency:
   ```
   cd installer && npm install enquirer@2.4.1
   ```

2. Create installer/src/installers/pip-installer.js implementing the test contracts:
   - Import: execa (require), chalk (require), t from ../i18n/index.js
   - Function installPipPackage(packageName, pythonCmd = 'python'):
     - Use execa to run: pythonCmd, ['-m', 'pip', 'install', packageName, '--user']
     - IMPORTANT: Always include --user flag to avoid Windows permission issues
     - On success: return { success: true, message: t('install.success', { package: packageName }), output: stdout }
     - On error: parse stderr for error type detection:
       - If stderr contains "Permission denied" or "Access is denied" -> errorType = 'permission'
       - If stderr contains "network" or "Connection" (case insensitive) -> errorType = 'network'
       - If stderr contains "not found" or "ENOENT" -> errorType = 'pipNotFound'
       - Otherwise -> errorType = 'unknown'
     - Return: { success: false, message: t('install.failed', { package: packageName }), error: errorType, errorDetails: errorMessage }
   - Function getErrorGuidance(errorType):
     - Return i18n key based on error type:
       - 'permission' -> 'guidance.installPermission'
       - 'network' -> 'guidance.installNetwork'
       - 'pipNotFound' -> 'guidance.installPipNotFound'
       - default -> 'guidance.installUnknown'

3. Add i18n keys to en.json:
   - "install.missingFound": "Found {count} missing Python package(s):"
   - "install.promptInstall": "Install {package}?"
   - "install.installing": "Installing {package}..."
   - "install.success": "{package} installed successfully"
   - "install.failed": "Failed to install {package}"
   - "install.summary": "Installation Summary:"
   - "install.installed": "Installed"
   - "install.skipped": "Skipped"
   - "guidance.installPermission": "Run Command Prompt as Administrator, or use: pip install {package} --user"
   - "guidance.installNetwork": "Check your internet connection and try again"
   - "guidance.installPipNotFound": "pip not found. Reinstall Python with pip included"
   - "guidance.installUnknown": "Manual install: pip install {package}"

4. Add i18n keys to zh.json:
   - "install.missingFound": "Found {count} missing Python package(s):" (Chinese: "Found {count} missing Python packages:" zh: "Found {count} missing Python packages:" zh-CN: "发现 {count} 个缺失的 Python 包:")
   - "install.promptInstall": "Install {package}?" (Chinese: "是否安装 {package}?")
   - "install.installing": "Installing {package}..." (Chinese: "正在安装 {package}...")
   - "install.success": "{package} installed successfully" (Chinese: "{package} 安装成功")
   - "install.failed": "Failed to install {package}" (Chinese: "{package} 安装失败")
   - "install.summary": "Installation Summary:" (Chinese: "安装摘要:")
   - "install.installed": "Installed" (Chinese: "已安装")
   - "install.skipped": "Skipped" (Chinese: "已跳过")
   - "guidance.installPermission": "Run Command Prompt as Administrator, or use: pip install {package} --user" (Chinese: "以管理员身份运行命令提示符,或使用: pip install {package} --user")
   - "guidance.installNetwork": "Check your internet connection and try again" (Chinese: "请检查网络连接后重试")
   - "guidance.installPipNotFound": "pip not found. Reinstall Python with pip included" (Chinese: "pip 未找到。请重新安装包含 pip 的 Python")
   - "guidance.installUnknown": "Manual install: pip install {package}" (Chinese: "手动安装: pip install {package}")

5. Run tests to verify implementation (GREEN phase): cd installer && npm test -- --testPathPattern=pip-installer
  </action>
  <verify>
    <automated>cd installer && npm test -- --testPathPattern=pip-installer</automated>
  </verify>
  <acceptance_criteria>
    - installer/package.json contains "enquirer": "^2.4.1" or "enquirer": "2.4.1" in dependencies
    - installer/src/installers/pip-installer.js exists and exports installPipPackage function
    - installer/src/installers/pip-installer.js contains "pip", "install", "--user" in execa call
    - installer/src/i18n/en.json contains "install.promptInstall" key
    - installer/src/i18n/zh.json contains "install.promptInstall" key
    - All pip-installer tests pass (10 tests from Wave 0)
  </acceptance_criteria>
  <done>
    enquirer installed, pip-installer module created with error handling, i18n keys added, all pip-installer tests pass (GREEN phase complete)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create installer orchestrator module (GREEN phase)</name>
  <files>installer/src/installers/index.js</files>
  <read_first>
    - installer/src/installers/pip-installer.js (from Task 1)
    - installer/src/detectors/index.js (reference for orchestration pattern)
    - installer/src/i18n/index.js (t function with params support)
    - installer/tests/installers/index.test.js (test contracts from Wave 0)
  </read_first>
  <action>
1. Create installer/src/installers/index.js implementing the test contracts:
   - Import: { Confirm } from 'enquirer', chalk, t from ../i18n/index.js
   - Import: { installPipPackage, getErrorGuidance } from './pip-installer.js'

   - Function promptAndInstall(packages, pythonCmd = 'python'):
     - packages is array of detection results: { name, installed, version, meetsMinimum, guidance }
     - Filter to get missing packages: packages.filter(p => !p.installed)
     - If no missing packages, return { installed: [], failed: [], skipped: [] }
     - Print: chalk.yellow('\n' + t('install.missingFound', { count: toInstall.length }))
     - Print each missing package name
     - For each missing package:
       - Create enquirer Confirm prompt with message: t('install.promptInstall', { package: pkg.name })
       - Set initial: true (default to yes)
       - If user confirms:
         - Print: chalk.gray(t('install.installing', { package: pkg.name }))
         - Call installPipPackage(pkg.name, pythonCmd)
         - On success: print chalk.green('[OK] ' + result.message), add to installed array
         - On failure: print chalk.red('[FAIL] ' + result.message), print chalk.gray('    -> ' + t(getErrorGuidance(result.error))), add to failed array
       - If user declines: add to skipped array
     - Return { installed, failed, skipped }

   - Function runInstaller(detectionResults, pythonCmd = 'python'):
     - Entry point called from main installer
     - Find Python detection result: detectionResults.find(r => r.name === 'Python')
     - Get pythonCmd from detection result or default to 'python'
     - Call promptAndInstall(detectionResults, pythonCmd)
     - Print summary:
       - console.log('\n' + t('install.summary'))
       - console.log(chalk.green('  ' + t('install.installed') + ': ' + results.installed.length))
       - console.log(chalk.red('  ' + t('install.failed') + ': ' + results.failed.length))
       - console.log(chalk.gray('  ' + t('install.skipped') + ': ' + results.skipped.length))
     - Return results

   - Export: { runInstaller, promptAndInstall }

2. Run tests to verify implementation (GREEN phase): cd installer && npm test -- --testPathPattern=installers/index
  </action>
  <verify>
    <automated>cd installer && npm test -- --testPathPattern=installers/index</automated>
  </verify>
  <acceptance_criteria>
    - installer/src/installers/index.js exists and exports runInstaller function
    - installer/src/installers/index.js contains "Confirm" from enquirer
    - installer/src/installers/index.js contains "installPipPackage" import
    - All installers/index tests pass (6 tests from Wave 0)
  </acceptance_criteria>
  <done>
    Installer orchestrator module created with enquirer prompts, all index tests pass (GREEN phase complete)
</done>
</task>

<task type="auto">
  <name>Task 3: Integrate installer into main entry point</name>
  <files>installer/src/index.js</files>
  <read_first>
    - installer/src/index.js (current main entry point)
    - installer/src/installers/index.js (from Task 2)
    - installer/src/detectors/index.js (understand runAllDetectors return value)
  </read_first>
  <action>
1. Modify installer/src/index.js:
   - Add import: const { runInstaller } = require('./installers/index.js');

   - Update main() function after Step 4 (environment detection):
     - Change: const allPassed = await runAllDetectors();
     - To: const { results, allPassed } = await runAllDetectors();

   - Add Step 5: Offer to install missing dependencies
     ```javascript
     // Step 5: Offer to install missing dependencies
     if (!allPassed) {
       const pipResults = results.filter(r =>
         r.name && r.name !== 'Python' && r.name !== 'Git' &&
         !r.name.includes('TortoiseGit') && !r.name.includes('PuTTY') && !r.name.includes('SSH')
       );

       if (pipResults.some(r => !r.installed)) {
         await runInstaller(results);
       }
     }
     ```

   - Update Step 6 comment to reflect completed Phase 16

2. Run full test suite to ensure no regressions: cd installer && npm test

3. Manual verification: cd installer && node bin/setup.js
   - Should show detection, then prompt to install missing packages
  </action>
  <verify>
    <automated>cd installer && npm test</automated>
  </verify>
  <acceptance_criteria>
    - installer/src/index.js contains require('./installers/index.js')
    - installer/src/index.js contains runInstaller(results)
    - installer/src/index.js extracts both results and allPassed from runAllDetectors()
    - All existing tests continue to pass
  </acceptance_criteria>
  <done>
    Installer integrated into main flow, missing Python packages trigger interactive installation prompt
  </done>
</task>

</tasks>

<verification>
## Automated Verification
- All installer tests pass: `cd installer && npm test`
- Specific installers tests: `cd installer && npm test -- --testPathPattern=installers`

## Manual Verification
1. Run installer: `cd installer && node bin/setup.js`
2. If requests package is missing, should see:
   - "Found 1 missing Python package(s):" message
   - "Install requests?" prompt
3. Press Enter (yes) to install
4. Should see installation progress and summary
5. If installation fails, should see error guidance message

## Requirement Traceability
| Requirement | Task | Verification |
|-------------|------|--------------|
| DEPS-01 | Task 2 | enquirer Confirm prompt asks user before installing |
| DEPS-02 | Task 1 | execa runs `python -m pip install requests --user` |
| DEPS-03 | Task 1, 2 | Error parsing + getErrorGuidance + i18n messages |
</verification>

<success_criteria>
- User is prompted to install missing Python libraries when detected (DEPS-01)
- Installer successfully runs pip install with --user flag (DEPS-02)
- User sees helpful error message and resolution guidance if installation fails (DEPS-03)
- Installation summary shows installed, failed, and skipped counts
- All tests pass (16 tests from Wave 0 + any additional tests)
</success_criteria>

<output>
After completion, create `.planning/phases/16-python-dependencies/16-01-SUMMARY.md`
</output>
