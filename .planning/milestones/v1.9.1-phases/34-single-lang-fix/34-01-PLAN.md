---
phase: 34-single-lang-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-single-lang-fix/34-DEFECTS.yaml
autonomous: true
requirements:
  - FIX1-01
user_setup: []
must_haves:
  truths:
    - "34-DEFECTS.yaml exists and contains exactly 4 defect entries with ids DEV-01, DEV-02, DEV-03, DEV-04"
    - "Each defect entry contains 9 required fields: id, description, category, steps_to_reproduce, expected, actual, status, fixed_in, verification"
    - "YAML file is parseable by Python yaml.safe_load with encoding='utf-8' on Windows"
    - "All 4 defect entries have status: fixed and non-empty fixed_in field"
    - "summary section contains total_defects: 4 and all_fixed: true"
  artifacts:
    - path: ".planning/phases/34-single-lang-fix/34-DEFECTS.yaml"
      provides: "Structured record of all Phase 32/33 known defects"
      min_lines: 50
  key_links: []
---

<objective>
Create 34-DEFECTS.yaml structured defect record file, formally documenting the 4 deviations (DEV-01 to DEV-04) discovered during Phase 32 Go E2E testing. Each entry includes full problem description, reproduction steps, expected/actual behavior, fix status, and verification evidence.

Purpose: Per D-04, use YAML format to record all known defects for machine-readable automated verification. This is the foundational deliverable of Phase 34; subsequent plans depend on the defect information recorded here.

Output: 34-DEFECTS.yaml (4 defect entries, all with status: fixed, including fixed_in and verification fields)

Addresses review concern: MEDIUM - YAML encoding on Windows (verify script uses encoding='utf-8'), LOW - inconsistent field lists (must_haves now lists exact 9 required fields matching YAML schema).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-single-lang-fix/34-CONTEXT.md
@.planning/phases/34-single-lang-fix/34-RESEARCH.md
@.planning/phases/32-go-single-lang-calc/32-01-SUMMARY.md
@.planning/phases/32-go-single-lang-calc/32-05-SUMMARY.md
@.planning/phases/33-python-single-lang-calc/33-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 34-DEFECTS.yaml structured defect record</name>
  <files>.planning/phases/34-single-lang-fix/34-DEFECTS.yaml</files>
  <read_first>
    - .planning/phases/32-go-single-lang-calc/32-01-SUMMARY.md (original DEV-01 to DEV-04 records and fix process)
    - .planning/phases/34-single-lang-fix/34-RESEARCH.md (Pattern 1: YAML example format)
    - .planning/phases/34-single-lang-fix/34-CONTEXT.md (D-04 defect format requirements)
  </read_first>
  <acceptance_criteria>
    - grep -c "id: DEV-0" returns exactly 4
    - grep -c "status: fixed" returns exactly 4
    - grep -c "fixed_in:" returns exactly 4
    - grep -c "verification:" returns exactly 4
    - grep "total_defects: 4" returns match
    - grep "all_fixed: true" returns match
    - grep "python_defects: 0" returns match
    - python -c "import yaml; yaml.safe_load(open('...', encoding='utf-8'))" succeeds
  </acceptance_criteria>
  <action>
    Create `.planning/phases/34-single-lang-fix/34-DEFECTS.yaml` with the following structure:

    ```yaml
    # Phase 34: Single-Language Defects Record
    # Generated: 2026-04-18
    # Source: Phase 32 (Go) E2E test deviations
    # Phase 33 (Python): 0 defects found - not included

    phase: "34-single-lang-fix"
    source_phases:
      - "32-go-single-lang-calc"
      - "33-python-single-lang-calc"

    defects:
      - id: DEV-01
        category: template
        severity: high
        template: golang.md
        discovered_in: "Phase 32, Plan 01"
        description: |
          golang.md template: parseGoStack returns anonymous struct incompatible with PointJSON internal Frame type.
          Frame was defined inside PointJSON as a local type, while parseGoStack returned a separate anonymous struct.
          This caused template code to fail compilation: parseGoStack return value could not be assigned to Entry.Frames ([]Frame).
        steps_to_reproduce: |
          1. Copy Base Library code from golang.md to new project codepoint/codepoint.go
          2. Create go.mod (module template-test)
          3. Run go build ./codepoint/...
          4. Compilation fails: undefined: Frame
        expected: "Frame is package-level type, parseGoStack returns []Frame, entire Base Library compiles independently"
        actual: "Frame defined inside PointJSON, parseGoStack returns anonymous struct, compilation fails"
        status: fixed
        fixed_in: "commit 088f086"
        fixed_by: "Phase 32 Plan 01"
        verification: |
          golang.md template independent compilation:
          cd tmp/template-test/go && go build ./...
          Expected: zero errors
          [VERIFIED: 2026-04-18]

      - id: DEV-02
        category: test_project
        severity: low
        template: null
        discovered_in: "Phase 32, Plan 01"
        description: |
          go-calculator/internal/history/store.go contains unused fmt import.
          The file imported fmt during implementation but never used it, causing go vet to report unused import error.
        steps_to_reproduce: |
          1. Enter go-calculator project
          2. Run go vet ./internal/history/...
          3. Error: imported and not used: "fmt"
        expected: "All imports are used, go vet zero warnings"
        actual: "store.go contains unused fmt import"
        status: fixed
        fixed_in: "Phase 32 Plan 01 (code fix)"
        fixed_by: "Phase 32 Plan 01"
        verification: |
          cd tmp/go-calculator && go vet ./...
          Expected: zero warnings
          [VERIFIED: 2026-04-18 - go vet no output]

      - id: DEV-03
        category: test_project
        severity: medium
        template: null
        discovered_in: "Phase 32, Plan 01"
        description: |
          go-calculator test expectation for (2+3)*4-10/2 is incorrect.
          Correct answer is 15.0 ((2+3)*4=20, 20-10/2=20-5=15), but test expected 10.0.
          This is a test assertion error, not a business logic defect.
        steps_to_reproduce: |
          1. Enter go-calculator project
          2. Run go test ./internal/calculator/... -run TestEvaluate -v
          3. Observe: expected value 10.0 does not match actual 15.0
        expected: "Test expects (2+3)*4-10/2 = 15.0, matching calculator.Evaluate actual result"
        actual: "Test expected 10.0, causing test failure (actual result is 15.0)"
        status: fixed
        fixed_in: "Phase 32 Plan 01 (test fix)"
        fixed_by: "Phase 32 Plan 01"
        verification: |
          cd tmp/go-calculator && go test ./internal/calculator/... -run TestEvaluate -v
          Expected: PASS
          [VERIFIED: 2026-04-18 - 31 tests PASS]

      - id: DEV-04
        category: test_project
        severity: medium
        template: null
        discovered_in: "Phase 32, Plan 01"
        description: |
          go-calculator History test calls handler directly, bypassing HTTP mux PathValue extraction.
          Direct http.Request calls do not go through http.NewServeMux route matching,
          causing r.PathValue("id") to return empty string, preventing correct history query testing.
        steps_to_reproduce: |
          1. Enter go-calculator project
          2. Run go test ./internal/api/... -run TestHistory -v
          3. Observe: test calls HandleHistoryGet directly, r.PathValue("id") returns ""
        expected: "Test routes through mux (srv.ServeHTTP), correctly extracting URL path parameters"
        actual: "Test calls handler function directly, PathValue cannot extract parameters"
        status: fixed
        fixed_in: "Phase 32 Plan 01 (test fix)"
        fixed_by: "Phase 32 Plan 01"
        verification: |
          cd tmp/go-calculator && go test ./internal/api/... -run TestHistory -v
          Expected: PASS - test routes via srv.ServeHTTP
          [VERIFIED: 2026-04-18 - API tests all PASS]

    summary:
      total_defects: 4
      template_defects: 1
      test_project_defects: 3
      all_fixed: true
      python_defects: 0
      notes: |
        Phase 33 (Python) E2E testing found no defects.
        All 4 Go defects were fixed during Phase 32 execution and verified.
        Phase 34 core work: formalize fix records and verify template compilation.
    ```

    Notes for executor:
    - Do NOT use languages other than Chinese for description and steps_to_reproduce fields
    - Ensure YAML is correctly formatted and parseable by Python yaml.safe_load
    - Each defect entry fields must match RESEARCH.md Pattern 1 example and D-04 requirements
    - CRITICAL: Any Python open() call must use encoding='utf-8' on Windows (addresses review concern MEDIUM-4)
  </action>
  <verify>
    <automated>python -c "
import yaml
d = yaml.safe_load(open('.planning/phases/34-single-lang-fix/34-DEFECTS.yaml', encoding='utf-8'))
assert len(d['defects']) == 4, f'Expected 4 defects, got {len(d[\"defects\"])}'
ids = [x['id'] for x in d['defects']]
assert ids == ['DEV-01', 'DEV-02', 'DEV-03', 'DEV-04'], f'Wrong ids: {ids}'
required = ['id','description','category','steps_to_reproduce','expected','actual','status','fixed_in','verification']
for i, defect in enumerate(d['defects']):
    missing = [k for k in required if k not in defect]
    assert not missing, f'DEV-{i+1} missing: {missing}'
    assert defect['status'] == 'fixed', f'{defect[\"id\"]} not fixed'
    assert defect['fixed_in'], f'{defect[\"id\"]} empty fixed_in'
    assert defect['verification'], f'{defect[\"id\"]} empty verification'
assert d['summary']['total_defects'] == 4
assert d['summary']['all_fixed'] == True
assert d['summary']['python_defects'] == 0
print('PASS: all 4 defects validated, all fields present')
"</automated>
  </verify>
  <done>
    34-DEFECTS.yaml exists in .planning/phases/34-single-lang-fix/ with 4 defect entries (DEV-01 to DEV-04). YAML is parseable by Python yaml.safe_load with encoding='utf-8'. All entries have status fixed with non-empty fixed_in and verification fields. Summary section contains total_defects: 4 and all_fixed: true.
  </done>
</task>

</tasks>

<verification>
1. 34-DEFECTS.yaml exists and is parseable by Python yaml.safe_load with encoding='utf-8'
2. Contains exactly 4 defect entries with ids DEV-01, DEV-02, DEV-03, DEV-04
3. Each entry contains all 9 required fields: id, description, category, steps_to_reproduce, expected, actual, status, fixed_in, verification
4. All entries have status "fixed" with non-empty fixed_in and verification
5. summary section contains total_defects: 4, all_fixed: true, python_defects: 0
</verification>

<success_criteria>
- 34-DEFECTS.yaml parseable by Python yaml.safe_load with encoding='utf-8', zero errors
- 4 defect entries fully documented with description, reproduction steps, expected/actual behavior
- File format is machine-readable for automated tooling
</success_criteria>

<output>
After completion, create `.planning/phases/34-single-lang-fix/34-01-SUMMARY.md`
</output>
