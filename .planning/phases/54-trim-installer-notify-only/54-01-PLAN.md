---
phase: 54-trim-installer-notify-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - installer/src/detectors/git.js
  - installer/src/detectors/ssh-tools.js
  - installer/src/detectors/index.js
  - installer/src/configurators/git-ssh.js
  - installer/src/configurators/git-user.js
  - installer/src/configurators/index.js
  - installer/src/marketplace/index.js
  - installer/src/marketplace/config-manager.js
  - installer/src/marketplace/plugin-discovery.js
  - installer/src/marketplace/plugin-installer.js
  - installer/src/uninstall/detector.js
  - installer/src/uninstall/remover.js
  - installer/src/uninstall/formatter.js
  - installer/src/uninstall/index.js
  - installer/src/index.js
  - installer/src/cli.js
  - installer/src/welcome.js
  - installer/src/i18n/en.json
  - installer/src/i18n/zh.json
  - installer/src/uninstall/paths.js
autonomous: true
requirements: [INS-01, INS-02, INS-03, INS-04]
user_setup: []

must_haves:
  truths:
    - "运行 `node -e \"require('./installer/src/index.js')\"` 退出码 0（主入口在裁剪后仍可加载）"
    - "installer 不再检测 git-ssh / git-user / TortoiseGit / PuTTY（runAllDetectors 仅并行 detectPython() 与 detectPipPackage('requests') 两项）"
    - "installer 不再运行 marketplace 集成（index.js 无 runMarketplaceIntegration import 与调用，marketplace/ 目录物理消失）"
    - "installer 仍提供 --uninstall 卸载入口，且卸载流程仅清理 claude-notify 组件（plugin / hooks / commands / Pushover env），不再探测 marketplace source"
    - "i18n (en.json / zh.json) 与 welcome 横幅仅描述 claude-notify 单技能范围，无 git/marketplace/多技能文案键"
    - "被删除的 i18n 键不再被任何 src 代码 t() 引用"
  artifacts:
    - "installer/src/detectors/index.js （runAllDetectors: 2 项 Promise.all）"
    - "installer/src/configurators/index.js （runAllConfigurators: 仅 Pushover）"
    - "installer/src/uninstall/paths.js （新文件：从已删 marketplace 迁移过来的 getSkillsDir/isPluginInstalled —— 仅这 2 个被 uninstall 实际使用；readClaudeConfig/writeClaudeConfig/getConfigPath 在 uninstall 中无消费者，不迁移以免产生死导出）"
    - "installer/src/uninstall/detector.js （裁剪：移除 marketplaceSource 类别，import 改指向 ./paths.js）"
    - "installer/src/uninstall/remover.js （裁剪：移除 marketplace cache/source 两步，import 改指向 ./paths.js）"
    - "installer/src/uninstall/formatter.js （裁剪：移除 marketplace 类别渲染）"
    - "installer/src/index.js （移除 marketplace 步骤、收窄 pip 过滤）"
    - "installer/src/welcome.js （feature 列表 3→1）"
    - "installer/src/i18n/en.json, installer/src/i18n/zh.json （移除 git/marketplace/多技能键）"
    - "installer/src/detectors/git.js, detectors/ssh-tools.js, configurators/git-ssh.js, configurators/git-user.js, marketplace/ 整目录 已删除"
  key_links:
    - "uninstall/{detector,remover,formatter,index}.js → uninstall/paths.js （marketplace 删除后路径/配置 helper 的迁移落点）"
    - "index.js main() → detectors/index.js runAllDetectors() （2 项）"
    - "index.js main() → configurators/index.js runAllConfigurators() （仅 Pushover）"
    - "index.js main() → hooks/index.js runHooksInstallation() （保留，claude-notify 核心）"
---

<objective>
裁剪 NPX 安装器源码（installer/src/）使其仅服务 claude-notify 单一技能：删除 git/ssh 检测器与配置器（INS-01）、删除整个 marketplace 目录并移除主流程中的 marketplace 步骤（INS-02）、裁剪 uninstall 模块为仅清理 claude-notify 组件同时保留 --uninstall 入口（INS-03）、收窄 i18n 与 welcome 横幅文案（INS-04）。本计划只动 installer/src/，测试在 02 计划处理。

Purpose: Phase 53 已删除技能目录并做了“不破坏加载”级最小改动；本阶段做深度裁剪，剥离与已删技能（windows-git-commit / codepoint）耦合的检测/配置/市场/卸载代码，保留 claude-notify 必需的 Windows 检测、Python/pip 检测、Pushover 配置、通知 hooks 注册、安装验证。这是 v3.0 破坏性瘦身里程碑的核心裁剪步骤。

Output: 一个仍可 `require('./installer/src/index.js')` 加载、主流程为 “Windows 检测 → 欢迎 → Python/pip 检测 → (pip 安装) → Pushover 配置 → hooks 注册 → 验证”、卸载入口仍可用且仅针对 claude-notify 的 installer 源码树。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-trim-installer-notify-only/54-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 git/ssh 检测器+配置器与整个 marketplace 目录，并裁剪 detectors/configurators 聚合器 (INS-01, INS-02)</name>
  <files>
    installer/src/detectors/git.js (删除),
    installer/src/detectors/ssh-tools.js (删除),
    installer/src/configurators/git-ssh.js (删除),
    installer/src/configurators/git-user.js (删除),
    installer/src/marketplace/index.js (删除),
    installer/src/marketplace/config-manager.js (删除),
    installer/src/marketplace/plugin-discovery.js (删除),
    installer/src/marketplace/plugin-installer.js (删除),
    installer/src/detectors/index.js (修改),
    installer/src/configurators/index.js (修改)
  </files>
  <read_first>
    installer/src/detectors/index.js （当前 4 项 Promise.all + detectGit/detectSSHTools import）,
    installer/src/configurators/index.js （当前 import configureGitSSH/configureGitUser + runAllConfigurators 三步 + displayConfigSummary）,
    installer/src/marketplace/plugin-installer.js （getSkillsDir/isPluginInstalled 将被 Task 2 迁移到 paths.js，先读懂其实现以备迁移 —— 仅这 2 个）,
    installer/src/marketplace/config-manager.js （getConfigPath/readClaudeConfig/writeClaudeConfig 仅作源码参考；NEW-MEDIUM-1：在 uninstall 下无消费者，Task 2 不迁移，仅先读懂为何不迁移）,
    CLAUDE.md （Windows、CJS、prefer Edit over new files）
  </read_first>
  <action>
    1. 删除 4 个模块文件（用 git rm）：
       - installer/src/detectors/git.js
       - installer/src/detectors/ssh-tools.js
       - installer/src/configurators/git-ssh.js
       - installer/src/configurators/git-user.js
       这 4 个文件服务于已删的 windows-git-commit（TortoisePlink/PuTTY 认证 + git user.info），claude-notify 不需要。保留 detectors/python.js、detectors/pip-package.js、configurators/pushover.js 不动。

    2. 删除整个 marketplace 目录（INS-02，多技能发现/安装机制，claude-notify 通过 npx skills add 与 runHooksInstallation 安装，不依赖 marketplace）：
       git rm -r installer/src/marketplace/
       （index.js / config-manager.js / plugin-discovery.js / plugin-installer.js 全部移除）
       注意：config-manager.js 的 getConfigPath/readClaudeConfig/writeClaudeConfig 与 plugin-installer.js 的 getSkillsDir/isPluginInstalled 仍被 uninstall/{detector,remover}.js 引用 —— Task 2 会先把其中**实际仍被使用的 2 个** helper（getSkillsDir/isPluginInstalled）迁移到新文件 installer/src/uninstall/paths.js 再删 marketplace（NEW-MEDIUM-1：另 3 个 config helper 在 uninstall 下无消费者，不迁移）。本 Task 必须在 Task 2 完成迁移后才执行 `git rm marketplace`，或本 Task 仅做 detectors/configurators 删除 + 聚合器裁剪，把 marketplace 物理删除交给 Task 3 收尾（执行器按 Task 顺序处理即可，Task 3 末尾执行 git rm marketplace）。

    3. 裁剪 installer/src/detectors/index.js（INS-01）：
       - 移除两行 import：`const { detectGit } = require('./git.js');` 与 `const { detectSSHTools } = require('./ssh-tools.js');`
       - 保留 `const { detectPython } = require('./python.js');` 与 `const { detectPipPackage } = require('./pip-package.js');`
       - runAllDetectors 的 Promise.all 从 4 项收为 2 项：`[detectPython(), detectPipPackage('requests')]`
       - 其余逻辑（printResult、failedCount/passedCount 统计、summary 输出）保持不变；保留 printResult 导出。
       - t('detection.checking') / t('detection.summary') 调用不变。

    4. 裁剪 installer/src/configurators/index.js（INS-01）：
       - 移除两行 import：`const { configureGitSSH } = require('./git-ssh.js');` 与 `const { configureGitUser } = require('./git-user.js');`
       - 保留 `const { configurePushover } = require('./pushover.js');` 与 chalk、t 的 import
       - runAllConfigurators：移除“Git SSH”与“Git user.info”两个步骤（含 `t('config.section.gitSSH')` / `t('config.section.gitUser')` 调用与对应 results.push），仅保留 Pushover 步骤（`t('config.section.pushover')` + configurePushover() + results.push Pushover 项）。
       - 保留 displayConfigSummary 函数与导出（Pushover 仍需 summary 渲染）。
  </action>
  <verify>
    <automated>
    test ! -f installer/src/detectors/git.js &&
    test ! -f installer/src/detectors/ssh-tools.js &&
    test ! -f installer/src/configurators/git-ssh.js &&
    test ! -f installer/src/configurators/git-user.js &&
    ! grep -q "detectGit\|detectSSHTools" installer/src/detectors/index.js &&
    ! grep -q "configureGitSSH\|configureGitUser" installer/src/configurators/index.js &&
    node -e "const {runAllDetectors}=require('./installer/src/detectors/index.js'); const {runAllConfigurators}=require('./installer/src/configurators/index.js'); console.log(typeof runAllDetectors, typeof runAllConfigurators)"
    </automated>
  </verify>
  <done>
    - git.js / ssh-tools.js / git-ssh.js / git-user.js 物理消失
    - detectors/index.js 的 runAllDetectors 仅并行 detectPython() 与 detectPipPackage('requests') 两项，无 detectGit/detectSSHTools 引用
    - configurators/index.js 的 runAllConfigurators 仅含 Pushover 步骤，无 configureGitSSH/configureGitUser 引用
    - displayConfigSummary 仍导出且可用
    - require detectors/index.js 与 configurators/index.js 不抛异常
    注意：此时尚未删除 marketplace 目录（Task 3 处理），index.js 仍能 require marketplace 是预期的；本 Task 结束时主 index.js 暂时仍引用 marketplace（Task 4 才移除）。
  </done>
</task>

<task type="auto">
  <name>Task 2: 迁移 marketplace 路径/配置 helper 到 uninstall/paths.js，并裁剪 uninstall 模块 (INS-03)</name>
  <files>
    installer/src/uninstall/paths.js (新建),
    installer/src/uninstall/detector.js (修改),
    installer/src/uninstall/remover.js (修改),
    installer/src/uninstall/formatter.js (修改),
    installer/src/uninstall/index.js (修改)
  </files>
  <read_first>
    installer/src/uninstall/detector.js （当前 import marketplace/plugin-installer 的 isPluginInstalled/getSkillsDir + marketplace/config-manager 的 readClaudeConfig/getConfigPath；含 marketplaceSource 类别）,
    installer/src/uninstall/remover.js （当前 import marketplace/config-manager 的 readClaudeConfig/writeClaudeConfig + marketplace/plugin-installer 的 getSkillsDir；含 Step 5 marketplace cache 与 Step 6 marketplace source 两步）,
    installer/src/uninstall/formatter.js （渲染 uninstall.category.marketplace 类别块）,
    installer/src/uninstall/index.js （countInstalled/countTotal 含 marketplaceSource 项；runUninstall 流程）,
    installer/src/marketplace/plugin-installer.js （getSkillsDir/isPluginInstalled 实现源 —— 仅这 2 个会被迁移到 paths.js）,
    installer/src/marketplace/config-manager.js （getConfigPath/readClaudeConfig/writeClaudeConfig 实现源 —— 仅作参考，NEW-MEDIUM-1：这三个 helper 在 uninstall 下无消费者，不迁移）
    注意：read_first 仍读这两个源文件以理解 helper 实现，但 paths.js 只迁移 getSkillsDir/isPluginInstalled 这 2 个。
  </read_first>
  <action>
    ★ INS-03 设计决策（依据 54-CONTEXT.md INS-03 推荐方案，明确记录以供 codex/opencode convergence 复核）★
    本阶段采用 **裁剪（trim）** 而非“完全移除 uninstall/”：保留 uninstall/ 模块与 --uninstall CLI 入口，但移除与已删 marketplace 耦合的检测/移除类别，仅保留 claude-notify 组件（plugin / hooks scripts / hooks registration / slash commands / Pushover env vars）。
    理由：(1) claude-notify 的卸载（清 plugin/hooks/commands/Pushover env）仍是用户真实需求；(2) 保留 --uninstall 维持安装/卸载对称性；(3) 裁剪与移除工作量相近（都要先解耦 marketplace 引用）。PLUGIN_NAMES 已在 Phase 53 收窄为 ['claude-notify']。若 convergence 复核认为应“完全移除”或交用户决定，则在执行前升级为用户决策（见 54-CONTEXT.md 备选方案）。

    1. 新建 installer/src/uninstall/paths.js（CJS，'use strict'）—— 把 marketplace 删除后仍被 uninstall **实际使用**的路径/配置 helper 迁移过来。**NEW-MEDIUM-1 根因修复**：经源码核对，detector.js 仅使用 `isPluginInstalled`（:20）+ `getSkillsDir`（:21），remover.js 仅使用 `getSkillsDir`（:90，Step 4）；`readClaudeConfig`（remover.js:110，detector.js:43）+ `writeClaudeConfig`（remover.js:114）+ `getConfigPath`（detector.js:46）的唯一调用点都在被删的 marketplace 检测/移除块内。marketplace 裁剪后这 3 个 config helper 在 uninstall/ 下零消费者，**迁移它们只会制造死导出**。故 paths.js 只迁移实际仍被使用的 2 个 helper，实现保持与原 marketplace/plugin-installer.js 一致：
       - getSkillsDir()：`path.join(os.homedir(), '.claude', 'skills')` （从 marketplace/plugin-installer.js 迁移）
       - isPluginInstalled(pluginName)：检查 `path.join(getSkillsDir(), pluginName, 'SKILL.md')` 是否存在 （从 marketplace/plugin-installer.js 迁移）
       - module.exports = { getSkillsDir, isPluginInstalled }
       依赖：require('fs'), require('path'), require('os')。
       **不要**迁移 readClaudeConfig/writeClaudeConfig/getConfigPath（uninstall 下无消费者，迁移即死导出 —— NEW-MEDIUM-1 根因）。
       **不要**迁移 registerMarketplaceSource（marketplace 已删，无人调用）。

    2. 裁剪 installer/src/uninstall/detector.js（MEDIUM-1 修正：import 仅保留实际仍被使用的 helper）：
       - 先 Read detector.js 确认实际用法：当前 `readClaudeConfig`/`getConfigPath` 仅被 Category 5 “Marketplace Source” 检测块（detector.js:43-46）使用；`isPluginInstalled`（detector.js:20）与 `getSkillsDir`（detector.js:21）是其余逻辑的唯一消费者。
       - 因此 import 改指向 ./paths.js 时**仅引入仍被使用的两个**：`const { isPluginInstalled, getSkillsDir } = require('./paths.js');`
       - **不要** import `readClaudeConfig`/`getConfigPath` 到 detector.js —— marketplace 检测块删除后它们在 detector.js 中无调用点，引入即死 import（MEDIUM-1）。
       - **NEW-MEDIUM-1 根因说明**：这三个 config helper（readClaudeConfig/writeClaudeConfig/getConfigPath）在 uninstall/ 下**无任何消费者** —— remover.js 的 `readClaudeConfig`(:110)/`writeClaudeConfig`(:114) 仅在被删的 Step 6 marketplace source 块内使用，删除后也成死 import。故 paths.js 根本不导出它们（见 step 1），不存在"供 remover.js 使用"一说。detector.js 仅需从 paths.js import `{ isPluginInstalled, getSkillsDir }`。
       - 移除对 marketplace/plugin-installer.js 与 marketplace/config-manager.js 的 require。
       - 保留 hooks-installer 的 import（isHooksInstalled/isHooksRegistered/isCommandsInstalled/getHooksDir/getCommandsDir）与 pushover 的 detectPushoverFull import 不变。
       - PLUGIN_NAMES 保持 ['claude-notify']。
       - 移除 Category 5 “Marketplace Source” 检测块（readClaudeConfig + config.marketplaceSources['work-skills'] 判断 + marketplaceSource 对象），不再产出 marketplaceSource 字段。
       - 移除返回对象中的 marketplaceSource 字段，并从 hasAnyInstalled 聚合表达式中删去 `marketplaceSource.installed ||`。
       - 返回对象字段：plugins, hooksScripts, hooksRegistered, commandsInstalled, envVars, hasAnyInstalled。
       - JSDoc 中“7 categories / 6 categories”描述更新为实际保留的 5 类（plugins, hook scripts, hook registration, slash commands, environment variables），不再提 marketplace。

    3. 裁剪 installer/src/uninstall/remover.js（NEW-MEDIUM-1：import 仅保留实际仍被使用的 getSkillsDir）：
       - import 改指向 ./paths.js：`const { getSkillsDir } = require('./paths.js');`
         **理由（NEW-MEDIUM-1 根因）**：删除 Step 5/6 后，`readClaudeConfig`（remover.js:110）与 `writeClaudeConfig`（:114）的唯一调用点随 Step 6 一起消失，仅 `getSkillsDir`（:90，Step 4）保留调用点。若仍 import `{readClaudeConfig, writeClaudeConfig, getSkillsDir}` 则前两者成死 import —— 与 MEDIUM-1（detector.js）同型的对称遗漏，本轮根因修复。
       - 移除对 marketplace/config-manager.js 与 marketplace/plugin-installer.js 的 require。
       - hooks-installer 的 import（_readSettings/_writeSettings/_removeExistingNotifyHooks/getHooksDir/getCommandsDir）保持不变。
       - removeAllComponents 中删除 Step 5（Marketplace Cache：删 cache/work-skills 与 marketplaces/work-skills 目录）与 Step 6（Marketplace Source：从 config.json 删 marketplaceSources['work-skills']）两个 removeStep 调用。
       - 保留 Step 1（Hook Scripts）、Step 2（Hook Registration）、Step 3（Slash Commands）、Step 4（Plugin Directories，遍历 detectionResults.plugins）、Step 7（Environment Variables，遍历 token/user）。
       - 更新文件顶部 JSDoc：从“7 removal steps”改为“5 removal steps”，移除 marketplace cache/source 描述。

    4. 裁剪 installer/src/uninstall/formatter.js：
       - 移除“Category: Marketplace Source”渲染块（formatCategoryHeader(t('uninstall.category.marketplace')) + 对应 formatRow marketSource）。
       - 保留 plugins / hooks / hookRegistration / commands / environment 五个类别块。

    5. 裁剪 installer/src/uninstall/index.js：
       - countInstalled：移除 `if (results.marketplaceSource.installed) count++;` 一行。
       - countTotal：注释与公式更新——移除 marketplaceSource 项，公式从 `plugins.length + 6` 改为 `plugins.length + 5`（1 hooksScripts + 1 hooksRegistered + 1 commandsInstalled + 2 envVars；plugins.length 现为 1 即 claude-notify，total=6）。更新“// 2 plugins + ...”注释为单 plugin 的正确计数说明。
       - runUninstall 流程本身（detect → format → nothing-installed → summary → confirm → remove → report）保持不变。
  </action>
  <verify>
    <automated>
    test -f installer/src/uninstall/paths.js &&
    ! grep -rn "require.*marketplace" installer/src/uninstall/ &&
    ! grep -q "marketplaceSource" installer/src/uninstall/detector.js &&
    ! grep -q "marketplaceSource" installer/src/uninstall/remover.js &&
    ! grep -q "Marketplace Cache\|Marketplace Source" installer/src/uninstall/remover.js &&
    ! grep -q "uninstall.category.marketplace" installer/src/uninstall/formatter.js &&
    node -e "const {detectAllInstalled}=require('./installer/src/uninstall/detector.js'); const {removeAllComponents}=require('./installer/src/uninstall/remover.js'); const {formatDetectionTable}=require('./installer/src/uninstall/formatter.js'); const {runUninstall}=require('./installer/src/uninstall/index.js'); const p=require('./installer/src/uninstall/paths.js'); if(typeof p.getSkillsDir!=='function'||typeof p.isPluginInstalled!=='function') process.exit(1); if(p.readClaudeConfig!==undefined||p.writeClaudeConfig!==undefined||p.getConfigPath!==undefined) process.exit(2); console.log(typeof detectAllInstalled, typeof removeAllComponents, typeof runUninstall, 'paths exports:', Object.keys(p).join(','))"
    </automated>
  </verify>
  <done>
    - installer/src/uninstall/paths.js 存在并导出 getSkillsDir/isPluginInstalled（仅迁移实际被使用的 2 个 helper；config helper（readClaudeConfig/writeClaudeConfig/getConfigPath）在 uninstall 中无消费者，不迁移以免死导出，NEW-MEDIUM-1 根因修复）
    - uninstall/{detector,remover}.js 不再 require 任何 marketplace 模块（grep require.*marketplace 在 uninstall/ 下无命中）
    - detector.js 仅从 paths.js import `{ isPluginInstalled, getSkillsDir }`（MEDIUM-1：marketplace 块删除后 readClaudeConfig/getConfigPath 在 detector.js 中无调用点，不引入死 import）
    - detector.js 不再产出 marketplaceSource 字段，hasAnyInstalled 不含该项
    - remover.js 的 removeAllComponents 仅剩 5 步（hooks scripts / hook registration / slash commands / plugins / env vars），无 marketplace cache/source 步
    - formatter.js 不再渲染 marketplace 类别
    - index.js countTotal 公式与 plugins.length+5 一致，countInstalled 无 marketplaceSource 项
    - require detector/remover/formatter/index/paths 均不抛异常
  </done>
</task>

<task type="auto">
  <name>Task 3: 物理删除 marketplace 目录，裁剪主 index.js 与 cli.js (INS-02, INS-03)</name>
  <files>
    installer/src/marketplace/ (目录删除，收尾),
    installer/src/index.js (修改),
    installer/src/cli.js (修改,仅注释微调)
  </files>
  <read_first>
    installer/src/index.js （当前 import runMarketplaceIntegration + Step 7 调用 + Step 5 pip 过滤逻辑含 Git/TortoiseGit/PuTTY/SSH 名）,
    installer/src/cli.js （--uninstall 选项，INS-03 保留）,
    确认 Task 2 已完成（uninstall 不再 require marketplace）
  </read_first>
  <action>
    前置：确认 Task 1 与 Task 2 已完成（detectors/configurators 聚合器已裁剪，uninstall 已改指向 paths.js）。此时 marketplace/ 仅被 installer/src/index.js 引用。

    1. 物理删除 marketplace 目录：`git rm -r installer/src/marketplace` （此时无人再 require marketplace，删除安全）

    2. 裁剪 installer/src/index.js（INS-02 主流程 + INS-01 pip 过滤收窄）：
       - 移除 import：`const { runMarketplaceIntegration } = require('./marketplace/index.js');` 一行。
       - 保留其余 import（checkPlatform/parseArgs/showWelcome/runAllDetectors/runInstaller/runAllConfigurators/runHooksInstallation/runVerification/runUninstall）不变。
       - main() 中移除 Step 7 marketplace 调用：删除 `// Step 7: Marketplace integration (Phase 18)` 与 `await runMarketplaceIntegration();` 两行。
       - 原“Step 7.5: Register global hooks”注释重编号为 Step 7（保持注释连续性，非必须但建议）。
       - Step 5 pip 过滤逻辑收窄：当前 `results.filter(r => r.name && r.name !== 'Python' && r.name !== 'Git' && !r.name.includes('TortoiseGit') && !r.name.includes('PuTTY') && !r.name.includes('SSH'))`。由于 detectors 已不产出 Git/TortoiseGit/PuTTY/SSH 结果（Task 1 已移除），简化为仅排除 Python：`results.filter(r => r.name && r.name !== 'Python')`。保留外层 `if (!allPassed) { ... if (pipResults.some(r => !r.installed)) { await runInstaller(results); } }` 结构不变。
       - main() 中 --uninstall 与 --verify 分支保持不变（INS-03 保留 --uninstall 入口）。

    3. installer/src/cli.js（INS-03）：保留 `--uninstall` 选项与 uninstallOnly 返回字段不变（卸载入口保留）。仅当描述文案需收窄时可把 `.description('Work Skills Setup - Claude Code skills installer for Windows developers')` 保持原样（已足够通用，无需改动）。本 Task 对 cli.js 原则上 zero-touch；如执行器发现无必要改动则跳过该文件，不强求修改。
  </action>
  <verify>
    <automated>
    test ! -d installer/src/marketplace &&
    test ! -f installer/src/marketplace/index.js &&
    ! grep -q "runMarketplaceIntegration" installer/src/index.js &&
    ! grep -q "marketplace" installer/src/index.js &&
    ! grep -q "TortoiseGit\|PuTTY\|'Git'\|includes('SSH')" installer/src/index.js &&
    node -e "require('./installer/src/index.js')" &&
    node -e "const {parseArgs}=require('./installer/src/cli.js'); const o=parseArgs(['node','setup.js','--uninstall']); console.log('uninstallOnly=',o.uninstallOnly)"
    </automated>
  </verify>
  <done>
    - installer/src/marketplace/ 目录物理消失
    - index.js 无 runMarketplaceIntegration import 与调用，无 marketplace 字样
    - index.js 的 pip 过滤仅排除 'Python'，无 Git/TortoiseGit/PuTTY/SSH 残留
    - cli.js 仍保留 --uninstall 选项且 parseArgs(['--uninstall']).uninstallOnly === true
    - `node -e "require('./installer/src/index.js')"` 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 4: 收窄 i18n (en/zh) 与 welcome 横幅为 claude-notify 单技能范围 (INS-04)</name>
  <files>
    installer/src/i18n/en.json (修改),
    installer/src/i18n/zh.json (修改),
    installer/src/welcome.js (修改)
  </files>
  <read_first>
    installer/src/i18n/en.json （含 gitSSH.* / gitUser.* / marketplace.* / ssh.* / guidance.installGit / guidance.installTortoiseGit / guidance.installPuTTY / guidance.installSSHTools / config.section.gitSSH / config.section.gitUser / uninstall.category.marketplace / uninstall.item.marketplaceSource 等待删键）,
    installer/src/i18n/zh.json （同上对应中文键）,
    installer/src/welcome.js （features 数组引用 welcome.feature1/2/3）,
    installer/src/detectors/python.js, installer/src/detectors/pip-package.js, installer/src/configurators/pushover.js, installer/src/hooks/index.js, installer/src/verification/*.js （grep 它们实际用到的 t() 键，确保不误删保留键）
  </read_first>
  <action>
    1. installer/src/i18n/en.json 与 installer/src/i18n/zh.json —— 删除以下键（en 与 zh 必须同步删除，保持两文件键集一致）：
       - guidance 子树中 git/ssh 相关：`guidance.installGit`, `guidance.installTortoiseGit`, `guidance.installPuTTY`, `guidance.installSSHTools`（保留 guidance.installPython, guidance.installRequests, guidance.installPermission, guidance.installNetwork, guidance.installPipNotFound, guidance.installUnknown, guidance.pushoverManual）
       - `ssh.tortoiseGitFound`, `ssh.puttyFound`, `ssh.noneFound`（整个 ssh.* 前缀，仅这 3 键）
       - `config.section.gitSSH`, `config.section.gitUser`（保留 config.section.pushover, config.summary, config.status.*）
       - 所有 `gitSSH.*` 键（configured/notConfigured/recommended/guidance/step1..4/command/docs/promptSkip/skipped，共约 12 键）
       - 所有 `gitUser.*` 键（alreadyConfigured/required/promptName/promptEmail/configured/failed/partiallyConfigured/promptKeepConfig/promptKeepName/promptKeepEmail/nameRequired/emailRequired，共约 12 键）
       - 所有 `marketplace.*` 键（title/registering/registered/fetching/available/select_plugins/none_selected/checking/already_installed/installing/install_success/install_skipped/install_failed/summary/summary_installed/summary_skipped/summary_failed/complete/error.network/error.git/error.unknown，共约 21 键）
       - `uninstall.category.marketplace` 与 `uninstall.item.marketplaceSource`（保留其余 uninstall.* 键：title/cliHelp/category.plugins/category.hooks/category.hookRegistration/category.commands/category.environment/status.*/summary/nothingFound/item.pluginDir/item.hookScript/item.hookReg/item.command/item.envVar/remove.*）

    2. welcome.js 文案收窄（feature 列表 3→1）：
       - features 数组从 3 项收为 1 项，仅保留 claude-notify 通知相关：
         `const features = [ color.yellow('* ') + t('welcome.feature1') ].join('\n  ');`
       - en.json / zh.json 的 welcome.feature1 改为 claude-notify 通知语义：
         - en: `"welcome.feature1": "Install claude-notify - push notifications when Claude Code tasks finish or need input"`
         - zh: `"welcome.feature1": "安装 claude-notify — Claude Code 任务完成或等待输入时推送通知"`
       - 删除 welcome.feature2 与 welcome.feature3 两个键（en 与 zh 同步）。
       - welcome.subtitle 可选收窄为单技能语义（建议）：
         - en: `"welcome.subtitle": "Claude Code notification skill (claude-notify) installer for Windows developers"`
         - zh: `"welcome.subtitle": "为 Windows 开发者准备的 Claude Code 通知技能 (claude-notify) 安装工具"`
       - welcome.title 保持 "Work Skills 安装器" / "Work Skills Setup" 不变（包名未改）。

    3. 删除后必须验证无孤立引用：在 installer/src/ 下 grep 已删键，确认无任何 t('已删键') 残留调用。重点检查 detectors/python.js、detectors/pip-package.js、configurators/pushover.js、hooks/、verification/、uninstall/ 是否仍引用已删键。若发现残留（例如某文件仍调用 t('guidance.installGit')），修正该调用（删除该分支或改为保留键）。

    4. JSON 合法性：删除后两文件必须是合法 JSON（逗号、引号正确，无尾逗号）。
  </action>
  <verify>
    <automated>
    node -e "JSON.parse(require('fs').readFileSync('installer/src/i18n/en.json','utf8')); JSON.parse(require('fs').readFileSync('installer/src/i18n/zh.json','utf8')); console.log('json ok')" &&
    ! grep -qE "(gitSSH\.|gitUser\.|marketplace\.|guidance\.installGit|guidance\.installTortoiseGit|guidance\.installPuTTY|guidance\.installSSHTools|ssh\.tortoiseGitFound|ssh\.puttyFound|ssh\.noneFound|config\.section\.gitSSH|config\.section\.gitUser|uninstall\.category\.marketplace|uninstall\.item\.marketplaceSource|welcome\.feature2|welcome\.feature3)" installer/src/i18n/en.json &&
    ! grep -qE "(gitSSH\.|gitUser\.|marketplace\.|guidance\.installGit|guidance\.installTortoiseGit|guidance\.installPuTTY|guidance\.installSSHTools|ssh\.tortoiseGitFound|ssh\.puttyFound|ssh\.noneFound|config\.section\.gitSSH|config\.section\.gitUser|uninstall\.category\.marketplace|uninstall\.item\.marketplaceSource|welcome\.feature2|welcome\.feature3)" installer/src/i18n/zh.json &&
    ! grep -rnE "t\('(gitSSH\.|gitUser\.|marketplace\.|guidance\.installGit|guidance\.installTortoiseGit|guidance\.installPuTTY|guidance\.installSSHTools|ssh\.tortoiseGitFound|ssh\.puttyFound|ssh\.noneFound|config\.section\.gitSSH|config\.section\.gitUser|uninstall\.category\.marketplace|uninstall\.item\.marketplaceSource|welcome\.feature2|welcome\.feature3)" installer/src/ &&
    node -e "const {showWelcome}=require('./installer/src/welcome.js'); showWelcome({useColors:false}); console.log('welcome ok')"
    </automated>
  </verify>
  <done>
    - en.json 与 zh.json 均为合法 JSON
    - 两文件均无 gitSSH.*/gitUser.*/marketplace.*/ssh.*/git 相关 guidance/config.section/uninstall.category.marketplace/welcome.feature2/welcome.feature3 键
    - installer/src/ 下任何 .js 文件的 t() 调用均不引用已删键（grep 无命中）
    - welcome.js 仅渲染 1 项 feature（welcome.feature1），输出含 claude-notify 语义
    - welcome.js 与 i18n 加载均不抛异常
  </done>
</task>

</tasks>

<verification>
本计划（源码裁剪）层面的验证：
1. `node -e "require('./installer/src/index.js')"` 退出码 0 —— 主入口可加载。
2. `test ! -d installer/src/marketplace` —— marketplace 目录消失。
3. `test ! -f installer/src/detectors/git.js` 且同 ssh-tools.js / configurators/git-ssh.js / configurators/git-user.js 均不存在。
4. `node -e "const {runAllDetectors}=require('./installer/src/detectors/index.js')"` 与 configurators/index.js / uninstall 各模块均可 require 不抛异常。
5. en.json / zh.json 合法 JSON 且无已删键。
6. 全量 jest 测试在 02 计划完成后统一验证（本计划不改测试，部分旧测试此时会因模块删除而失败是预期的 —— 由 02 计划修复）。
</verification>

<success_criteria>
- SC1（无 git/ssh 检测配置）：detectors/git.js、detectors/ssh-tools.js、configurators/git-ssh.js、configurators/git-user.js 已删除；runAllDetectors 仅 2 项；runAllConfigurators 仅 Pushover；保留 python.js/pip-package.js/pushover.js。✅ 由 Task 1 满足。
- SC2（无 marketplace 集成）：marketplace/ 目录删除；index.js 无 runMarketplaceIntegration；claude-notify 安装/hook 注册仅由 runHooksInstallation 承担。✅ 由 Task 3 满足。
- SC3（卸载裁剪 + --uninstall 保留）：uninstall/ 裁剪为仅 claude-notify 组件（移除 marketplace source 检测/移除），--uninstall CLI 入口保留；marketplace 中**实际被 uninstall 使用**的 2 个 helper（getSkillsDir/isPluginInstalled）迁移到 uninstall/paths.js（NEW-MEDIUM-1：另 3 个 config helper 无消费者，不迁移）。✅ 由 Task 2 + Task 3 满足。
- SC4（i18n + welcome 收窄）：en/zh.json 移除 git/marketplace/多技能键且无孤立引用；welcome feature 3→1。✅ 由 Task 4 满足。
- SC5（剩余测试通过）：由 02 计划负责（测试裁剪 + jest 全绿）。
- `node -e "require('./installer/src/index.js')"` 退出码 0。
</success_criteria>

<artifacts>
## Artifacts this phase produces

### 新增符号（New）
- `installer/src/uninstall/paths.js`（新文件）：从已删 marketplace 迁移的路径/配置 helper 模块。**NEW-MEDIUM-1 根因修复**：仅迁移 uninstall 下实际仍被使用的 2 个 helper。导出：
  - `getSkillsDir()` — 返回 ~/.claude/skills
  - `isPluginInstalled(pluginName)` — 检查 SKILL.md 存在性
  理由：marketplace 目录删除后，uninstall 子系统仅需要这 2 个 helper（detector.js 用 isPluginInstalled+getSkillsDir；remover.js 仅用 getSkillsDir）。原 marketplace 的 getConfigPath/readClaudeConfig/writeClaudeConfig 在 uninstall 下无消费者（其唯一调用点在被删的 marketplace 检测/移除块内），迁移它们只会制造死导出，故不迁移。解耦 + 保持 CJS 模块边界。

### 修改的导出/契约（Changed）
- `detectors/index.js` runAllDetectors：Promise.all 从 4 项（Python/Git/SSH/requests）→ 2 项（Python/requests）。返回结构 {results, allPassed} 不变，但 results.length 由 4 变 2。
- `configurators/index.js` runAllConfigurators：从 3 步（Pushover/Git SSH/Git user）→ 1 步（Pushover）。displayConfigSummary 导出不变。
- `uninstall/detector.js` detectAllInstalled：返回对象移除 `marketplaceSource` 字段；hasAnyInstalled 聚合不再含 marketplace 项。import 从 `../marketplace/{plugin-installer,config-manager}.js` 收窄为仅 `const { isPluginInstalled, getSkillsDir } = require('./paths.js')`（MEDIUM-1：marketplace 块删除后 readClaudeConfig/getConfigPath 在 detector.js 无调用点，不引入）。保留字段：plugins / hooksScripts / hooksRegistered / commandsInstalled / envVars / hasAnyInstalled。
- `uninstall/remover.js` removeAllComponents：从 7 步 → 5 步（移除 Marketplace Cache 与 Marketplace Source 两步）。plugins 遍历仍存在（仅 claude-notify 一项）。import 从 `../marketplace/{config-manager,plugin-installer}.js` 收窄为仅 `const { getSkillsDir } = require('./paths.js')`（NEW-MEDIUM-1：删 Step 5/6 后 readClaudeConfig/writeClaudeConfig 在 remover.js 无调用点，不引入死 import；与 detector.js 对称）。
- `uninstall/formatter.js` formatDetectionTable：移除 marketplace 类别渲染块。
- `uninstall/index.js`：countTotal 公式 `plugins.length + 6` → `plugins.length + 5`；countInstalled 移除 marketplaceSource 项。
- `index.js` main()：移除 marketplace 步骤；pip 过滤简化为仅排除 'Python'。
- `welcome.js`：features 从 3 项 → 1 项。
- `i18n/en.json` / `zh.json`：移除约 50+ 个 git/marketplace/多技能相关键；welcome.feature1 改为 claude-notify 语义；新增无键（仅删除/改值）。

### 删除的符号/文件（Removed）
- `installer/src/detectors/git.js`（detectGit 导出消失）
- `installer/src/detectors/ssh-tools.js`（detectSSHTools 导出消失）
- `installer/src/configurators/git-ssh.js`（configureGitSSH / detectGitSSH 导出消失）
- `installer/src/configurators/git-user.js`（configureGitUser / detectGitUser 导出消失）
- `installer/src/marketplace/` 整目录（index.js 的 runMarketplaceIntegration、config-manager.js 的 getConfigPath/readClaudeConfig/writeClaudeConfig/registerMarketplaceSource、plugin-discovery.js、plugin-installer.js 的 getSkillsDir/isPluginInstalled/installPlugin/installPlugins/REPO_URL 全部消失；其中 **getSkillsDir/isPluginInstalled** 已迁移到 uninstall/paths.js 续命；getConfigPath/readClaudeConfig/writeClaudeConfig 因 uninstall 下无消费者而**不迁移**，随 marketplace 目录一同删除 —— NEW-MEDIUM-1 根因修复）
- i18n 键：gitSSH.*、gitUser.*、marketplace.*、ssh.*、guidance.installGit/installTortoiseGit/installPuTTY/installSSHTools、config.section.gitSSH/gitUser、uninstall.category.marketplace、uninstall.item.marketplaceSource、welcome.feature2、welcome.feature3
</artifacts>

<output>
完成时创建 `.planning/phases/54-trim-installer-notify-only/54-01-SUMMARY.md`
</output>
