---
phase: 37-fullstack-fix
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - tmp/gojs-calculator/internal/history/store.go
  - tmp/gojs-calculator/frontend/src/api/client.ts
autonomous: true
requirements: [FIX2-02, FIX2-03]

must_haves:
  truths:
    - "gojs-calculator compiles and all tests pass (go build + go test)"
    - "pyts-calculator unit tests pass (pytest tests/test_calculator.py)"
    - "gojs-calculator history endpoint returns JSON with lowercase field names (id, expression, result)"
    - "gojs-calculator batch endpoint response is correctly unwrapped by frontend client"
    - "pyts-calculator collector receive() writes meta field to cp-ts log"
    - "Updated golang.md template contains all key enhanced collector patterns"
    - "Updated python.md template contains meta writing pattern"
    - "Windows-specific paths use forward slashes (no filepath.Clean in SPA code)"
  artifacts:
    - path: "tmp/gojs-calculator/internal/history/store.go"
      provides: "History store with correct JSON tags"
      contains: "json:\"id\""
    - path: "tmp/gojs-calculator/frontend/src/api/client.ts"
      provides: "API client with batch response unwrapping"
      contains: "data.results || data"
    - path: "plugins/codepoint/references/golang.md"
      provides: "Updated template verified to contain enhanced collector patterns"
      contains: "getOrCreateTsFlowFile"
    - path: "plugins/codepoint/references/python.md"
      provides: "Updated template verified to contain meta writing pattern"
      contains: "_json.dumps(meta)"
  key_links:
    - from: "tmp/gojs-calculator/internal/history/store.go"
      to: "JSON serialization"
      via: "struct tags"
      pattern: "json:\"id\""
    - from: "tmp/gojs-calculator/frontend/src/api/client.ts"
      to: "/api/batch"
      via: "batch response handling"
      pattern: "data\\.results \\|\\| data"
    - from: "plugins/codepoint/references/golang.md"
      to: "enhanced collector verification"
      via: "pattern grep"
      pattern: "sync\\.Mutex.*getOrCreateTsFlowFile.*path\\.Clean"
    - from: "plugins/codepoint/references/python.md"
      to: "meta writing verification"
      via: "pattern grep"
      pattern: "_json\\.dumps\\(meta\\)"
---

<objective>
Re-verify all fixes in both gojs-calculator and pyts-calculator test projects, AND verify that the updated template files (golang.md, python.md) from Plan 37-02 contain the correct enhanced patterns. Per review feedback (37-REVIEWS.md), this plan now includes: (1) grep-based verification of updated templates, (2) Windows-specific checks, (3) documented rationale for collect-only integration tests.

Purpose: Phase 35 and 36 fixed bugs during execution but never ran a final consolidated verification. Plan 37-02 updated templates. This plan runs compilation, unit tests, targeted test-project checks, AND template pattern verification to close the loop.

Output: Verified gojs-calculator and pyts-calculator projects with all tests passing; confirmed template updates contain correct patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-fullstack-fix/37-DEFECTS.yaml
@.planning/phases/37-fullstack-fix/37-REVIEWS.md
@.planning/phases/37-fullstack-fix/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify gojs-calculator project fixes</name>
  <files>tmp/gojs-calculator/internal/history/store.go, tmp/gojs-calculator/frontend/src/api/client.ts</files>
  <read_first>
    - tmp/gojs-calculator/internal/history/store.go (verify json:"id", json:"expression", json:"result" tags present on Record struct)
    - tmp/gojs-calculator/frontend/src/api/client.ts (verify data.results || data fallback in batch response handling)
    - tmp/gojs-calculator/main.go (verify path.Clean usage at line ~41, NOT filepath.Clean)
    - tmp/gojs-calculator/codepoint/collector.go (verify enhanced collector with sync.Mutex, flow_id routing)
  </read_first>
  <acceptance_criteria>
    - `cd C:/WorkSpace/agent/work-skills/tmp/gojs-calculator && go build ./...` exits with code 0 (zero errors)
    - `cd C:/WorkSpace/agent/work-skills/tmp/gojs-calculator && go vet ./...` exits with code 0 (zero warnings)
    - `cd C:/WorkSpace/agent/work-skills/tmp/gojs-calculator && go test ./... -count=1` exits with code 0 (all 24 tests pass)
    - `grep "getOrCreateTsFlowFile" tmp/gojs-calculator/codepoint/collector.go` returns matches
    - `grep "path.Clean" tmp/gojs-calculator/main.go` returns matches
    - `grep "filepath.Clean" tmp/gojs-calculator/main.go` returns ZERO matches (no filepath.Clean anywhere in SPA handler)
    - `grep 'json:"id"' tmp/gojs-calculator/internal/history/store.go` returns matches
    - `grep "data.results || data" tmp/gojs-calculator/frontend/src/api/client.ts` returns matches
    - `grep "/" tmp/gojs-calculator/codepoint/collector.go | head -1` shows no backslash-only path patterns (forward slash paths used throughout)
  </acceptance_criteria>
  <action>Re-verify the gojs-calculator project is in a working state with all Phase 35 fixes applied:

1. **Run `cd C:/WorkSpace/agent/work-skills/tmp/gojs-calculator && go build ./...`** -- must compile with zero errors
2. **Run `cd C:/WorkSpace/agent/work-skills/tmp/gojs-calculator && go vet ./...`** -- must pass with zero warnings
3. **Run `cd C:/WorkSpace/agent/work-skills/tmp/gojs-calculator && go test ./... -count=1`** -- all 24 tests must pass (no -race flag per Windows CGO limitation documented in STATE.md)
4. **Verify DEV-FS-01 fix (collector enhancement):** grep `getOrCreateTsFlowFile` in `tmp/gojs-calculator/codepoint/collector.go` -- must be present
5. **Verify DEV-FS-03 fix (path.Clean):** grep `path.Clean` in `tmp/gojs-calculator/main.go` -- must be present. Also verify `filepath.Clean` is NOT used in the SPA fallback handler (grep `filepath.Clean` returns zero matches or only matches in unrelated sections)
6. **Windows-specific check:** Verify that the collector code uses forward-slash paths for log file names. Check that sanitizeFlowID does not produce backslash-containing filenames. Run `grep -n '\\\\\\\\' tmp/gojs-calculator/codepoint/collector.go` -- should return zero matches (no raw backslash in string literals used for file paths)
7. **Verify DEV-FS-04 fix (JSON tags):** grep `json:"id"` in `tmp/gojs-calculator/internal/history/store.go` -- must be present
8. **Verify DEV-FS-05 fix (batch unwrap):** grep `data.results || data` in `tmp/gojs-calculator/frontend/src/api/client.ts` -- must be present

If any verification fails, fix the issue directly in the test project file. All fixes should already be in place from Phase 35 execution -- this task is a re-verification pass.
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills/tmp/gojs-calculator && go build ./... && go vet ./... && go test ./... -count=1</automated>
  </verify>
  <done>
    - gojs-calculator compiles with `go build ./...` -- zero errors
    - gojs-calculator passes `go vet ./...` -- zero warnings
    - gojs-calculator passes all tests with `go test ./... -count=1` -- all 24 pass
    - collector.go has getOrCreateTsFlowFile (enhanced collector)
    - main.go uses path.Clean (Windows SPA fix)
    - main.go does NOT use filepath.Clean in SPA handler
    - store.go has json:"id" tag (JSON field mapping fix)
    - client.ts has data.results || data (batch unwrap fix)
    - No backslash path patterns in collector.go (Windows forward-slash verified)
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-verify pyts-calculator project fixes</name>
  <files>tmp/pyts-calculator/codepoint/collector.py</files>
  <read_first>
    - tmp/pyts-calculator/codepoint/collector.py (verify receive() writes meta field)
    - tmp/pyts-calculator/tests/conftest.py (verify _kill_port_holder function exists)
    - tmp/pyts-calculator/tests/test_linkage.py (verify batch test sends newline-separated string, not array)
    - tmp/pyts-calculator/tests/test_toggle.py (verify toggle cleanup exists)
  </read_first>
  <acceptance_criteria>
    - `cd C:/WorkSpace/agent/work-skills/tmp/pyts-calculator && python -m pytest tests/test_calculator.py -x -q` exits with code 0 (all 38 unit tests pass)
    - `grep "_json.dumps(meta)" tmp/pyts-calculator/codepoint/collector.py` returns matches
    - `grep "_kill_port_holder" tmp/pyts-calculator/tests/conftest.py` returns matches
    - `cd C:/WorkSpace/agent/work-skills/tmp/pyts-calculator && python -m pytest tests/test_linkage.py tests/test_toggle.py --collect-only -q` shows 8 tests collected (4 linkage + 4 toggle)
    - `grep "taskkill" tmp/pyts-calculator/tests/conftest.py` returns matches (Windows process cleanup present)
  </acceptance_criteria>
  <action>Re-verify the pyts-calculator project is in a working state with all Phase 36 fixes applied:

1. **Run `cd C:/WorkSpace/agent/work-skills/tmp/pyts-calculator && python -m pytest tests/test_calculator.py -x -q`** -- all 38 unit tests must pass
2. **Verify DEV-FS-02 fix (collector meta writing):** grep `_json.dumps(meta)` in `tmp/pyts-calculator/codepoint/collector.py` -- must be present. The receive() function must extract meta from entry and write it as JSON.
3. **Verify DEV-FS-06 fix (batch format):** grep `\\\\n` or literal newline pattern in `tmp/pyts-calculator/tests/test_linkage.py` -- the batch test must send newline-separated string, not array. Specifically look for the string `"1+1\\n2*3\\n10/2"` or equivalent.
4. **Verify DEV-FS-07 fix (port holder kill):** grep `_kill_port_holder` in `tmp/pyts-calculator/tests/conftest.py` -- must be present. Also grep `taskkill` to confirm the Windows-specific process cleanup is present.
5. **Verify DEV-FS-08 fix (toggle restoration):** check that test_toggle.py restores toggle files after tests (grep for touch or toggle restoration logic)
6. **Collect integration tests:** Run `cd C:/WorkSpace/agent/work-skills/tmp/pyts-calculator && python -m pytest tests/test_linkage.py tests/test_toggle.py --collect-only -q` -- all 8 integration tests must be collected (4 linkage + 4 toggle).

**Rationale for collect-only (not running) integration tests:** The 8 integration tests (4 linkage + 4 toggle) require starting a FastAPI server on port 18091, which conflicts with any running manual server instance. On Windows, port cleanup via netstat+taskkill is available but adds flakiness risk. The unit tests (38 in test_calculator.py) already verify the core computation logic. Integration tests were verified passing during Phase 36 execution (confirmed in 36-02-SUMMARY.md). Collect-only confirms the test discovery is correct and no imports or fixtures are broken. This is the documented acceptance criteria: 8 tests collected = PASS.

If any verification fails, fix the issue directly in the test project file. All fixes should already be in place from Phase 36 execution -- this task is a re-verification pass.
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills/tmp/pyts-calculator && python -m pytest tests/test_calculator.py -x -q</automated>
  </verify>
  <done>
    - pyts-calculator unit tests pass: 38/38 in test_calculator.py
    - collector.py receive() writes meta with _json.dumps(meta)
    - conftest.py has _kill_port_holder with taskkill for Windows process management
    - test_linkage.py batch test sends newline-separated string
    - 8 integration tests collected (4 linkage + 4 toggle) -- collect-only accepted per documented rationale
    - test_toggle.py has toggle restoration in teardown
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify updated template files contain correct enhanced patterns</name>
  <files></files>
  <read_first>
    - plugins/codepoint/references/golang.md (verify enhanced collector patterns: sync.Mutex, getOrCreateTsFlowFile, path.Clean, inline comments, SPA separation, integration notes)
    - plugins/codepoint/references/python.md (verify meta writing pattern: _json.dumps(meta), entry.get("meta"), threading.Lock)
  </read_first>
  <acceptance_criteria>
    - `grep "getOrCreateTsFlowFile" plugins/codepoint/references/golang.md` returns >= 1 match
    - `grep "sync.Mutex" plugins/codepoint/references/golang.md` returns >= 1 match
    - `grep "path.Clean" plugins/codepoint/references/golang.md` returns >= 1 match
    - `grep "sanitizeFlowID" plugins/codepoint/references/golang.md` returns >= 1 match
    - `grep "_json.dumps(meta)" plugins/codepoint/references/python.md` returns >= 1 match
    - `grep 'entry.get("meta")' plugins/codepoint/references/python.md` returns >= 1 match
    - `grep -c "filepath.Clean" plugins/codepoint/references/golang.md` returns 0 (no filepath.Clean in SPA section)
    - `grep -i "SPA\|spa fallback\|single-page" plugins/codepoint/references/golang.md` returns >= 1 match (SPA is in a separated section)
    - `grep -i "integration note\|PointWithMeta" plugins/codepoint/references/golang.md` returns >= 1 match (integration notes present)
    - `grep "sessionState" plugins/codepoint/references/golang.md` returns >= 1 match (Base Library untouched)
    - `grep "sessionState" plugins/codepoint/references/python.md` returns >= 1 match (Base Library untouched)
  </acceptance_criteria>
  <action>Verify that the template files updated in Plan 37-02 contain all the required enhanced patterns. This task addresses the review concern (37-REVIEWS.md HIGH): "Plan 37-03 verifies test projects but NOT updated templates." Read the updated template files and confirm key patterns are present.

**Golang.md checks (plugins/codepoint/references/golang.md):**

1. **Enhanced collector patterns:** grep for each of these -- ALL must be present:
   - `getOrCreateTsFlowFile` -- per-flow file routing function
   - `sync.Mutex` -- thread safety for concurrent writes
   - `sanitizeFlowID` -- filename sanitization
   - `path.Clean` -- Windows-compatible SPA fallback
   - `CloseCollector` -- cleanup function

2. **Inline comments present:** Verify the enhanced collector code block contains comments (lines starting with `//` inside the Go code blocks) explaining:
   - sync.Mutex purpose
   - flow_id routing logic
   - meta vs non-meta handling
   Run: `grep -c "// " plugins/codepoint/references/golang.md` -- should be >= 5 in the collector section area

3. **SPA fallback separated:** Verify there is a heading or sub-section that clearly separates the SPA fallback fix from the collector code. grep for "SPA" or "spa fallback" or "single-page" in a heading context.

4. **Integration notes present:** grep for "PointWithMeta" or "integration note" -- confirms the integration notes explaining how enhanced collector coordinates with base library are present.

5. **No filepath.Clean in SPA section:** `grep "filepath.Clean" plugins/codepoint/references/golang.md` must return ZERO matches. The SPA section must use path.Clean only.

6. **Base Library untouched:** `grep "sessionState" plugins/codepoint/references/golang.md` must still return matches.

**Python.md checks (plugins/codepoint/references/python.md):**

1. **Meta writing pattern:** grep for `_json.dumps(meta)` -- must be present
2. **Meta extraction:** grep for `entry.get("meta")` -- must be present
3. **Thread safety:** grep for `threading.Lock` -- must be present (from original, confirm not broken)
4. **Backward compatibility:** grep for `if meta:` -- must be present (conditional meta handling)
5. **Base Library untouched:** `grep "sessionState" plugins/codepoint/references/python.md` must still return matches

If any check fails, the template was not correctly updated in Plan 37-02. Report the specific missing pattern as a defect -- do NOT fix the template in this task (that is Plan 37-02's responsibility).
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && echo "=== golang.md checks ===" && grep -c "getOrCreateTsFlowFile" plugins/codepoint/references/golang.md && grep -c "sync.Mutex" plugins/codepoint/references/golang.md && grep -c "path.Clean" plugins/codepoint/references/golang.md && grep -c "sanitizeFlowID" plugins/codepoint/references/golang.md && grep -c "filepath.Clean" plugins/codepoint/references/golang.md && grep -c "sessionState" plugins/codepoint/references/golang.md && echo "=== python.md checks ===" && grep -c "_json.dumps(meta)" plugins/codepoint/references/python.md && grep -c 'entry.get("meta")' plugins/codepoint/references/python.md && grep -c "sessionState" plugins/codepoint/references/python.md</automated>
  </verify>
  <done>
    - golang.md contains getOrCreateTsFlowFile, sync.Mutex, path.Clean, sanitizeFlowID (enhanced collector)
    - golang.md contains zero instances of filepath.Clean (Windows SPA fix correct)
    - golang.md has SPA fallback in separated section (not mixed into collector code)
    - golang.md has integration notes mentioning PointWithMeta
    - golang.md Base Library untouched (sessionState present)
    - python.md contains _json.dumps(meta) and entry.get("meta") (meta writing)
    - python.md Base Library untouched (sessionState present)
  </done>
</task>

</tasks>

<verification>
- gojs-calculator: `cd tmp/gojs-calculator && go build ./... && go test ./... -count=1` passes
- pyts-calculator: `cd tmp/pyts-calculator && python -m pytest tests/test_calculator.py -x -q` passes
- Template files (golang.md, python.md) contain enhanced patterns verified by grep
- golang.md has zero filepath.Clean matches (Windows SPA fix correct)
- golang.md has separated SPA section and integration notes
- All 8 defects in 37-DEFECTS.yaml confirmed fixed
- 8 integration tests collected (collect-only, rationale documented)
</verification>

<success_criteria>
- gojs-calculator compiles and all 24 tests pass
- pyts-calculator all 38 unit tests pass
- pyts-calculator 8 integration tests collected (collect-only per documented rationale)
- All defect fixes verified present in test project code
- golang.md template verified to contain sync.Mutex, getOrCreateTsFlowFile, path.Clean, sanitizeFlowID
- golang.md template verified to have separated SPA section, integration notes, and zero filepath.Clean
- python.md template verified to contain _json.dumps(meta) and entry.get("meta")
- Both templates' Base Library sections untouched
- Windows-specific: no filepath.Clean in SPA section, no backslash paths in collector, taskkill in conftest
</success_criteria>

<output>
After completion, create `.planning/phases/37-fullstack-fix/37-03-SUMMARY.md`
</output>
