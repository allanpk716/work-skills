---
phase: 34-single-lang-fix
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - .planning/phases/34-single-lang-fix/34-03-SUMMARY.md
autonomous: false
requirements:
  - FIX1-02
  - FIX1-03
user_setup: []
must_haves:
  truths:
    - "/codepoint:scan runs on go-calculator and output contains business flow identification keywords (at least one of: 'API', 'batch', 'history', 'flow')"
    - "/codepoint:scan runs on python-calculator and output contains business flow identification keywords (at least one of: 'API', 'batch', 'history', 'flow')"
    - "/codepoint:plan runs on go-calculator and output contains probe location references (at least one of: 'point_id', 'flow_id', 'probe', 'codepoint')"
    - "/codepoint:plan runs on python-calculator and output contains probe location references (at least one of: 'point_id', 'flow_id', 'probe', 'codepoint')"
    - "/codepoint:implement runs on go-calculator and output contains generated code or confirmation (at least one of: 'PointWithMeta', 'codepoint', 'probe', 'implemented')"
    - "/codepoint:implement runs on python-calculator and output contains generated code or confirmation (at least one of: 'point_json', 'codepoint', 'probe', 'implemented')"
    - "go-calculator compiles after implement: go build ./... exits 0"
    - "python-calculator runs after implement: python -m pytest tests/ -q passes all 57 tests"
  artifacts:
    - path: ".planning/phases/34-single-lang-fix/34-03-SUMMARY.md"
      provides: "Skill E2E verification results for both Go and Python"
      min_lines: 40
  key_links:
    - from: "/codepoint:scan skill"
      to: "tmp/go-calculator"
      via: "Claude Code skill invocation"
      pattern: "codepoint:scan"
    - from: "/codepoint:scan skill"
      to: "tmp/python-calculator"
      via: "Claude Code skill invocation"
      pattern: "codepoint:scan"
    - from: "/codepoint:plan skill"
      to: "tmp/go-calculator"
      via: "Claude Code skill invocation"
      pattern: "codepoint:plan"
    - from: "/codepoint:plan skill"
      to: "tmp/python-calculator"
      via: "Claude Code skill invocation"
      pattern: "codepoint:plan"
    - from: "/codepoint:implement skill"
      to: "tmp/go-calculator"
      via: "Claude Code skill invocation"
      pattern: "codepoint:implement"
    - from: "/codepoint:implement skill"
      to: "tmp/python-calculator"
      via: "Claude Code skill invocation"
      pattern: "codepoint:implement"
---

<objective>
Run /codepoint:scan, /codepoint:plan, and /codepoint:implement skill workflows on BOTH go-calculator and python-calculator projects, verifying the complete skill chain produces correct outputs and the resulting code compiles/runs.

Purpose: Per D-03, confirm that Phase 32's fixed template code and both test projects work correctly under the AI skill automation pipeline. This is the final Phase 34 verification - not only confirming code compiles, but that the full AI-driven scan->plan->implement workflow operates correctly on both language test projects.

Output: Skill E2E verification results for Go and Python (scan/plan/implement output summaries, automated grep checks, compile/run verification)

Addresses review concerns: HIGH-1 (Python skill E2E verification added - now covers both go-calculator AND python-calculator), HIGH-2 (all verification criteria are now grep-verifiable with specific keyword patterns, no subjective judgments), MEDIUM-5 (fallback verification adds unique checks not overlapping with Plan 02: skill-specific probe presence, flow_id coverage, stack log file existence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-single-lang-fix/34-CONTEXT.md
@.planning/phases/34-single-lang-fix/34-RESEARCH.md
@.planning/phases/34-single-lang-fix/34-DEFECTS.yaml
@.planning/phases/34-single-lang-fix/34-02-SUMMARY.md
@tmp/go-calculator/internal/calculator/calculator.go
@tmp/go-calculator/internal/api/server.go
@tmp/go-calculator/codepoint/codepoint.go
@tmp/python-calculator/calculator/calculator.py
@tmp/python-calculator/api/server.py
@tmp/python-calculator/codepoint/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /codepoint:scan on go-calculator and python-calculator, verify outputs</name>
  <files>tmp/go-calculator, tmp/python-calculator, .planning/phases/34-single-lang-fix/34-03-SUMMARY.md</files>
  <read_first>
    - tmp/go-calculator/internal/calculator/calculator.go (core shared computation code - what scan should identify)
    - tmp/go-calculator/internal/api/server.go (API handler code - business flow entry points)
    - tmp/python-calculator/calculator/calculator.py (Python core computation code)
    - tmp/python-calculator/api/server.py (Python API handler code)
  </read_first>
  <acceptance_criteria>
    - After scan on go-calculator: grep output contains at least 2 keywords from set {API, batch, history, flow, shared}
    - After scan on python-calculator: grep output contains at least 2 keywords from set {API, batch, history, flow, shared}
    - go-calculator still compiles: go build ./... exits 0
    - python-calculator tests still pass: python -m pytest tests/ -q exits 0
  </acceptance_criteria>
  <action>
    **Part A: Run /codepoint:scan on go-calculator**

    1. Confirm go-calculator project exists at tmp/go-calculator/
    2. Confirm .codepoints/ directory exists (Phase 32 created it; keep existing state per RESEARCH.md Open Question 1)
    3. Run /codepoint:scan with project path tmp/go-calculator/
    4. Capture scan output. Verify it contains business flow identification keywords by checking for at least 2 matches from: "API", "batch", "history", "flow", "shared"
    5. Record scan output key findings in 34-03-SUMMARY.md

    **Part B: Run /codepoint:scan on python-calculator**

    1. Confirm python-calculator project exists at tmp/python-calculator/
    2. Confirm .codepoints/ directory exists (Phase 33 created it)
    3. Run /codepoint:scan with project path tmp/python-calculator/
    4. Capture scan output. Verify it contains business flow identification keywords (same 2-keyword minimum check)
    5. Record scan output key findings in 34-03-SUMMARY.md

    **Fallback (if skill unavailable)** - these checks are UNIQUE to Plan 03 and do NOT overlap with Plan 02:
    Instead of repeating compile checks (Plan 02 territory), verify skill-specific artifacts:
    - grep -r "codepoint" tmp/go-calculator/internal/ returns matches (probe code present in business logic)
    - grep -r "flow_id" tmp/go-calculator/internal/ returns matches (V2 probe pattern in use)
    - grep -r "point_id" tmp/go-calculator/internal/ returns matches (V2 probe pattern in use)
    - grep -r "codepoint" tmp/python-calculator/ --include="*.py" returns matches (probe code present)
    - grep -r "flow_id" tmp/python-calculator/ --include="*.py" returns matches (V2 probe pattern)
    - ls ~/.codepoint/go-calculator/ 2>/dev/null or ls ~/.codepoint/python-calculator/ 2>/dev/null returns log files (probe output exists)
    - These fallback checks verify probe integration depth that Plan 02 (template compile only) does not cover
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/work-skills/tmp/go-calculator" && go build ./... 2>&1 && cd "C:/WorkSpace/agent/work-skills/tmp/python-calculator" && python -m pytest tests/ -q 2>&1 && echo "PASS"</automated>
  </verify>
  <done>
    /codepoint:scan successfully runs on both go-calculator and python-calculator. Output contains business flow identification keywords for both projects. Results recorded in 34-03-SUMMARY.md. Both projects remain buildable/testable after scan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run /codepoint:plan and /codepoint:implement on go-calculator and python-calculator</name>
  <files>tmp/go-calculator, tmp/python-calculator, .planning/phases/34-single-lang-fix/34-03-SUMMARY.md</files>
  <read_first>
    - tmp/go-calculator/internal/calculator/calculator.go (plan should identify key paths)
    - tmp/python-calculator/calculator/calculator.py (plan should identify key paths)
    - .planning/phases/34-single-lang-fix/34-RESEARCH.md (skill E2E verification notes)
    - .planning/phases/34-single-lang-fix/34-02-SUMMARY.md (template compile results - include in final summary)
  </read_first>
  <acceptance_criteria>
    - After plan on go-calculator: output contains at least 1 keyword from {point_id, flow_id, probe, codepoint, placement}
    - After plan on python-calculator: output contains at least 1 keyword from {point_id, flow_id, probe, codepoint, placement}
    - After implement on go-calculator: output contains at least 1 keyword from {PointWithMeta, codepoint, probe, implemented, inserted}
    - After implement on python-calculator: output contains at least 1 keyword from {point_json, codepoint, probe, implemented, inserted}
    - go-calculator compiles after implement: go build ./... exits 0
    - python-calculator tests pass after implement: python -m pytest tests/ -q exits 0 with "57 passed"
    - grep -r "PointWithMeta" tmp/go-calculator/internal/ returns >= 3 matches (V2 probe pattern in multiple locations)
    - grep -r "point_json\|point_with_meta" tmp/python-calculator/ --include="*.py" returns >= 3 matches
  </acceptance_criteria>
  <action>
    **Part A: Run /codepoint:plan on go-calculator and python-calculator**

    Go:
    1. Run /codepoint:plan with project path tmp/go-calculator/
    2. Check output contains probe location references: at least 1 match from "point_id", "flow_id", "probe", "codepoint", "placement"

    Python:
    1. Run /codepoint:plan with project path tmp/python-calculator/
    2. Check output contains probe location references: same keyword check

    Record plan outputs in 34-03-SUMMARY.md.

    **Part B: Run /codepoint:implement on go-calculator and python-calculator**

    Go:
    1. Run /codepoint:implement with project path tmp/go-calculator/
    2. Check output contains generated code confirmation: at least 1 match from "PointWithMeta", "codepoint", "probe", "implemented", "inserted"
    3. Verify generated code uses V2 pattern:
       ```bash
       grep -r "PointWithMeta" tmp/go-calculator/internal/ | wc -l
       ```
       Must return >= 3 (probes in multiple business flow locations)
    4. Post-implement compile check:
       ```bash
       cd tmp/go-calculator && go build ./...
       ```
       Must exit 0.

    Python:
    1. Run /codepoint:implement with project path tmp/python-calculator/
    2. Check output contains generated code confirmation: at least 1 match from "point_json", "codepoint", "probe", "implemented", "inserted"
    3. Verify generated code uses V2 pattern:
       ```bash
       grep -r "point_json\|point_with_meta" tmp/python-calculator/ --include="*.py" | wc -l
       ```
       Must return >= 3
    4. Post-implement test check:
       ```bash
       cd tmp/python-calculator && python -m pytest tests/ -q
       ```
       Must show "57 passed".

    **Fallback (if skill unavailable)** - verify existing probe integration depth (unique from Plan 02):
    - grep -c "PointWithMeta" tmp/go-calculator/internal/api/server.go returns >= 1
    - grep -c "flow_id" tmp/go-calculator/internal/api/server.go returns >= 1
    - grep -c "point_id" tmp/go-calculator/internal/api/server.go returns >= 1
    - grep -rc "point_json\|point_with_meta" tmp/python-calculator/ --include="*.py" | grep -v ":0$" returns >= 2 files
    - Verify probe density: flow_id values include "flow-api-calculate", "flow-batch-process", "flow-history-query" (Go) and equivalent Python flow IDs

    **Part C: Create 34-03-SUMMARY.md complete verification report**

    Create .planning/phases/34-single-lang-fix/34-03-SUMMARY.md containing:

    ```markdown
    ---
    phase: 34
    plan: "34-03"
    status: complete
    ---

    # Plan 34-03 Summary: Skill E2E Verification (Go + Python)

    ## Completed

    ### /codepoint:scan Verification

    #### go-calculator
    - Result: [PASS/Manual-verified]
    - Keywords found: [list matched keywords from {API, batch, history, flow, shared}]
    - Key findings: [business flows and shared code points identified]

    #### python-calculator
    - Result: [PASS/Manual-verified]
    - Keywords found: [list matched keywords]
    - Key findings: [business flows and shared code points identified]

    ### /codepoint:plan Verification

    #### go-calculator
    - Result: [PASS/Manual-verified]
    - Keywords found: [list matched keywords from {point_id, flow_id, probe, codepoint, placement}]

    #### python-calculator
    - Result: [PASS/Manual-verified]
    - Keywords found: [list matched keywords]

    ### /codepoint:implement Verification

    #### go-calculator
    - Result: [PASS/Manual-verified]
    - Keywords found: [list matched keywords]
    - V2 probe count (PointWithMeta): [N]
    - Post-implement build: [PASS/FAIL]

    #### python-calculator
    - Result: [PASS/Manual-verified]
    - Keywords found: [list matched keywords]
    - V2 probe count (point_json/point_with_meta): [N]
    - Post-implement tests: [PASS/FAIL] ([N] passed)

    ## Phase 34 Final Verification Checklist

    | Success Criteria | Status | Evidence |
    |-------------------|--------|----------|
    | SC1: All defects recorded | PASS | 34-DEFECTS.yaml (4 defects, all fixed) |
    | SC2: All defects fixed and verified | PASS | go test (5 pkgs) + pytest (57 tests) all green |
    | SC3: Template code compiles/runs | PASS | golang.md go build OK, python.md import OK |
    | SC4: Go skill E2E (scan/plan/implement) | PASS | Keyword checks passed, build green |
    | SC5: Python skill E2E (scan/plan/implement) | PASS | Keyword checks passed, tests green |

    ## Key Files

    - .planning/phases/34-single-lang-fix/34-DEFECTS.yaml
    - plugins/codepoint/references/golang.md (Quick Compile Check added)
    - plugins/codepoint/references/python.md (Quick Import Check added)
    ```
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/work-skills/tmp/go-calculator" && go build ./... 2>&1 && grep -c "PointWithMeta" "C:/WorkSpace/agent/work-skills/tmp/go-calculator/internal/api/server.go" && cd "C:/WorkSpace/agent/work-skills/tmp/python-calculator" && python -m pytest tests/ -q 2>&1 && echo "ALL PASS"</automated>
  </verify>
  <done>
    /codepoint:plan and /codepoint:implement run successfully on BOTH go-calculator and python-calculator (or manual fallback verification passes). Go: PointWithMeta probes present in >= 3 locations, go build exits 0. Python: point_json/point_with_meta probes in >= 3 locations, pytest 57 tests pass. 34-03-SUMMARY.md contains complete E2E verification report with keyword-verified results for both languages.
  </done>
</task>

</tasks>

<verification>
1. /codepoint:scan runs on go-calculator, output contains >= 2 keywords from {API, batch, history, flow, shared}
2. /codepoint:scan runs on python-calculator, output contains >= 2 keywords from {API, batch, history, flow, shared}
3. /codepoint:plan runs on go-calculator, output contains >= 1 keyword from {point_id, flow_id, probe, codepoint, placement}
4. /codepoint:plan runs on python-calculator, output contains >= 1 keyword from {point_id, flow_id, probe, codepoint, placement}
5. /codepoint:implement runs on go-calculator, output contains >= 1 keyword from {PointWithMeta, codepoint, probe, implemented}
6. /codepoint:implement runs on python-calculator, output contains >= 1 keyword from {point_json, codepoint, probe, implemented}
7. go-calculator compiles after implement: go build ./... exits 0
8. python-calculator tests pass after implement: pytest shows 57 passed
9. Go V2 probes: grep PointWithMeta returns >= 3 matches across internal/
10. Python V2 probes: grep point_json/point_with_meta returns >= 3 matches
11. 34-03-SUMMARY.md records all verification results
</verification>

<success_criteria>
- /codepoint:scan/plan/implement run successfully on BOTH go-calculator AND python-calculator
- All verification criteria are grep-verifiable with specific keyword patterns (no subjective judgments)
- Both projects compile/test successfully after skill execution
- V2 probe pattern (PointWithMeta/point_json with flow_id) present in both projects
- 34-03-SUMMARY.md contains complete E2E verification record for both languages
</success_criteria>

<output>
After completion, create `.planning/phases/34-single-lang-fix/34-03-SUMMARY.md`
</output>
