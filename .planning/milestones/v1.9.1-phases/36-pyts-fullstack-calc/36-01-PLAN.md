---
phase: 36-pyts-fullstack-calc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tmp/pyts-calculator/main.py
  - tmp/pyts-calculator/api/__init__.py
  - tmp/pyts-calculator/api/server.py
  - tmp/pyts-calculator/calculator/__init__.py
  - tmp/pyts-calculator/calculator/core.py
  - tmp/pyts-calculator/history/__init__.py
  - tmp/pyts-calculator/history/store.py
  - tmp/pyts-calculator/batch/__init__.py
  - tmp/pyts-calculator/batch/processor.py
  - tmp/pyts-calculator/codepoint/__init__.py
  - tmp/pyts-calculator/codepoint/collector.py
  - tmp/pyts-calculator/tests/test_calculator.py
  - tmp/pyts-calculator/frontend/package.json
  - tmp/pyts-calculator/frontend/tsconfig.json
  - tmp/pyts-calculator/frontend/tsconfig.app.json
  - tmp/pyts-calculator/frontend/tsconfig.node.json
  - tmp/pyts-calculator/frontend/vite.config.ts
  - tmp/pyts-calculator/frontend/index.html
  - tmp/pyts-calculator/frontend/src/main.tsx
  - tmp/pyts-calculator/frontend/src/App.tsx
  - tmp/pyts-calculator/frontend/src/index.css
  - tmp/pyts-calculator/frontend/src/vite-env.d.ts
  - tmp/pyts-calculator/frontend/src/lib/codepoint.ts
  - tmp/pyts-calculator/frontend/src/api/client.ts
  - tmp/pyts-calculator/frontend/src/components/Calculator.tsx
  - tmp/pyts-calculator/frontend/src/components/History.tsx
  - tmp/pyts-calculator/frontend/src/components/BatchCalc.tsx
autonomous: true
requirements: [FULL-05]

must_haves:
  truths:
    - "Python+TS 全栈计算器项目存在，包含后端 Python 和前端 React TS"
    - "三业务流（API计算、历史查询、批量处理）共享核心计算管道 parse->validate->compute->format"
    - "FastAPI 同时提供 API 端点、collector 端点和静态文件服务"
    - "前端构建产物通过 FastAPI StaticFiles mount 正常提供"
    - "单元测试验证核心计算管道正确性"
  artifacts:
    - path: "tmp/pyts-calculator/main.py"
      provides: "FastAPI 启动入口"
      contains: "uvicorn.run"
    - path: "tmp/pyts-calculator/api/server.py"
      provides: "FastAPI app + routes + collector + StaticFiles"
      exports: ["create_app"]
      contains: ["/__codepoint__", "/api/calculate", "/api/history", "StaticFiles"]
    - path: "tmp/pyts-calculator/calculator/core.py"
      provides: "核心计算管道"
      contains: "evaluate"
    - path: "tmp/pyts-calculator/codepoint/collector.py"
      provides: "前端 collector"
      contains: ["receive", "is_ts_enabled", "close_ts_collector"]
    - path: "tmp/pyts-calculator/frontend/dist/index.html"
      provides: "前端构建产物"
  key_links:
    - from: "tmp/pyts-calculator/api/server.py"
      to: "tmp/pyts-calculator/calculator/core.py"
      via: "from calculator.core import evaluate"
      pattern: "from calculator.core import evaluate"
    - from: "tmp/pyts-calculator/api/server.py"
      to: "tmp/pyts-calculator/codepoint/collector.py"
      via: "from codepoint.collector import receive, is_ts_enabled"
      pattern: "from codepoint.collector import"
    - from: "tmp/pyts-calculator/api/server.py"
      to: "frontend/dist/"
      via: "StaticFiles mount at /"
      pattern: "StaticFiles"
    - from: "tmp/pyts-calculator/frontend/src/components/Calculator.tsx"
      to: "/api/calculate"
      via: "fetch POST"
      pattern: "calculate"
---

<objective>
创建 Python+TS 全栈计算器项目，包含 FastAPI 后端和 React TS 前端。

Purpose: 建立跨语言探针联动验证所需的完整项目结构，三业务流共享核心计算管道，FastAPI 同时提供 API、collector 和静态文件服务（per D-01, D-02, D-03, D-04, D-06）。

Output: tmp/pyts-calculator/ 目录下完整可运行的全栈项目，前端已构建，后端可启动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-pyts-fullstack-calc/36-CONTEXT.md
@.planning/phases/36-pyts-fullstack-calc/36-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From tmp/python-calculator/calculator/core.py:
```python
def evaluate(expr: str, flow_id: str = "") -> Tuple[str, str]:
    """Full evaluation pipeline: parse -> validate -> compute -> format.
    Returns (result_string, error_string)."""
```

From tmp/python-calculator/codepoint/__init__.py:
```python
def point_json(name: str, meta: Optional[dict[str, Any]] = None) -> None:
    """Write JSON code point with metadata."""

def is_enabled() -> bool:
    """Returns whether code points are enabled."""
```

From tmp/python-calculator/history/store.py:
```python
class HistoryStore:
    def add(self, expr: str, result: str) -> int:
    def get(self, record_id: int) -> dict:
    def get_all(self) -> list:
```

From tmp/python-calculator/batch/processor.py:
```python
def process_expressions(expressions: str, flow_id: str = "flow-batch-process") -> list:
    """Process multiple expressions (one per line) through the shared calculator pipeline."""
```

From plugins/codepoint/references/python.md Section 7:
```python
# codepoint/collector.py exports:
def is_ts_enabled() -> bool:
def receive(entry: dict[str, Any]) -> bool:
def close_ts_collector() -> None:
```

From tmp/gojs-calculator/frontend/src/api/client.ts:
```typescript
// BatchResult in gojs-calculator has lineNumber/duration fields
// Python backend does NOT produce these -- adjust BatchResult type to match Python output
// Python batch output: {"results": [{"expression": str, "output": str, "error": str}]}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Python 后端（FastAPI + 核心模块 + collector）</name>
  <files>
    tmp/pyts-calculator/main.py
    tmp/pyts-calculator/api/__init__.py
    tmp/pyts-calculator/api/server.py
    tmp/pyts-calculator/calculator/__init__.py
    tmp/pyts-calculator/calculator/core.py
    tmp/pyts-calculator/history/__init__.py
    tmp/pyts-calculator/history/store.py
    tmp/pyts-calculator/batch/__init__.py
    tmp/pyts-calculator/batch/processor.py
    tmp/pyts-calculator/codepoint/__init__.py
    tmp/pyts-calculator/codepoint/collector.py
    tmp/pyts-calculator/tests/test_calculator.py
  </files>
  <read_first>
    tmp/python-calculator/calculator/core.py
    tmp/python-calculator/codepoint/__init__.py
    tmp/python-calculator/history/store.py
    tmp/python-calculator/batch/processor.py
    tmp/python-calculator/api/server.py
    tmp/python-calculator/tests/test_calculator.py
    plugins/codepoint/references/python.md (Section 7, lines 490-566)
  </read_first>
  <action>
    1. Create directory structure: tmp/pyts-calculator/{api,calculator,history,batch,codepoint,tests}/

    2. Copy these files VERBATIM from tmp/python-calculator/ to tmp/pyts-calculator/:
       - calculator/core.py (核心计算管道: parse->validate->compute->format, evaluate with flow_id)
       - history/store.py (HistoryStore with thread-safe lock)
       - batch/processor.py (process_expressions with flow_id)
       - codepoint/__init__.py (point_json, point_with_meta, is_enabled)
       - tests/test_calculator.py

    3. Create empty __init__.py files in: api/, calculator/, history/, batch/
       Note: calculator/ and codepoint/ already have __init__.py from step 2 -- only create for api/, history/, batch/ if they don't already exist after the copy.

    4. Create codepoint/collector.py -- copy EXACTLY from plugins/codepoint/references/python.md Section 7 (lines 499-565). The collector must contain:
       - _ts_toggle = Path.home() / ".codepoint" / ".codepoint-ts"
       - _ts_enabled = _ts_toggle.exists()
       - _init_ts_collector() -- creates cp-ts-*.log in ~/.codepoint/{project_name}/
       - is_ts_enabled() -> bool
       - receive(entry) -> bool -- thread-safe write with threading.Lock
       - close_ts_collector()
       - Module-level _init_ts_collector() call

    5. Create api/server.py -- translate tmp/python-calculator/api/server.py from http.server to FastAPI. Must contain:
       - create_app() factory function returning FastAPI instance
       - POST /__codepoint__ collector endpoint (returns 404 if not is_ts_enabled(), else receive(entry) and return 204)
       - POST /api/calculate -- calls evaluate(expr, flow_id="flow-api-calculate"), stores result in HistoryStore, returns {expression, result, error}
       - GET /api/history -- returns store.get_all()
       - GET /api/history/{record_id} -- retrieves record, recomputes via evaluate(expr, flow_id="flow-history-query"), returns {id, expression, result, recomputed}
       - POST /api/batch -- calls process_expressions(input_text, flow_id="flow-batch-process"), stores results, returns {"results": [{"expression", "output", "error"}]}
       - Shutdown handler: @app.on_event("shutdown") calling close_ts_collector() -- addresses review concern about file handle flush on Windows
       - StaticFiles mount LAST: if frontend/dist/ exists, app.mount("/", StaticFiles(directory=..., html=True))
       - All API routes and collector MUST be registered BEFORE the StaticFiles mount (per RESEARCH Pitfall 1)
       - Use pathlib.Path for all file paths (per RESEARCH Pitfall 5)

       Each route must include point_json probes matching the pattern from python-calculator/api/server.py:
       - cp-api-calc-entry, cp-api-calc-done (calculate route)
       - cp-history-entry, cp-history-lookup, cp-history-done (history detail route)
       - cp-batch-entry, cp-batch-done (batch route)

    6. Create main.py:
       ```python
       """PyTS Calculator - FastAPI entry point."""
       import uvicorn
       from api.server import create_app

       if __name__ == "__main__":
           app = create_app()
           uvicorn.run(app, host="0.0.0.0", port=18091)
       ```
       Use port 18091 to avoid conflict (per RESEARCH Pitfall 6 -- Phase 35 used 18090 for gojs-calculator).

    7. Install FastAPI: `python -m pip install fastapi`
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/work-skills/tmp/pyts-calculator" && python -c "from api.server import create_app; app = create_app(); print('FastAPI app created:', len(app.routes), 'routes')" && python -m pytest tests/test_calculator.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - tmp/pyts-calculator/ 目录包含完整的 Python 后端
    - FastAPI app 创建成功，注册了 /__codepoint__、/api/calculate、/api/history、/api/history/{id}、/api/batch 路由
    - codepoint/collector.py 包含 receive/is_ts_enabled/close_ts_collector 函数
    - calculator/core.py 的 evaluate() 支持 flow_id 参数
    - pytest tests/test_calculator.py 通过
  </done>
  <acceptance_criteria>
    - `tmp/pyts-calculator/main.py` contains `uvicorn.run(app, host="0.0.0.0", port=18091)`
    - `tmp/pyts-calculator/api/server.py` contains `from codepoint.collector import receive, is_ts_enabled, close_ts_collector`
    - `tmp/pyts-calculator/api/server.py` contains `@app.post("/__codepoint__")`
    - `tmp/pyts-calculator/api/server.py` contains `StaticFiles`
    - `tmp/pyts-calculator/api/server.py` contains `/api/calculate`, `/api/history`, `/api/batch`
    - `tmp/pyts-calculator/api/server.py` contains `@app.on_event("shutdown")` and `close_ts_collector()`
    - `tmp/pyts-calculator/codepoint/collector.py` contains `_ts_lock = threading.Lock()`
    - `tmp/pyts-calculator/codepoint/collector.py` contains `def receive(entry: dict`
    - `tmp/pyts-calculator/calculator/core.py` contains `def evaluate(expr: str, flow_id: str = "")`
    - `python -m pytest tests/test_calculator.py -x -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 创建 React TS 前端 + 构建集成验证</name>
  <files>
    tmp/pyts-calculator/frontend/package.json
    tmp/pyts-calculator/frontend/tsconfig.json
    tmp/pyts-calculator/frontend/tsconfig.app.json
    tmp/pyts-calculator/frontend/tsconfig.node.json
    tmp/pyts-calculator/frontend/vite.config.ts
    tmp/pyts-calculator/frontend/index.html
    tmp/pyts-calculator/frontend/src/main.tsx
    tmp/pyts-calculator/frontend/src/App.tsx
    tmp/pyts-calculator/frontend/src/index.css
    tmp/pyts-calculator/frontend/src/vite-env.d.ts
    tmp/pyts-calculator/frontend/src/lib/codepoint.ts
    tmp/pyts-calculator/frontend/src/api/client.ts
    tmp/pyts-calculator/frontend/src/components/Calculator.tsx
    tmp/pyts-calculator/frontend/src/components/History.tsx
    tmp/pyts-calculator/frontend/src/components/BatchCalc.tsx
  </files>
  <read_first>
    tmp/gojs-calculator/frontend/package.json
    tmp/gojs-calculator/frontend/tsconfig.json
    tmp/gojs-calculator/frontend/tsconfig.app.json
    tmp/gojs-calculator/frontend/tsconfig.node.json
    tmp/gojs-calculator/frontend/vite.config.ts
    tmp/gojs-calculator/frontend/index.html
    tmp/gojs-calculator/frontend/src/main.tsx
    tmp/gojs-calculator/frontend/src/App.tsx
    tmp/gojs-calculator/frontend/src/index.css
    tmp/gojs-calculator/frontend/src/vite-env.d.ts
    tmp/gojs-calculator/frontend/src/lib/codepoint.ts
    tmp/gojs-calculator/frontend/src/api/client.ts
    tmp/gojs-calculator/frontend/src/components/Calculator.tsx
    tmp/gojs-calculator/frontend/src/components/History.tsx
    tmp/gojs-calculator/frontend/src/components/BatchCalc.tsx
  </read_first>
  <action>
    1. Create tmp/pyts-calculator/frontend/ directory

    2. Copy these files VERBATIM from tmp/gojs-calculator/frontend/:
       - src/main.tsx
       - src/index.css
       - src/vite-env.d.ts
       - src/lib/codepoint.ts (TS frontend probe library -- dual-mode browser/Node.js)
       - src/components/Calculator.tsx
       - src/components/History.tsx
       - src/components/BatchCalc.tsx
       - vite.config.ts
       - tsconfig.json
       - tsconfig.app.json
       - tsconfig.node.json

    3. Copy package.json from gojs-calculator/frontend/ but rename: `"name": "pyts-calculator-frontend"`

    4. Copy index.html from gojs-calculator/ and change title to "PyTS Calculator"

    5. Copy src/App.tsx from gojs-calculator/ and change the h1 text from "Go+JS Calculator" to "Python+TS Calculator"

    6. Create src/api/client.ts -- adapted from gojs-calculator/frontend/src/api/client.ts with these changes:
       - Remove `lineNumber` and `duration` fields from BatchResult interface (Python backend does not produce these)
       - BatchResult interface should be: `{ expression: string; output: string; error: string; }`
       - Keep all other types and functions identical: CalcRequest, CalcResponse, HistoryRecord, HistoryDetailResponse, calculate, getHistory, getHistoryDetail, batchCalculate
       - Keep the `data.results || data` unwrap pattern in batchCalculate (per Phase 35 bug fix)

    7. Run `cd tmp/pyts-calculator/frontend && npm install`

    8. Run `cd tmp/pyts-calculator/frontend && npm run build` -- verify dist/ is created

    9. Smoke test: start FastAPI backend (`cd tmp/pyts-calculator && python main.py` in background), wait for startup (poll `curl -s http://localhost:18091/api/history` until it returns `[]` or a valid JSON response, max 10 retries with 1-second intervals), then verify `curl http://localhost:18091/` returns HTML and `curl -X POST http://localhost:18091/api/calculate -H "Content-Type: application/json" -d "{\"expression\":\"2+3\"}"` returns JSON with result "5.0". Kill the server process after verification -- on Windows, use `taskkill /F /PID {pid}` with the PID captured from the background process.
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/work-skills/tmp/pyts-calculator/frontend" && npm run build 2>&1 | tail -3 && ls dist/index.html 2>&1</automated>
  </verify>
  <done>
    - frontend/ 目录包含完整 React TS 项目
    - npm run build 成功，dist/ 目录生成
    - App.tsx 标题为 "Python+TS Calculator"
    - client.ts BatchResult 类型匹配 Python 后端输出（无 lineNumber/duration）
    - FastAPI 启动后，curl 根路径返回 HTML，curl /api/calculate 返回 JSON
  </done>
  <acceptance_criteria>
    - `tmp/pyts-calculator/frontend/src/App.tsx` contains `Python+TS Calculator`
    - `tmp/pyts-calculator/frontend/src/api/client.ts` contains `BatchResult` and does NOT contain `lineNumber` or `duration`
    - `tmp/pyts-calculator/frontend/src/api/client.ts` contains `data.results || data`
    - `tmp/pyts-calculator/frontend/src/lib/codepoint.ts` contains `sendToCollector` and `/__codepoint__`
    - `tmp/pyts-calculator/frontend/src/components/Calculator.tsx` contains `pointWithMeta`
    - `tmp/pyts-calculator/frontend/src/components/History.tsx` contains `flow-history-query`
    - `tmp/pyts-calculator/frontend/src/components/BatchCalc.tsx` contains `flow-batch-process`
    - `tmp/pyts-calculator/frontend/dist/index.html` exists
    - `tmp/pyts-calculator/frontend/package.json` name is `pyts-calculator-frontend`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> FastAPI /api/* | Untrusted HTTP input from browser |
| Browser -> FastAPI /__codepoint__ | Untrusted JSON from frontend probe |
| FastAPI -> calculator/core.py | Internal, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | /api/calculate expression input | mitigate | calculator/core.py parse_expression rejects invalid characters (only digits, operators, parens) |
| T-36-02 | Tampering | /__codepoint__ JSON body | accept | Test project, no PII, collector just writes to local log file |
| T-36-03 | Denial of Service | All endpoints | accept | Test project on localhost, low-value target |
</threat_model>

<verification>
1. `python -m pytest tmp/pyts-calculator/tests/test_calculator.py -x -q` passes
2. `cd tmp/pyts-calculator/frontend && npm run build` produces dist/index.html
3. FastAPI starts on port 18091, root path returns HTML, /api/calculate returns JSON
4. collector endpoint returns 404 when .codepoint-ts toggle is absent
</verification>

<success_criteria>
- tmp/pyts-calculator/ 包含完整后端（FastAPI + 5个API路由 + collector）和前端（React TS + 三组件）
- calculator/core.py 的 evaluate() 支持 flow_id 参数，包含 4 个探针点（parse/validate/compute/format）
- codepoint/collector.py 从 python.md Section 7 完整复制，包含线程安全写入
- api/server.py 中 StaticFiles mount 在所有 API 路由之后
- api/server.py 包含 @app.on_event("shutdown") 调用 close_ts_collector()
- 前端构建成功，dist/ 可被 FastAPI StaticFiles 提供
- 端到端冒烟测试：前端页面可访问 + API 计算正确
</success_criteria>

<output>
After completion, create `.planning/phases/36-pyts-fullstack-calc/36-01-SUMMARY.md`
</output>
