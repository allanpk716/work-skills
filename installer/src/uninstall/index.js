'use strict';

/**
 * Run uninstall detection — detects all installed components.
 * Full implementation in Plan 02; this is the entry point stub.
 * @returns {Promise<{hasAnyInstalled: boolean}>}
 */
async function runUninstallDetection() {
  return { hasAnyInstalled: false };
}

module.exports = {
  runUninstallDetection
};
