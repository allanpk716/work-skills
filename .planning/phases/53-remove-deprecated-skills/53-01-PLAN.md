---
phase: 53-remove-deprecated-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - windows-git-commit/
  - codepoint/
  - docs/codepoint/
  - docs/windows-git-commit/
  - README.md
  - README.zh.md
  - package.json
  - installer/src/i18n/en.json
  - installer/src/i18n/zh.json
  - installer/src/uninstall/detector.js
autonomous: true
requirements:
  - REM-01
  - REM-02
  - REM-03
  - REM-04

must_haves:
  truths:
    # 直接映射 ROADMAP Phase 53 的 4 条 Success Criteria
    - "windows-git-commit/ 目录在仓库工作树中不存在 (git ls-files 无条目, 工作树无目录)"
    - "codepoint/ 目录在仓库工作树中不存在 (git ls-files 无条目, 工作树无目录)"
    - "docs/codepoint/ 文档与调研工作区已删除, 而 docs/claude-notify/ 保留完好 (test -d 通过)"
    - "docs/windows-git-commit/ (已弃用技能的修复文档) 已删除, 与 REM-03 同源意图"
    - "仓库内 (README, README.zh, CLAUDE.md, package.json, installer i18n) 不存在把 windows-git-commit 或 codepoint 当作现役能力的活跃引用"
    - "installer 仍可被 Node 加载且不抛异常 (node -e \"require('./installer/src/index.js')\")"
    - "CHANGELOG.md 的历史版本条目未被篡改 (历史提及保留)"
  artifacts:
    - "(删除阶段 — 本阶段移除 artifact 而非创建符号)"
  key_links:
    - "package.json.keywords / description 不再引用已删技能 (v3.0.0 版本号留给 Phase 55, 本阶段不动 version)"
    - "installer/src/uninstall/detector.js 的 PLUGIN_NAMES 不再把已删技能列为现役 (不破坏卸载检测加载)"
    - "installer/src/i18n/{en,zh}.json 的 verification 文案不再宣称 windows-git-commit 可用"
---

<objective>
删除两个已弃用技能 (windows-git-commit、codepoint) 的目录及其全部文档与调研工作区,并清理仓库内把它们当作"现役能力"的活跃引用,使仓库物理上回归聚焦 claude-notify 的单一技能项目形态。本阶段为纯删除/引用清理,不修改 claude-notify 任何行为,也不对 installer 做深度裁剪 (深度裁剪属 Phase 54)。

Purpose: 完成 M015 v3.0 里程碑的首阶段瘦身 — 物理移除多技能遗留,为 Phase 54 (installer 裁剪) 与 Phase 55 (发版+回归) 铺路。
Output: 3 个目录被 `git rm -r`、2 个 README + package.json 元数据收窄、installer 仅做"不破坏加载"级最小 i18n/detector 收窄、CHANGELOG/.planning 历史记录原样保留。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-remove-deprecated-skills/53-CONTEXT.md

# 现役引用锚点 (executor 执行前用 grep 核对当前行号)
# - README.md: 行5(description), 行18-19(技能表 2 行), 行26-27(项目结构 2 行)
# - README.zh.md: 行5(description), 行18-19, 行26-27 (中文镜像)
# - package.json: 行4 description, 行20-21 keywords
# - installer/src/i18n/en.json: 行122-123 verification 文案
# - installer/src/i18n/zh.json: 行122-123 verification 文案
# - installer/src/uninstall/detector.js: 行8 PLUGIN_NAMES 数组
# - CLAUDE.md (根): 经 grep 确认 0 引用 (无需改动)
# - CHANGELOG.md: 含历史版本提及 — 历史记录, 原样保留, 不清理
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除两个技能目录 + docs/codepoint + docs/windows-git-commit (REM-01, REM-02, REM-03)</name>
  <files>
    windows-git-commit/ (deleted)
    codepoint/ (deleted)
    docs/codepoint/ (deleted)
    docs/windows-git-commit/ (deleted)
  </files>
  <read_first>
    - windows-git-commit/README.md (确认这是要删的技能, 非误删)
    - codepoint/README.md (确认根技能入口)
    - docs/codepoint/ (ls -R, 确认内含 plans/、research/(含 workspace、images)、specs/)
    - docs/claude-notify/ (确认此目录必须保留 — 本任务不动它)
    - docs/windows-git-commit/ (ls, 确认是已弃用技能的修复文档, 属 REM-03 同源删除范围)
  </read_first>
  <action>
使用 `git rm -r` (非裸 `rm`, 以保留 git 历史跟踪) 删除以下目录:

1. `git rm -r windows-git-commit` (per REM-01) — 含 scanner/、hooks/、references/、README、.claude-plugin/plugin.json、.pytest_cache 等全部内容。
2. `git rm -r codepoint` (per REM-02) — 含根 SKILL.md + 8 个子技能 SKILL.md、templates/、references/、README、.claude-plugin/plugin.json。
3. `git rm -r docs/codepoint` (per REM-03) — 含 plans/、research/(含 workspace/、images/)、specs/。
4. `git rm -r docs/windows-git-commit` (per REM-03 同源意图 — 已弃用技能的修复文档 fixes/bash-agent-fix.md、duplicate-commands-fix.md)。

**保留 (绝不触碰):** `claude-notify/`、`docs/claude-notify/`、`docs/project/`、`docs/README.md`、`installer/`、根文件。

claude-notify 已验证为自包含 (CONTEXT.md: claude-notify/ 内无 windows-git-commit / codepoint 引用), 删除兄弟技能不会破坏它。

完成后用 git status 确认 4 个目录均处于 deleted 状态。不要提交 (commit 由 execute-plan 流程在阶段末统一处理)。
  </action>
  <verify>
    <automated>
cd "C:/WorkSpace/agent/work-skills" &&
test ! -d windows-git-commit &&
test ! -d codepoint &&
test ! -d docs/codepoint &&
test ! -d docs/windows-git-commit &&
test -d docs/claude-notify &&
test -d claude-notify &&
[ -z "$(git ls-files windows-git-commit codepoint docs/codepoint docs/windows-git-commit)" ] &&
echo "TASK1-OK"
    </automated>
  </verify>
  <done>
- windows-git-commit/、codepoint/、docs/codepoint/、docs/windows-git-commit/ 工作树与 git 索引中均不存在
- docs/claude-notify/ 与 claude-notify/ 保留完好
- `git ls-files` 对 4 个已删路径返回空
  </done>
</task>

<task type="auto">
  <name>Task 2: 清理根 README/package.json 中把已删技能当现役的活跃引用 (REM-04)</name>
  <files>
    README.md
    README.zh.md
    package.json
  </files>
  <read_first>
    - README.md (全文 38 行 — 重点行5 description、行18-19 技能表、行26-27 项目结构)
    - README.zh.md (全文 39 行 — 中文镜像)
    - package.json (全文 42 行 — 重点行4 description、行15-22 keywords)
  </read_first>
  <action>
**README.md (根, 英文) — 移除 2 行技能表 + 2 行项目结构 + 收窄 description:**
- 行5 description: 将 "Provides notification alerts, Git workflow automation, and AI-assisted code observability." 改为仅 claude-notify 范围, 例如 "Provides task-completion notifications for Claude Code (Pushover mobile push + Windows Toast desktop)."
- 技能表: 删除 windows-git-commit 行(行18)与 codepoint 行(行19), 仅保留 claude-notify 行。
- 项目结构块: 删除 windows-git-commit/(行26)与 codepoint/(行27)两行, 仅保留 claude-notify/ 与 README.md。

**README.zh.md (根, 中文) — 同步镜像改动:**
- 行5 description: 改为仅通知范围, 例如 "为 Claude Code 提供任务完成通知 (Pushover 移动推送 + Windows Toast 桌面通知)。" (注意: 项目规范要求 BAT 脚本不含中文, 但这是 Markdown 文档, 中文是正确的, 保留中文。)
- 技能表: 删除 windows-git-commit(行18)与 codepoint(行19)两行。
- 项目结构块: 删除 windows-git-commit/(行26)与 codepoint/(行27)两行。

**package.json (根) — 收窄 keywords 与 description (per REM-04):**
- description (行4): 将 "Personal skills collection for Claude Code — notification, Git workflow, and code observability" 改为 "Personal skill for Claude Code — task-completion notifications (Pushover + Windows Toast)"。
- keywords (行15-22): 从数组中删除 `"windows-git-commit",` (行20) 与 `"codepoint"` (行21) 两个条目。保留 `"claude-code"`、`"skills"`、`"agentskills"`、`"claude-notify"`。注意 JSON 数组末尾逗号合法性 — 删除后 claude-notify 应为最后一个元素, 无尾逗号。

**绝不改动:** `"version": "1.9.0"` — 版本号升至 3.0.0 属 Phase 55 (REL-03), 本阶段不动。`"name"`、`bin`、`files`、`dependencies` 均不动。

用 Edit 工具做精确替换 (非整文件 Write)。每处改动后确保 JSON 仍合法。
  </action>
  <verify>
    <automated>
cd "C:/WorkSpace/agent/work-skills" &&
node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('JSON-OK')" &&
! grep -qi "windows-git-commit" package.json &&
! grep -qi "codepoint" package.json &&
! grep -qiE "windows-git-commit|codepoint" README.md &&
! grep -qiE "windows-git-commit|codepoint" README.zh.md &&
grep -qi "claude-notify" README.md &&
grep -qi "claude-notify" README.zh.md &&
grep -q '"claude-notify"' package.json &&
echo "TASK2-OK"
    </automated>
  </verify>
  <done>
- README.md / README.zh.md 技能表仅剩 claude-notify 一行, 项目结构仅剩 claude-notify/ + README.md, description 仅描述通知能力
- package.json keywords 不含 windows-git-commit / codepoint, description 仅描述通知, version 仍为 1.9.0 (未动), JSON 合法可解析
  </done>
</task>

<task type="auto">
  <name>Task 3: installer "不破坏加载" 级最小引用收窄 + 历史记录保留声明 (REM-04 末段)</name>
  <files>
    installer/src/i18n/en.json
    installer/src/i18n/zh.json
    installer/src/uninstall/detector.js
  </files>
  <read_first>
    - installer/src/i18n/en.json (重点行122-123 verification.nextStep3/4)
    - installer/src/i18n/zh.json (重点行122-123)
    - installer/src/uninstall/detector.js (重点行8 PLUGIN_NAMES 数组)
    - installer/src/index.js (确认无 require 指向已删目录 — 已预 grep 验证为 0, 加载不会崩)
  </read_first>
  <action>
**本阶段范围界定 (重要):** installer 的深度裁剪 (移除 marketplace/、git/ssh detectors、uninstall 模块、完整 i18n 重写) 属 Phase 54 (INS-01..05), 本阶段不做。本任务仅做"删除目录后 installer 仍能加载、且不把已删技能当现役对外宣称"的最小改动。

**1. installer/src/i18n/en.json (per REM-04) — 行122-123:**
- "verification.nextStep3" (行122): 将 `"3. Available skills: /windows-git-commit, /notify-test"` 改为 `"3. Available skill: /notify-test"`。
- "verification.nextStep4" (行123): 将 `"4. Or just say: 'Use windows-git-commit to commit my changes'"` 改为指向 claude-notify 范围, 例如 `"4. Or just say: 'Notify me when my task is done'"`。

**2. installer/src/i18n/zh.json (per REM-04) — 行122-123 (中文镜像):**
- "verification.nextStep3" (行122): 将 `"3. 可用技能: /windows-git-commit, /notify-test"` 改为 `"3. 可用技能: /notify-test"`。
- "verification.nextStep4" (行123): 将 `"4. 或直接说: '使用 windows-git-commit 提交我的修改'"` 改为 `"4. 或直接说: '我的任务完成后通知我'"`。

**3. installer/src/uninstall/detector.js (per REM-04 — 仅数据层, 不删模块):**
- 行8 `PLUGIN_NAMES` 数组: 将 `['claude-notify', 'windows-git-commit', 'codepoint']` 收窄为 `['claude-notify']`。
- 此改动仅为让卸载检测不再把已删技能当现役去探测; uninstall/ 模块整体的裁剪/移除属 Phase 54 (INS-03), 本阶段保留 uninstall/ 模块文件原样。

**绝不改动 (Phase 54 范围):** installer/marketplace/、installer/detectors/git.js、installer/detectors/ssh-tools.js、installer/configurators/git-*.js、installer/uninstall/ 模块整体、installer/tests/ 测试文件 (测试中 windows-git-commit 字面量是历史测试夹具, 深度清理属 Phase 54 INS-05)。

**历史记录保留声明:** CHANGELOG.md 内既往版本对 windows-git-commit/codepoint 的提及是合法历史 (2 处 windows-git-commit 命中均为历史条目), 不清理。`.planning/` 下既往阶段/需求引用同样为历史规划记录, 不清理。v3.0.0 的 CHANGELOG 新条目本身属 Phase 55 (REL-02)。

用 Edit 工具精确替换, 每处改完后确保 JSON 合法。
  </action>
  <verify>
    <automated>
cd "C:/WorkSpace/agent/work-skills" &&
node -e "JSON.parse(require('fs').readFileSync('installer/src/i18n/en.json','utf8')); JSON.parse(require('fs').readFileSync('installer/src/i18n/zh.json','utf8')); console.log('I18N-JSON-OK')" &&
node -e "require('./installer/src/index.js'); console.log('INSTALLER-LOAD-OK')" &&
! grep -qi "windows-git-commit" installer/src/i18n/en.json &&
! grep -qi "windows-git-commit" installer/src/i18n/zh.json &&
! grep -qiE "codepoint|windows-git-commit" installer/src/uninstall/detector.js &&
echo "TASK3-OK"
    </automated>
  </verify>
  <done>
- installer/src/index.js 可被 node 加载不抛异常 (证明删除目录未破坏 installer 加载链)
- installer/src/i18n/{en,zh}.json 的 verification 文案不再宣称 windows-git-commit 可用, JSON 合法
- installer/src/uninstall/detector.js 的 PLUGIN_NAMES 仅剩 claude-notify, 不再把已删技能当现役
- uninstall/ 模块、marketplace/、git/ssh detectors、installer 测试文件均原样保留 (深度裁剪留待 Phase 54)
- CHANGELOG.md 与 .planning/ 历史记录未被篡改
  </done>
</task>

</tasks>

<verification>
阶段末整体验证 (4 条 ROADMAP Success Criteria 映射):

```bash
cd "C:/WorkSpace/agent/work-skills"

# SC1: windows-git-commit 目录不存在
test ! -e windows-git-commit && [ -z "$(git ls-files windows-git-commit)" ] && echo "SC1-OK: windows-git-commit removed"

# SC2: codepoint 目录不存在
test ! -e codepoint && [ -z "$(git ls-files codepoint)" ] && echo "SC2-OK: codepoint removed"

# SC3: docs/codepoint 删除, docs/claude-notify 保留
test ! -e docs/codepoint && test -d docs/claude-notify && echo "SC3-OK: docs/codepoint gone, docs/claude-notify intact"

# SC4: 活跃引用清理 (排除历史 CHANGELOG/.planning)
# 根元数据 + installer src (排除 tests 夹具, 其清理属 Phase 54)
HITS=$(grep -rIl --include="*.md" --include="*.json" --include="*.js" -iE "windows-git-commit|codepoint" README.md README.zh.md package.json CLAUDE.md installer/src/ 2>/dev/null)
test -z "$HITS" && echo "SC4-OK: no active references in root metadata + installer/src" || { echo "SC4-REMAINING:"; echo "$HITS"; }

# 历史记录保留确认 (不在清理范围)
grep -qi "windows-git-commit" CHANGELOG.md && echo "HIST-OK: CHANGELOG historical entries preserved"
test -d docs/project && echo "STRUCT-OK: docs/project preserved"
```

**注:** `installer/tests/` 内仍有 windows-git-commit 字面量作为测试夹具 — 这是 Phase 54 (INS-05) 的清理范围, 本阶段 SC4 验证范围限定为 `installer/src/` 与根元数据, 不含 tests。
</verification>

<success_criteria>
- [ ] windows-git-commit/、codepoint/、docs/codepoint/、docs/windows-git-commit/ 均已 `git rm -r`, 工作树与索引均无残留
- [ ] docs/claude-notify/ 与 claude-notify/ 保留完好
- [ ] README.md / README.zh.md 技能表仅剩 claude-notify, description 收窄, 项目结构收窄
- [ ] package.json keywords/description 不含已删技能, version 仍为 1.9.0 (未动 — 留给 Phase 55)
- [ ] installer/src/i18n/{en,zh}.json verification 文案不再宣称 windows-git-commit 可用
- [ ] installer/src/uninstall/detector.js PLUGIN_NAMES 仅剩 claude-notify
- [ ] `node -e "require('./installer/src/index.js')"` 加载不抛异常
- [ ] CHANGELOG.md 历史版本条目原样保留 (未被篡改)
- [ ] installer 深度裁剪 (marketplace、git/ssh detectors、uninstall 模块、tests) 明确未做 — 留待 Phase 54
</success_criteria>

<output>
## Artifacts this phase produces

本阶段为纯删除/引用清理阶段, **不创建任何新代码符号或新文件**。产出为"负 artifact":

- 删除: `windows-git-commit/` (整个技能目录)
- 删除: `codepoint/` (整个技能目录)
- 删除: `docs/codepoint/` (文档与调研工作区)
- 删除: `docs/windows-git-commit/` (已弃用技能修复文档)
- 修改 (元数据收窄, 无新增能力): `README.md`、`README.zh.md`、`package.json`
- 修改 (最小引用收窄, 无新增能力): `installer/src/i18n/en.json`、`installer/src/i18n/zh.json`、`installer/src/uninstall/detector.js`

后续阶段消费关系:
- Phase 54 (trim-installer-notify-only) 在本阶段"installer 仍可加载"的基础上, 做深度模块裁剪
- Phase 55 (release-v3-metadata-regression) 在本阶段元数据收窄的基础上, 做版本号升级与 CHANGELOG v3.0.0 新条目

完成后创建 `.planning/phases/53-remove-deprecated-skills/53-01-SUMMARY.md`
</output>
