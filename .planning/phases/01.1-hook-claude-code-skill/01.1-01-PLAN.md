---
phase: 01.1-hook-claude-code-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - skills/claude-notify/SKILL.md
  - hooks/hooks.json
  - hooks/scripts/notify.py
autonomous: true
requirements: []
user_setup:
  - service: pushover
    why: "Push notification delivery to mobile devices"
    env_vars:
      - name: PUSHOVER_TOKEN
        source: "Pushover Dashboard -> Your Applications -> Create Application"
      - name: PUSHOVER_USER
        source: "Pushover Dashboard -> Your User Key"

must_haves:
  truths:
    - "Plugin can be installed from marketplace URL via /plugin install"
    - "Hook automatically triggers notification on Claude Code Stop event"
    - "Pushover notification is sent to mobile device when credentials configured"
    - "Windows Toast notification appears on desktop"
    - "Notification includes project name and task summary"
    - "Installation verification script checks all dependencies"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace metadata for plugin discovery"
      contains: '"plugins"'
    - path: "skills/claude-notify/SKILL.md"
      provides: "Skill documentation and usage guide"
      min_lines: 50
    - path: "hooks/hooks.json"
      provides: "Hook configuration for Stop event"
      contains: '"Stop"'
    - path: "hooks/scripts/notify.py"
      provides: "Notification script implementation"
      exports: ["main", "send_pushover_notification", "send_windows_notification"]
  key_links:
    - from: "hooks/hooks.json"
      to: "hooks/scripts/notify.py"
      via: "command field with ${CLAUDE_PLUGIN_ROOT}"
      pattern: "\\$\\{CLAUDE_PLUGIN_ROOT\\}"
---

<objective>
创建 Claude Code 插件的标准目录结构和配置文件,实现通过插件市场分发通知功能

Purpose: 将 Phase 1 的项目级 Hook 实现转换为可复用的 Claude Code Skill 插件,使其他开发者能够通过市场安装使用
Output: 插件目录结构、marketplace.json、hooks.json、SKILL.md 文档
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-01-SUMMARY.md
@.planning/phases/01.1-hook-claude-code-skill/01.1-CONTEXT.md
@.planning/phases/01.1-hook-claude-code-skill/01.1-RESEARCH.md
@.claude/hooks/notify.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建插件目录结构和 marketplace.json</name>
  <files>.claude-plugin/marketplace.json, skills/claude-notify/SKILL.md</files>
  <action>
创建 Claude Code 插件的标准目录结构:

1. 创建 `.claude-plugin/marketplace.json` - 市场元数据配置:
   - name: "claude-notify-marketplace"
   - owner.name 和 owner.email
   - metadata.description: "Claude Code notification plugins"
   - metadata.version: "1.0.0"
   - plugins 数组包含一个插件条目:
     - name: "claude-notify"
     - description: "Task completion notifications via Pushover and Windows Toast"
     - source: "./"
     - strict: false
     - skills: ["./skills/claude-notify"]

2. 创建 `skills/claude-notify/SKILL.md` - 技能文档:
   - frontmatter 包含: name (claude-notify), description, version (1.0.0)
   - 技能描述: Hook 触发的自动通知功能
   - 功能特性: Pushover 推送 + Windows Toast + AI 摘要
   - 安装步骤: 通过市场安装的命令示例
   - 环境变量配置: PUSHOVER_TOKEN 和 PUSHOVER_USER 设置方法
   - 使用示例: 安装后自动生效的说明
   - 常见问题: 环境变量缺失、通知未发送等

参考 RESEARCH.md 中的 marketplace.json 和 SKILL.md frontmatter 示例格式。注意 hooks.json 不在这里创建(下一个任务)。
  </action>
  <verify>
    <automated>
powershell -Command "Test-Path .claude-plugin/marketplace.json; Test-Path skills/claude-notify/SKILL.md"
    </automated>
    <manual>验证 marketplace.json 包含 plugins 数组,SKILL.md 包含 frontmatter 和安装说明</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
marketplace.json 文件存在且包含有效的 plugins 配置
SKILL.md 文件存在且包含完整的功能说明和安装指南
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 Hook 配置和通知脚本</name>
  <files>hooks/hooks.json, hooks/scripts/notify.py</files>
  <action>
创建插件级 Hook 配置和通知脚本:

1. 创建 `hooks/hooks.json` - 插件级 Hook 配置:
   - 必须包含 description 字段
   - hooks 包装器格式 (与项目级 settings.json 格式不同)
   - Stop 事件配置:
     - matcher: "*"
     - type: command
     - command: 使用 `${CLAUDE_PLUGIN_ROOT}/hooks/scripts/notify.py` (注意大括号语法)
     - async: true
     - timeout: 10

   参考 RESEARCH.md "Pattern 1: Plugin Hook Configuration" 示例,注意使用正确的插件级格式。

2. 创建 `hooks/scripts/notify.py` - 通知脚本:
   - 从 `.claude/hooks/notify.py` 复制代码
   - 保持所有核心逻辑不变 (get_project_name, get_claude_summary, send_pushover_notification, send_windows_notification, main)
   - 保持超时控制策略: Claude CLI 2s, Pushover 2s, Windows 1s, 总体 4s
   - 保持并行执行架构: ThreadPoolExecutor(max_workers=2)
   - 保持日志目录: APPDATA/claude-notify/logs (用户级目录,不受插件安装位置影响)
   - 确保 UTF-8 编码支持

关键点: 不需要修改路径引用,因为日志目录使用 APPDATA 环境变量是用户级目录,不依赖插件位置。
  </action>
  <verify>
    <automated>
powershell -Command "Test-Path hooks/hooks.json; Test-Path hooks/scripts/notify.py; python hooks/scripts/notify.py"
    </automated>
    <manual>验证 hooks.json 使用 ${CLAUDE_PLUGIN_ROOT},notify.py 可独立执行</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
hooks.json 存在且使用插件级格式 (description + hooks 包装器)
notify.py 存在且功能完整,可在无环境变量时优雅降级
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建验证脚本和 README 文档</name>
  <files>scripts/verify-installation.py, README.md, tests/.gitkeep</files>
  <action>
创建安装验证脚本和用户文档:

1. 创建 `scripts/verify-installation.py` - 安装验证脚本:
   - 检查 Python 版本 (>= 3.8)
   - 检查必需的 Python 标准库 (requests, subprocess, pathlib, concurrent.futures)
   - 检查环境变量配置状态 (PUSHOVER_TOKEN, PUSHOVER_USER)
   - 测试 Pushover API 连接 (发送测试通知,可选)
   - 测试 Windows Toast 通知功能
   - 输出清晰的成功/失败报告,失败时提供解决建议

   参考 CONTEXT.md 中的验证脚本需求清单。

2. 创建 `README.md` - 插件安装和使用指南:
   - 插件简介: Claude Code 任务完成通知
   - 功能特性: Pushover 推送 + Windows Toast + AI 摘要
   - 安装步骤:
     1. 通过市场安装: `/plugin install <marketplace-url>`
     2. 配置环境变量: PUSHOVER_TOKEN, PUSHOVER_USER
     3. 配置 Claude Code Hooks: 说明 hooks.json 已随插件提供,无需手动配置
     4. 运行验证: `python scripts/verify-installation.py`
   - 使用说明: Hook 自动触发,无需手动调用
   - 故障排查: 常见问题和解决方案
   - 技术细节: 超时控制、并行执行、日志位置

3. 创建 `tests/.gitkeep` - 测试目录占位符:
   - 为未来的测试脚本保留目录结构
   - tests/test_notify.py 将在后续版本实现 (Claude's Discretion)

不要在 README 中使用中文,保持英文文档以符合插件市场规范。
  </action>
  <verify>
    <automated>
powershell -Command "Test-Path scripts/verify-installation.py; Test-Path README.md; Test-Path tests/.gitkeep; python scripts/verify-installation.py"
    </automated>
    <manual>验证 README 包含完整安装步骤,验证脚本输出清晰的成功/失败报告</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
verify-installation.py 存在且可执行,输出清晰的验证报告
README.md 存在且包含完整的安装和使用指南
tests/.gitkeep 存在,预留测试目录结构
  </done>
</task>

</tasks>

<verification>
## Plan Verification

完成后验证:
- [ ] 所有文件创建在正确位置
- [ ] marketplace.json 符合市场规范
- [ ] hooks.json 使用插件级格式 (description + hooks 包装器)
- [ ] hooks.json 使用 ${CLAUDE_PLUGIN_ROOT} 路径变量
- [ ] notify.py 功能完整,保持 Phase 1 的所有核心逻辑
- [ ] verify-installation.py 输出清晰的验证报告
- [ ] README.md 包含完整安装步骤,使用英文
- [ ] 目录结构符合 Claude Code 插件标准
</verification>

<success_criteria>
## Success Criteria

插件打包完成标准:
1. 插件目录结构完整,符合 Claude Code 插件市场规范
2. marketplace.json 包含有效的市场元数据和插件列表
3. hooks.json 使用插件级格式,路径引用使用 ${CLAUDE_PLUGIN_ROOT}
4. notify.py 保持 Phase 1 的所有核心功能 (并行通知、AI 摘要、超时控制)
5. verify-installation.py 能够验证所有依赖项并输出清晰报告
6. README.md 提供完整的英文安装和使用指南
7. 所有文件已提交到 git

用户可通过 `/plugin install` 从市场安装插件,配置环境变量后自动接收任务完成通知。
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-hook-claude-code-skill/01.1-01-SUMMARY.md`
</output>
