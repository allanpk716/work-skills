---
phase: 33
plan: 33-02
name: "codepoint scan/plan/implement 全流程验证"
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - tmp/python-calculator/.codepoints/**
autonomous: false
requirements:
  - SING-07
user_setup: []

must_haves:
  truths:
    - "/codepoint:scan correctly identifies Python project's shared code points and business flows"
    - "/codepoint:plan generates probe plan with correct placements on critical paths"
    - "/codepoint:implement generates Python probe code that runs without errors"
    - "Existing probes in calculator/core.py (cp-calc-parse, cp-calc-validate, cp-calc-compute, cp-calc-format) are recognized by scan"
    - "flow_id propagation is maintained in all generated probes"
  artifacts:
    - path: "tmp/python-calculator/.codepoints/"
      provides: "Codepoint V2 data model files (collections, flows, points)"
      contains: "index.json" and "col-calculator"
    - path: "tmp/python-calculator/"
      provides: "Python source files with V2 probes (PointWithMeta style)"
      contains: "point_json" and "flow_id"
  key_links:
    - from: "scan output"
      to: "plan input"
      via: ".codepoints/ directory structure"
    - from: "plan output"
      to: "implement probe code"
      via: "plan task specifications consumed by implement skill"

---

# Plan 33-02: codepoint scan/plan/implement 全流程验证

<objective>
在 Python 计算器项目上运行完整的 codepoint scan -> plan -> implement 流程，验证技能对 Python 语言的完整支持。与 Phase 32 的 Go 验证形成对照，确认技能的跨语言工作能力。

**Key validation points:**
- scan 正确识别 Python traceback 堆栈格式
- plan 使用 Python 探针模板 (point_json/point_with_meta)
- implement 生成的代码使用 Python 语法（非 Go 语法）
- flow_id 通过参数传递传播（非 context.Context）
</objective>

<must_haves>
- [ ] codepoint:scan 识别共享代码点和 3 个业务流
- [ ] codepoint:plan 生成正确的探针规划
- [ ] codepoint:implement 生成的 Python 探针代码可运行
- [ ] 所有探针包含 point_id 和 flow_id
- [ ] 项目测试通过
</must_haves>

<tasks>

<task id="1" type="execute">
<name>Task 1: 运行 codepoint scan，验证业务流识别</name>
<read_first>
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\calculator\core.py
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\api\server.py
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\batch\processor.py
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\history\store.py
</read_first>
<action>
## 1.1 Enable Python codepoint toggle

```bash
mkdir -p ~/.codepoint && touch ~/.codepoint/.codepoint-python
```

## 1.2 Run codepoint scan

```bash
cd tmp/python-calculator
```

Invoke `/codepoint:scan` on the Python calculator project. Verify:
1. Scan identifies the 3 business flows (REST API, Batch, History)
2. Scan identifies shared code points in calculator/core.py
3. Scan output includes Python file references (.py files)
4. .codepoints/ directory created with proper structure

## 1.3 Verify scan output

Check `.codepoints/index.json` for:
- At least 1 collection (col-calculator or equivalent)
- At least 3 flows (flow-api-calculate, flow-batch-process, flow-history-query or equivalents)
- Shared points in calculator/core.py

## 1.4 Handle non-deterministic scan output

If scan output is non-deterministic (common with AI-driven scan), run a correction procedure:
- Verify key elements are present
- Manually adjust if critical flows or points are missing
- Document any scan issues found for Phase 34 (FIX1)
</action>
<acceptance_criteria>
- [ ] `.codepoints/` directory exists under tmp/python-calculator/
- [ ] Scan output references Python source files (.py)
- [ ] At least 3 business flows identified
- [ ] Shared code points in calculator/core.py identified
</acceptance_criteria>
</task>

<task id="2" type="execute">
<name>Task 2: 运行 codepoint plan，验证探针规划质量</name>
<read_first>
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\.codepoints\index.json
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\calculator\core.py
</read_first>
<action>
## 2.1 Run codepoint plan

Invoke `/codepoint:plan` on the Python calculator project. The plan should:
1. Place entry probes at flow boundaries (API handlers, batch entry)
2. Place boundary probes at shared path entries (parse, validate, compute, format)
3. Use Python probe template (point_json, not PointWithMeta)

## 2.2 Verify plan quality

- Entry probes: cp-api-calc-entry, cp-api-calc-done (or equivalent)
- Shared probes: cp-calc-parse, cp-calc-validate, cp-calc-compute, cp-calc-format
- Batch probes: cp-batch-entry
- History probes: cp-history-entry, cp-history-lookup, cp-history-done

## 2.3 Verify Python template usage

Plans should use:
```python
from codepoint import point_json
point_json("cp-xxx", {"point_id": "cp-xxx", "flow_id": "flow-xxx"})
```
NOT:
```go
codepoint.PointWithMeta("cp-xxx", map[string]any{...})
```
</action>
<acceptance_criteria>
- [ ] Plan output exists with probe specifications
- [ ] Plans reference Python files (.py), not Go files
- [ ] Probe template uses point_json (Python), not PointWithMeta (Go)
- [ ] flow_id included in all probe metadata
- [ ] Probes placed on critical paths (entry/boundary/state-change)
</acceptance_criteria>
</task>

<task id="3" type="execute">
<name>Task 3: 运行 codepoint implement，验证探针代码生成</name>
<read_first>
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\calculator\core.py
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\api\server.py
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\batch\processor.py
- C:\WorkSpace\agent\work-skills\tmp\python-calculator\codepoint\__init__.py
</read_first>
<action>
## 3.1 Run codepoint implement

Invoke `/codepoint:implement` on the Python calculator project using the plan output.

## 3.2 Verify generated probe code

Check that generated probes:
1. Use `from codepoint import point_json` (Python import)
2. Use `point_json(name, meta)` call format
3. Include both `point_id` and `flow_id` in metadata
4. Are placed at correct locations (entry points, shared path boundaries)
5. Do NOT modify existing business logic (additive only)

## 3.3 Verify probes compile and run

```bash
cd tmp/python-calculator
python -c "from calculator.core import evaluate; print(evaluate('2+3*4', 'flow-test'))"
python -m pytest tests/ -v --tb=short
```

## 3.4 Verify toggle and output

```bash
# Check toggle
ls ~/.codepoint/.codepoint-python

# Run a quick probe test
cd tmp/python-calculator
python -c "
from codepoint import is_enabled, output_path
print(f'Enabled: {is_enabled()}')
from calculator.core import evaluate
evaluate('2+3', 'flow-test')
print(f'Output: {output_path()}')
"

# Check log file
ls ~/.codepoint/python-calculator/
```

## 3.5 Record findings

Document any issues found during scan/plan/implement for Phase 34 (FIX1):
- Scan issues (misidentification, missing flows)
- Plan issues (wrong template, missing probes)
- Implement issues (compilation errors, missing imports)
</action>
<acceptance_criteria>
- [ ] Generated probe code uses Python syntax (point_json, not PointWithMeta)
- [ ] `cd tmp/python-calculator && python -m pytest tests/ -v` all tests pass
- [ ] Probe code includes `from codepoint import point_json`
- [ ] flow_id present in all probe metadata
- [ ] Probe output written to ~/.codepoint/python-calculator/cp-python-*.log
- [ ] Business logic unchanged (probes are additive only)
- [ ] Findings documented for Phase 34 if issues found
</acceptance_criteria>
</task>

</tasks>

<verification>
1. codepoint:scan identifies Python project structure correctly
2. codepoint:plan generates Python-format probe specifications
3. codepoint:implement generates working Python probe code
4. All generated probes include point_id and flow_id
5. Project tests pass after probe insertion
6. Probe output to log file working
7. Toggle mechanism working
</verification>

<success_criteria>
- [ ] Scan correctly identifies 3 business flows and shared code points
- [ ] Plan uses Python probe template
- [ ] Implement generates syntactically correct Python code
- [ ] All probes include flow_id via parameter passing
- [ ] Tests pass, probe output verified
</success_criteria>

<output>
After completion, create `.planning/phases/33-python-single-lang-calc/33-02-SUMMARY.md`
</output>
