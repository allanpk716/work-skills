---
phase: 06-core-scanning-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/windows-git-commit/skills/windows-git-commit/scanner/rules/cache_files.py
  - plugins/windows-git-commit/skills/windows-git-commit/scanner/rules/config_files.py
  - plugins/windows-git-commit/skills/windows-git-commit/scanner/utils/file_utils.py
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
  - CACHE-03
  - CACHE-04
  - CONF-01
  - CONF-02
  - CONF-03

must_haves:
  truths:
    - "扫描器可以检测到 Python 缓存文件 (__pycache__/, *.pyc, *.pyo, *.pyd)"
    - "扫描器可以检测到 Node.js 依赖 (node_modules/, .npm/, .yarn/, yarn.lock, package-lock.json)"
    - "扫描器可以检测到编译产物 (*.class, target/, build/, dist/, out/)"
    - "扫描器可以检测到系统临时文件 (*.log, *.tmp, .DS_Store, Thumbs.db)"
    - "扫描器可以检测到 .env 文件 (.env, .env.local, .env.*.local)"
    - "扫描器可以检测到凭证文件 (credentials.json, secrets.yaml, secrets.xml)"
    - "扫描器可以检测到配置文件中的敏感字段 (password, api_key, secret, token)"
  artifacts:
    - path: "plugins/windows-git-commit/skills/windows-git-commit/scanner/rules/cache_files.py"
      provides: "缓存文件检测规则"
      min_lines: 60
      exports: ["CACHE_FILE_RULES", "is_cache_file"]
    - path: "plugins/windows-git-commit/skills/windows-git-commit/scanner/rules/config_files.py"
      provides: "配置文件检测规则"
      min_lines: 80
      exports: ["CONFIG_FILE_RULES", "SENSITIVE_FIELD_RULE", "is_config_file"]
    - path: "plugins/windows-git-commit/skills/windows-git-commit/scanner/utils/file_utils.py"
      provides: "文件路径匹配工具"
      min_lines: 40
      exports: ["match_path_pattern"]
  key_links:
    - from: "scanner/rules/cache_files.py"
      to: "pathlib.Path"
      via: "路径匹配"
      pattern: "Path.*suffix|parts"
    - from: "scanner/rules/config_files.py"
      to: "re.compile"
      via: "配置内容扫描"
      pattern: "re\\.compile.*password|api_key"
---

<objective>
实现缓存文件和配置文件检测规则,包括 Python/Node.js/Java 缓存、系统临时文件、环境配置文件和凭证文件的检测。使用路径模式匹配和内容扫描相结合的方式,确保高检测率同时避免过多误报。

Purpose: 扩展扫描器检测能力,覆盖缓存文件和配置文件,防止不必要的文件进入版本控制
Output: 缓存文件检测规则 (cache_files.py),配置文件检测规则 (config_files.py),文件路径匹配工具 (file_utils.py)
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-core-scanning-infrastructure/06-CONTEXT.md
@.planning/phases/06-core-scanning-infrastructure/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建缓存文件检测规则模块</name>
  <files>
    - plugins/windows-git-commit/skills/windows-git-commit/scanner/rules/cache_files.py
    - plugins/windows-git-commit/skills/windows-git-commit/scanner/utils/file_utils.py
  </files>
  <action>
创建缓存文件检测规则模块和文件路径匹配工具:

1. **创建 file_utils.py (文件路径匹配工具):**

   ```python
   from pathlib import Path
   from typing import List

   def match_path_pattern(file_path: Path, patterns: List[str]) -> bool:
       """
       检查文件路径是否匹配任意模式

       Args:
           file_path: 文件路径
           patterns: 模式列表 (支持通配符和路径匹配)

       Returns:
           True 如果匹配任意模式, False 如果不匹配

       Examples:
           >>> match_path_pattern(Path('src/__pycache__/module.pyc'), ['__pycache__', '*.pyc'])
           True
           >>> match_path_pattern(Path('node_modules/package/index.js'), ['node_modules'])
           True
       """
       # 规范化路径为正斜杠 (跨平台兼容)
       path_str = str(file_path).replace('\\', '/')

       for pattern in patterns:
           if pattern.startswith('*.'):
               # 扩展名匹配
               if path_str.endswith(pattern[1:]):
                   return True
           else:
               # 路径包含匹配
               if f'/{pattern}/' in path_str or path_str.endswith(f'/{pattern}') or path_str.startswith(f'{pattern}/'):
                   return True

       return False
   ```

2. **创建 cache_files.py (缓存文件检测规则):**

   参考 RESEARCH.md 的缓存文件检测模式:

   ```python
   from pathlib import Path
   from typing import List
   from dataclasses import dataclass
   from ..utils.file_utils import match_path_pattern

   @dataclass
   class CacheFileRule:
       """缓存文件检测规则"""
       rule_id: str                    # 规则 ID (如 CACHE-01)
       description: str                # 规则描述(中文)
       patterns: List[str]             # 匹配模式列表
       tags: List[str]                 # 分类标签

   # CACHE-01: Python 缓存文件
   PYTHON_CACHE_RULE = CacheFileRule(
       rule_id="CACHE-01",
       description="检测 Python 缓存文件",
       patterns=[
           '__pycache__',
           '*.pyc',
           '*.pyo',
           '*.pyd',
           '.Python',
       ],
       tags=["python", "cache", "bytecode"]
   )

   # CACHE-02: Node.js 依赖
   NODEJS_CACHE_RULE = CacheFileRule(
       rule_id="CACHE-02",
       description="检测 Node.js 依赖和缓存",
       patterns=[
           'node_modules',
           '.npm',
           '.yarn',
           'yarn.lock',
           'package-lock.json',
       ],
       tags=["nodejs", "npm", "yarn", "dependencies"]
   )

   # CACHE-03: 编译产物
   COMPILED_CACHE_RULE = CacheFileRule(
       rule_id="CACHE-03",
       description="检测编译产物和构建文件",
       patterns=[
           '*.class',
           'target',
           'build',
           'dist',
           'out',
           '*.o',
           '*.so',
           '*.exe',
       ],
       tags=["compiled", "build", "artifacts"]
   )

   # CACHE-04: 系统临时文件
   SYSTEM_CACHE_RULE = CacheFileRule(
       rule_id="CACHE-04",
       description="检测系统临时文件和日志",
       patterns=[
           '*.log',
           '*.tmp',
           '.DS_Store',
           'Thumbs.db',
           'desktop.ini',
       ],
       tags=["system", "temp", "logs"]
   )

   # 所有缓存文件规则
   CACHE_FILE_RULES = [
       PYTHON_CACHE_RULE,
       NODEJS_CACHE_RULE,
       COMPILED_CACHE_RULE,
       SYSTEM_CACHE_RULE,
   ]

   def is_cache_file(file_path: Path) -> bool:
       """
       检测文件是否为缓存文件

       Args:
           file_path: 文件路径

       Returns:
           True 如果是缓存文件, False 如果不是
       """
       for rule in CACHE_FILE_RULES:
           if match_path_pattern(file_path, rule.patterns):
               return True
       return False
   ```

**实现注意事项:**
- 使用 pathlib.Path 处理路径,确保 Windows 兼容性
- 路径规范化为正斜杠,避免 Windows 反斜杠问题
- 支持 *.ext 扩展名匹配和 path/segment 路径包含匹配
- 每个 rule_id 对应 REQUIREMENTS.md 中的需求 ID (CACHE-01 到 CACHE-04)
- description 使用中文
  </action>
  <verify>
    <automated>
# 验证缓存文件规则模块存在且包含所有规则
python -c "
import sys
sys.path.insert(0, 'plugins/windows-git-commit/skills/windows-git-commit')
from scanner.rules.cache_files import (
    PYTHON_CACHE_RULE,
    NODEJS_CACHE_RULE,
    COMPILED_CACHE_RULE,
    SYSTEM_CACHE_RULE,
    CACHE_FILE_RULES,
    is_cache_file,
)
from pathlib import Path

# 验证规则数量
assert len(CACHE_FILE_RULES) == 4, f'Expected 4 rules, got {len(CACHE_FILE_RULES)}'

# 验证每个规则的属性
for rule in CACHE_FILE_RULES:
    assert hasattr(rule, 'rule_id'), f'Missing rule_id in {rule}'
    assert hasattr(rule, 'description'), f'Missing description in {rule}'
    assert hasattr(rule, 'patterns'), f'Missing patterns in {rule}'
    assert len(rule.patterns) > 0, f'Empty patterns in {rule}'

# 测试 is_cache_file 函数
test_files = [
    (Path('src/__pycache__/module.pyc'), True),
    (Path('node_modules/package/index.js'), True),
    (Path('target/classes/Main.class'), True),
    (Path('debug.log'), True),
    (Path('src/main.py'), False),
]

for file_path, expected in test_files:
    result = is_cache_file(file_path)
    assert result == expected, f'is_cache_file({file_path}) returned {result}, expected {expected}'

print('✓ All cache file detection rules validated successfully')
"
    </automated>
    <manual>检查 cache_files.py 文件,确认所有 4 个缓存文件规则已实现</manual>
    <sampling_rate>运行于此任务提交后,下一个任务开始前</sampling_rate>
  </verify>
  <done>
- scanner/rules/cache_files.py 文件存在
- 所有 4 个缓存文件检测规则已定义 (CACHE-01 到 CACHE-04)
- is_cache_file 函数可以检测缓存文件
- scanner/utils/file_utils.py 文件存在
- match_path_pattern 函数可以匹配路径模式
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建配置文件检测规则模块</name>
  <files>
    - plugins/windows-git-commit/skills/windows-git-commit/scanner/rules/config_files.py
  </files>
  <action>
创建配置文件检测规则模块,包括文件名检测和内容扫描:

1. **创建 config_files.py:**

   参考 RESEARCH.md 的配置文件检测模式:

   ```python
   from pathlib import Path
   from typing import List, Tuple
   from dataclasses import dataclass
   import re

   @dataclass
   class ConfigFileRule:
       """配置文件检测规则"""
       rule_id: str                    # 规则 ID (如 CONF-01)
       description: str                # 规则描述(中文)
       file_patterns: List[str]        # 文件名模式
       content_pattern: re.Pattern     # 内容扫描模式(可选)
       tags: List[str]                 # 分类标签

   # CONF-01: 环境配置文件
   ENV_FILE_RULE = ConfigFileRule(
       rule_id="CONF-01",
       description="检测环境配置文件",
       file_patterns=[
           '.env',
           '.env.local',
           '.env.*.local',
       ],
       content_pattern=None,  # 不扫描内容,仅检测文件名
       tags=["env", "config", "secrets"]
   )

   # CONF-02: 凭证文件
   CREDENTIALS_FILE_RULE = ConfigFileRule(
       rule_id="CONF-02",
       description="检测凭证文件",
       file_patterns=[
           'credentials.json',
           'secrets.yaml',
           'secrets.yml',
           'secrets.xml',
       ],
       content_pattern=None,  # 不扫描内容,仅检测文件名
       tags=["credentials", "secrets", "config"]
   )

   # CONF-03: 包含敏感字段的配置文件
   SENSITIVE_FIELD_RULE = ConfigFileRule(
       rule_id="CONF-03",
       description="检测配置文件中的敏感字段",
       file_patterns=[],  # 不限制文件名,扫描所有文件内容
       content_pattern=re.compile(
           r'(?i)(password|api_key|secret|token)\s*[=:]\s*[\'"]?([^\'"}\s]+)',
           re.MULTILINE
       ),
       tags=["sensitive", "config", "secrets"]
   )

   # 所有配置文件规则
   CONFIG_FILE_RULES = [
       ENV_FILE_RULE,
       CREDENTIALS_FILE_RULE,
       SENSITIVE_FIELD_RULE,
   ]

   def is_config_file(file_path: Path) -> Tuple[bool, str]:
       """
       检测文件是否为配置文件(基于文件名)

       Args:
           file_path: 文件路径

       Returns:
           (is_config, rule_id) 元组
           - is_config: True 如果是配置文件
           - rule_id: 匹配的规则 ID (如 "CONF-01")
       """
       file_name = file_path.name

       for rule in CONFIG_FILE_RULES:
           if not rule.file_patterns:  # 跳过仅扫描内容的规则
               continue

           for pattern in rule.file_patterns:
               if pattern.startswith('.env'):
                   # 环境变量文件特殊匹配
                   if file_name == pattern or file_name.startswith('.env.'):
                       return True, rule.rule_id
               else:
                   # 精确文件名匹配
                   if file_name == pattern:
                       return True, rule.rule_id

       return False, ""

   def scan_config_content(content: str) -> List[Tuple[str, str, int]]:
       """
       扫描配置文件内容,检测敏感字段

       Args:
           content: 文件内容

       Returns:
           List of (field_name, masked_value, line_number) tuples
       """
       matches = SENSITIVE_FIELD_RULE.content_pattern.finditer(content)
       results = []

       for match in matches:
           field_name = match.group(1)
           value = match.group(2)
           line_number = content[:match.start()].count('\n') + 1

           # 脱敏显示: 前 4 字符 + *** + 后 4 字符
           if len(value) > 12:
               masked_value = f"{value[:4]}***{value[-4:]}"
           else:
               masked_value = f"{value[:2]}***{value[-2:]}"

           results.append((field_name, masked_value, line_number))

       return results
   ```

**实现注意事项:**
- .env 文件支持 .env.local 和 .env.*.local 模式
- CONF-03 规则扫描所有文件内容,不限制文件名
- 内容扫描使用正则表达式匹配敏感字段
- 敏感字段脱敏显示 (前 4 字符 + *** + 后 4 字符)
- 每个 rule_id 对应 REQUIREMENTS.md 中的需求 ID (CONF-01 到 CONF-03)
  </action>
  <verify>
    <automated>
# 验证配置文件规则模块存在且包含所有规则
python -c "
import sys
sys.path.insert(0, 'plugins/windows-git-commit/skills/windows-git-commit')
from scanner.rules.config_files import (
    ENV_FILE_RULE,
    CREDENTIALS_FILE_RULE,
    SENSITIVE_FIELD_RULE,
    CONFIG_FILE_RULES,
    is_config_file,
    scan_config_content,
)
from pathlib import Path

# 验证规则数量
assert len(CONFIG_FILE_RULES) == 3, f'Expected 3 rules, got {len(CONFIG_FILE_RULES)}'

# 验证每个规则的属性
for rule in CONFIG_FILE_RULES:
    assert hasattr(rule, 'rule_id'), f'Missing rule_id in {rule}'
    assert hasattr(rule, 'description'), f'Missing description in {rule}'
    assert hasattr(rule, 'file_patterns'), f'Missing file_patterns in {rule}'
    assert hasattr(rule, 'tags'), f'Missing tags in {rule}'

# 测试 is_config_file 函数
test_files = [
    (Path('.env'), True, 'CONF-01'),
    (Path('.env.local'), True, 'CONF-01'),
    (Path('config/.env.production.local'), True, 'CONF-01'),
    (Path('credentials.json'), True, 'CONF-02'),
    (Path('secrets.yaml'), True, 'CONF-02'),
    (Path('src/main.py'), False, ''),
]

for file_path, expected_is_config, expected_rule_id in test_files:
    is_config, rule_id = is_config_file(file_path)
    assert is_config == expected_is_config, f'is_config_file({file_path}) returned {is_config}, expected {expected_is_config}'
    if expected_is_config:
        assert rule_id == expected_rule_id, f'rule_id for {file_path} is {rule_id}, expected {expected_rule_id}'

# 测试 scan_config_content 函数
test_content = '''
api_key = "sk-1234567890abcdef"
password = "my_secret_pass"
database_url = "postgres://localhost/mydb"
'''

results = scan_config_content(test_content)
assert len(results) >= 2, f'Expected at least 2 sensitive fields, got {len(results)}'

print('✓ All config file detection rules validated successfully')
"
    </automated>
    <manual>检查 config_files.py 文件,确认所有 3 个配置文件规则已实现</manual>
    <sampling_rate>运行于此任务提交后,下一个任务开始前</sampling_rate>
  </verify>
  <done>
- scanner/rules/config_files.py 文件存在
- 所有 3 个配置文件检测规则已定义 (CONF-01 到 CONF-03)
- is_config_file 函数可以检测配置文件 (基于文件名)
- scan_config_content 函数可以扫描文件内容中的敏感字段
- 敏感字段已脱敏显示
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新规则导出列表</name>
  <files>
    - plugins/windows-git-commit/skills/windows-git-commit/scanner/rules/__init__.py
  </files>
  <action>
更新 scanner/rules/__init__.py,导出所有规则 (包括敏感信息、缓存文件、配置文件):

```python
# 敏感信息检测规则
from .secrets import (
    DetectionRule,
    AWS_ACCESS_KEY_RULE,
    AWS_SECRET_KEY_RULE,
    AWS_SESSION_TOKEN_RULE,
    GITHUB_TOKEN_RULE,
    GITLAB_TOKEN_RULE,
    BITBUCKET_TOKEN_RULE,
    GENERIC_API_KEY_RULE,
    SSH_KEY_RULE,
    PGP_KEY_RULE,
    PEM_CERT_RULE,
)

# 缓存文件检测规则
from .cache_files import (
    CacheFileRule,
    PYTHON_CACHE_RULE,
    NODEJS_CACHE_RULE,
    COMPILED_CACHE_RULE,
    SYSTEM_CACHE_RULE,
    CACHE_FILE_RULES,
    is_cache_file,
)

# 配置文件检测规则
from .config_files import (
    ConfigFileRule,
    ENV_FILE_RULE,
    CREDENTIALS_FILE_RULE,
    SENSITIVE_FIELD_RULE,
    CONFIG_FILE_RULES,
    is_config_file,
    scan_config_content,
)

__all__ = [
    # 基类
    'DetectionRule',
    'CacheFileRule',
    'ConfigFileRule',

    # 敏感信息规则
    'AWS_ACCESS_KEY_RULE',
    'AWS_SECRET_KEY_RULE',
    'AWS_SESSION_TOKEN_RULE',
    'GITHUB_TOKEN_RULE',
    'GITLAB_TOKEN_RULE',
    'BITBUCKET_TOKEN_RULE',
    'GENERIC_API_KEY_RULE',
    'SSH_KEY_RULE',
    'PGP_KEY_RULE',
    'PEM_CERT_RULE',

    # 缓存文件规则
    'PYTHON_CACHE_RULE',
    'NODEJS_CACHE_RULE',
    'COMPILED_CACHE_RULE',
    'SYSTEM_CACHE_RULE',
    'CACHE_FILE_RULES',
    'is_cache_file',

    # 配置文件规则
    'ENV_FILE_RULE',
    'CREDENTIALS_FILE_RULE',
    'SENSITIVE_FIELD_RULE',
    'CONFIG_FILE_RULES',
    'is_config_file',
    'scan_config_content',
]
```

**验证导出:**
运行 Python 验证所有规则都可以从 scanner.rules 导入。
  </action>
  <verify>
    <automated>
# 验证所有规则都可以从 scanner.rules 导入
python -c "
import sys
sys.path.insert(0, 'plugins/windows-git-commit/skills/windows-git-commit')

# 导入所有规则
from scanner.rules import (
    # 敏感信息规则
    AWS_ACCESS_KEY_RULE,
    GITHUB_TOKEN_RULE,
    GITLAB_TOKEN_RULE,
    GENERIC_API_KEY_RULE,
    SSH_KEY_RULE,
    PGP_KEY_RULE,
    PEM_CERT_RULE,

    # 缓存文件规则
    PYTHON_CACHE_RULE,
    NODEJS_CACHE_RULE,
    COMPILED_CACHE_RULE,
    SYSTEM_CACHE_RULE,
    CACHE_FILE_RULES,
    is_cache_file,

    # 配置文件规则
    ENV_FILE_RULE,
    CREDENTIALS_FILE_RULE,
    SENSITIVE_FIELD_RULE,
    CONFIG_FILE_RULES,
    is_config_file,
    scan_config_content,
)

# 验证规则数量
assert len(CACHE_FILE_RULES) == 4, f'Expected 4 cache rules, got {len(CACHE_FILE_RULES)}'
assert len(CONFIG_FILE_RULES) == 3, f'Expected 3 config rules, got {len(CONFIG_FILE_RULES)}'

print('✓ All rules exported successfully from scanner.rules')
"
    </automated>
    <manual>检查 rules/__init__.py 文件,确认所有规则都已导出</manual>
    <sampling_rate>运行于此任务提交后,进入下一阶段前</sampling_rate>
  </verify>
  <done>
- scanner/rules/__init__.py 已更新
- 所有 7 个敏感信息规则已导出
- 所有 4 个缓存文件规则已导出
- 所有 3 个配置文件规则已导出
- 所有工具函数已导出 (is_cache_file, is_config_file, scan_config_content)
  </done>
</task>

</tasks>

<verification>
**Phase 6 Plan 02 完成标准:**

1. **代码结构验证:**
   - scanner/rules/cache_files.py 文件存在
   - scanner/rules/config_files.py 文件存在
   - scanner/utils/file_utils.py 文件存在

2. **规则完整性验证:**
   - 所有 4 个缓存文件检测规则已定义 (CACHE-01 到 CACHE-04)
   - 所有 3 个配置文件检测规则已定义 (CONF-01 到 CONF-03)
   - 每个规则有正确的属性 (rule_id, description, patterns/tags)

3. **工具函数验证:**
   - is_cache_file 函数可以检测缓存文件
   - is_config_file 函数可以检测配置文件
   - scan_config_content 函数可以扫描配置内容
   - match_path_pattern 函数可以匹配路径模式

4. **导出验证:**
   - rules/__init__.py 正确导出所有规则
   - 所有规则可以从 scanner.rules 导入

5. **Windows 兼容性验证:**
   - 路径使用 pathlib.Path (跨平台)
   - 路径规范化为正斜杠 (Windows 兼容)
</verification>

<success_criteria>
Plan 02 成功完成的标志:

1. **功能完整性:**
   - ✓ 所有缓存文件检测规则已定义 (CACHE-01 到 CACHE-04)
   - ✓ 所有配置文件检测规则已定义 (CONF-01 到 CONF-03)
   - ✓ 文件路径匹配工具已实现
   - ✓ 配置内容扫描工具已实现

2. **代码质量:**
   - ✓ 使用 dataclass 定义规则,代码结构清晰
   - ✓ 正则表达式已预编译,性能优化
   - ✓ 类型注解完整,IDE 友好

3. **测试验证:**
   - ✓ 自动化验证脚本通过
   - ✓ 所有规则可以导入
   - ✓ 工具函数测试通过

4. **文档完整性:**
   - ✓ 代码注释清晰
   - ✓ 函数文档字符串完整
   - ✓ 中文描述符合用户决策
</success_criteria>

<output>
完成后创建 `.planning/phases/06-core-scanning-infrastructure/06-02-SUMMARY.md`
</output>
