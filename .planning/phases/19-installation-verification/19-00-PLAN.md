---
phase: 19-installation-verification
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04]
user_setup: []
must_haves:
  truths:
    - "Test files exist for all verification modules"
    - "Test framework can discover and run verification tests"
  artifacts:
    - path: "installer/tests/verification/index.test.js"
      provides: "Integration test scaffold"
      min_lines: 20
    - path: "installer/tests/verification/runner.test.js"
      provides: "Python script execution tests"
      min_lines: 20
    - path: "installer/tests/verification/parser.test.js"
      provides: "Output parsing tests"
      min_lines: 20
    - path: "installer/tests/verification/formatter.test.js"
      provides: "Table formatting tests"
      min_lines: 20
  key_links:
    - from: "installer/tests/verification/*.test.js"
      to: "installer/jest.config.js"
      via: "npm test"
      pattern: "test\\("
---

<objective>
Create TDD test scaffolds for Phase 19 verification modules.

Purpose: Establish test-first development pattern for verification functionality
Output: 4 test files with skeleton test cases covering all requirements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-installation-verification/19-CONTEXT.md
@.planning/phases/19-installation-verification/19-RESEARCH.md
@.planning/phases/19-installation-verification/19-VALIDATION.md

## Reference: Python Script Output Format

From `plugins/claude-notify/scripts/verify-installation.py`:
```
  [OK] Python version: PASS
      Current: 3.9.1, Required: >=3.8
  [X] Environment Variables: FAIL
      PUSHOVER_TOKEN not set
```

Pattern: `  [OK|X] {name}: {PASS|FAIL|SKIP}` followed by optional detail line.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create runner.test.js scaffold</name>
  <files>installer/tests/verification/runner.test.js</files>
  <read_first>
    - installer/src/installers/pip-installer.js (execa error handling pattern)
    - installer/tests/installers/pip-installer.test.js (test structure reference)
  </read_first>
  <behavior>
    - Test 1: runPythonVerification returns success with stdout when script exits 0
    - Test 2: runPythonVerification handles timeout (30s) and returns error
    - Test 3: runPythonVerification handles Python not found (ENOENT)
    - Test 4: runPythonVerification handles script execution failure
  </behavior>
  <action>Create test scaffold with 4 describe blocks for Python script execution. Use Jest mock for execa. Each test should have descriptive name and expect assertions. Mark tests as `.skip` initially - implementation will enable them.</action>
  <verify>
    <automated>cd installer && npm test -- tests/verification/runner.test.js 2>&1 | grep -E "(PASS|FAIL|skipped)"</automated>
  </verify>
  <done>File exists with 4 test cases, npm test runs without syntax errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create parser.test.js scaffold</name>
  <files>installer/tests/verification/parser.test.js</files>
  <read_first>
    - installer/src/i18n/index.js (t() function pattern)
  </read_first>
  <behavior>
    - Test 1: parseVerificationOutput extracts PASS results with [OK] symbol
    - Test 2: parseVerificationOutput extracts FAIL results with [X] symbol
    - Test 3: parseVerificationOutput captures detail lines (indented text after result)
    - Test 4: parseVerificationOutput handles SKIP status
    - Test 5: parseVerificationOutput returns empty array for empty input
  </behavior>
  <action>Create test scaffold with 5 describe blocks for output parsing. Include sample Python output strings as test data. Tests should verify regex pattern matching and structure of returned objects.</action>
  <verify>
    <automated>cd installer && npm test -- tests/verification/parser.test.js 2>&1 | grep -E "(PASS|FAIL|skipped)"</automated>
  </verify>
  <done>File exists with 5 test cases, npm test runs without syntax errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create formatter.test.js scaffold</name>
  <files>installer/tests/verification/formatter.test.js</files>
  <read_first>
    - installer/src/marketplace/index.js (displayPluginTable simple table pattern)
  </read_first>
  <behavior>
    - Test 1: formatVerificationTable creates table with Check/Status/Details headers
    - Test 2: formatVerificationTable shows green PASS icon for passed checks
    - Test 3: formatVerificationTable shows red FAIL icon for failed checks
    - Test 4: formatVerificationTable shows gray SKIP icon for skipped checks
    - Test 5: calculateSummary returns correct passed/total counts
  </behavior>
  <action>Create test scaffold with 5 describe blocks for table formatting. Test the simple table format (not cli-table3) consistent with marketplace/index.js pattern. Verify chalk color usage.</action>
  <verify>
    <automated>cd installer && npm test -- tests/verification/formatter.test.js 2>&1 | grep -E "(PASS|FAIL|skipped)"</automated>
  </verify>
  <done>File exists with 5 test cases, npm test runs without syntax errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create index.test.js scaffold (integration)</name>
  <files>installer/tests/verification/index.test.js</files>
  <read_first>
    - installer/tests/configurators/index.test.js (orchestrator test pattern)
  </read_first>
  <behavior>
    - Test 1: runVerification returns object with success, passed, total properties
    - Test 2: runVerification displays verification title using i18n
    - Test 3: runVerification shows rerun command at the end
    - Test 4: runVerification displays common solutions when checks fail
  </behavior>
  <action>Create integration test scaffold with 4 test cases. Mock runner, parser, formatter modules. Test the orchestrator function that ties everything together.</action>
  <verify>
    <automated>cd installer && npm test -- tests/verification/index.test.js 2>&1 | grep -E "(PASS|FAIL|skipped)"</automated>
  </verify>
  <done>File exists with 4 test cases, npm test runs without syntax errors</done>
</task>

</tasks>

<verification>
After all tasks:
1. Run `cd installer && npm test` - should complete without syntax errors
2. Verify all 4 test files exist in installer/tests/verification/
3. Verify each test file contains describe blocks with skip or test cases
</verification>

<success_criteria>
- 4 test scaffold files created in installer/tests/verification/
- Each file has 4-5 test cases matching the behaviors specified
- npm test completes without errors (tests may be skipped)
- Test structure follows project patterns (Jest, describe/it)
</success_criteria>

<output>
After completion, create `.planning/phases/19-installation-verification/19-00-SUMMARY.md`
</output>
