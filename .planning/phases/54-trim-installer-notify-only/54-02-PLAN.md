---
phase: 54-trim-installer-notify-only
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - installer/tests/detectors/git.test.js
  - installer/tests/detectors/ssh-tools.test.js
  - installer/tests/detectors/index.test.js
  - installer/tests/config-git-ssh-detect.js
  - installer/tests/config-git-ssh-guide.js
  - installer/tests/config-git-user-detect.js
  - installer/tests/config-pushover-detect.js
  - installer/tests/config-pushover-input.js
  - installer/tests/config-pushover-save.js
  - installer/tests/run-all.js
  - installer/tests/configurators/git-ssh.test.js
  - installer/tests/configurators/git-user.test.js
  - installer/tests/configurators/unified-flow.test.js
  - installer/tests/marketplace/config-manager.test.js
  - installer/tests/marketplace/plugin-discovery.test.js
  - installer/tests/marketplace/plugin-installer.test.js
  - installer/tests/index.test.js
  - installer/tests/uninstall/detector.test.js
  - installer/tests/uninstall/remover.test.js
  - installer/tests/uninstall/formatter.test.js
  - installer/tests/uninstall/index.test.js
  - .planning/phases/54-trim-installer-notify-only/54-02-baseline.txt
  - .planning/phases/54-trim-installer-notify-only/54-02-posttrim.txt
autonomous: true
requirements: [INS-05]
user_setup: []

must_haves:
  truths:
    - "已移除模块（git/ssh 检测、git 配置、marketplace）的测试文件已删除"
    - "保留的测试文件已更新以反映裁剪后的源码（2 个 detector、1 个 configurator、无 marketplace 步骤、uninstall 无 marketplaceSource、PLUGIN_NAMES=['claude-notify'] 仅 1 项）"
    - "Phase 54 不引入任何新的 jest 失败：post-trim 的失败套件集合 ⊆ 基线预存失败集合（基线 = 10 failed suites / 8 failed tests，均为 v3.0 前预存负债，out of scope）"
    - "本阶段删除的测试文件（git/ssh/marketplace 相关 10 个）若在基线中为失败套件，则它们的消失减少失败计数（允许且有益）；不要求修复基线中其余 4 个预存失败（bin.test.js CRLF / verification/runner.test.js python / pushover.test.js process.exit IIFE / unified-flow.test.js process.exit IIFE）"
    - "cd installer && node tests/run-all.js（若保留 pushover 配置测试则更新该 runner；否则删除该 runner）通过或已被删除"
  artifacts:
    - "installer/tests/detectors/index.test.js （更新：mock 2 个 detector，断言 results.length===2）"
    - "installer/tests/index.test.js （更新：移除 marketplace mock，断言 main 流程无 marketplace 调用）"
    - "installer/tests/configurators/unified-flow.test.js （裁剪：仅保留 pushover 相关用例，删除 git-user/git-ssh 用例；★ 不修复其顶层 process.exit IIFE —— 该 IIFE 是预存负债，out of scope ★）"
    - "installer/tests/uninstall/detector.test.js （更新：单 plugin claude-notify、无 marketplaceSource 字段、import 改 mock ../../src/uninstall/paths.js）"
    - "installer/tests/uninstall/remover.test.js （更新：5 步移除、无 marketplace cache/source、import 改 mock ../../src/uninstall/paths.js）"
    - "installer/tests/uninstall/formatter.test.js （更新：无 marketplace 类别渲染，命令式移除 formatter.test.js:120 的 toHaveBeenCalledWith('uninstall.category.marketplace') 断言）"
    - "installer/tests/uninstall/index.test.js （更新：fixture 降为 1 plugin + 无 marketplaceSource；三对 {found,total} 显式重算为 {6,6}/{0,6}/{3,6}）"
    - ".planning/phases/54-trim-installer-notify-only/54-02-baseline.txt （新建：执行 Task 4 前捕获的 npm test 基线输出，用于 post-trim 子集比较）"
    - "git/marketplace 相关测试文件已删除"
  key_links:
    - "tests/uninstall/*.test.js → mock ../../src/uninstall/paths.js （与 01 计划 Task 2 的 paths.js 迁移对齐；detector.test.js mock 仅含 detector 实际使用的 isPluginInstalled/getSkillsDir 等，对齐 01 计划 MEDIUM-1）"
    - "tests/index.test.js → mock 已裁剪的 src/index.js 流程（无 marketplace）"
    - "cd installer && npm test post-trim 失败套件 ⊆ 基线失败套件（SC5 最终验证：no new failures）"
---

<objective>
裁剪 installer 测试树以匹配 01 计划裁剪后的源码：删除已移除模块（git/ssh 检测、git 配置、marketplace）的测试文件，更新保留测试以反映 2 detector / 1 configurator / 无 marketplace / uninstall 无 marketplaceSource / 单 plugin claude-notify 的新契约。验收门经 convergence cycle 1 复核后由 codex+opencode 共识 **rescope 为“不引入新失败”**（相对 v3.0 前既有红色基线），**不**修复 4 个预存失败套件（bin.test.js CRLF、verification/runner.test.js python 路径、pushover.test.js process.exit IIFE、unified-flow.test.js process.exit IIFE —— 均为 v3.0 前预存负债，经用户确认 out of scope）。

Purpose: 满足 ROADMAP Phase 54 SC5（已移除模块的测试文件已删除；Phase 54 不引入新的 jest 失败）。01 计划删除/裁剪源码后，旧测试会因引用已删模块（detectGit/configureGitSSH/runMarketplaceIntegration/marketplace config-manager/2 个 plugin 含 windows-git-commit 等）而失败，本计划修复这些由 Phase 54 触发的测试断裂，使 post-trim 失败套件集合成为基线预存失败集合的子集（即零新增失败）。基线本身（10 failed suites / 8 failed tests）的 4 个非裁剪相关失败属 v3.0 前负债，不在本里程碑修复范围（见 REQUIREMENTS.md Out of Scope 表）。

Output: 一个与裁剪后源码一致的 installer 测试树；post-trim 的 jest 失败套件 ⊆ 基线预存失败套件（零新增失败），且本阶段删除的失败套件（git/ssh/marketplace 相关）从计数中消失。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-trim-installer-notify-only/54-CONTEXT.md
@.planning/phases/54-trim-installer-notify-only/54-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除已移除模块的测试文件 (INS-05)</name>
  <files>
    installer/tests/detectors/git.test.js (删除),
    installer/tests/detectors/ssh-tools.test.js (删除),
    installer/tests/config-git-ssh-detect.js (删除),
    installer/tests/config-git-ssh-guide.js (删除),
    installer/tests/config-git-user-detect.js (删除),
    installer/tests/configurators/git-ssh.test.js (删除),
    installer/tests/configurators/git-user.test.js (删除),
    installer/tests/marketplace/config-manager.test.js (删除),
    installer/tests/marketplace/plugin-discovery.test.js (删除),
    installer/tests/marketplace/plugin-installer.test.js (删除),
    installer/tests/marketplace/ (空目录删除)
  </files>
  <read_first>
    先逐一打开下列文件确认其“纯测试已删模块”（避免误删也覆盖 pushover/python 的文件）：
    installer/tests/detectors/git.test.js （应仅测 detectGit）,
    installer/tests/detectors/ssh-tools.test.js （应仅测 detectSSHTools）,
    installer/tests/config-git-ssh-detect.js, installer/tests/config-git-ssh-guide.js （仅测 configureGitSSH/detectGitSSH）,
    installer/tests/config-git-user-detect.js （仅测 configureGitUser/detectGitUser）,
    installer/tests/configurators/git-ssh.test.js, installer/tests/configurators/git-user.test.js （仅测 git 配置）,
    installer/tests/marketplace/config-manager.test.js, installer/tests/marketplace/plugin-discovery.test.js, installer/tests/marketplace/plugin-installer.test.js （仅测已删 marketplace 模块）
    注意：config-pushover-*.js 与 configurators/pushover.test.js 与 unified-flow.test.js 不要在本 Task 删除（Task 2/3 处理）。
  </read_first>
  <action>
    1. 用 git rm 删除下列确认仅测试已删模块的文件：
       - installer/tests/detectors/git.test.js
       - installer/tests/detectors/ssh-tools.test.js
       - installer/tests/config-git-ssh-detect.js
       - installer/tests/config-git-ssh-guide.js
       - installer/tests/config-git-user-detect.js
       - installer/tests/configurators/git-ssh.test.js
       - installer/tests/configurators/git-user.test.js
       - installer/tests/marketplace/config-manager.test.js
       - installer/tests/marketplace/plugin-discovery.test.js
       - installer/tests/marketplace/plugin-installer.test.js

    2. 删除空目录 installer/tests/marketplace/（git rm 删完文件后 rmdir 或 git 会自动处理）。

    3. 删除前必须先 Read 每个文件（已在 read_first 列出）确认其不含 pushover/python/pip 用例。若某文件意外含保留模块用例（例如 config-git-ssh-detect.js 里混了 pushover 断言），不要整文件删除，改为 Edit 仅删 git 部分——但根据 54-CONTEXT.md 与文件命名，这些文件均为单一已删模块的测试，预期可整文件删除。
  </action>
  <verify>
    <automated>
    test ! -f installer/tests/detectors/git.test.js &&
    test ! -f installer/tests/detectors/ssh-tools.test.js &&
    test ! -f installer/tests/config-git-ssh-detect.js &&
    test ! -f installer/tests/config-git-ssh-guide.js &&
    test ! -f installer/tests/config-git-user-detect.js &&
    test ! -f installer/tests/configurators/git-ssh.test.js &&
    test ! -f installer/tests/configurators/git-user.test.js &&
    test ! -f installer/tests/marketplace/config-manager.test.js &&
    test ! -f installer/tests/marketplace/plugin-discovery.test.js &&
    test ! -f installer/tests/marketplace/plugin-installer.test.js &&
    test ! -d installer/tests/marketplace
    </automated>
  </verify>
  <done>
    - 上述 10 个测试文件全部物理消失
    - installer/tests/marketplace/ 目录不存在
    - 保留文件未误删：detectors/index.test.js、detectors/python.test.js、detectors/pip-package.test.js、configurators/pushover.test.js、configurators/unified-flow.test.js、config-pushover-*.js、uninstall/*.test.js、index.test.js、cli.test.js 等仍在
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 detectors/index.test.js、index.test.js、unified-flow.test.js、config-pushover runner (INS-05)</name>
  <files>
    installer/tests/detectors/index.test.js (修改),
    installer/tests/index.test.js (修改),
    installer/tests/configurators/unified-flow.test.js (修改),
    installer/tests/run-all.js (修改或删除)
  </files>
  <read_first>
    installer/tests/detectors/index.test.js （当前 mock 4 个 detector 含 git.js/ssh-tools.js，断言 results.length===4；需改 2）,
    installer/tests/index.test.js （当前 jest.mock('../src/marketplace/index.js')，断言 main 调用 marketplace；需移除）,
    installer/tests/configurators/unified-flow.test.js （混合：UFLOW-01/02 pushover 用例 + git-user/git-ssh 用例；需裁剪保留 pushover 部分）,
    installer/tests/run-all.js （Phase 17 runner，引用 config-git-ssh-detect/guide/config-git-user-detect，这些已被 Task 1 删除；需更新或删除）,
    installer/src/detectors/index.js （01 计划裁剪后：2 项 Promise.all）,
    installer/src/index.js （01 计划裁剪后：无 marketplace 步骤）
  </read_first>
  <action>
    1. installer/tests/detectors/index.test.js（适配 2 detector）：
       - 移除两个 jest.mock：`jest.mock('../../src/detectors/git.js', ...)` 与 `jest.mock('../../src/detectors/ssh-tools.js', ...)`。
       - 移除对应 require：`const { detectGit } = require('../../src/detectors/git.js');` 与 `const { detectSSHTools } = require('../../src/detectors/ssh-tools.js');`。
       - 保留 detectPython 与 detectPipPackage 的 mock 与 require。
       - i18n mock 的 translations 中删除 `'guidance.installGit'` 与 `'guidance.installSSHTools'`（保留 installPython/installRequests），避免误判（非必须，但保持整洁）。
       - 三个 runAllDetectors 用例（“returns array / all pass / any fail”）：
         - 移除所有 `detectGit.mockResolvedValue(...)` 与 `detectSSHTools.mockResolvedValue(...)` 设置。
         - 断言 `result.results).toHaveLength(4)` 改为 `toHaveLength(2)`。
         - “any fail” 用例：让 detectPython.mockResolvedValue(installed:false) 即可触发 allPassed=false（不再需要 git/ssh mock）。
       - printResult 用例（3 个，用 Python 作样本）保持不变。

    2. installer/tests/index.test.js（适配无 marketplace 主流程）：
       - 移除 `jest.mock('../src/marketplace/index.js', () => ({ runMarketplaceIntegration: jest.fn()... }))` 整块。
       - 其余 mock（uninstall/cli/welcome/detectors/installers/configurators/hooks/verification）保持不变。
       - 现有 4 个用例（main exported / checkPlatform / runs on Windows / routes to --uninstall）不依赖 marketplace 调用断言，保持不变。
       - 若有任何断言 `expect(runMarketplaceIntegration).toHaveBeenCalled()`（当前文件无此断言，仅 mock），无需补充。
       - 注意：此文件本就只 mock 不调用 marketplace，删除 mock 块后测试仍通过；重点是避免 require 已删除的 marketplace 模块。

    3. installer/tests/configurators/unified-flow.test.js（裁剪为仅 pushover；★ process.exit IIFE 是预存负债，本阶段不修 ★）：
       - 这是一个手写 assert 风格（非 jest）的测试文件，含 14 个 test() 用例。
       - 删除用例 Test 2（detectGitUser 结构）、Test 3（detectGitSSH 结构）、Test 13（git-user 4-case 映射）、Test 14（git-ssh configured 短路）—— 这些 require 已删的 configurators/git-user.js 与 git-ssh.js。
       - 删除 helper clearGitUserCache() 与 clearGitSSHCache() 函数定义，以及 clearAllConfiguratorCaches() 中对它们的调用（仅保留 clearPushoverCache 与 clearIndexCache）。
       - 保留用例：Test 1（detectPushoverFull 结构）、Test 4/5/6（pushover 全配/Case B/Case C）、Test 7（displayConfigSummary，但样本 results 数组需改为仅 Pushover 项，移除 'Git SSH'/'Git user.name'/'Git user.email' 样本）、Test 8（runAllConfigurators/displayConfigSummary 导出）、Test 9/10/11/12（pushover Case D/A/B/C 映射，纯逻辑无 require）。
       - Test 7 的 sampleResults 改为：`[{ name: 'Pushover', status: 'configured', details: 'validated and saved' }]`（单元素即可验证 displayConfigSummary 不抛异常）。
       - **★ out-of-scope 声明（HIGH-1/HIGH-2 共识）★**：文件末尾的自执行 summary 块（`(async () => {... process.exit(0/1) })()` IIFE，约 lines 395-410）是 Phase 17/20 遗留的手写 runner 模式，Jest 30 会拦截测试文件内的 `process.exit` 并判失败（报 "must contain at least one test" 或 "process.exit called"）。**本阶段仅裁剪 git 测试体，不修复该 IIFE/process.exit** —— 它是 v3.0 前预存负债，经用户确认 out of scope（见 REQUIREMENTS.md Out of Scope 表）。裁剪后该套件预期仍因同一预存 IIFE 原因失败，这是**可接受的（非新增失败）**，因为它在基线中本就失败。Task 4 的 baseline-comparison 闸门会把 unified-flow.test.js 列入预存失败白名单。
       - 若执行器裁剪 git 用例后该 IIFE 因失去 test() 调用而抛新错（非 IIFE 本身的预存失败），则需最小修复使裁剪不引入*新*失败（例如保留至少一个 pushover test() 使 IIFE 内 Promise.all 非空）；但**禁止**尝试重写 IIFE 为 describe/it 或移除 process.exit（那是单独的预存负债修复，超出本阶段范围）。

    4. installer/tests/run-all.js（Phase 17 runner）：
       - 当前 require 了 config-git-ssh-detect.js / config-git-ssh-guide.js / config-git-user-detect.js（Task 1 已删），会导致 require 抛错。
       - 处理方式（二选一，执行器择优）：
         (a) 推荐：裁剪 runner 为仅 pushover —— 移除 gitSSHDetect/gitSSHGuide/gitUser 的 require 与 runAllTests 中对应调用，仅保留 pushoverDetect/pushoverInput/pushoverSave 三项。更新标题注释为“Phase 17 Pushover Configuration Tests”。
         (b) 若该 runner 已无价值（pushover 有 jest 版 configurators/pushover.test.js 覆盖），直接 git rm installer/tests/run-all.js。
       - 执行器 Read config-pushover-*.js 确认它们仍存在且可独立运行后决定。若选 (a)，确保 `node installer/tests/run-all.js` 退出码 0；若选 (b)，确保无其他文件 require run-all.js（grep 确认）。
  </action>
  <verify>
    <automated>
    ! grep -q "git.js\|ssh-tools.js" installer/tests/detectors/index.test.js &&
    ! grep -q "toHaveLength(4)" installer/tests/detectors/index.test.js &&
    grep -q "toHaveLength(2)" installer/tests/detectors/index.test.js &&
    ! grep -q "marketplace" installer/tests/index.test.js &&
    ! grep -q "configurators/git-user.js\|configurators/git-ssh.js\|detectGitUser\|detectGitSSH" installer/tests/configurators/unified-flow.test.js &&
    cd installer && npx jest tests/detectors/index.test.js tests/index.test.js 2>&1 | tail -20
    </automated>
    注：unified-flow.test.js 因预存 process.exit IIFE（v3.0 前负债，out of scope）单独运行会失败，故不纳入本 verify 的 jest 命令；其裁剪正确性由上面的 grep（无 git-user/git-ssh require）保证，最终由 Task 4 baseline-comparison 闸门确认它“未引入新失败”（它在基线本就失败）。
  </verify>
  <done>
    - detectors/index.test.js 无 git.js/ssh-tools.js 引用，断言 results.length===2，3 个 runAllDetectors 用例 + 3 个 printResult 用例通过
    - index.test.js 无 marketplace mock/引用，4 个 main 用例通过
    - unified-flow.test.js 无 git-user/git-ssh require，仅 pushover 用例；其顶层 process.exit IIFE 未被修复（预存负债，out of scope），裁剪后该套件仍因预存 IIFE 失败属可接受（非新增失败，Task 4 baseline 闸门白名单）
    - run-all.js 或裁剪为仅 pushover（且 node tests/run-all.js 退出 0）或已删除
    - detectors/index.test.js、index.test.js 单独运行通过（unified-flow.test.js 因预存 IIFE 预期失败，不要求其单独通过）
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 uninstall 测试套件以适配 paths.js 迁移与 marketplace 类别移除 (INS-05)</name>
  <files>
    installer/tests/uninstall/detector.test.js (修改),
    installer/tests/uninstall/remover.test.js (修改),
    installer/tests/uninstall/formatter.test.js (修改),
    installer/tests/uninstall/index.test.js (修改)
  </files>
  <read_first>
    installer/tests/uninstall/detector.test.js （当前 mock ../../src/marketplace/plugin-installer.js 与 config-manager.js；断言 plugins 2 项含 windows-git-commit；断言 marketplaceSource 字段）,
    installer/tests/uninstall/remover.test.js （当前 mock ../../src/marketplace/config-manager.js 与 plugin-installer.js；makeAllInstalledResults 含 2 plugin + marketplaceSource；断言 9 个 removal 结果）,
    installer/tests/uninstall/formatter.test.js （i18n mock 含 uninstall.category.marketplace 等；断言渲染 marketplace 行）,
    installer/tests/uninstall/index.test.js （countTotal/countInstalled 间接通过 detectAllInstalled mock；i18n mock 含 uninstall.summary）,
    installer/src/uninstall/detector.js, remover.js, formatter.js, index.js, paths.js （01 计划裁剪后的新契约：无 marketplaceSource 字段、5 步移除、import ./paths.js、单 plugin claude-notify）
  </read_first>
  <action>
    1. installer/tests/uninstall/detector.test.js（适配 paths.js + 单 plugin + 无 marketplaceSource）：
       - 将 `jest.mock('../../src/marketplace/plugin-installer.js', ...)` 改为 `jest.mock('../../src/uninstall/paths.js', () => ({ isPluginInstalled: jest.fn(), getSkillsDir: jest.fn(), readClaudeConfig: jest.fn(), getConfigPath: jest.fn(), writeClaudeConfig: jest.fn() }))`。
       - 将 `jest.mock('../../src/marketplace/config-manager.js', ...)` 整块删除（readClaudeConfig/getConfigPath 已合并进 paths.js mock）。
       - 所有 require 从 marketplace 改指向 paths.js：`const { isPluginInstalled, getSkillsDir, readClaudeConfig, getConfigPath } = require('../../src/uninstall/paths.js');`
       - 移除对 marketplace 的 require 行。
       - hooks-installer 与 pushover 的 mock 保持不变。
       - 用例“plugins array has 2 entries”：改为“plugins array has 1 entry (claude-notify)”——`expect(result.plugins).toHaveLength(1)` 与 `expect(result.plugins[0]).toHaveProperty('name','claude-notify')`。删除对 windows-git-commit 的断言。
       - 用例“detects installed plugins”：`isPluginInstalled.mockImplementation(() => true)` 或 `(name) => name === 'claude-notify'`；断言 `result.plugins[0].installed === true` 与 `result.plugins[0].name === 'claude-notify'`。删除 plugins[1] 断言。
       - 用例“returns correct structure with all expected keys”：从断言集合中移除 `expect(result).toHaveProperty('marketplaceSource')`（保留 plugins/hooksScripts/hooksRegistered/commandsInstalled/envVars/hasAnyInstalled）。
       - 删除 3 个 marketplace source 用例：“detects marketplace source installed”、“detects marketplace source not installed”、“detects marketplace source missing when marketplaceSources key absent”。
       - 用例“calls the correct underlying detection functions”：移除 `expect(isPluginInstalled).toHaveBeenCalledWith('windows-git-commit')`（保留 toHaveBeenCalledWith('claude-notify')）；移除 `expect(readClaudeConfig).toHaveBeenCalled()` 断言（paths.js 的 readClaudeConfig 不再被 detector 调用，因为 marketplaceSource 检测已删）。
       - pushover env vars 用例与 hooks 用例保持不变。

    2. installer/tests/uninstall/remover.test.js（适配 5 步移除 + paths.js + 单 plugin + 无 marketplace cache/source）：
       - 将 `jest.mock('../../src/marketplace/config-manager.js', ...)` 与 `jest.mock('../../src/marketplace/plugin-installer.js', ...)` 合并为单一 `jest.mock('../../src/uninstall/paths.js', () => ({ readClaudeConfig: jest.fn(), writeClaudeConfig: jest.fn(), getSkillsDir: jest.fn(() => 'C:/Users/test/.claude/skills'), isPluginInstalled: jest.fn(), getConfigPath: jest.fn() }))`。
       - require 改指向 paths.js：`const { readClaudeConfig, writeClaudeConfig, getSkillsDir } = require('../../src/uninstall/paths.js');`
       - makeAllInstalledResults() 与 makeNothingInstalledResults()：
         - plugins 数组改为单元素 claude-notify（删除 windows-git-commit 项）。
         - 删除 `marketplaceSource: { installed: ... }` 字段。
         - 保留 hooksScripts/hooksRegistered/commandsInstalled/envVars(token,user)/hasAnyInstalled。
       - 用例“with all-installed ... returns results for each”：移除 marketplace cache/source 两步后，5 步移除 = 1 plugin + 1 hooksScripts + 1 hooksReg + 1 commands + 2 envVars(token,user) = 6 个结果（注意：plugins 遍历对单 plugin 产出 1 项）。把 `expect(removalResults.length).toBe(9)` 改为 `toBe(6)`，`removedCount` 期望相应改为 6。
       - 用例“with nothing-installed ... all-skipped”：`expect(removalResults.length).toBe(9)` 改为 `toBe(6)`，skippedCount 改为 6；移除对 readClaudeConfig 的“not to have been called”断言可保留（nothing-installed 时 marketplace 步已不存在，readClaudeConfig 自然不被调用，但 hooks 注册仍调 _readSettings）。
       - 用例“with partial installation”：重算 removed/skipped 数 —— 新样本仅含 claude-notify plugin + hooksScripts + token installed，移除 marketplaceSource 字段；期望 removedItems.length 与 skippedItems.length 按新 6 项重算（claude-notify + hooksScripts + token = 3 removed；hooksReg + commands + user = 3 skipped）。
       - 删除两个 marketplace 专用用例：“marketplace source removal calls readClaudeConfig ... writeClaudeConfig” 与 “marketplace cache removal deletes both cache/work-skills and marketplaces/work-skills directories”。（readClaudeConfig/writeClaudeConfig 现仅历史调用， remover 不再调用，故删这两个用例。）
       - 用例“plugin directory removal calls fs.rmSync with recursive:true, force:true for each installed plugin”：pluginCalls 过滤条件移除 `path.includes('windows-git-commit')`，仅保留 `path.includes('claude-notify')`；`expect(pluginCalls.length).toBe(2)` 改为 `toBe(1)`。
       - 用例“when hook script removal fails”、“when registry deletion fails”、“hook registration removal calls _readSettings...”、“slash command removal deletes all 4 command .md files”、“env var removal calls execa with correct reg delete arguments”：这些用例不依赖 marketplace，但需移除其中 makeAllInstalledResults 引发的 marketplace 步计数影响 —— 由于它们只断言特定步骤的行为（用 toHaveBeenCalledWith 或 find status==='failed'），重算后仍成立，仅需确保 readClaudeConfig mock 仍设置（部分用例 setup 中有 readClaudeConfig.mockReturnValue）可保留无害。
       - 整体：搜索 remover.test.js 中所有 `toBe(9)` 改为 `toBe(6)`，所有 windows-git-commit 引用删除，marketplaceSource 字段删除。

    3. installer/tests/uninstall/formatter.test.js（命令式移除 marketplace 类别断言，MEDIUM-3）：
       - **命令式删除** formatter.test.js:120 的断言行：`expect(t).toHaveBeenCalledWith('uninstall.category.marketplace');`（位于 "includes all category headers" 用例内，约 line 120）。该断言一旦 01 计划 Task 2 从 formatter.js 移除 marketplace 渲染块（formatter.js:58）必失败，必须删除，非可选。
       - 同一 "includes all category headers" 用例保留对其余 5 个类别头的断言：`uninstall.category.plugins`、`uninstall.category.hooks`、`uninstall.category.hookRegistration`、`uninstall.category.commands`、`uninstall.category.environment`。
       - i18n mock 的 translations 中**删除** `'uninstall.category.marketplace': 'Marketplace Source'` 与 `'uninstall.item.marketplaceSource': 'Marketplace Source'` 两键（与 en/zh.json 删除同步，保持 mock 与真实 i18n 键集一致；非可选）。
       - makeAllNotInstalledResults() 与 makeSomeInstalledResults() fixture：删除 `marketplaceSource: { installed: ..., path: ... }` 字段；plugins 数组从 2 项（claude-notify + windows-git-commit）降为 1 项（仅 claude-notify）。
       - 保留 plugins/hooks/hookRegistration/commands/environment 类别的渲染断言（含 [GREEN:]/[GRAY:] 图标、路径详情、nothingFound、separator、status 文本等用例，这些不依赖 marketplace）。
       - 删除后用例数：原 7 个 test()，断言行删除后仍为 7 个（仅删一行 expect，不删整个 test）；fixture 字段删除不影响用例计数。

    4. installer/tests/uninstall/index.test.js（适配 countTotal 新公式 + 显式 fixture 重算，MEDIUM-2）：
       - 该文件通过 jest.mock('../../src/uninstall/detector.js') 注入 detectAllInstalled，故 countTotal/countInstalled 通过 mock 控制的 fixture 计算（fixture-driven，非真实 detector 驱动）。
       - **fixture 修改（两处 helper + 一处内联 partial 样本）**：
         - makeAllInstalledResults()（约 lines 54-69）：plugins 数组从 2 项降为 1 项（删除 `{ name: 'windows-git-commit', ... }` 项，仅保留 claude-notify）；**删除** `marketplaceSource: { installed: true, ... }` 字段。
         - makeNothingInstalledResults()（约 lines 72-88）：plugins 数组降为 1 项（删除 windows-git-commit 项）；**删除** `marketplaceSource: { installed: false, ... }` 字段。
         - "counts partial installation correctly" 用例的内联 results（约 lines 161-175）：plugins 数组降为 1 项（删除 windows-git-commit 项）；**删除** `marketplaceSource` 字段。
       - **三对 `{found,total}` 断言显式重算表**（countTotal = plugins.length + 5；countInstalled 统计 installed===true 的项数，不含 marketplaceSource）：

         | 用例 | fixture plugins | installed 项 | found | total | 断言行 |
         |------|-----------------|-------------|-------|-------|--------|
         | "prints summary with correct installed count"（makeAllInstalledResults） | 1 (claude-notify) | claude-notify + hooksScripts + hooksRegistered + commands + token + user = 6 | **6** | **6** | `expect(t).toHaveBeenCalledWith('uninstall.summary', { found: 6, total: 6 });` （原 line 146 `{found:8,total:8}`） |
         | "handles nothing installed without error"（makeNothingInstalledResults） | 1 (claude-notify, installed:false) | 0 | **0** | **6** | `expect(t).toHaveBeenCalledWith('uninstall.summary', { found: 0, total: 6 });` （原 line 157 `{found:0,total:8}`） |
         | "counts partial installation correctly"（内联：claude-notify installed + hooksScripts installed + token installed） | 1 (claude-notify, installed:true) | claude-notify + hooksScripts + token = 3 | **3** | **6** | `expect(t).toHaveBeenCalledWith('uninstall.summary', { found: 3, total: 6 });` （原 line 183 `{found:3,total:8}`） |

       - 更新 "prints summary with correct installed count" 用例上方注释（line 145）：从 `// All 8 items installed (2 plugins + 1 hooks + 1 hookReg + 1 commands + 1 marketplace + 2 envVars)` 改为 `// 6 items installed (1 plugin + 1 hooks + 1 hookReg + 1 commands + 2 envVars)`。
       - "returns correct result for successful uninstall" 用例（约 lines 298-316）的 removalResults 样本含两行 Plugins（claude-notify + windows-git-commit，lines 301-302）：降为 1 行（仅 claude-notify）。
       - i18n mock（lines 25-44）的 translations **不含** uninstall.category.marketplace（原 mock 也未含，无需改动），保持不变。
       - 核心：runUninstall 流程（detect → format → nothing-installed 分支 → confirm → remove → report）用例不直接断言 marketplace，仅需上述计数与 fixture 重算。
  </action>
  <verify>
    <automated>
    ! grep -rn "marketplace" installer/tests/uninstall/ &&
    ! grep -q "windows-git-commit" installer/tests/uninstall/ &&
    ! grep -q "src/marketplace" installer/tests/uninstall/ &&
    grep -q "src/uninstall/paths.js" installer/tests/uninstall/detector.test.js &&
    grep -q "src/uninstall/paths.js" installer/tests/uninstall/remover.test.js &&
    ! grep -q "toHaveLength(2)" installer/tests/uninstall/detector.test.js &&
    cd installer && npx jest tests/uninstall/ 2>&1 | tail -25
    </automated>
  </verify>
  <done>
    - uninstall/ 下 4 个测试文件无 marketplace 字样、无 windows-git-commit、无 src/marketplace require
    - detector.test.js 与 remover.test.js 改 mock src/uninstall/paths.js
    - detector.test.js 断言 plugins.length===1（claude-notify），无 marketplaceSource 用例
    - remover.test.js 断言 6 个移除结果（5 步 + 单 plugin），无 marketplace cache/source 用例
    - formatter.test.js 已命令式删除 line 120 的 `toHaveBeenCalledWith('uninstall.category.marketplace')` 断言，i18n mock 删除 marketplace 键，fixture 降为单 plugin + 无 marketplaceSource
    - index.test.js 三对 {found,total} 已重算为 {6,6}/{0,6}/{3,6}，fixture（两 helper + 内联 partial）降为单 plugin + 无 marketplaceSource
    - cd installer && npx jest tests/uninstall/ 全绿
  </done>
</task>

<task type="auto">
  <name>Task 4: Baseline-comparison 验证闸门 —— 确认 Phase 54 零新增失败 (INS-05 最终验证)</name>
  <files>
    .planning/phases/54-trim-installer-notify-only/54-02-baseline.txt （新建：捕获基线 npm test 输出，供子集比较）,
    .planning/phases/54-trim-installer-notify-only/54-02-posttrim.txt （新建：捕获 post-trim npm test 输出）
  </files>
  <read_first>
    installer/package.json （scripts.test = "node --experimental-vm-modules node_modules/jest/bin/jest.js"）,
    .planning/REQUIREMENTS.md （INS-05 已 RESCOPE 为“不引入新失败”；Out of Scope 表列出 4 个预存失败套件）,
    .planning/phases/54-trim-installer-notify-only/54-REVIEWS.md （HIGH-1/HIGH-2 共识：基线 RED，10 failed suites / 8 failed tests，均为预存负债）,
    确认 Task 1/2/3 已完成
  </read_first>
  <action>
    ★ INS-05 验收门 RESCOPE 说明（依据 REQUIREMENTS.md INS-05 + Out of Scope 表 + codex/opencode convergence HIGH-1/HIGH-2 共识 + 用户决策）★
    原 `npm test exits 0` / “全部通过” 闸门在当前条件下**不可达成**：installer 测试树基线为 RED（10 failed suites / 8 failed tests），其中 4 个失败套件是 v3.0 前预存负债，与本里程碑裁剪无关，经用户确认 out of scope。故验收门改为 **baseline-comparison（零新增失败）**，而非全绿。

    **预存失败白名单（4 个套件，out of scope，本阶段不修）**：
    1. `tests/bin.test.js` —— CRLF 行尾致 shebang `\r\n` ≠ `#!/usr/bin/env node`（环境/编码问题）
    2. `tests/verification/runner.test.js` —— python 脚本路径 `script_not_found`（环境问题，4 个失败测试）
    3. `tests/configurators/pushover.test.js` —— KEPT 文件，顶层 IIFE `process.exit(0)`（约 lines 172-188），Jest 30 拦截
    4. `tests/configurators/unified-flow.test.js` —— 本阶段裁剪其 git 用例，但其顶层 IIFE `process.exit(0/1)`（约 lines 395-410）是预存负债，不修；裁剪后预期仍因同一 IIFE 失败（可接受，非新增）

    **执行步骤**：

    1. **记录基线（BEFORE changes 视角，但本 Task 在裁剪后执行，故基线已在 54-REVIEWS.md 实跑确认）**：
       基线实跑结果（54-REVIEWS.md cycle 1 已确认，本阶段以此为基准）：
       `Test Suites: 10 failed, 20 passed, 30 total` / `Tests: 8 failed, 155 passed, 163 total`
       基线失败套件名单（10 个）：
       - tests/bin.test.js（预存，白名单）
       - tests/verification/runner.test.js（预存，白名单）
       - tests/marketplace/config-manager.test.js（本阶段 Task 1 删除）
       - tests/marketplace/plugin-discovery.test.js（本阶段 Task 1 删除）
       - tests/marketplace/plugin-installer.test.js（本阶段 Task 1 删除）
       - tests/configurators/git-user.test.js（本阶段 Task 1 删除）
       - tests/configurators/git-ssh.test.js（本阶段 Task 1 删除）
       - tests/uninstall/detector.test.js（本阶段 Task 3 修复 → 转绿）
       - tests/configurators/pushover.test.js（预存，白名单）
       - tests/configurators/unified-flow.test.js（预存，白名单；本阶段裁剪 git 用例但不修 IIFE）
       将此名单写入 .planning/phases/54-trim-installer-notify-only/54-02-baseline.txt（含 "10 failed / 20 passed" 摘要行 + 10 个失败套件名）。

    2. **运行 post-trim 全量 jest**：`cd installer && npm test 2>&1 | tee ../.planning/phases/54-trim-installer-notify-only/54-02-posttrim.txt | tail -30`。将完整输出存入 posttrim.txt。

    3. **闸门判定（全部三项须满足）**：
       (a) **文件删除验证**：本阶段删除的测试文件均不存在 ——
           `test ! -f installer/tests/detectors/git.test.js && test ! -f installer/tests/detectors/ssh-tools.test.js && test ! -f installer/tests/config-git-ssh-detect.js && test ! -f installer/tests/config-git-ssh-guide.js && test ! -f installer/tests/config-git-user-detect.js && test ! -f installer/tests/configurators/git-ssh.test.js && test ! -f installer/tests/configurators/git-user.test.js && test ! -f installer/tests/marketplace/config-manager.test.js && test ! -f installer/tests/marketplace/plugin-discovery.test.js && test ! -f installer/tests/marketplace/plugin-installer.test.js && test ! -d installer/tests/marketplace`
       (b) **零新增失败**：post-trim 的失败套件集合 ⊆ 基线失败套件集合。即 post-trim 失败套件名单中的每一项都必须出现在基线 10 个失败套件名单中。预期 post-trim 失败套件 = 基线 10 个减去本阶段删除/修复的 6 个 = 剩余 4 个（bin.test.js、verification/runner.test.js、pushover.test.js、unified-flow.test.js），全部在白名单内。**任何不在基线名单中的新失败套件 → 闸门失败**，须回到对应 Task 修复后重跑。
       (c) **已删失败套件确实消失**：基线中因本阶段删除而消失的失败套件（marketplace ×3、git-ssh、git-user 共 5 个）与因 Task 3 修复而转绿的（detector.test.js）在 post-trim 中不再为失败（减少失败计数是允许且有益的）。

    4. **主入口加载验证**：`node -e "require('./installer/src/index.js')"` 从仓库根运行，期望退出码 0。

    5. **残留引用扫描（防漏）**：
       - `grep -rn "require.*marketplace" installer/src installer/tests` —— 应无命中（marketplace 已全删）。
       - `grep -rn "detectGit\b\|detectSSHTools\|configureGitSSH\|configureGitUser\|detectGitUser\|detectGitSSH" installer/src installer/tests` —— 应无命中（已删模块符号）。
       - `grep -rn "windows-git-commit\|codepoint" installer/src installer/tests` —— 应无命中（Phase 53 已删技能，installer 不应残留）。

    6. 若任一扫描有命中或闸门 (b) 出现新失败，回到对应 Task 修复后重跑步骤 2-3。**禁止**为了通过闸门而修复白名单中的 4 个预存失败（那是 out of scope，属未来单独的预存负债清理工作）。
  </action>
  <verify>
    <automated>
    test -f .planning/phases/54-trim-installer-notify-only/54-02-baseline.txt &&
    test -f .planning/phases/54-trim-installer-notify-only/54-02-posttrim.txt &&
    test ! -f installer/tests/marketplace/plugin-installer.test.js &&
    test ! -d installer/tests/marketplace &&
    node -e "require('./installer/src/index.js')" &&
    ! grep -rn "require.*marketplace" installer/src installer/tests 2>/dev/null &&
    ! grep -rnE "detectGit\(|detectSSHTools|configureGitSSH|configureGitUser|detectGitUser|detectGitSSH" installer/src installer/tests 2>/dev/null &&
    echo "Baseline-comparison gate: see 54-02-baseline.txt vs 54-02-posttrim.txt; post-trim failing suites must be a subset of baseline (10 failed). Whitelist of 4 pre-existing: bin.test.js / verification/runner.test.js / configurators/pushover.test.js / configurators/unified-flow.test.js"
    </automated>
    注：闸门 (b) 的子集判定需执行器人工或脚本对比 baseline.txt 与 posttrim.txt 的失败套件名单（jest 输出 "FAIL src/..." 行），确认无新增。自动化 grep 无法完全表达子集语义，故执行器须在 SUMMARY 中明确记录 "post-trim 失败套件 X 个，全部在基线白名单内，零新增失败"。
  </verify>
  <done>
    - .planning/phases/54-trim-installer-notify-only/54-02-baseline.txt 存在（含 10 failed / 20 passed 摘要 + 10 个失败套件名）
    - .planning/phases/54-trim-installer-notify-only/54-02-posttrim.txt 存在（post-trim 全量 npm test 输出）
    - 本阶段删除的 10 个测试文件均不存在，installer/tests/marketplace/ 目录消失
    - post-trim 失败套件集合 ⊆ 基线失败套件集合（零新增失败）；预期 post-trim 失败 = 4 个白名单套件（bin.test.js / verification/runner.test.js / pushover.test.js / unified-flow.test.js）
    - node -e "require('./installer/src/index.js')" 退出码 0
    - installer/src 与 installer/tests 下无 marketplace require、无已删模块符号、无 windows-git-commit/codepoint 残留
    - Phase 54 SC5（RESCOPED：不引入新失败）满足
  </done>
</task>

</tasks>

<verification>
1. `cd installer && npm test` post-trim 失败套件 ⊆ 基线失败套件（零新增失败）—— SC5 最终验证（RESCOPED：不要求全绿，要求不引入新失败）。对比 54-02-baseline.txt（10 failed）与 54-02-posttrim.txt。
2. `node -e "require('./installer/src/index.js')"` 退出码 0 —— 主入口加载。
3. 已删模块测试文件全部不存在（Task 1 验收）。
4. 保留测试文件已更新（detectors/index.test.js 断言 2 项；index.test.js 无 marketplace；unified-flow.test.js 仅 pushover 且其 IIFE 预存失败不修；uninstall/*.test.js 适配 paths.js + 单 plugin + 无 marketplaceSource；formatter.test.js 命令式删 line 120 marketplace 断言；index.test.js 三对 {found,total} 重算为 {6,6}/{0,6}/{3,6}）。
5. 残留引用扫描全清。
6. 4 个预存失败套件（bin.test.js CRLF / verification/runner.test.js python / pushover.test.js process.exit / unified-flow.test.js process.exit）显式标记为 out-of-scope 预存负债，不在本阶段修复（见 REQUIREMENTS.md Out of Scope 表）。
</verification>

<success_criteria>
- SC5（剩余测试不引入新失败，RESCOPED）：已移除模块（git/ssh 检测、git 配置、marketplace）的测试文件已删除；Phase 54 不引入新的 jest 失败（post-trim 失败套件 ⊆ 基线 10 个失败套件）。基线 4 个预存失败（bin.test.js / verification/runner.test.js / pushover.test.js / unified-flow.test.js）为 v3.0 前负债，out of scope，本阶段不修。✅ 由 Task 1+2+3+4 满足（baseline-comparison 闸门）。
- 配合 01 计划：SC1-SC4 由 01 计划满足源码侧，本计划使测试侧与源码一致且零新增失败。
</success_criteria>

<artifacts>
## Artifacts this phase produces

### 新增符号（New）
无新文件（本计划仅删除/修改测试文件）。

### 修改的测试契约（Changed）
- `tests/detectors/index.test.js`：mock 从 4 detector → 2 detector，断言 results.length 4→2。
- `tests/index.test.js`：移除 marketplace mock（无 src/marketplace require）。
- `tests/configurators/unified-flow.test.js`：从 14 用例裁剪为约 10 用例（删除 git-user/git-ssh 相关 4 用例与对应 cache helper）。★ 其顶层 process.exit IIFE 是 v3.0 前预存负债，本阶段不修，裁剪后预期仍因预存 IIFE 失败（非新增）★。
- `tests/uninstall/detector.test.js`：mock 从 marketplace/* → uninstall/paths.js；plugins 2→1；删除 marketplaceSource 相关 3 用例 + 结构断言。
- `tests/uninstall/remover.test.js`：mock 从 marketplace/* → uninstall/paths.js；移除结果计数 9→6；删除 marketplace cache/source 2 用例；plugins 2→1。
- `tests/uninstall/formatter.test.js`：命令式删除 line 120 的 `toHaveBeenCalledWith('uninstall.category.marketplace')` 断言；i18n mock 删除 marketplace 键；fixture 降为单 plugin + 无 marketplaceSource。
- `tests/uninstall/index.test.js`：fixture（两 helper + 内联 partial）降为单 plugin + 无 marketplaceSource；三对 `{found,total}` 显式重算为 `{6,6}`/`{0,6}`/`{3,6}`（MEDIUM-2）。
- `tests/run-all.js`：裁剪为仅 pushover runner 或删除。

### 新增的验证工件（New — 非测试代码）
- `.planning/phases/54-trim-installer-notify-only/54-02-baseline.txt`：基线 npm test 输出（10 failed suites / 8 failed tests + 失败套件名单），用于 post-trim 子集比较。
- `.planning/phases/54-trim-installer-notify-only/54-02-posttrim.txt`：post-trim npm test 全量输出，用于与基线对比判定零新增失败。

### 删除的测试文件（Removed）
- installer/tests/detectors/git.test.js
- installer/tests/detectors/ssh-tools.test.js
- installer/tests/config-git-ssh-detect.js
- installer/tests/config-git-ssh-guide.js
- installer/tests/config-git-user-detect.js
- installer/tests/configurators/git-ssh.test.js
- installer/tests/configurators/git-user.test.js
- installer/tests/marketplace/config-manager.test.js
- installer/tests/marketplace/plugin-discovery.test.js
- installer/tests/marketplace/plugin-installer.test.js
- installer/tests/marketplace/（空目录）
- （可能）installer/tests/run-all.js（若执行器选择删除而非裁剪）
</artifacts>

<output>
完成时创建 `.planning/phases/54-trim-installer-notify-only/54-02-SUMMARY.md`
</output>
