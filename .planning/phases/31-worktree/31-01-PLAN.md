---
phase: 31-worktree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/claude-notify/hooks/scripts/flags.py
  - plugins/claude-notify/tests/test_flags.py
autonomous: true
requirements:
  - WTREE-01

must_haves:
  truths:
    - "get_git_branch() 在 git 仓库中返回当前分支名"
    - "get_git_branch() 在非 git 目录返回 None"
    - "get_git_branch() 在 DETACHED HEAD 状态返回 None"
    - "get_git_branch() 在 git 不在 PATH 时返回 None (FileNotFoundError)"
    - "build_notification_title('proj', 'main') 返回 '[proj:main]'"
    - "build_notification_title('proj', None) 返回 '[proj]'"
    - "build_notification_title('proj', 'main', suffix='Attention Needed') 返回 '[proj:main] Attention Needed'"
    - "build_notification_title('proj', None, suffix='Attention Needed') 返回 '[proj] Attention Needed'"
    - "find_project_root() 在 git worktree 中正确识别项目根 (.git 是文件不是目录)"
  artifacts:
    - path: "plugins/claude-notify/hooks/scripts/flags.py"
      provides: "get_git_branch(), build_notification_title(), fixed find_project_root()"
      contains: "def get_git_branch"
      contains: "def build_notification_title"
    - path: "plugins/claude-notify/tests/test_flags.py"
      provides: "TestGetGitBranch, TestBuildNotificationTitle, TestFindProjectRootWorktree"
      contains: "class TestGetGitBranch"
      contains: "class TestBuildNotificationTitle"
  key_links:
    - from: "flags.py"
      to: "subprocess.run"
      via: "git branch --show-current call"
      pattern: "git.*branch.*--show-current"
    - from: "build_notification_title"
      to: "get_git_branch"
      via: "lazy call inside function body"
      pattern: "get_git_branch\\(\\)"
---

<objective>
为 flags.py 新增 git 分支检测函数 get_git_branch()、共享标题构建函数 build_notification_title(),并修复 find_project_root() 在 git worktree 场景下的兼容性问题。

Purpose: WTREE-01 要求通知标题包含 git 分支名,需要可靠的分支检测函数和统一的标题构建逻辑。将标题构建集中在 flags.py 中 (而非分散在 notify.py 和 notify-attention.py 中) 消除 DRY 违反 (review concern HIGH)。同时修复 find_project_root() 的 pre-existing bug (worktree 中 .git 是文件导致检测失败),确保分支检测和项目名识别在 worktree 场景下都正确。

Output: flags.py 新增 get_git_branch()、build_notification_title() 并修复 find_project_root(); test_flags.py 新增完整测试覆盖。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-worktree/31-RESEARCH.md

@plugins/claude-notify/hooks/scripts/flags.py
@plugins/claude-notify/tests/test_flags.py
</context>

<interfaces>
<!-- flags.py exports that downstream (Plan 02) will consume -->

From plugins/claude-notify/hooks/scripts/flags.py (AFTER this plan completes):
```python
# Existing exports (unchanged):
def check_notification_flags() -> Dict: ...
def find_project_root() -> Optional[Path]: ...  # MODIFIED: .is_dir() -> .exists()
def get_project_name() -> str: ...

# NEW exports:
def get_git_branch() -> Optional[str]:
    """
    Get current git branch name via 'git branch --show-current'.
    Returns branch name string, or None if not in git repo / DETACHED HEAD / error.
    """
    ...

def build_notification_title(project_name: str, git_branch: Optional[str] = None, suffix: Optional[str] = None) -> str:
    """
    Build notification title with optional git branch and suffix.

    Args:
        project_name: Project name (from get_project_name())
        git_branch: Git branch name (from get_git_branch()), None for no branch
        suffix: Optional suffix like "Attention Needed"

    Returns:
        str: "[project:branch] suffix" or "[project] suffix"
    """
    ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_git_branch(), build_notification_title(), and fix find_project_root() with tests</name>
  <files>plugins/claude-notify/hooks/scripts/flags.py, plugins/claude-notify/tests/test_flags.py</files>
  <read_first>
    - plugins/claude-notify/hooks/scripts/flags.py (entire file - target of modifications, note line 151 has the .is_dir() bug, note there is NO subprocess import yet)
    - plugins/claude-notify/tests/test_flags.py (entire file - existing test patterns to follow, especially mock patterns with __truediv__)
    - .planning/phases/31-worktree/31-RESEARCH.md (Code Examples section - implementation reference)
  </read_first>
  <behavior>
    <!-- get_git_branch tests -->
    - Test 1: get_git_branch() 在 git 返回分支名时 (subprocess.run returncode=0, stdout="feature-branch\n") 返回 "feature-branch"
    - Test 2: get_git_branch() 在非 git 目录 (returncode=128) 返回 None
    - Test 3: get_git_branch() 在 DETACHED HEAD (returncode=0, stdout="\n" 空白) 返回 None
    - Test 4: get_git_branch() 在 git 命令不存在 (FileNotFoundError) 返回 None (Addresses review concern: MEDIUM FileNotFoundError)
    - Test 5: get_git_branch() 在超时 (TimeoutExpired) 返回 None
    - Test 6: get_git_branch() 在分支名含特殊字符 (stdout="feature/JIRA-123:fix\n") 返回 "feature/JIRA-123:fix" (Addresses review concern: special chars)
    <!-- build_notification_title tests -->
    - Test 7: build_notification_title("proj", "main") 返回 "[proj:main]"
    - Test 8: build_notification_title("proj", None) 返回 "[proj]"
    - Test 9: build_notification_title("proj", "") 返回 "[proj]" (空字符串视为无分支)
    - Test 10: build_notification_title("proj", "main", suffix="Attention Needed") 返回 "[proj:main] Attention Needed"
    - Test 11: build_notification_title("proj", None, suffix="Attention Needed") 返回 "[proj] Attention Needed"
    - Test 12: build_notification_title("proj", "main", suffix=None) 返回 "[proj:main]" (无 suffix 时不加后缀)
    <!-- find_project_root worktree test -->
    - Test 13: find_project_root() 在 worktree 场景 (.git 是文件 exists=True, is_dir=False) 正确返回当前目录 (per D-05)
  </behavior>
  <acceptance_criteria>
    - flags.py 包含 `import subprocess` 在文件顶部
    - flags.py 包含 `def get_git_branch()` 函数定义
    - flags.py 中 get_git_branch 函数体包含 `'git', 'branch', '--show-current'` 参数
    - flags.py 中 get_git_branch 函数体包含 `timeout=1`
    - flags.py 中 get_git_branch 函数体包含 `encoding='utf-8'` (Addresses review concern: MEDIUM encoding)
    - flags.py 中 get_git_branch 函数体包含 `stderr=subprocess.DEVNULL` (Addresses review suggestion)
    - flags.py 中 get_git_branch 函数体包含 `except FileNotFoundError` (Addresses review concern: MEDIUM FileNotFoundError)
    - flags.py 包含 `def build_notification_title(project_name, git_branch=None, suffix=None)` 函数定义
    - flags.py 中 build_notification_title 函数体包含 `if git_branch:` 条件判断
    - flags.py 的 find_project_root() 中原来的 `(current / '.git').is_dir()` 已改为 `(current / '.git').exists()` (per D-05)
    - test_flags.py 包含 `class TestGetGitBranch` 测试类
    - test_flags.py 包含 `class TestBuildNotificationTitle` 测试类
    - test_flags.py 包含 `class TestFindProjectRootWorktree` 测试类
    - test_flags.py 中 TestGetGitBranch 包含 test_branch_with_special_characters 测试方法
    - `python -m pytest plugins/claude-notify/tests/test_flags.py -v` 全部通过
    - `python -m pytest plugins/claude-notify/tests/ -v` 全部通过 (85 existing + new tests)
  </acceptance_criteria>
  <action>
    **RED phase - Write tests first:**

    在 test_flags.py 末尾 (if __name__ 之前) 新增三个测试类:

    1. `class TestGetGitBranch(unittest.TestCase)`:
       - 遵循现有测试文件中 `@patch('subprocess.run')` 模式 (参考现有 test_notify.py 第 46 行)
       - `test_returns_branch_name`: mock subprocess.run 返回 returncode=0, stdout="feature-branch\n" -> 断言返回 "feature-branch"
       - `test_returns_none_not_git_repo`: mock returncode=128, stdout="" -> 断言返回 None
       - `test_returns_none_detached_head`: mock returncode=0, stdout="\n" (空白) -> 断言返回 None
       - `test_returns_none_git_not_found`: mock side_effect=FileNotFoundError -> 断言返回 None
       - `test_returns_none_timeout`: mock side_effect=subprocess.TimeoutExpired(cmd='git', timeout=1) -> 断言返回 None
       - `test_branch_with_special_characters`: mock returncode=0, stdout="feature/JIRA-123:fix\n" -> 断言返回 "feature/JIRA-123:fix" (Addresses review concern: special chars in branch names)

       注意: `get_git_branch` 在 flags.py 中使用 `subprocess.run`,所以 patch 路径是 `flags.subprocess.run`。

    2. `class TestBuildNotificationTitle(unittest.TestCase)`:
       - `test_with_branch`: build_notification_title("my-project", "feature-x") == "[my-project:feature-x]"
       - `test_without_branch`: build_notification_title("my-project", None) == "[my-project]"
       - `test_empty_branch`: build_notification_title("my-project", "") == "[my-project]"
       - `test_with_branch_and_suffix`: build_notification_title("my-project", "feature-x", suffix="Attention Needed") == "[my-project:feature-x] Attention Needed"
       - `test_without_branch_with_suffix`: build_notification_title("my-project", None, suffix="Attention Needed") == "[my-project] Attention Needed"
       - `test_with_branch_no_suffix`: build_notification_title("my-project", "main", suffix=None) == "[my-project:main]"
       - `test_special_char_branch_in_title`: build_notification_title("proj", "feature/JIRA-123:fix") == "[proj:feature/JIRA-123:fix]"

    3. `class TestFindProjectRootWorktree(unittest.TestCase)`:
       - `test_worktree_git_file_not_dir`: mock Path.cwd() 返回的目录中 `.git` mock 对象 is_file()=True, is_dir()=False。修改后 find_project_root 使用 `.exists()` 应识别并返回 CWD。
       - 构造方式参考现有 TestFindProjectRoot 中的 mock 模式 (test_flags.py 第 747 行起),但 `.git` mock 对象需要设置 exists.return_value=True, is_dir.return_value=False, is_file.return_value=True。

    **GREEN phase - Implement:**

    修改 flags.py:

    1. **添加 import** (文件顶部,约第 17 行后):
       ```python
       import subprocess
       ```

    2. **修复 find_project_root()** (约第 151 行):
       将:
       ```python
       if (current / '.git').is_dir():
       ```
       改为:
       ```python
       if (current / '.git').exists():
       ```
       这样 worktree 中 `.git` 是文件时也能正确识别项目根 (per D-05)。同时更新该函数的 docstring,将 "Looks for .git directory" 改为 "Looks for .git (directory or file for worktree support)"。

    3. **新增 get_git_branch() 函数**:
       在 get_project_name() 之后添加。实现:

       ```python
       def get_git_branch() -> Optional[str]:
           """
           Get current git branch name.

           Uses 'git branch --show-current' which correctly handles:
           - Normal branches: returns branch name
           - DETACHED HEAD: returns empty string (exit 0)
           - Not a git repo: exit code 128
           - Worktrees: returns the worktree's checked-out branch

           Returns:
               Optional[str]: Branch name or None if not available
           """
           try:
               result = subprocess.run(
                   ['git', 'branch', '--show-current'],
                   capture_output=True,
                   text=True,
                   timeout=1,
                   encoding='utf-8',
                   stderr=subprocess.DEVNULL
               )
               if result.returncode == 0 and result.stdout.strip():
                   branch = result.stdout.strip()
                   logger.info(f"Detected git branch: {branch}")
                   return branch
               return None
           except subprocess.TimeoutExpired:
               logger.warning("Git branch detection timeout (1s)")
               return None
           except FileNotFoundError:
               logger.warning("Git not found in PATH")
               return None
           except Exception as e:
               logger.error(f"Failed to detect git branch: {e}")
               return None
       ```

       关键实现细节 (addresses review concerns):
       - `encoding='utf-8'`: 显式设置编码,避免 Windows 默认编码问题 (review MEDIUM)
       - `stderr=subprocess.DEVNULL`: 抑制 git 警告输出,保持 stderr 干净 (review suggestion)
       - `except FileNotFoundError`: 捕获 git 不在 PATH 的情况 (review MEDIUM)
       - `text=True`: 确保返回字符串而非 bytes

    4. **新增 build_notification_title() 函数** (addresses review HIGH: DRY consolidation):
       在 get_git_branch() 之后添加:

       ```python
       def build_notification_title(project_name: str, git_branch: Optional[str] = None, suffix: Optional[str] = None) -> str:
           """
           Build notification title with optional git branch and suffix.

           Consolidates title formatting for both stop and attention hooks
           to avoid duplication (per WTREE-01, D-01, D-02).

           Args:
               project_name: Project name (from get_project_name())
               git_branch: Git branch name (from get_git_branch()), None for no branch
               suffix: Optional suffix like "Attention Needed"

           Returns:
               str: "[project:branch] suffix" or "[project] suffix" or "[project:branch]" or "[project]"
           """
           if git_branch:
               title = f"[{project_name}:{git_branch}]"
           else:
               title = f"[{project_name}]"

           if suffix:
               title = f"{title} {suffix}"

           return title
       ```

    **Run tests to verify all pass:**
    ```bash
    python -m pytest plugins/claude-notify/tests/ -v
    ```
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && python -m pytest plugins/claude-notify/tests/test_flags.py::TestGetGitBranch -v && python -m pytest plugins/claude-notify/tests/test_flags.py::TestBuildNotificationTitle -v && python -m pytest plugins/claude-notify/tests/test_flags.py::TestFindProjectRootWorktree -v && python -m pytest plugins/claude-notify/tests/ -v</automated>
  </verify>
  <done>flags.py 包含 get_git_branch() 函数 (使用 git branch --show-current, 1s timeout, encoding='utf-8', stderr=DEVNULL, FileNotFoundError 处理); build_notification_title() 共享标题构建函数 (消除 DRY 违反); find_project_root() 使用 .exists() 替代 .is_dir() 支持 worktree; test_flags.py 新增 TestGetGitBranch、TestBuildNotificationTitle、TestFindProjectRootWorktree 测试类; 全部 85+ 测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess -> git CLI | flags.py 调用外部 git 命令获取分支名 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | get_git_branch() subprocess call | mitigate | subprocess.run 使用列表参数 (非 shell=True),分支名仅用于字符串拼接到通知标题,不传递给任何 shell 命令 |
| T-31-02 | Denial of Service | get_git_branch() timeout | mitigate | 设置 1s timeout,超时返回 None 退化为 [project] 格式,不影响通知发送 |
| T-31-03 | Information Disclosure | Branch name in notification title | accept | git 分支名是项目公开信息,不包含敏感数据;通知仅发送到用户自己的设备 |
| T-31-04 | Tampering | Special chars in branch name | accept | 分支名用于 f-string 拼接到 title 参数中,不传递给 shell 命令或 XML 解析层;notify-attention.py 的 send_windows_notification 已使用 xml.sax.saxutils.escape() 防 XML 注入 |

</threat_model>

<verification>
1. 全部测试通过: `python -m pytest plugins/claude-notify/tests/ -v`
2. flags.py 包含 `def get_git_branch` 和 `import subprocess`
3. flags.py 包含 `def build_notification_title(project_name, git_branch=None, suffix=None)`
4. get_git_branch() 包含 `encoding='utf-8'` 和 `stderr=subprocess.DEVNULL`
5. get_git_branch() 包含 `except FileNotFoundError`
6. find_project_root() 使用 `.exists()` 而非 `.is_dir()`
</verification>

<success_criteria>
- get_git_branch() 在 git 仓库返回正确分支名,非 git/DETACHED HEAD/error 返回 None
- get_git_branch() 正确处理 FileNotFoundError (git 不在 PATH)、编码 (utf-8)、特殊字符分支名
- build_notification_title() 统一标题格式: [project:branch] suffix / [project] suffix
- find_project_root() 在 worktree 场景 (.git 为文件) 正确识别项目根
- 全部 85+ 测试通过 (含新增测试)
</success_criteria>

<output>
After completion, create `.planning/phases/31-worktree/31-01-SUMMARY.md`
</output>
