---
phase: 37-fullstack-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37-fullstack-fix/37-DEFECTS.yaml
autonomous: true
requirements: [FIX2-01]

must_haves:
  truths:
    - "37-DEFECTS.yaml exists with all defects from Phase 35 and Phase 36 fullstack testing"
    - "Each defect has id, category, severity, description, steps_to_reproduce, expected, actual, status fields"
    - "Template defects (golang.md collector, python.md collector) are categorized as 'template'"
    - "Test project defects are categorized as 'test_project'"
  artifacts:
    - path: ".planning/phases/37-fullstack-fix/37-DEFECTS.yaml"
      provides: "Structured YAML record of all fullstack E2E defects"
      contains: "defects:"
  key_links: []
---

<objective>
Create 37-DEFECTS.yaml documenting all defects discovered during Phase 35 (Go+JS) and Phase 36 (Python+TS) fullstack E2E testing.

Purpose: Establish a single structured record of every defect found, following the same YAML format as 34-DEFECTS.yaml from Phase 34. This record drives FIX2-02 (fix defects) and FIX2-03 (verify frontend template quality).

Output: .planning/phases/37-fullstack-fix/37-DEFECTS.yaml with all defects catalogued.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-single-lang-fix/34-DEFECTS.yaml
@.planning/phases/35-gojs-fullstack-calc/35-04-SUMMARY.md
@.planning/phases/36-pyts-fullstack-calc/36-02-SUMMARY.md
@.planning/phases/36-pyts-fullstack-calc/36-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 37-DEFECTS.yaml with all fullstack E2E defects</name>
  <files>.planning/phases/37-fullstack-fix/37-DEFECTS.yaml</files>
  <read_first>
    - .planning/phases/34-single-lang-fix/34-DEFECTS.yaml (format reference -- follow this exact YAML structure with id, category, severity, template, discovered_in, description, steps_to_reproduce, expected, actual, status fields)
    - .planning/phases/35-gojs-fullstack-calc/35-04-SUMMARY.md (Phase 35 bugs: Windows path separator, JSON field mapping, batch response unwrap)
    - .planning/phases/36-pyts-fullstack-calc/36-02-SUMMARY.md (Phase 36 bugs: collector.py receive() missing meta, orphaned process, batch format)
    - plugins/codepoint/references/golang.md (collector template -- the CollectorHandler at line ~681 only does fmt.Fprintf with plain text, no flow_id routing, no mutex, no JSON output for meta)
    - plugins/codepoint/references/python.md (collector template -- receive() at line ~547 only writes name and stack, discards meta dict containing flow_id)
  </read_first>
  <action>Create `.planning/phases/37-fullstack-fix/37-DEFECTS.yaml` following the exact format of 34-DEFECTS.yaml. Include ALL 8 defects from Phase 35 and Phase 36, categorized as either "template" (skill template defect affecting all users) or "test_project" (defect in the test project only):

**TEMPLATE defects (3 total):**

DEV-FS-01:
- category: template
- severity: high
- template: golang.md
- discovered_in: "Phase 35, Plan 01"
- description: "golang.md collector template (Frontend Collector section) is a simplified version that lacks sync.Mutex for thread safety, flow_id routing to per-flow log files, and JSON output for meta-bearing entries. The template's CollectorHandler only does fmt.Fprintf(tsOutFile, \"[CODEPOINT] %s\\n%s\\n\", entry.Name, entry.Stack), discarding the meta field entirely and providing no flow-specific file routing. The actual gojs-calculator project required an enhanced collector with all three features to support cross-language flow_id correlation."
- steps_to_reproduce: "1. Copy collector code from golang.md Frontend Collector section to new project\n2. Start Go server with concurrent browser requests\n3. Observe: no mutex protecting file writes, no flow_id-based file routing, meta (containing flow_id) is silently discarded\n4. Cross-language flow_id correlation fails"
- expected: "Collector has sync.Mutex for concurrent writes, routes entries with flow_id in meta to per-flow files (cp-ts-{flow}-{timestamp}.log), writes JSON for meta-bearing entries"
- actual: "Collector has no mutex, no flow routing, meta discarded -- only plain text [CODEPOINT] output to single file"
- status: fixed (in gojs-calculator project code; template NOT yet updated)

DEV-FS-02:
- category: template
- severity: high
- template: python.md
- discovered_in: "Phase 36, Plan 02"
- description: "python.md collector template receive() function only writes name and stack to cp-ts log, discarding the meta dict which contains flow_id. This means cross-language flow_id correlation is impossible when using the template code directly. The actual pyts-calculator project required fixing receive() to extract meta and write it as JSON alongside the stack."
- steps_to_reproduce: "1. Copy collector.py from python.md template to new project\n2. POST {\"name\":\"test\",\"stack\":\"Error\",\"meta\":{\"flow_id\":\"flow-test\"}} to /__codepoint__\n3. Read cp-ts log\n4. Observe: meta field (flow_id) is not present in output"
- expected: "receive() writes meta JSON to cp-ts log so flow_id is available for cross-language correlation"
- actual: "receive() only writes [CODEPOINT] {name}\\n{stack}, meta dict is silently discarded"
- status: fixed (in pyts-calculator project code; template NOT yet updated)

DEV-FS-03:
- category: template
- severity: medium
- template: golang.md
- discovered_in: "Phase 35, Plan 04"
- description: "golang.md integration example in Frontend Collector section uses http.FileServer(http.FS(frontendDist)) for SPA fallback. On Windows, when embed.FS.Open receives a path with backslashes (from filepath.Clean or browser URL normalization), it fails to find files. The fix is to use path.Clean (which preserves forward slashes) instead of filepath.Clean (which converts to backslashes on Windows)."
- steps_to_reproduce: "1. Copy main.go SPA fallback code from golang.md integration example\n2. Build and run on Windows\n3. Open browser to http://localhost:8080/\n4. Observe: SPA fallback fails because embed.FS.Open receives backslash path"
- expected: "SPA fallback works on Windows using path.Clean to preserve forward slashes for embed.FS"
- actual: "SPA fallback may fail on Windows if filepath.Clean is used, as embed.FS requires forward-slash paths"
- status: fixed (in gojs-calculator main.go using path.Clean; template example NOT yet updated)

**TEST PROJECT defects (5 total):**

DEV-FS-04:
- category: test_project
- severity: medium
- template: null
- discovered_in: "Phase 35, Plan 04"
- description: "gojs-calculator/internal/history/store.go: history.Record struct fields (ID, Expression, Result) lacked JSON tags. Go's encoding/json defaults to field names with uppercase first letter, but the frontend client expected lowercase (id, expression, result). Adding json:\"id\", json:\"expression\", json:\"result\" tags fixed the field mapping."
- steps_to_reproduce: "1. Start gojs-calculator server\n2. POST /api/calculate with expression\n3. GET /api/history\n4. Observe: JSON response has uppercase field names (ID, Expression, Result)"
- expected: "JSON response uses lowercase field names: {\"id\": 1, \"expression\": \"2+3\", \"result\": \"5.0\"}"
- actual: "JSON response used uppercase field names: {\"ID\": 1, \"Expression\": \"2+3\", \"Result\": \"5.0\"}"
- status: fixed (in gojs-calculator/internal/history/store.go)

DEV-FS-05:
- category: test_project
- severity: low
- template: null
- discovered_in: "Phase 35, Plan 04"
- description: "gojs-calculator/frontend/src/api/client.ts batchCalculate() received {\"results\":[...]} from Go backend but expected a plain array. The Go batch endpoint wraps results in a struct {Results []Result}, but the frontend client.map()d the response directly. Fixed with `data.results || data` fallback pattern."
- steps_to_reproduce: "1. Start gojs-calculator server\n2. Use batch calculation in UI\n3. Observe: batch results not rendering because client tried to .map() on {results:[...]} object instead of array"
- expected: "batch response correctly unwrapped: data.results if present, otherwise data directly"
- actual: "batch response not unwrapped: client expected array but received {results:[...]} object"
- status: fixed (in gojs-calculator/frontend/src/api/client.ts with data.results || data)

DEV-FS-06:
- category: test_project
- severity: medium
- template: null
- discovered_in: "Phase 36, Plan 02"
- description: "pyts-calculator/tests/test_linkage.py batch test initially sent {\"expressions\": [\"1+1\", \"2*3\", \"10/2\"]} as a JSON array, but the Python backend process_expressions() splits on newlines expecting a single string. The correct format is {\"expressions\": \"1+1\\n2*3\\n10/2\"}."
- steps_to_reproduce: "1. Send {\"expressions\": [\"1+1\", \"2*3\"]} to POST /api/batch\n2. Observe: backend fails or returns incorrect results because it calls input_text.split(chr(10)) on the array"
- expected: "Batch test sends newline-separated string: {\"expressions\": \"1+1\\n2*3\\n10/2\"}"
- actual: "Batch test sent JSON array: {\"expressions\": [\"1+1\", \"2*3\", \"10/2\"]}"
- status: fixed (in pyts-calculator/tests/test_linkage.py)

DEV-FS-07:
- category: test_project
- severity: low
- template: null
- discovered_in: "Phase 36, Plan 02"
- description: "pyts-calculator tests/conftest.py: previous manual server process still held file handles on Windows, causing PermissionError when test tried to delete old log files. Fixed by adding _kill_port_holder() that uses netstat + taskkill to kill processes holding port 18091 before starting new server."
- steps_to_reproduce: "1. Run a manual pyts-calculator server on port 18091\n2. Run integration tests\n3. Observe: PermissionError when trying to clean old log files"
- expected: "Tests gracefully handle orphaned processes by killing port holders before server start"
- actual: "PermissionError on log file cleanup because previous process still holds file handles"
- status: fixed (in pyts-calculator/tests/conftest.py with _kill_port_holder())

DEV-FS-08:
- category: test_project
- severity: low
- template: null
- discovered_in: "Phase 36, Plan 02"
- description: "pyts-calculator toggle tests (test_toggle.py combination 4) delete both toggle files as the final test. This leaves the project in a disabled state, requiring manual restoration (touch both toggle files) before next server start."
- steps_to_reproduce: "1. Run test_toggle.py\n2. After tests, check ~/.codepoint/.codepoint-python and ~/.codepoint/.codepoint-ts\n3. Observe: both files deleted by last test"
- expected: "Toggle files restored after test completion or test framework handles cleanup"
- actual: "Toggle files deleted by last test, requiring manual touch before next server start"
- status: fixed (toggle files restored in test teardown)

Add a summary section at the bottom:
```yaml
summary:
  total_defects: 8
  template_defects: 3
  test_project_defects: 5
  all_fixed_in_code: true
  templates_updated: false
  notes: |
    3 template defects require updating golang.md and python.md reference docs.
    5 test project defects were fixed during Phase 35/36 execution.
    Phase 37 Plan 02 will update the template files to match enhanced implementations.
```
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && python -c "import yaml; d=yaml.safe_load(open('.planning/phases/37-fullstack-fix/37-DEFECTS.yaml')); assert len(d['defects'])==8, f'Expected 8 defects, got {len(d[\"defects\"])}'; cats=[x['category'] for x in d['defects']]; assert cats.count('template')==3, f'Expected 3 template defects'; assert cats.count('test_project')==5, f'Expected 5 test_project defects'; print('VALID: 8 defects (3 template + 5 test_project)')</automated>
  </verify>
  <done>37-DEFECTS.yaml exists with 8 defects (3 template + 5 test_project), each with all required fields, summary section present</done>
</task>

</tasks>

<verification>
- `python -c "import yaml; yaml.safe_load(open('.planning/phases/37-fullstack-fix/37-DEFECTS.yaml'))"` parses without error
- 8 defects total: 3 template (DEV-FS-01, DEV-FS-02, DEV-FS-03), 5 test_project (DEV-FS-04 through DEV-FS-08)
</verification>

<success_criteria>
- 37-DEFECTS.yaml exists and is valid YAML
- All 8 defects from Phase 35 and Phase 36 are documented
- Template defects reference golang.md and python.md
- Summary section has correct counts
</success_criteria>

<output>
After completion, create `.planning/phases/37-fullstack-fix/37-01-SUMMARY.md`
</output>
