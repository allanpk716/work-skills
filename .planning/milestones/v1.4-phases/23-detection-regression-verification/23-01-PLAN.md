---
phase: 23-detection-regression-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/23-detection-regression-verification/23-VERIFICATION.md
autonomous: false
requirements: [DETECT-01, DETECT-02, DETECT-03]

must_haves:
  truths:
    - "isPluginInstalled('windows-git-commit') returns true after reinstall"
    - "windows-git-commit shows [installed] marker in marketplace table on re-run"
    - "isPluginInstalled('claude-notify') still returns true (regression check)"
  artifacts:
    - path: ".planning/phases/23-detection-regression-verification/23-VERIFICATION.md"
      provides: "Verification results for all three DETECT requirements"
      contains: "DETECT-01"
  key_links:
    - from: "~/.claude/skills/windows-git-commit/SKILL.md"
      to: "isPluginInstalled()"
      via: "fs.existsSync"
      pattern: "existsSync.*windows-git-commit.*SKILL.md"
---

<objective>
Verify that Phase 22 plugin structure fix enables correct detection of windows-git-commit by the installer, without breaking claude-notify detection.

Purpose: Confirm the root-cause fix (SKILL.md at plugin root) resolves the detection issue end-to-end through actual installer execution.
Output: Verified DETECT-01/02/03 requirements, results recorded in 23-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-plugin-structure-fix/22-01-SUMMARY.md

<interfaces>
<!-- Detection logic the executor must understand (not modify) -->

From installer/src/marketplace/plugin-installer.js:
```javascript
// Line 17-19: Get skills directory
function getSkillsDir() {
  return path.join(os.homedir(), '.claude', 'skills');
}

// Line 26-29: Detection check - the core of this verification
function isPluginInstalled(pluginName) {
  const skillPath = path.join(getSkillsDir(), pluginName, 'SKILL.md');
  return fs.existsSync(skillPath);
}
```

From installer/src/marketplace/index.js:
```javascript
// Line 29: [installed] marker in displayPluginTable
const installed = isPluginInstalled(plugin.name);
const status = installed ? chalk.green(' [installed]') : '';

// Line 105: MultiSelect disabled for already-installed plugins
disabled: isPluginInstalled(p.name)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old broken install and reinstall windows-git-commit via installer</name>
  <files>.planning/phases/23-detection-regression-verification/23-VERIFICATION.md</files>
  <read_first>
    - installer/src/marketplace/plugin-installer.js (understand install flow)
    - installer/src/marketplace/index.js (understand display logic)
  </read_first>
  <acceptance_criteria>
    - ~/.claude/skills/windows-git-commit/SKILL.md exists (node -e check returns "true")
    - node -e "const {isPluginInstalled} = require('./installer/src/marketplace/plugin-installer.js'); console.log(isPluginInstalled('windows-git-commit'));" outputs "true"
    - node -e "const {isPluginInstalled} = require('./installer/src/marketplace/plugin-installer.js'); console.log(isPluginInstalled('claude-notify'));" outputs "true"
  </acceptance_criteria>
  <action>
    Per D-01 (manual installer execution), D-03 (no cleanup logic needed, but we remove old broken install manually to ensure clean state):

    Step 1 — Remove old broken install directory:
    Run: `rm -rf ~/.claude/skills/windows-git-commit`
    This removes the old nested structure where SKILL.md was at skills/windows-git-commit/SKILL.md instead of at root.

    Step 2 — Verify pre-install detection is false:
    Run: `node -e "const {isPluginInstalled} = require('./installer/src/marketplace/plugin-installer.js'); console.log('windows-git-commit before:', isPluginInstalled('windows-git-commit'));"`
    Expected output: "windows-git-commit before: false"

    Step 3 — Verify claude-notify detection still true (pre-check for DETECT-03):
    Run: `node -e "const {isPluginInstalled} = require('./installer/src/marketplace/plugin-installer.js'); console.log('claude-notify before:', isPluginInstalled('claude-notify'));"`
    Expected output: "claude-notify before: true"

    Step 4 — Run installer to reinstall windows-git-commit:
    Run: `npx @allanpk716/work-skills-setup`
    In the marketplace UI, select only "windows-git-commit" for installation.
    Wait for installation to complete successfully.

    Step 5 — Verify DETECT-01 (isPluginInstalled returns true after install):
    Run: `node -e "const {isPluginInstalled} = require('./installer/src/marketplace/plugin-installer.js'); console.log('DETECT-01 - windows-git-commit installed:', isPluginInstalled('windows-git-commit'));"`
    Expected output: "DETECT-01 - windows-git-commit installed: true"

    Also verify SKILL.md exists at the correct path directly:
    Run: `node -e "const fs=require('fs'); const path=require('path'); const os=require('os'); const p=path.join(os.homedir(),'.claude','skills','windows-git-commit','SKILL.md'); console.log('SKILL.md at root:', fs.existsSync(p));"`
    Expected output: "SKILL.md at root: true"

    Step 6 — Verify DETECT-03 (claude-notify regression):
    Run: `node -e "const {isPluginInstalled} = require('./installer/src/marketplace/plugin-installer.js'); console.log('DETECT-03 - claude-notify installed:', isPluginInstalled('claude-notify'));"`
    Expected output: "DETECT-03 - claude-notify installed: true"

    Step 7 — Record DETECT-01 and DETECT-03 results in 23-VERIFICATION.md by updating the Per-Task Verification Map table rows for 23-01-01 and 23-01-03 from "pending" to the actual result.
  </action>
  <verify>
    <automated>node -e "const {isPluginInstalled} = require('./installer/src/marketplace/plugin-installer.js'); const r1 = isPluginInstalled('windows-git-commit'); const r2 = isPluginInstalled('claude-notify'); console.log('DETECT-01:', r1, 'DETECT-03:', r2); process.exit(r1 && r2 ? 0 : 1);"</automated>
  </verify>
  <done>
    - Old broken windows-git-commit install directory removed and replaced with new correct structure
    - isPluginInstalled('windows-git-commit') returns true (DETECT-01)
    - isPluginInstalled('claude-notify') returns true (DETECT-03)
    - SKILL.md exists at ~/.claude/skills/windows-git-commit/SKILL.md
    - 23-VERIFICATION.md updated with DETECT-01 and DETECT-03 results
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify [installed] marker on re-run (DETECT-02)</name>
  <files>.planning/phases/23-detection-regression-verification/23-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/23-detection-regression-verification/23-VERIFICATION.md (check current state after Task 1)
  </read_first>
  <acceptance_criteria>
    - 23-VERIFICATION.md contains "DETECT-02" with a non-"pending" status
  </acceptance_criteria>
  <action>
    Per D-02 (minimal verification scope). Human visually verifies the [installed] marker by running `npx @allanpk716/work-skills-setup` and observing the marketplace table output. Then records the result in 23-VERIFICATION.md.
  </action>
  <what-built>
    Reinstalled windows-git-commit plugin via installer. Detection logic confirmed working via Node.js calls (DETECT-01, DETECT-03). Now need human to visually verify the [installed] marker in the interactive marketplace UI.
  </what-built>
  <how-to-verify>
    1. Run the installer again: `npx @allanpk716/work-skills-setup`
    2. In the marketplace table, verify that windows-git-commit shows a green "[installed]" marker next to its row
    3. Verify that windows-git-commit is listed as disabled (cannot be re-selected) in the MultiSelect prompt
    4. Verify that claude-notify also shows "[installed]" marker
    5. Exit the installer without selecting any additional plugins (Ctrl+C or deselect all and confirm)

    Expected behavior:
    - windows-git-commit row: shows "[installed]", disabled in selection
    - claude-notify row: shows "[installed]", disabled in selection
    - Both plugins should NOT prompt for reinstallation

    After visual verification, record the DETECT-02 result by editing .planning/phases/23-detection-regression-verification/23-VERIFICATION.md:
    - Update row 23-01-02 status from "pending" to the actual result
  </how-to-verify>
  <verify>
    <automated>grep -c "DETECT-02" .planning/phases/23-detection-regression-verification/23-VERIFICATION.md</automated>
  </verify>
  <done>
    - Human confirmed [installed] marker visible next to windows-git-commit in marketplace table
    - Human confirmed windows-git-commit is disabled (not selectable) in MultiSelect prompt
    - Human confirmed claude-notify also shows [installed] marker
    - 23-VERIFICATION.md updated with DETECT-02 result
  </done>
  <resume-signal>Type "approved" if [installed] markers shown correctly, or describe what you observed if there are issues</resume-signal>
</task>

</tasks>

<verification>
1. Run existing installer tests to confirm no regressions: `node installer/tests/marketplace/plugin-installer.test.js`
2. All three DETECT requirements verified:
   - DETECT-01: isPluginInstalled('windows-git-commit') === true
   - DETECT-02: [installed] marker shown in marketplace UI (human-verified)
   - DETECT-03: isPluginInstalled('claude-notify') === true
3. 23-VERIFICATION.md updated with all results
</verification>

<success_criteria>
1. `isPluginInstalled('windows-git-commit')` returns true (DETECT-01)
2. windows-git-commit shows [installed] in marketplace table and is disabled in selection on re-run (DETECT-02)
3. `isPluginInstalled('claude-notify')` returns true (DETECT-03)
4. Existing installer tests still pass
5. 23-VERIFICATION.md records all results
</success_criteria>

<output>
After completion, create `.planning/phases/23-detection-regression-verification/23-01-SUMMARY.md`
</output>
