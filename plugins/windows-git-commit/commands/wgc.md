---
description: Windows Git commit and push using command-line git with plink + PPK authentication (no GUI)
argument-hint: [commit message]
allowed-tools: Skill(windows-git-commit), Bash
---

Use the windows-git-commit skill to perform Git operations using command-line git with PPK authentication: $ARGUMENTS
