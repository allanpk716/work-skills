---
phase: 41-test-specification-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - plugins/codepoint/skills/codepoint/SKILL.md
autonomous: true
requirements:
  - TSPEC-01

must_haves:
  truths:
    - "Developer sees /codepoint-test-plan listed in the main skill command table"
    - "Main skill storage structure section shows test-plans/ subdirectory"
  artifacts:
    - path: "plugins/codepoint/skills/codepoint/SKILL.md"
      provides: "Updated command table including test-plan skill"
      contains: "/codepoint-test-plan"
  key_links:
    - from: "plugins/codepoint/skills/codepoint/SKILL.md"
      to: "plugins/codepoint/skills/test-plan/SKILL.md"
      via: "Command table entry"
      pattern: "/codepoint-test-plan"
---

<objective>
Register the new test-plan skill in the main codepoint SKILL.md command table and storage structure.

Purpose: Ensure developers discover /codepoint-test-plan when they invoke the parent skill, and understand the .codepoints/test-plans/ directory.

Output: Updated main SKILL.md with new command entry and storage structure diagram.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-test-specification-foundation/41-CONTEXT.md
@.planning/phases/41-test-specification-foundation/41-01-SUMMARY.md
@plugins/codepoint/skills/codepoint/SKILL.md

<interfaces>
<!-- Current command table from codepoint/SKILL.md -->
```
| Command | Purpose |
|---------|---------|
| `/codepoint-scan` | Scan existing codebase, identify business flows, suggest probe locations |
| `/codepoint-plan` | Plan code points for a new feature being developed |
| `/codepoint-implement` | Execute probe insertion with TDD-style automated verification |
```

<!-- Current storage structure from codepoint/SKILL.md -->
```
.codepoints/
├── index.json                  # Global index
├── collections/
│   └── user-management.md
├── flows/
│   ├── user-login.md
│   └── user-register.md
├── points/
│   ├── cp-auth-check.md
│   └── cp-login-entry.md
└── verification/
    └── user-login-verify.md
```

<!-- Frontmatter description that needs updating -->
```
description: >
  Code point V2 skill — collection-based runtime observability for AI-assisted development.
  Provides three core capabilities: scan existing code to identify business flows and probe locations,
  plan code points for new features, and implement probes with TDD-style automated verification.
  Triggers on: "code point", "代码点", "埋点", "codepoint", "runtime probe", "codepoint scan",
  "codepoint plan", "codepoint implement", "扫描代码点", "规划埋点", "埋点验收".
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update main SKILL.md command table and storage structure</name>
  <files>plugins/codepoint/skills/codepoint/SKILL.md</files>
  <read_first>
    - plugins/codepoint/skills/codepoint/SKILL.md (current content to modify)
  </read_first>
  <action>
Read `plugins/codepoint/skills/codepoint/SKILL.md` and make these specific modifications:

1. **Update frontmatter description** -- Change "Provides three core capabilities" to "Provides four core capabilities" and append to the trigger keyword list: `"codepoint test plan", "测试计划", "test-plan"`. The full updated description:
```
description: >
  Code point V2 skill — collection-based runtime observability for AI-assisted development.
  Provides four core capabilities: scan existing code to identify business flows and probe locations,
  plan code points for new features, implement probes with TDD-style automated verification,
  and generate structured test plans for frontend flows.
  Triggers on: "code point", "代码点", "埋点", "codepoint", "runtime probe", "codepoint scan",
  "codepoint plan", "codepoint implement", "codepoint test plan", "扫描代码点", "规划埋点", "埋点验收",
  "测试计划", "test-plan".
```

2. **Add row to Commands table** -- After the `/codepoint-implement` row, add:
```
| `/codepoint-test-plan` | Generate structured test plans for frontend flows with probe snippet references |
```

3. **Add Quick Start section for test planning** -- After the "For New Feature Development" subsection, add a new subsection:
```
### For Test Planning

1. Run `/codepoint-scan` or `/codepoint-plan` to define flows and code points
2. Run `/codepoint-test-plan` with the flow you want to test
3. Review the generated test plan in `.codepoints/test-plans/`
4. Run `/codepoint-implement` to insert probes and verify against the test plan
```

4. **Update Storage Structure diagram** -- Add `test-plans/` subdirectory between `points/` and `verification/`:
```
├── points/
│   ├── cp-auth-check.md
│   └── cp-login-entry.md
├── test-plans/                  ← NEW
│   └── flow-user-login.md
└── verification/
    └── user-login-verify.md
```

Do NOT modify any other sections (Core Principle, Three-Layer Data Model, Probe Output Format, Key Principles, Language Support).
  </action>
  <acceptance_criteria>
    - Frontmatter description contains "four core capabilities" (not "three")
    - Frontmatter description contains "测试计划" and "test-plan"
    - Commands table has 4 rows (scan, plan, implement, test-plan)
    - Commands table contains `/codepoint-test-plan` with description about test plans
    - Quick Start section has "For Test Planning" subsection with 4 numbered steps
    - Storage structure diagram contains `test-plans/` directory with comment `← NEW`
    - File still contains all original sections: Core Principle, Three-Layer Data Model, Commands, Quick Start, Toggle Mechanism, Storage Structure, Probe Output Format, Key Principles, Language Support
  </acceptance_criteria>
  <verify>
    <automated>grep -c "codepoint-test-plan\|test-plans/\|four core capabilities\|For Test Planning" plugins/codepoint/skills/codepoint/SKILL.md</automated>
  </verify>
  <done>Main SKILL.md updated with /codepoint-test-plan in command table, test-plans/ in storage structure, and updated frontmatter description</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This task only modifies a documentation file (SKILL.md), no runtime code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03 | N/A | Documentation only | accept | No security-relevant changes |

</threat_model>

<verification>
1. `plugins/codepoint/skills/codepoint/SKILL.md` contains `/codepoint-test-plan` in Commands table
2. Storage structure shows `test-plans/` directory
3. Quick Start section includes "For Test Planning" subsection
4. Frontmatter updated to "four core capabilities"
5. No original sections were removed
</verification>

<success_criteria>
- /codepoint-test-plan appears in the main skill command table
- .codepoints/test-plans/ appears in the storage structure diagram
- Frontmatter reflects four capabilities and includes new trigger keywords
</success_criteria>

<output>
After completion, create `.planning/phases/41-test-specification-foundation/41-02-SUMMARY.md`
</output>
