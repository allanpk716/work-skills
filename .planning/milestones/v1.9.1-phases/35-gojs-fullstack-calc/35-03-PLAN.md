---
phase: 35
plan: 03
type: execute
wave: 3
depends_on:
  - "35-02"
files_modified: []
autonomous: false
requirements:
  - FULL-02
user_setup: []
must_haves:
  truths:
    - "/codepoint:scan identifies Go backend business flows (API calculate, history query, batch process)"
    - "/codepoint:scan identifies frontend JS components and their codepoints"
    - "Scan output includes cross-language call chain information (frontend -> API -> shared calculator)"
    - "Scan output is structured and consumable by /codepoint:plan"
    - "If scan cannot detect cross-language project, manual .codepoints/ creation fallback succeeds"
  artifacts:
    - path: "tmp/gojs-calculator/.codepoints/index.json"
      provides: "Scan output with project structure and codepoint metadata"
      exists: true
  key_links:
    - from: "scan output"
      to: "Go source files"
      via: "code point location references"
      pattern: "internal/api/server.go"
    - from: "scan output"
      to: "Frontend source files"
      via: "code point location references"
      pattern: "src/components/"
---

<objective>
Run /codepoint:scan on the Go+JS fullstack calculator project and verify it identifies both frontend and backend business flows, including the cross-language call chains. If scan cannot detect the cross-language nature, use a documented fallback strategy to manually create .codepoints/ and record the scan skill gap for Phase 37.

Purpose: Validate that the codepoint scan skill works correctly on a multi-language (Go+JS) project and produces structured output that describes the three business flows and their cross-language relationships. This is primarily a scan skill validation test — failure indicates a scan skill bug, not a Phase 35 issue.

Output: Scan results in `.codepoints/` directory with identified flows and code points, verified for cross-language coverage. Fallback .codepoints/ created if scan fails.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-gojs-fullstack-calc/35-CONTEXT.md
@.planning/phases/35-gojs-fullstack-calc/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /codepoint:scan and analyze output for cross-language flow identification</name>
  <files>
    tmp/gojs-calculator/.codepoints/index.json
  </files>
  <read_first>
    - tmp/gojs-calculator/main.go (project structure reference)
    - tmp/gojs-calculator/internal/api/server.go (API routes and probe locations)
    - tmp/gojs-calculator/frontend/src/components/Calculator.tsx (frontend probe locations)
    - tmp/gojs-calculator/frontend/src/components/History.tsx (frontend probe locations)
    - tmp/gojs-calculator/frontend/src/components/BatchCalc.tsx (frontend probe locations)
    - tmp/gojs-calculator/codepoint/codepoint.go (Go probe library)
    - tmp/gojs-calculator/codepoint/collector.go (collector with flow_id routing)
    - tmp/go-calculator/.codepoints/index.json (reference: Go-only scan output format from Phase 32)
  </read_first>
  <action>
    Run /codepoint:scan on the gojs-calculator project and verify the output:

    **1. Navigate to the project directory:**
    ```bash
    cd C:/WorkSpace/agent/work-skills/tmp/gojs-calculator
    ```

    **2. Run the codepoint scan skill:**
    Invoke `/codepoint:scan` in the project directory. This is an interactive skill that will analyze the project structure.

    **3. After scan completes, verify the output:**
    - Check that `.codepoints/` directory was created
    - Check that `index.json` exists and contains structured data
    - Verify the scan identified:
      a) Go backend business flows:
         - flow-api-calculate (POST /api/calculate -> calculator.Evaluate)
         - flow-history-query (GET /api/history/{id} -> calculator.Evaluate recompute)
         - flow-batch-process (POST /api/batch -> batch.ProcessExpressions -> calculator.Evaluate)
      b) Frontend JS codepoints:
         - cp-fe-calc-submit, cp-fe-calc-response (Calculator.tsx)
         - cp-fe-history-click, cp-fe-history-detail (History.tsx)
         - cp-fe-batch-submit, cp-fe-batch-response (BatchCalc.tsx)
      c) Shared code path: calculator.Evaluate() is identified as the shared codepoint across all three flows

    **4. Pass/fail criteria** (Addresses review concern: formalize pass/fail):

    **PASS conditions (any one is sufficient for FULL-02):**
    - Scan identifies BOTH Go and JS codepoints in a single run, with cross-language flow connections
    - Scan identifies Go codepoints with backend flows correctly, even if frontend requires separate scan
    - Manual fallback produces correct .codepoints/ output covering both languages

    **ACCEPTABLE limitations (not failures):**
    - Scan detects only Go backend (single language) — indicates scan skill gap, not Phase 35 issue
    - Scan requires running twice (once for Go, once for frontend/) — document as workaround
    - Scan does not auto-detect cross-language connections — manual verification in checkpoint covers this

    **Document the result** in a structured format:
    ```
    Scan Result: PASS | PARTIAL | FAIL
    Languages detected: Go | JS | Both
    Backend flows identified: [list or "none"]
    Frontend codepoints identified: [list or "none"]
    Cross-language connections: auto | manual | none
    ```

    **5. Fallback strategy if scan fails** (Addresses review concern: no fallback if scan fails):
    If scan does not detect the cross-language project structure (e.g., detects only Go or only JS), do NOT proceed without .codepoints/. Execute this fallback:

    a) Create `.codepoints/` directory manually:
    ```bash
    mkdir -p tmp/gojs-calculator/.codepoints/{collections,flows,points}
    ```

    b) Create `index.json` following the format from `tmp/go-calculator/.codepoints/index.json` (Phase 32 reference), but extended with frontend entries:
    - Reference the existing go-calculator index.json structure
    - Add entries for frontend components (Calculator.tsx, History.tsx, BatchCalc.tsx)
    - Add entries for frontend probe points (cp-fe-calc-submit, etc.)
    - Document cross-language connections: frontend fetch -> API route -> shared calculator.Evaluate()

    c) The fallback index.json MUST include:
    - At minimum: 3 backend flows (flow-api-calculate, flow-history-query, flow-batch-process)
    - At minimum: 3 frontend codepoints (cp-fe-calc-submit, cp-fe-history-click, cp-fe-batch-submit)
    - Shared codepoint: calculator.Evaluate() referenced by all flows

    d) Record this as a scan skill finding for Phase 37 (FIX2 phase):
    - Document: "scan skill does not detect Go+JS dual-language projects as a single project"
    - Include: exact scan output/error for reproducibility
    - This does NOT block Phase 35 execution — plans 01 and 02 already created the project correctly

    **6. Document scan findings:**
    After scan (or fallback), list the output:
    ```bash
    ls -la C:/WorkSpace/agent/work-skills/tmp/gojs-calculator/.codepoints/
    cat C:/WorkSpace/agent/work-skills/tmp/gojs-calculator/.codepoints/index.json
    ```
  </action>
  <verify>
    <automated>test -f C:/WorkSpace/agent/work-skills/tmp/gojs-calculator/.codepoints/index.json && echo "SCAN OUTPUT EXISTS" || echo "SCAN OUTPUT MISSING"</automated>
  </verify>
  <done>
    - `.codepoints/index.json` exists with scan results (either from scan or fallback)
    - Scan output identifies at least 3 Go backend codepoints (cp-api-calc-entry, cp-history-entry, cp-batch-entry)
    - Scan output identifies at least 3 frontend codepoints (cp-fe-calc-submit, cp-fe-history-click, cp-fe-batch-submit)
    - Scan output or manual analysis confirms cross-language flow connections
    - Pass/fail result documented with structured format
    - If scan failed, fallback .codepoints/ created AND finding recorded for Phase 37
  <acceptance_criteria>
    - `.codepoints/index.json` exists with scan results (either from scan or fallback)
    - Scan output identifies at least 3 Go backend codepoints (cp-api-calc-entry, cp-history-entry, cp-batch-entry)
    - Scan output identifies at least 3 frontend codepoints (cp-fe-calc-submit, cp-fe-history-click, cp-fe-batch-submit)
    - Scan output or manual analysis confirms cross-language flow connections
    - Pass/fail result documented with structured format
    - If scan failed, fallback .codepoints/ created AND finding recorded for Phase 37
  </acceptance_criteria>
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    /codepoint:scan was run on tmp/gojs-calculator and produced output in .codepoints/ directory (or fallback was used). The scan identified Go backend business flows, frontend JS codepoints, and the cross-language call chains (or these were manually documented).
  </what-built>
  <how-to-verify>
    1. Read `.codepoints/index.json` — verify it contains valid JSON with structured data
    2. Verify the three backend flows are identified: flow-api-calculate, flow-history-query, flow-batch-process
    3. Verify frontend codepoints are present: cp-fe-calc-submit, cp-fe-history-click, cp-fe-batch-submit
    4. Check if cross-language connections are captured (frontend -> API -> calculator.Evaluate shared path)
    5. If scan did not detect both languages: this is acceptable — confirm the fallback .codepoints/ covers both languages
    6. Confirm that this does NOT block subsequent plans (collector verification in Plan 04 works regardless of scan result)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to collector verification, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude -> codepoint:scan | Skill reads project files and generates analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | I | Scan output | accept | Scan is a diagnostic tool, output is for developer use only |
</threat_model>

<verification>
1. `.codepoints/index.json` file exists (from scan or fallback)
2. File contains valid JSON with flows and codepoints
3. At least 3 backend codepoints identified
4. At least 3 frontend codepoints identified
5. Cross-language flow connections documented (auto or manual)
6. Pass/fail result documented in structured format
</verification>

<success_criteria>
- /codepoint:scan ran successfully on the Go+JS project (or fallback was executed)
- Scan output exists in .codepoints/ directory with valid JSON
- Output covers both Go backend and frontend JS codepoints
- Cross-language flow relationships are captured or documented manually
- If scan failed, fallback .codepoints/ created and finding recorded for Phase 37
- Human verified the scan results are reasonable and complete
</success_criteria>

<output>
After completion, create `.planning/phases/35-gojs-fullstack-calc/35-03-SUMMARY.md`
</output>
