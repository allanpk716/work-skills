---
phase: 30-integration-into-notification-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/claude-notify/hooks/scripts/notify.py
  - plugins/claude-notify/hooks/scripts/notify-attention.py
  - plugins/claude-notify/tests/test_notify.py
autonomous: true
requirements: [PROJ-04, PROJ-05]

must_haves:
  truths:
    - "notify.py and notify-attention.py no longer contain a local get_project_name() function definition"
    - "Both scripts import get_project_name from flags module alongside check_notification_flags"
    - "Calling get_project_name() from either script invokes flags.py's upward-traversal version"
    - "test_notify.py mocks flags.find_project_root instead of os.getcwd"
    - "All existing tests pass after migration"
  artifacts:
    - path: "plugins/claude-notify/hooks/scripts/notify.py"
      provides: "Notification script using flags.get_project_name()"
      contains: "from flags import check_notification_flags, get_project_name"
    - path: "plugins/claude-notify/hooks/scripts/notify-attention.py"
      provides: "Attention notification script using flags.get_project_name()"
      contains: "from flags import check_notification_flags, get_project_name"
    - path: "plugins/claude-notify/tests/test_notify.py"
      provides: "Updated test mocks for imported get_project_name"
      contains: "patch('flags.find_project_root'"
  key_links:
    - from: "notify.py"
      to: "flags.py"
      via: "from flags import get_project_name"
      pattern: "from flags import.*get_project_name"
    - from: "notify-attention.py"
      to: "flags.py"
      via: "from flags import get_project_name"
      pattern: "from flags import.*get_project_name"
    - from: "test_notify.py"
      to: "flags.py"
      via: "patch('flags.find_project_root'"
      pattern: "patch\\('flags\\.find_project_root'"
---

<objective>
Replace the local get_project_name() functions in notify.py and notify-attention.py with the new upward-traversal version from flags.py.

Purpose: When notification scripts are triggered from a project subdirectory (e.g., plugins/claude-notify/hooks/scripts/), they currently display the subdirectory name ("scripts"). After this change, they will traverse upward to find the project root and display the correct project name (e.g., "work-skills").

Output: Both notification scripts import and use flags.get_project_name(), tests updated and passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-find-up-project-root-logic/29-02-SUMMARY.md

<interfaces>
<!-- Executor MUST use these interfaces directly — no codebase exploration needed. -->

From plugins/claude-notify/hooks/scripts/flags.py (lines 168-182):
```python
def get_project_name():
    """
    Get the current project name by finding the project root directory.

    Returns the directory name of the project root. Falls back to
    the current working directory's basename if no project root
    markers (.git or CLAUDE.md) are found.

    Returns:
        str: Project name (directory name)
    """
    root = find_project_root()
    if root is not None:
        return root.name
    return Path.cwd().name
```

From plugins/claude-notify/hooks/scripts/flags.py (lines 130-165):
```python
def find_project_root():
    """
    Find the project root directory by searching upward from CWD.
    Looks for .git directory or CLAUDE.md file as project root markers.
    Returns the first (closest) directory containing either marker.
    Returns:
        Optional[Path]: The project root directory, or None if no markers found
    """
```

Current import line in both notify.py and notify-attention.py (line 21):
```python
from flags import check_notification_flags
```

Target import line (add get_project_name):
```python
from flags import check_notification_flags, get_project_name
```

Call sites (no change needed — function signature identical):
- notify.py line 369: `project_name = get_project_name()`
- notify-attention.py line 197: `project_name = get_project_name()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate get_project_name() in both scripts and update tests</name>
  <files>
    plugins/claude-notify/hooks/scripts/notify.py
    plugins/claude-notify/hooks/scripts/notify-attention.py
    plugins/claude-notify/tests/test_notify.py
  </files>
  <read_first>
    plugins/claude-notify/hooks/scripts/notify.py
    plugins/claude-notify/hooks/scripts/notify-attention.py
    plugins/claude-notify/hooks/scripts/flags.py
    plugins/claude-notify/tests/test_notify.py
  </read_first>
  <behavior>
    - Test 1 (test_get_project_name_success): Mock flags.find_project_root to return Path('/path/to/my-project'), assert get_project_name() returns 'my-project'
    - Test 2 (test_get_project_name_with_spaces): Mock flags.find_project_root to return Path('C:/Users/Test/My Project'), assert get_project_name() returns 'My Project'
    - Test 3 (test_get_project_name_with_chinese): Mock flags.find_project_root to return Path('C:/Users/Test/测试项目'), assert get_project_name() returns '测试项目'
  </behavior>
  <action>
    Per D-01, D-02, D-03, D-04, D-05, D-06:

    **Step 1: Update notify.py (per D-01, D-02)**
    - On line 21, change `from flags import check_notification_flags` to `from flags import check_notification_flags, get_project_name`
    - Delete the local get_project_name() function definition (lines 38-51, the entire function including docstring)
    - The call site at line 369 (`project_name = get_project_name()`) stays unchanged per D-03 — same function name, same signature (no params, returns str)

    **Step 2: Update notify-attention.py (per D-01, D-02)**
    - On line 21, change `from flags import check_notification_flags` to `from flags import check_notification_flags, get_project_name`
    - Delete the local get_project_name() function definition (lines 38-51, the entire function including docstring)
    - The call site at line 197 (`project_name = get_project_name()`) stays unchanged per D-03

    **Step 3: Update test_notify.py (per D-06)**
    - The import on line 18 (`from notify import get_project_name, get_claude_summary, cleanup_old_logs`) stays the same — it imports from notify, which now re-exports from flags
    - For test_get_project_name_success (line 28-32): Change `with patch('os.getcwd', return_value='/path/to/my-project'):` to `with patch('flags.find_project_root', return_value=Path('/path/to/my-project')):`. Ensure `Path` is imported (already imported on line 11). The assertion `self.assertEqual(result, 'my-project')` stays the same.
    - For test_get_project_name_with_spaces (line 34-38): Change `with patch('os.getcwd', return_value='C:/Users/Test/My Project'):` to `with patch('flags.find_project_root', return_value=Path('C:/Users/Test/My Project')):`. Assertion stays the same.
    - For test_get_project_name_with_chinese (line 40-44): Change `with patch('os.getcwd', return_value='C:/Users/Test/测试项目'):` to `with patch('flags.find_project_root', return_value=Path('C:/Users/Test/测试项目')):`. Assertion stays the same.

    Note: After this migration, when notify.py is imported and calls `get_project_name()`, it calls the version from flags.py which uses `find_project_root()` upward traversal instead of `os.path.basename(os.getcwd())`. The mock target changes from `os.getcwd` to `flags.find_project_root` because that is the function the new get_project_name() calls internally.
  </action>
  <verify>
    <automated>cd plugins/claude-notify && python -m pytest tests/test_notify.py -v --tb=short 2>&1 && python -m pytest tests/test_flags.py -v --tb=short 2>&1</automated>
  </verify>
  <done>
    - notify.py has no local get_project_name() definition, imports it from flags
    - notify-attention.py has no local get_project_name() definition, imports it from flags
    - test_notify.py uses patch('flags.find_project_root') instead of patch('os.getcwd')
    - All tests in test_notify.py pass (8 tests)
    - All tests in test_flags.py pass (29 tests)
  </done>
</task>

</tasks>

<verification>
1. Run all tests: `cd plugins/claude-notify && python -m pytest tests/ -v --tb=short`
2. Verify no local get_project_name() in either script: `grep -n "def get_project_name" plugins/claude-notify/hooks/scripts/notify.py plugins/claude-notify/hooks/scripts/notify-attention.py` should return nothing
3. Verify import exists: `grep -n "from flags import.*get_project_name" plugins/claude-notify/hooks/scripts/notify.py plugins/claude-notify/hooks/scripts/notify-attention.py`
4. Verify test mocks updated: `grep -n "flags.find_project_root" plugins/claude-notify/tests/test_notify.py`
5. Quick smoke test: `cd plugins/claude-notify/hooks/scripts && python -c "from notify import get_project_name; print(get_project_name())"` should print "work-skills" (not "scripts")
</verification>

<success_criteria>
- Both notification scripts use flags.get_project_name() (no local definitions)
- All 37 tests pass (8 in test_notify.py + 29 in test_flags.py)
- Running get_project_name() from subdirectory returns project root name, not subdirectory name
</success_criteria>

<output>
After completion, create `.planning/phases/30-integration-into-notification-scripts/30-01-SUMMARY.md`
</output>
