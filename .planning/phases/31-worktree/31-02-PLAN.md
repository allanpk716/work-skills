---
phase: 31-worktree
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - plugins/claude-notify/hooks/scripts/notify.py
  - plugins/claude-notify/hooks/scripts/notify-attention.py
  - plugins/claude-notify/tests/test_notify.py
autonomous: true
requirements:
  - WTREE-01
  - WTREE-02

must_haves:
  truths:
    - "Stop hook 通知标题格式为 [project:branch],在多个 worktree 并行时可区分每个通知来源"
    - "非 git 仓库场景下,通知标题退化为 [project] 格式,不影响已有功能"
    - "Attention hook 通知标题格式为 [project:branch] Attention Needed"
    - "Attention hook 通知消息内容包含 session_id 字段,用户可据此追溯具体会话"
  artifacts:
    - path: "plugins/claude-notify/hooks/scripts/notify.py"
      provides: "Stop hook with [project:branch] title format"
      contains: "get_git_branch"
    - path: "plugins/claude-notify/hooks/scripts/notify-attention.py"
      provides: "Attention hook with [project:branch] title and session_id in message"
      contains: "get_git_branch"
    - path: "plugins/claude-notify/tests/test_notify.py"
      provides: "Title format tests for both scripts"
      contains: "TestWorktreeTitleFormat"
  key_links:
    - from: "notify.py"
      to: "flags.get_git_branch"
      via: "import and call"
      pattern: "from flags import.*get_git_branch"
    - from: "notify-attention.py"
      to: "flags.get_git_branch"
      via: "import and call"
      pattern: "from flags import.*get_git_branch"
---

<objective>
修改 notify.py 和 notify-attention.py 的通知标题格式,添加 git 分支名以支持 worktree 场景区分;确认 notify-attention.py 的 session_id 展示满足 WTREE-02。

Purpose: 用户在多个 worktree 并行工作时,从通知标题即可区分来源项目和分支。Attention 通知中的 session_id 帮助用户定位需要关注的具体会话。

Output: 两个通知脚本的标题格式从 [project] 升级为 [project:branch];新增测试覆盖标题格式。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-worktree/31-RESEARCH.md
@.planning/phases/31-worktree/31-01-SUMMARY.md

@plugins/claude-notify/hooks/scripts/notify.py
@plugins/claude-notify/hooks/scripts/notify-attention.py
@plugins/claude-notify/tests/test_notify.py
</context>

<interfaces>
<!-- From Plan 01 (flags.py) - executor must use these exports -->

From plugins/claude-notify/hooks/scripts/flags.py:
```python
def get_git_branch() -> Optional[str]:
    """
    Get current git branch name via 'git branch --show-current'.
    Returns branch name string, or None if not in git repo / DETACHED HEAD / error.
    """
    ...

def get_project_name() -> str:
    """Returns project directory name."""
    ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update notification title formats with git branch and add session_id tests</name>
  <files>plugins/claude-notify/hooks/scripts/notify.py, plugins/claude-notify/hooks/scripts/notify-attention.py, plugins/claude-notify/tests/test_notify.py</files>
  <read_first>
    - plugins/claude-notify/hooks/scripts/notify.py (entire file - modification target, focus on main() function lines 327-410, especially lines 353-373 where project_name and notifications are built)
    - plugins/claude-notify/hooks/scripts/notify-attention.py (entire file - modification target, focus on main() lines 153-234, especially lines 181-195 where title and message are built)
    - plugins/claude-notify/tests/test_notify.py (entire file - existing test patterns to follow)
    - plugins/claude-notify/hooks/scripts/flags.py (read to confirm get_git_branch exists from Plan 01)
  </read_first>
  <behavior>
    - Test 1: notify.py 在有 git 分支时标题格式为 "[project:branch]"
    - Test 2: notify.py 在无 git 分支时标题格式退化为 "[project]"
    - Test 3: notify-attention.py 在有 git 分支时标题格式为 "[project:branch] Attention Needed"
    - Test 4: notify-attention.py 在无 git 分支时标题格式退化为 "[project] Attention Needed"
    - Test 5: notify-attention.py 消息体包含 "Session: " 前缀和 session_id 值
  </behavior>
  <acceptance_criteria>
    - notify.py 顶部 import 行包含 `from flags import check_notification_flags, get_project_name, get_git_branch`
    - notify.py main() 函数中调用 `get_git_branch()` 获取分支名
    - notify.py 中标题构建逻辑: 有分支时 `f"[{project_name}:{branch}]"`, 无分支时 `f"[{project_name}]"`
    - notify.py 中标题变量传递给 send_pushover_notification 和 send_windows_notification (不再直接传 project_name)
    - notify-attention.py 顶部 import 行包含 `from flags import check_notification_flags, get_project_name, get_git_branch`
    - notify-attention.py main() 函数中调用 `get_git_branch()` 获取分支名
    - notify-attention.py 标题构建逻辑: 有分支时 `f"[{project_name}:{branch}] Attention Needed"`, 无分支时 `f"[{project_name}] Attention Needed"`
    - notify-attention.py 消息体仍包含 `f"Session: {session_id}"` (第 195 行,已有)
    - test_notify.py 包含 `class TestWorktreeTitleFormat` 测试类
    - `python -m pytest plugins/claude-notify/tests/ -v` 全部通过
  </acceptance_criteria>
  <action>
    **RED phase - Write tests first:**

    在 test_notify.py 末尾 (if __name__ 之前) 新增 `class TestWorktreeTitleFormat(unittest.TestCase)`:

    由于 notify.py 的 main() 函数是完整流程 (parse args -> get project name -> get summary -> check flags -> send notifications),直接测试 main() 需要大量 mock。更好的方式是测试标题构建逻辑。

    在 notify.py 中提取一个 `build_title(project_name, git_branch=None)` 辅助函数,然后在测试中直接测试这个函数。这比 mock 整个 main() 更可靠:

    1. `test_build_title_with_branch`: build_title("my-project", "feature-x") 返回 "[my-project:feature-x]"
    2. `test_build_title_without_branch`: build_title("my-project", None) 返回 "[my-project]"
    3. `test_build_title_empty_branch`: build_title("my-project", "") 返回 "[my-project]" (空字符串视为无分支)

    对于 notify-attention.py,类似地提取 `build_attention_title(project_name, git_branch=None)`:
    4. `test_build_attention_title_with_branch`: 返回 "[my-project:feature-x] Attention Needed"
    5. `test_build_attention_title_without_branch`: 返回 "[my-project] Attention Needed"

    WTREE-02 测试 (notify-attention.py 的 session_id 已在消息体中):
    6. `test_attention_message_contains_session_id`: 模拟 hook_input 含 session_id="test-session-123",验证消息体包含 "Session: test-session-123"

    **GREEN phase - Implement:**

    **修改 notify.py:**

    1. 在 import 行 (第 21 行) 添加 `get_git_branch`:
       ```python
       from flags import check_notification_flags, get_project_name, get_git_branch
       ```

    2. 在 get_claude_summary() 之后 (约第 79 行) 新增辅助函数:
       ```python
       def build_title(project_name, git_branch=None):
           """
           Build notification title with optional git branch.

           Args:
               project_name (str): Project name
               git_branch (str, optional): Git branch name

           Returns:
               str: Formatted title like [project:branch] or [project]
           """
           if git_branch:
               return f"[{project_name}:{git_branch}]"
           return f"[{project_name}]"
       ```

    3. 在 main() 函数中 (约第 353-369 行),获取 project_name 后立即获取分支并构建标题:
       ```python
       # Get project name
       project_name = get_project_name()

       # Get git branch for title (WTREE-01)
       git_branch = get_git_branch()
       title = build_title(project_name, git_branch)
       ```

    4. 将所有 `send_pushover_notification(project_name, summary)` 和 `send_windows_notification(project_name, summary)` 改为使用 title:
       ```python
       futures[executor.submit(send_pushover_notification, title, summary)] = 'pushover'
       # ...
       futures[executor.submit(send_windows_notification, title, summary)] = 'windows'
       ```

    **修改 notify-attention.py:**

    1. 在 import 行 (第 21 行) 添加 `get_git_branch`:
       ```python
       from flags import check_notification_flags, get_project_name, get_git_branch
       ```

    2. 在 send_windows_notification() 之后 (约第 150 行) 新增辅助函数:
       ```python
       def build_attention_title(project_name, git_branch=None):
           """
           Build attention notification title with optional git branch.

           Args:
               project_name (str): Project name
               git_branch (str, optional): Git branch name

           Returns:
               str: Formatted title like [project:branch] Attention Needed or [project] Attention Needed
           """
           if git_branch:
               return f"[{project_name}:{git_branch}] Attention Needed"
           return f"[{project_name}] Attention Needed"
       ```

    3. 在 main() 函数中 (约第 181-187 行),获取 project_name 后获取分支并构建标题:
       当前代码:
       ```python
       # Get project name
       project_name = get_project_name()
       ...
       # Build notification content
       title = f"[{project_name}] Attention Needed"
       ```
       改为:
       ```python
       # Get project name
       project_name = get_project_name()

       # Get git branch for title (WTREE-01)
       git_branch = get_git_branch()
       ...
       # Build notification content
       title = build_attention_title(project_name, git_branch)
       ```

    4. session_id 部分 (WTREE-02): 确认第 171 行 `session_id = hook_input.get('session_id', 'unknown')` 和第 195 行 `message = f"Session: {session_id}\nType: {notification_type}\n{details}"` 已满足需求。如果格式没问题则无需修改。

    **Run full test suite:**
    ```bash
    python -m pytest plugins/claude-notify/tests/ -v
    ```
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills && python -m pytest plugins/claude-notify/tests/ -v</automated>
  </verify>
  <done>notify.py 和 notify-attention.py 的通知标题格式为 [project:branch] (有分支时) 和 [project] (无分支时); notify-attention.py 消息包含 session_id; 全部 85+ 测试通过; 新增 TestWorktreeTitleFormat 测试类覆盖标题格式和 session_id。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flags.get_git_branch() -> title string | git 分支名拼接到通知标题中 |
| hook_input JSON -> notify-attention.py | 外部 JSON 输入包含 session_id 和 message 字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering | Branch name injection in notification title | mitigate | 分支名仅用于 f-string 拼接到 title 参数,不传递给任何 shell 命令或 XML 解析。notify-attention.py 的 send_windows_notification 已使用 xml.sax.saxutils.escape() 防 XML 注入 (第 105-106 行) |
| T-31-05 | Tampering | Hook input JSON parsing | accept | json.loads() 解析失败时已 catch JSONDecodeError (第 224 行),脚本安全退出 |
| T-31-06 | Information Disclosure | session_id in notification | accept | session_id 是 Claude Code 生成的会话标识符,不包含认证令牌或敏感数据,仅发送到用户自己的 Pushover 设备和本地 Windows Toast |
</threat_model>

<verification>
1. 全部测试通过: `python -m pytest plugins/claude-notify/tests/ -v`
2. notify.py 包含 `from flags import.*get_git_branch` import
3. notify.py 包含 `def build_title` 函数
4. notify-attention.py 包含 `from flags import.*get_git_branch` import
5. notify-attention.py 包含 `def build_attention_title` 函数
6. 两个脚本在 git_branch=None 时退化为 [project] 格式
</verification>

<success_criteria>
- Stop hook 通知标题格式为 [project:branch] (有分支) 或 [project] (无分支)
- Attention hook 通知标题格式为 [project:branch] Attention Needed (有分支) 或 [project] Attention Needed (无分支)
- Attention hook 消息包含 session_id 字段
- 全部 85+ 测试通过 (含新增 WorktreeTitleFormat 测试)
</success_criteria>

<output>
After completion, create `.planning/phases/31-worktree/31-02-SUMMARY.md`
</output>
