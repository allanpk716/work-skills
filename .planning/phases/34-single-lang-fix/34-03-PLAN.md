---
phase: 34-single-lang-fix
plan: 03
type: execute
wave: 2
depends_on: ["34-01", "34-02"]
files_modified: []
autonomous: false
requirements:
  - FIX1-02
  - FIX1-03
user_setup: []
must_haves:
  truths:
    - "/codepoint:scan runs on go-calculator and identifies shared codepoints with multiple business flows"
    - "/codepoint:plan runs on go-calculator and produces a valid probe plan consumable by implement"
    - "/codepoint:implement runs on go-calculator and generates compilable probe code"
    - "Generated probe code compiles with go build and passes go vet"
    - "Stack output from running probes is correctly formatted and human-readable"
    - "Same verification performed on python-calculator (scan/plan/implement all work)"
  artifacts:
    - path: "tmp/go-calculator/"
      provides: "Go test project for skill validation"
      contains: "codepoint.go"
    - path: "tmp/python-calculator/"
      provides: "Python test project for skill validation"
      contains: "__init__.py"
  key_links:
    - from: "/codepoint:scan"
      to: "go-calculator source tree"
      via: "skill reads project files to identify shared codepoints"
    - from: "/codepoint:implement"
      to: "go-calculator source tree"
      via: "skill inserts probe code into project files"
    - from: "probe code"
      to: "go build / python -c"
      via: "generated code must compile/import correctly"
---

<objective>
Run the full /codepoint:scan, /codepoint:plan, and /codepoint:implement skill workflow on both go-calculator and python-calculator test projects to verify end-to-end correctness of the skill after defect fixes.

Per D-03: verify the complete skill workflow works correctly on the existing test projects.

Purpose: Confirm that the codepoint skill (scan -> plan -> implement) produces working output on both Go and Python projects after the Phase 34 fixes.
Output: Verified skill workflow on both test projects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-single-lang-fix/34-CONTEXT.md
@.planning/phases/34-single-lang-fix/34-DEFECTS.yaml (from Plan 34-01 — defect list to verify fixes against)

<interfaces>
From tmp/go-calculator/:
- `codepoint/codepoint.go` — Codepoint V2 base library with Frame type
- `internal/calculator/calculator.go` — Shared pipeline: Evaluate(ctx, expr)
- `internal/api/server.go` — REST handlers with existing probes
- `internal/batch/processor.go` — Batch processor with existing probes

From tmp/python-calculator/:
- `codepoint/__init__.py` — Codepoint V2 base library
- `calculator/calculator.py` — Shared pipeline: evaluate(expr)
- `api/server.py` — FastAPI handlers with existing probes
- `batch/processor.py` — Batch processor with existing probes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify codepoint skill workflow on go-calculator</name>
  <files>tmp/go-calculator/</files>
  <read_first>
    - .planning/phases/34-single-lang-fix/34-DEFECTS.yaml (verify against defect list)
    - .planning/phases/32-go-single-lang-calc/32-02-SUMMARY.md (prior scan results for comparison)
    - .planning/phases/32-go-single-lang-calc/32-03-SUMMARY.md (prior plan results for comparison)
    - .planning/phases/32-go-single-lang-calc/32-04-SUMMARY.md (prior implement results for comparison)
    - tmp/go-calculator/codepoint/codepoint.go (current state of base library)
  </read_first>
  <action>
Run the codepoint skill workflow on the go-calculator project to verify the full scan/plan/implement chain works correctly.

Step 1: Navigate to go-calculator and verify existing code compiles:
```bash
cd C:/WorkSpace/agent/work-skills/tmp/go-calculator
go build ./...
go vet ./...
go test ./... -count=1
```
All must pass.

Step 2: Run `/codepoint:scan` on the go-calculator project:
- Set CWD to `C:/WorkSpace/agent/work-skills/tmp/go-calculator`
- Invoke the scan skill to identify shared codepoints
- Verify the scan output identifies the 3 business flows (REST API, Batch, History)
- Verify the scan output identifies the shared computation path (calculator.Evaluate)

Step 3: Run `/codepoint:plan` on the go-calculator project:
- Use the scan output as input
- Verify the plan produces probe locations on the shared computation path
- Verify the plan output includes point_id and flow_id for V2 probes

Step 4: Run `/codepoint:implement` on the go-calculator project:
- Use the plan output as input
- Verify the generated probe code compiles: `go build ./...`
- Verify the generated probe code passes vet: `go vet ./...`
- Verify existing tests still pass: `go test ./... -count=1`

Step 5: Verify stack output quality:
- If probes were inserted, run the server briefly and trigger API requests
- Check that stack output in ~/.codepoint/ is correctly formatted JSON
- Verify the stack frames contain function names, file paths, and line numbers

Record results: For each step, note PASS/FAIL and any issues encountered. If any step fails, document the failure clearly.
  </action>
  <verify>
    <automated>cd C:/WorkSpace/agent/work-skills/tmp/go-calculator &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <done>
    - `/codepoint:scan` identifies 3 business flows and shared codepoints
    - `/codepoint:plan` produces valid probe plan with point_id and flow_id
    - `/codepoint:implement` generates compilable code
    - `go build`, `go vet`, `go test` all pass after implement
    - Stack output is correctly formatted and human-readable (if probes inserted)
    - Any issues documented with reproduction steps
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify codepoint skill workflow on python-calculator and human review</name>
  <files>tmp/python-calculator/</files>
  <read_first>
    - .planning/phases/34-single-lang-fix/34-DEFECTS.yaml (Python section confirms 0 defects)
    - .planning/phases/33-python-single-lang-calc/33-02-SUMMARY.md (prior scan/plan/implement results)
    - tmp/python-calculator/codepoint/__init__.py (current state of base library)
  </read_first>
  <action>
Step 1: Navigate to python-calculator and verify existing code imports correctly:
```bash
cd C:/WorkSpace/agent/work-skills/tmp/python-calculator
python -c "from codepoint import point, point_json, point_with_meta, analyze_overlap, is_enabled, collector; print('OK')"
python -m pytest tests/ -v --tb=short
```
Both must pass.

Step 2: Run `/codepoint:scan` on the python-calculator project:
- Set CWD to `C:/WorkSpace/agent/work-skills/tmp/python-calculator`
- Invoke the scan skill
- Verify scan identifies 3 business flows (REST API, Batch, History)
- Verify scan identifies shared computation path (calculator.evaluate)

Step 3: Run `/codepoint:plan` on the python-calculator project:
- Use scan output as input
- Verify plan produces probe locations with point_id and flow_id

Step 4: Run `/codepoint:implement` on the python-calculator project:
- Use plan output as input
- Verify generated code passes import check: `python -c "from codepoint import ..."`
- Verify existing tests pass: `python -m pytest tests/ -v`

Record results for each step.

Step 5: Present the full Phase 34 verification summary to the user for review:
- Go workflow: scan/plan/implement results (PASS/FAIL)
- Python workflow: scan/plan/implement results (PASS/FAIL)
- DEFECTS.yaml status: all 4 defects confirmed fixed
- Template compile checks: both golang.md and python.md verified
- Any remaining issues or recommendations
  </action>
  <what-built>
    - Go codepoint skill workflow verified end-to-end (Task 1)
    - Python codepoint skill workflow verified end-to-end (Task 2)
    - Full Phase 34 verification summary compiled
  </what-built>
  <how-to-verify>
    1. Review the Go workflow results: were scan, plan, and implement all successful?
    2. Review the Python workflow results: were scan, plan, and implement all successful?
    3. Confirm the DEFECTS.yaml (from Plan 34-01) correctly reflects the 4 defects found
    4. Confirm golang.md and python.md (from Plan 34-02) include compile check instructions
    5. Verify that `go test ./...` and `python -m pytest tests/ -v` both pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
  <done>
    - Python `/codepoint:scan` identifies business flows and shared codepoints
    - Python `/codepoint:plan` produces valid probe plan
    - Python `/codepoint:implement` generates runnable code
    - Python tests pass after implement
    - Human has reviewed and approved the full Phase 34 verification summary
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codepoint skill -> project files | Skill writes probe code into project source files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | I | codepoint:implement generated code | mitigate | Verify `go build`/`python -c` passes after implement; existing tests must not regress |
| T-34-02 | D | probe output in ~/.codepoint/ | accept | Stack traces contain file paths and function names (no secrets/PII in test project) |
</threat_model>

<verification>
- Go: `go build ./...`, `go vet ./...`, `go test ./... -count=1` all pass
- Python: `python -c "from codepoint import ..."` passes, `pytest tests/ -v` passes
- Scan output identifies shared codepoints on both projects
- Plan output includes point_id and flow_id
- Implement output compiles/runs
- Human review checkpoint passed
- FIX1-02 fully satisfied: all defects fixed and re-verified
- FIX1-03 fully satisfied: templates generate compilable/runnable code
</verification>

<success_criteria>
- Both Go and Python codepoint skill workflows complete successfully
- No regressions in existing test suites
- Stack output is correctly formatted and readable
- Human confirms Phase 34 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/34-single-lang-fix/34-03-SUMMARY.md`
</output>
