---
phase: 02-configuration-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .claude/hooks/notify.py
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04

must_haves:
  truths:
    - "All errors and warnings are written to log files"
    - "Log files are named with date and PID for concurrent safety"
    - "Old log files (>5 days) are automatically cleaned up"
    - "Diagnostic mode shows environment variables, API status, and configuration"
    - "Running --diagnose sends a test notification to verify connectivity"
  artifacts:
    - path: ".claude/hooks/notify.py"
      provides: "Logging infrastructure and diagnostic tooling"
      contains:
        - "cleanup_old_logs"
        - "argparse.ArgumentParser"
        - "--diagnose"
        - "debug-{date}-{pid}.log"
  key_links:
    - from: "notify.py main()"
      to: "log cleanup"
      via: "cleanup_old_logs() call"
      pattern: "cleanup_old_logs\\(.*log_dir"
    - from: "--diagnose flag"
      to: "diagnostic checks"
      via: "argparse"
      pattern: "args\\.diagnose"
---

<objective>
Add automatic log cleanup and diagnostic tooling to verify configuration and API connectivity.

Purpose: Keep log files manageable by auto-deleting old logs, and provide users with a diagnostic tool to verify their setup.
Output: Enhanced notify.py with log cleanup and --diagnose mode.
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-diagnostics/02-CONTEXT.md
@.planning/phases/02-configuration-diagnostics/02-RESEARCH.md
@.claude/hooks/notify.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add automatic log file cleanup functionality</name>
  <files>.claude/hooks/notify.py</files>
  <action>
Add a function to automatically delete log files older than 5 days and integrate it into the main script execution.

**Current state:** Lines 21-23 create log files with date and PID naming (already satisfies LOG-02).

**Implementation:**

1. Add log cleanup function after `send_windows_notification()` (around line 199):
```python
def cleanup_old_logs(log_dir, days_to_keep=5):
    """
    Clean up log files older than specified days.

    Args:
        log_dir (Path): Log directory path
        days_to_keep (int): Number of days to keep logs (default: 5)
    """
    import time

    if not log_dir.exists():
        logger.debug(f"Log directory does not exist: {log_dir}")
        return

    # Calculate cutoff timestamp
    cutoff_timestamp = time.time() - (days_to_keep * 86400)  # 86400 seconds/day

    logger.info(f"Cleaning up log files older than {days_to_keep} days")

    deleted_count = 0
    kept_count = 0

    # Delete old log files matching pattern
    for log_file in log_dir.glob('claude-notify-*.log'):
        if log_file.is_file():
            file_mtime = log_file.stat().st_mtime

            if file_mtime < cutoff_timestamp:
                try:
                    log_file.unlink()
                    logger.debug(f"Deleted old log file: {log_file.name}")
                    deleted_count += 1
                except Exception as e:
                    logger.error(f"Failed to delete {log_file.name}: {e}")
            else:
                kept_count += 1

    logger.info(f"Log cleanup complete: {deleted_count} deleted, {kept_count} kept")
```

2. Call cleanup in `main()` at the start (around line 209):
```python
logger.info("=== Claude Code Notification Script Started ===")

# Clean up old log files
cleanup_old_logs(log_dir, days_to_keep=5)
```

**Critical:**
- Use `time.time()` and file's `st_mtime` (both are timestamps, not datetime objects)
- Use `log_file.unlink()` to delete (not `os.remove()`, pathlib is cleaner)
- Wrap deletion in try/except to prevent cleanup failures from breaking notifications
- Log cleanup runs BEFORE notifications (not after, to avoid timing issues)
</action>
  <verify>
```xml
<automated>python -c "from pathlib import Path; import time; p = Path('tmp/test-old.log'); p.touch(); import os; os.utime(p, (time.time() - 6*86400, time.time() - 6*86400))" &amp;&amp; python -c "import sys; sys.path.insert(0, '.claude/hooks'); import notify; notify.cleanup_old_logs(Path('tmp'), 5)" 2>&amp;1 | grep -i "cleanup"</automated>
<manual>Create a log file older than 5 days and verify it gets deleted</manual>
<sampling_rate>run after this task commits</sampling_rate>
</verify>
  <done>Log files older than 5 days are automatically deleted on each run. Recent logs are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add diagnostic mode with --diagnose flag</name>
  <files>.claude/hooks/notify.py</files>
  <action>
Add argparse support for --diagnose flag and create diagnostic function to verify configuration and API connectivity.

**Implementation:**

1. Add import at top (around line 15):
```python
import argparse
```

2. Add diagnostic function after `cleanup_old_logs()` (around line 250):
```python
def diagnose_configuration():
    """
    Run configuration diagnostics to verify setup.
    """
    print("=" * 60)
    print("Claude Notify - Configuration Diagnostics")
    print("=" * 60)

    # 1. Environment Variables
    print("\n[1] Environment Variables")
    token = os.environ.get('PUSHOVER_TOKEN')
    user = os.environ.get('PUSHOVER_USER')

    if token:
        # Show first 4 and last 4 chars for confirmation
        if len(token) >= 10:
            masked = f"{token[:4]}...{token[-4:]}"
        else:
            masked = f"{token[:2]}...{token[-2:]}" if len(token) >= 4 else "****"
        print(f"  PUSHOVER_TOKEN: {masked} (length: {len(token)})")
    else:
        print("  PUSHOVER_TOKEN: NOT SET")

    if user:
        if len(user) >= 10:
            masked = f"{user[:4]}...{user[-4:]}"
        else:
            masked = f"{user[:2]}...{user[-2:]}" if len(user) >= 4 else "****"
        print(f"  PUSHOVER_USER: {masked} (length: {len(user)})")
    else:
        print("  PUSHOVER_USER: NOT SET")

    # 2. Project Configuration Files
    print("\n[2] Project Configuration Files")
    project_dir = Path.cwd()
    no_pushover = project_dir / '.no-pushover'
    no_windows = project_dir / '.no-windows'

    if no_pushover.is_file():
        print("  .no-pushover: FOUND (Pushover notifications disabled)")
    else:
        print("  .no-pushover: Not found (Pushover enabled)")

    if no_windows.is_file():
        print("  .no-windows: FOUND (Windows notifications disabled)")
    else:
        print("  .no-windows: Not found (Windows enabled)")

    # 3. Log Files
    print("\n[3] Log Files")
    if log_dir.exists():
        log_files = list(log_dir.glob('claude-notify-*.log'))
        print(f"  Log directory: {log_dir}")
        print(f"  Total log files: {len(log_files)}")
        if log_files:
            latest = max(log_files, key=lambda f: f.stat().st_mtime)
            print(f"  Latest log: {latest.name}")
    else:
        print(f"  Log directory does not exist: {log_dir}")

    # 4. Pushover API Connection Test
    print("\n[4] Pushover API Connection Test")
    if token and user:
        try:
            print("  Sending test notification...")
            response = requests.post(
                'https://api.pushover.net/1/messages.json',
                data={
                    'token': token,
                    'user': user,
                    'title': 'Claude Notify Diagnostics',
                    'message': 'Test notification from Claude Code',
                    'priority': 0
                },
                timeout=5
            )
            if response.status_code == 200:
                print("  Status: SUCCESS - Test notification sent")
                print("  Check your device for the test notification")
            else:
                print(f"  Status: FAILED - HTTP {response.status_code}")
                print(f"  Error: {response.text}")
        except requests.Timeout:
            print("  Status: FAILED - Connection timeout")
        except Exception as e:
            print(f"  Status: FAILED - {e}")
    else:
        print("  Status: SKIPPED - Credentials not configured")

    print("\n" + "=" * 60)
    print("Diagnostics complete.")
    print("=" * 60)
```

3. Modify `main()` function to support argparse (replace entire main function):
```python
def main():
    """
    Main function: Parse arguments and run in diagnose or notify mode.
    """
    parser = argparse.ArgumentParser(
        description='Claude Code Notification Script'
    )
    parser.add_argument(
        '--diagnose', '-d',
        action='store_true',
        help='Run configuration diagnostics and exit'
    )
    args = parser.parse_args()

    # Diagnostic mode
    if args.diagnose:
        diagnose_configuration()
        return 0

    # Normal notification mode
    logger.info("=== Claude Code Notification Script Started ===")

    try:
        # Clean up old log files
        cleanup_old_logs(log_dir, days_to_keep=5)

        # Get project name
        project_name = get_project_name()

        # Get Claude CLI summary
        summary = get_claude_summary(project_name)

        logger.info(f"Final summary: {summary}")

        # Check project-level notification flags
        flags = check_notification_flags()

        # Send notifications in parallel
        with ThreadPoolExecutor(max_workers=2) as executor:
            futures = {}

            # Only submit Pushover if not disabled
            if not flags['pushover_disabled']:
                futures[executor.submit(send_pushover_notification, project_name, summary)] = 'pushover'

            # Only submit Windows if not disabled
            if not flags['windows_disabled']:
                futures[executor.submit(send_windows_notification, project_name, summary)] = 'windows'

            # If both disabled, log and exit
            if not futures:
                logger.info("All notifications disabled by project flags")
                return 0

            # Wait for all notifications with overall timeout
            completed = 0
            failed = 0

            for future in as_completed(futures, timeout=4):
                channel = futures[future]
                try:
                    result = future.result()
                    if result:
                        logger.info(f"{channel} notification succeeded")
                        completed += 1
                    else:
                        logger.warning(f"{channel} notification failed")
                        failed += 1
                except Exception as e:
                    logger.error(f"{channel} notification exception: {e}")
                    failed += 1

        logger.info(f"Notifications completed: {completed}, failed: {failed}")
        logger.info("=== Claude Code Notification Script Finished ===")

        return 0

    except Exception as e:
        logger.error(f"Script failed: {e}")
        logger.info("=== Claude Code Notification Script Finished (with errors) ===")
        return 1
```

**Critical:**
- Use `argparse` for --diagnose flag (standard library, clean API)
- Show masked credentials (first 4 + last 4 chars) for security
- Test Pushover API with actual notification (user can verify on device)
- Diagnose mode does NOT run normal notification logic
- Exit cleanly after diagnostics
</action>
  <verify>
```xml
<automated>python .claude/hooks/notify.py --diagnose 2>&amp;1 | grep -c "\[.\]"</automated>
<manual>Run python .claude/hooks/notify.py --diagnose and verify 4 diagnostic sections appear</manual>
<sampling_rate>run after this task commits</sampling_rate>
</verify>
  <done>Running `python .claude/hooks/notify.py --diagnose` shows environment variables, project config, log status, and tests Pushover API connectivity.</done>
</task>

<task type="auto">
  <name>Task 3: Test logging and diagnostic functionality</name>
  <files>.claude/hooks/notify.py</files>
  <action>
Create a test script to verify log cleanup and diagnostic mode work correctly.

**Create:** `tmp/test-02-02-logging-diagnostics.py`

```python
#!/usr/bin/env python3
"""Test script for Phase 2 logging and diagnostics features."""

import os
import sys
import time
import tempfile
from pathlib import Path
from datetime import datetime

# Add parent directory to path for imports
sys.path.insert(0, str(Path(__file__).parent.parent / '.claude' / 'hooks'))

def test_log_cleanup():
    """Test LOG-03: Automatic log cleanup."""
    print("\n[TEST 1] Log File Cleanup")

    with tempfile.TemporaryDirectory() as tmpdir:
        tmpdir = Path(tmpdir)

        # Create old log file (6 days old)
        old_log = tmpdir / 'claude-notify-20260218-12345.log'
        old_log.touch()
        # Set modification time to 6 days ago
        old_time = time.time() - (6 * 86400)
        os.utime(old_log, (old_time, old_time))

        # Create recent log file (1 day old)
        recent_log = tmpdir / 'claude-notify-20260223-67890.log'
        recent_log.touch()
        recent_time = time.time() - (1 * 86400)
        os.utime(recent_log, (recent_time, recent_time))

        import notify
        notify.cleanup_old_logs(tmpdir, days_to_keep=5)

        # Old file should be deleted
        assert not old_log.exists(), "Old log file should be deleted"
        print("  ✓ Old log file (6 days) deleted")

        # Recent file should be kept
        assert recent_log.exists(), "Recent log file should be kept"
        print("  ✓ Recent log file (1 day) kept")

def test_diagnostic_mode():
    """Test LOG-04: Diagnostic mode."""
    print("\n[TEST 2] Diagnostic Mode")

    # Save current state
    old_token = os.environ.get('PUSHOVER_TOKEN')
    old_user = os.environ.get('PUSHOVER_USER')

    try:
        # Test with credentials set
        os.environ['PUSHOVER_TOKEN'] = 'test_token_12345678'
        os.environ['PUSHOVER_USER'] = 'test_user_12345678'

        import subprocess
        result = subprocess.run(
            [sys.executable, '.claude/hooks/notify.py', '--diagnose'],
            capture_output=True,
            text=True,
            timeout=10
        )

        output = result.stdout

        # Check for required sections
        assert '[1] Environment Variables' in output, "Should show environment variables section"
        print("  ✓ Environment variables section present")

        assert 'PUSHOVER_TOKEN: test...5678' in output, "Should show masked token"
        print("  ✓ Token masking works correctly")

        assert '[2] Project Configuration Files' in output, "Should show project config section"
        print("  ✓ Project configuration section present")

        assert '[3] Log Files' in output, "Should show log files section"
        print("  ✓ Log files section present")

        assert '[4] Pushover API Connection Test' in output, "Should show API test section"
        print("  ✓ API connection test section present")

        # Test --help flag
        result = subprocess.run(
            [sys.executable, '.claude/hooks/notify.py', '--help'],
            capture_output=True,
            text=True
        )
        assert '--diagnose' in result.stdout, "Help should show --diagnose flag"
        print("  ✓ --diagnose flag documented in help")

    finally:
        # Restore environment
        if old_token:
            os.environ['PUSHOVER_TOKEN'] = old_token
        else:
            if 'PUSHOVER_TOKEN' in os.environ:
                del os.environ['PUSHOVER_TOKEN']

        if old_user:
            os.environ['PUSHOVER_USER'] = old_user
        else:
            if 'PUSHOVER_USER' in os.environ:
                del os.environ['PUSHOVER_USER']

if __name__ == '__main__':
    print("=" * 60)
    print("Phase 2 - Logging & Diagnostics Tests")
    print("=" * 60)

    test_log_cleanup()
    test_diagnostic_mode()

    print("\n" + "=" * 60)
    print("All tests passed!")
    print("=" * 60)
```

**Run the test:**
```bash
python tmp/test-02-02-logging-diagnostics.py
```

**Expected output:** All tests pass with ✓ marks.
</action>
  <verify>
```xml
<automated>python tmp/test-02-02-logging-diagnostics.py 2>&amp;1 | grep -c "✓"</automated>
<manual>Run test script and verify 7 checkmarks appear</manual>
<sampling_rate>run after this task commits</sampling_rate>
</verify>
  <done>Test script confirms log cleanup removes old files and diagnostic mode provides comprehensive configuration information.</done>
</task>

</tasks>

<verification>
**LOG-01 Verification:**
- [ ] All errors use `logger.error()`
- [ ] All warnings use `logger.warning()`
- [ ] Log files are created in `%APPDATA%\claude-notify\logs\`

**LOG-02 Verification:**
- [ ] Log files follow pattern: `claude-notify-YYYYMMDD-{pid}.log`
- [ ] Concurrent runs create separate log files with different PIDs

**LOG-03 Verification:**
- [ ] Log cleanup runs on every script execution
- [ ] Files older than 5 days are deleted
- [ ] Recent files are preserved

**LOG-04 Verification:**
- [ ] `--diagnose` flag runs diagnostic mode
- [ ] Shows environment variables (masked)
- [ ] Shows project configuration files
- [ ] Shows log file status
- [ ] Tests Pushover API connectivity
</verification>

<success_criteria>
1. Log files older than 5 days are automatically cleaned up
2. Recent log files are preserved
3. Running `--diagnose` shows all 4 diagnostic sections
4. Credentials are properly masked in diagnostic output
5. Diagnostic mode sends test notification to verify connectivity
6. Test script passes all 7 test cases
7. All functionality maintains 5-second timeout for normal mode
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-diagnostics/02-02-SUMMARY.md`
</output>
