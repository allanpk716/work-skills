---
phase: 15-environment-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - installer/src/detectors/python.js
  - installer/src/detectors/git.js
  - installer/tests/detectors/python.test.js
  - installer/tests/detectors/git.test.js
  - installer/src/i18n/en.json
  - installer/src/i18n/zh.json
autonomous: true
requirements:
  - ENV-01
  - ENV-02

must_haves:
  truths:
    - "User can see Python 3.8+ detection status with version number"
    - "User can see Git detection status with version number"
    - "Detection shows [OK] for installed dependencies"
    - "Detection shows [FAIL] with guidance for missing dependencies"
  artifacts:
    - path: "installer/src/detectors/python.js"
      provides: "Python version detection"
      exports: ["detectPython"]
    - path: "installer/src/detectors/git.js"
      provides: "Git version detection"
      exports: ["detectGit"]
    - path: "installer/tests/detectors/python.test.js"
      provides: "Python detector unit tests"
    - path: "installer/tests/detectors/git.test.js"
      provides: "Git detector unit tests"
  key_links:
    - from: "installer/src/detectors/python.js"
      to: "execa"
      via: "execa('python', ['--version'])"
      pattern: "execa\\(.*python.*--version"
    - from: "installer/src/detectors/git.js"
      to: "execa"
      via: "execa('git', ['--version'])"
      pattern: "execa\\(.*git.*--version"
---

<objective>
Implement Python 3.8+ and Git version detection modules with bilingual support.

Purpose: Enable environment detection for core dependencies before proceeding with installation.
Output: Working detector modules with unit tests and i18n support.
</objective>

<execution_context>
@C:/Users/allan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-environment-detection/15-RESEARCH.md
@.planning/phases/15-environment-detection/15-VALIDATION.md

<interfaces>
<!-- Key interfaces from existing codebase -->

From installer/src/platform.js:
```javascript
function isWindows() { return process.platform === 'win32'; }
function checkPlatform() { /* exits if not Windows */ }
module.exports = { isWindows, checkPlatform };
```

From installer/src/i18n/index.js:
```javascript
function t(key) { /* returns translated string */ }
function getLanguage() { /* returns 'en' or 'zh' */ }
function setLanguage(lang) { /* sets current language */ }
module.exports = { t, detectLanguage, getLanguage, setLanguage };
```

From installer/src/i18n/en.json:
```json
{
  "welcome.title": "Work Skills Setup",
  "error.windowsOnly": "This installer is designed for Windows only."
}
```

Expected detector return type (from RESEARCH.md):
```javascript
{
  name: string,           // Display name (e.g., "Python")
  installed: boolean,     // Whether the tool is installed
  version: string | null, // Version string (e.g., "3.11.0")
  meetsMinimum: boolean,  // Whether version meets minimum requirements
  command: string | null, // The command that worked (e.g., "python", "py")
  guidance: string        // i18n key for installation guidance
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install execa dependency</name>
  <files>installer/package.json</files>
  <read_first>
    - C:\WorkSpace\work-skills\installer\package.json
  </read_first>
  <behavior>
    - Test: package.json contains execa in dependencies
    - Test: npm list execa in installer/ directory succeeds
  </behavior>
  <action>
    Install execa package for command execution with better Windows support.

    Run the following command:
    ```bash
    cd installer && npm install execa
    ```

    Verify execa version 9.x is installed (the latest stable version).
  </action>
  <verify>
    <automated>cd installer && npm list execa | grep -q "execa@"</automated>
  </verify>
  <done>
    - package.json contains execa in dependencies
    - npm list execa shows installed version
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Python detector with tests</name>
  <files>installer/src/detectors/python.js, installer/tests/detectors/python.test.js</files>
  <read_first>
    - C:\WorkSpace\work-skills\installer\src\platform.js
    - C:\WorkSpace\work-skills\installer\tests\platform.test.js
    - C:\WorkSpace\work-skills\.planning\phases\15-environment-detection\15-RESEARCH.md
  </read_first>
  <behavior>
    - Test: detectPython returns installed: true when Python 3.8+ is available
    - Test: detectPython returns installed: false when Python is not found
    - Test: detectPython returns meetsMinimum: false for Python < 3.8
    - Test: detectPython returns meetsMinimum: true for Python >= 3.8
    - Test: detectPython tries multiple commands (python, python3, py)
  </behavior>
  <action>
    Create Python version detector module following RESEARCH.md Pattern 1.

    Create installer/src/detectors/python.js with:
    1. Import execa: `const { execa } = require('execa');`
    2. Export detectPython async function
    3. Try commands in order: ['python', 'python3', 'py'] with ['--version'] argument
    4. Parse version from stdout using regex: `/Python (\d+\.\d+\.\d+)/i`
    5. Check minimum version: major > 3 OR (major === 3 AND minor >= 8)
    6. Return object with: name, installed, version, meetsMinimum, command, guidance
    7. Use guidance key: 'guidance.installPython'

    Create installer/tests/detectors/python.test.js with:
    1. Mock execa to simulate Python found/not found scenarios
    2. Test successful detection with version 3.11.0 (meets minimum)
    3. Test successful detection with version 3.7.0 (below minimum)
    4. Test detection failure (ENOENT error)
    5. Test fallback to alternative commands

    Follow existing test patterns from platform.test.js.
  </action>
  <verify>
    <automated>cd installer && npm test -- --testPathPattern=python.test.js 2>&1 | grep -q "passed"</automated>
  </verify>
  <done>
    - installer/src/detectors/python.js exists with detectPython export
    - installer/tests/detectors/python.test.js exists
    - All tests pass: npm test -- --testPathPattern=python.test.js
    - detectPython returns correct structure for all scenarios
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Git detector with tests</name>
  <files>installer/src/detectors/git.js, installer/tests/detectors/git.test.js</files>
  <read_first>
    - C:\WorkSpace\work-skills\installer\src\detectors\python.js
    - C:\WorkSpace\work-skills\installer\tests\detectors\python.test.js
    - C:\WorkSpace\work-skills\.planning\phases\15-environment-detection\15-RESEARCH.md
  </read_first>
  <behavior>
    - Test: detectGit returns installed: true when Git is available
    - Test: detectGit returns installed: false when Git is not found
    - Test: detectGit extracts version from "git version 2.43.0.windows.1" format
    - Test: detectGit returns meetsMinimum: true (any version is acceptable)
  </behavior>
  <action>
    Create Git version detector module following RESEARCH.md Pattern 1.

    Create installer/src/detectors/git.js with:
    1. Import execa: `const { execa } = require('execa');`
    2. Export detectGit async function
    3. Run: `execa('git', ['--version'])`
    4. Parse version from stdout using regex: `/git version (\d+\.\d+\.\d+)/i`
       - Note: Windows Git outputs "git version 2.43.0.windows.1", extract "2.43.0"
    5. Git has no minimum version requirement, so meetsMinimum is always true if installed
    6. Return object with: name, installed, version, meetsMinimum, guidance
    7. Use guidance key: 'guidance.installGit'

    Create installer/tests/detectors/git.test.js with:
    1. Mock execa to simulate Git found/not found scenarios
    2. Test successful detection with Windows format version
    3. Test detection failure (ENOENT error)

    Follow patterns from python.test.js.
  </action>
  <verify>
    <automated>cd installer && npm test -- --testPathPattern=git.test.js 2>&1 | grep -q "passed"</automated>
  </verify>
  <done>
    - installer/src/detectors/git.js exists with detectGit export
    - installer/tests/detectors/git.test.js exists
    - All tests pass: npm test -- --testPathPattern=git.test.js
    - detectGit returns correct structure for all scenarios
  </done>
</task>

<task type="auto">
  <name>Task 4: Add detection i18n keys</name>
  <files>installer/src/i18n/en.json, installer/src/i18n/zh.json</files>
  <read_first>
    - C:\WorkSpace\work-skills\installer\src\i18n\en.json
    - C:\WorkSpace\work-skills\installer\src\i18n\zh.json
  </read_first>
  <action>
    Add translation keys for detection messages to both language files.

    Add to installer/src/i18n/en.json:
    ```json
    "detection.title": "Environment Detection",
    "detection.checking": "Checking environment dependencies...",
    "detection.summary": "Detection complete: {passed}/{total} passed",
    "guidance.installPython": "Install Python 3.8+ from https://www.python.org/downloads/",
    "guidance.installGit": "Install Git from https://git-scm.com/download/win"
    ```

    Add to installer/src/i18n/zh.json:
    ```json
    "detection.title": "环境检测",
    "detection.checking": "正在检查环境依赖...",
    "detection.summary": "检测完成: {passed}/{total} 项通过",
    "guidance.installPython": "从 https://www.python.org/downloads/ 安装 Python 3.8+",
    "guidance.installGit": "从 https://git-scm.com/download/win 安装 Git"
    ```

    Ensure valid JSON format (trailing commas are not allowed).
  </action>
  <verify>
    <automated>cd installer && node -e "require('./src/i18n/en.json'); require('./src/i18n/zh.json'); console.log('JSON valid')" 2>&1 | grep -q "JSON valid"</automated>
  </verify>
  <done>
    - en.json contains all detection.* and guidance.* keys
    - zh.json contains all detection.* and guidance.* keys with Chinese translations
    - Both files are valid JSON
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `cd installer && npm test` - all tests should pass
2. Verify detector modules export correct functions
3. Verify i18n keys are accessible via t() function
</verification>

<success_criteria>
- Python detector detects version and checks minimum (3.8+)
- Git detector detects version (any version acceptable)
- All detectors handle errors gracefully
- Bilingual support for all messages
- Unit tests pass for all detectors
</success_criteria>

<output>
After completion, create `.planning/phases/15-environment-detection/15-01-SUMMARY.md`
</output>
