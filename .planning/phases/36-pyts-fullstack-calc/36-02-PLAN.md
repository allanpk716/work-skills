---
phase: 36-pyts-fullstack-calc
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - tmp/pyts-calculator/tests/test_linkage.py
  - tmp/pyts-calculator/tests/test_toggle.py
  - tmp/pyts-calculator/tests/conftest.py
autonomous: true
requirements: [FULL-06, FULL-07]

must_haves:
  truths:
    - "collector 端点正确收集前端 TS 探针数据，写入 cp-ts-*.log"
    - "Python 后端探针写入 cp-python-*.log，包含 flow_id"
    - "同一跨语言代码点在不同业务流程下输出不同的堆栈和 flow_id"
    - "Toggle 四组合独立控制：两个都开/只Python/只TS/两个都关，日志输出正确"
    - "Toggle 切换后重启服务立即生效"
  artifacts:
    - path: "tmp/pyts-calculator/tests/conftest.py"
      provides: "Server process management fixtures with PID tracking, readiness polling, and Windows cleanup"
      contains: ["start_server", "stop_server", "wait_for_server"]
    - path: "tmp/pyts-calculator/tests/test_linkage.py"
      provides: "跨语言联动集成测试（collector + 多流程堆栈 + flow_id 关联）"
      contains: ["test_collector_receives_probe", "test_multi_flow_stack_differentiation"]
    - path: "tmp/pyts-calculator/tests/test_toggle.py"
      provides: "Toggle 四组合独立控制验证测试"
      contains: ["test_toggle_combinations"]
    - path: "~/.codepoint/pyts-calculator/cp-python-*.log"
      provides: "Python 后端探针日志（三业务流）"
    - path: "~/.codepoint/pyts-calculator/cp-ts-*.log"
      provides: "前端 TS 探针日志（通过 collector）"
  key_links:
    - from: "Browser probe (codepoint.ts)"
      to: "/__codepoint__"
      via: "fetch POST"
      pattern: "sendToCollector"
    - from: "/__codepoint__"
      to: "cp-ts-*.log"
      via: "collector.receive()"
      pattern: "receive(entry)"
    - from: "api/server.py routes"
      to: "cp-python-*.log"
      via: "point_json()"
      pattern: "point_json"
    - from: "Toggle .codepoint-python"
      to: "cp-python-*.log output"
      via: "codepoint/__init__.py _enabled"
      pattern: "_enabled: bool"
    - from: "Toggle .codepoint-ts"
      to: "cp-ts-*.log output"
      via: "collector.py _ts_enabled"
      pattern: "_ts_enabled: bool"
---

<objective>
验证跨语言探针联动（前端 TS 探针通过 Python collector 收集）和 Toggle 四组合独立控制。

Purpose: 确认 codepoint 技能在 Python+TS 全栈环境下的跨语言联动正确性，验证 Toggle 机制前后端独立控制（per D-05）。Addresses review concerns: Windows process management, server readiness polling, collector payload specification, cleanup on failure, test file separation.

Output: 集成测试通过，三业务流的跨语言日志确认 flow_id 关联，Toggle 四组合验证完成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-pyts-fullstack-calc/36-CONTEXT.md
@.planning/phases/36-pyts-fullstack-calc/36-RESEARCH.md
@.planning/phases/36-pyts-fullstack-calc/36-01-SUMMARY.md

<interfaces>
<!-- From Task 36-01 outputs -->

From tmp/pyts-calculator/api/server.py:
```python
# FastAPI app with routes:
# POST /__codepoint__ -- collector endpoint
# POST /api/calculate -- flow_id="flow-api-calculate"
# GET /api/history -- list all
# GET /api/history/{id} -- flow_id="flow-history-query", recomputes
# POST /api/batch -- flow_id="flow-batch-process"
# StaticFiles mount at / (last)
```

From tmp/pyts-calculator/codepoint/__init__.py:
```python
_toggle_path: Path = Path.home() / ".codepoint" / ".codepoint-python"
_enabled: bool = _toggle_path.exists()
def point_json(name: str, meta: Optional[dict[str, Any]] = None) -> None:
```

From tmp/pyts-calculator/codepoint/collector.py:
```python
_ts_toggle: Path = Path.home() / ".codepoint" / ".codepoint-ts"
_ts_enabled: bool = _ts_toggle.exists()
def is_ts_enabled() -> bool:
def receive(entry: dict[str, Any]) -> bool:
def close_ts_collector() -> None:
```

From tmp/pyts-calculator/calculator/core.py:
```python
def evaluate(expr: str, flow_id: str = "") -> Tuple[str, str]:
    # 4 probe points: cp-calc-parse, cp-calc-validate, cp-calc-compute, cp-calc-format
```

Toggle file paths:
- .codepoint-python -> controls cp-python-*.log output
- .codepoint-ts -> controls cp-ts-*.log output (via collector)
- Both in ~/.codepoint/ directory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建服务器管理基础设施 + 跨语言联动测试</name>
  <files>
    tmp/pyts-calculator/tests/conftest.py
    tmp/pyts-calculator/tests/test_linkage.py
  </files>
  <read_first>
    tmp/pyts-calculator/api/server.py
    tmp/pyts-calculator/codepoint/__init__.py
    tmp/pyts-calculator/codepoint/collector.py
    tmp/pyts-calculator/calculator/core.py
    tmp/pyts-calculator/history/store.py
    tmp/pyts-calculator/main.py
    tmp/pyts-calculator/frontend/src/lib/codepoint.ts
    .planning/phases/35-gojs-fullstack-calc/35-04-SUMMARY.md
  </read_first>
  <action>
    This task addresses review concern HIGH (Windows process management), MEDIUM (server readiness polling), and MEDIUM (collector payload specification). It creates the shared server management infrastructure and the cross-language linkage tests.

    **Part A: Create tests/conftest.py -- Server Process Management Fixtures**

    Create a conftest.py with reusable fixtures that solve the Windows process management problem (addresses review HIGH concern). The fixtures must include:

    ```python
    """Shared test fixtures for pyts-calculator integration tests.
    
    Provides reliable server start/stop on Windows with PID tracking,
    readiness polling, and guaranteed cleanup via try/finally.
    """
    import os
    import signal
    import subprocess
    import time
    import pytest
    import urllib.request
    import urllib.error
    from pathlib import Path

    BASE_URL = "http://localhost:18091"
    PROJECT_DIR = Path(__file__).parent.parent
    CODEPOINT_DIR = Path.home() / ".codepoint"
    PYTHON_TOGGLE = CODEPOINT_DIR / ".codepoint-python"
    TS_TOGGLE = CODEPOINT_DIR / ".codepoint-ts"
    LOG_DIR = CODEPOINT_DIR / "pyts-calculator"

    def wait_for_server(url: str = BASE_URL, timeout: float = 15.0, interval: float = 0.5) -> bool:
        """Poll server until it responds. Returns True if server ready, False on timeout.
        
        Addresses review concern: no timing/retry logic for server readiness.
        Polls /api/history (lightweight GET endpoint) until it returns 200.
        """
        deadline = time.time() + timeout
        while time.time() < deadline:
            try:
                req = urllib.request.Request(f"{url}/api/history")
                with urllib.request.urlopen(req, timeout=2) as resp:
                    if resp.status == 200:
                        return True
            except (urllib.error.URLError, ConnectionRefusedError, OSError):
                pass
            time.sleep(interval)
        return False

    def start_server(toggle_python: bool = True, toggle_ts: bool = True) -> subprocess.Popen:
        """Start FastAPI server with specified toggle configuration.
        
        Addresses review concern: Windows process management.
        - Sets toggle files BEFORE starting process (module-level toggle check)
        - Starts uvicorn as subprocess
        - Captures PID for reliable cleanup
        - Polls for readiness before returning
        
        Args:
            toggle_python: Create .codepoint-python toggle file
            toggle_ts: Create .codepoint-ts toggle file
            
        Returns:
            subprocess.Popen with the server process
        """
        # Set toggle state BEFORE starting server
        CODEPOINT_DIR.mkdir(parents=True, exist_ok=True)
        if toggle_python:
            PYTHON_TOGGLE.touch()
        else:
            PYTHON_TOGGLE.unlink(missing_ok=True)
        if toggle_ts:
            TS_TOGGLE.touch()
        else:
            TS_TOGGLE.unlink(missing_ok=True)

        # Clean previous log files
        if LOG_DIR.exists():
            for f in LOG_DIR.glob("cp-*.log"):
                f.unlink()

        proc = subprocess.Popen(
            ["python", "main.py"],
            cwd=str(PROJECT_DIR),
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE,
            creationflags=subprocess.CREATE_NEW_PROCESS_GROUP if os.name == "nt" else 0,
        )

        if not wait_for_server():
            # Server failed to start -- clean up
            proc.terminate()
            try:
                proc.wait(timeout=5)
            except subprocess.TimeoutExpired:
                proc.kill()
                proc.wait()
            raise RuntimeError(
                f"FastAPI server failed to start within 15s. "
                f"stderr: {proc.stderr.read().decode() if proc.stderr else 'N/A'}"
            )

        return proc

    def stop_server(proc: subprocess.Popen) -> None:
        """Stop FastAPI server reliably on Windows.
        
        Addresses review concern: orphaned processes causing 'Address already in use'.
        Uses taskkill /F /T on Windows for reliable process tree cleanup.
        Falls back to proc.kill() on non-Windows.
        """
        if proc.poll() is not None:
            return  # Already stopped

        if os.name == "nt":
            # Windows: use taskkill for reliable process tree cleanup
            try:
                subprocess.run(
                    ["taskkill", "/F", "/T", "/PID", str(proc.pid)],
                    capture_output=True, timeout=5,
                )
            except (subprocess.TimeoutExpired, FileNotFoundError):
                proc.kill()
        else:
            proc.terminate()
        
        try:
            proc.wait(timeout=5)
        except subprocess.TimeoutExpired:
            proc.kill()
            proc.wait()
    ```

    **Part B: Create tests/test_linkage.py -- Cross-Language Linkage Tests**

    This file addresses FULL-06 (跨语言联动/多流程堆栈验证). Create it with:

    ```python
    """Cross-language probe linkage integration tests (FULL-06).
    
    Verifies:
    - Collector endpoint receives frontend TS probe data -> cp-ts-*.log
    - Python backend probes -> cp-python-*.log with flow_id
    - Same codepoint produces different stacks across flows
    - flow_id correlation between frontend and backend logs
    """
    import json
    import pytest
    import urllib.request
    import urllib.error
    from pathlib import Path

    from conftest import (
        BASE_URL, LOG_DIR, start_server, stop_server,
        PYTHON_TOGGLE, TS_TOGGLE,
    )

    # Exact collector probe payload -- addresses review concern about underspecified payload.
    # This matches what codepoint.ts sends in browser mode via sendToCollector().
    FRONTEND_PROBE_PAYLOAD = {
        "name": "cp-fe-calc-submit",
        "stack": "Error: probe\n"
                 "    at pointWithMeta (codepoint.ts:42:15)\n"
                 "    at Calculator (Calculator.tsx:28:7)\n"
                 "    at renderWithHooks (react-dom.js:16103:18)",
        "meta": {
            "point_id": "cp-fe-calc-submit",
            "flow_id": "flow-api-calculate",
            "expr": "2+3",
        },
    }

    @pytest.mark.integration
    class TestCrossLanguageLinkage:
        """Tests that require a running FastAPI server."""

        @pytest.fixture(autouse=True)
        def _server_lifecycle(self):
            """Start/stop server for each test -- ensures clean state."""
            self.proc = start_server(toggle_python=True, toggle_ts=True)
            yield
            stop_server(self.proc)

        def _post_json(self, path: str, data: dict):
            """Helper: POST JSON and return parsed response."""
            body = json.dumps(data).encode("utf-8")
            req = urllib.request.Request(
                f"{BASE_URL}{path}",
                data=body,
                headers={"Content-Type": "application/json"},
                method="POST",
            )
            with urllib.request.urlopen(req, timeout=5) as resp:
                return json.loads(resp.read().decode("utf-8")), resp.status

        def _get_json(self, path: str):
            """Helper: GET JSON and return parsed response."""
            req = urllib.request.Request(f"{BASE_URL}{path}")
            with urllib.request.urlopen(req, timeout=5) as resp:
                return json.loads(resp.read().decode("utf-8")), resp.status

        def _read_python_log(self) -> str:
            """Read the most recent cp-python log content."""
            logs = sorted(LOG_DIR.glob("cp-python-*.log"))
            assert logs, "No cp-python-*.log found"
            return logs[-1].read_text(encoding="utf-8")

        def _read_ts_log(self) -> str:
            """Read the most recent cp-ts log content."""
            logs = sorted(LOG_DIR.glob("cp-ts-*.log"))
            assert logs, "No cp-ts-*.log found"
            return logs[-1].read_text(encoding="utf-8")

        def test_collector_receives_probe(self):
            """POST to /__codepoint__ returns 204, data appears in cp-ts log."""
            # Send exact frontend probe payload
            body = json.dumps(FRONTEND_PROBE_PAYLOAD).encode("utf-8")
            req = urllib.request.Request(
                f"{BASE_URL}/__codepoint__",
                data=body,
                headers={"Content-Type": "application/json"},
                method="POST",
            )
            with urllib.request.urlopen(req, timeout=5) as resp:
                assert resp.status == 204

            # Verify cp-ts log contains the probe
            ts_log = self._read_ts_log()
            assert "cp-fe-calc-submit" in ts_log, f"Probe name not in ts log. Log content:\n{ts_log}"

        def test_collector_disabled_returns_404(self):
            """When .codepoint-ts is absent, collector returns 404.
            Note: This requires a server restart. Tested in test_toggle.py instead.
            This test verifies collector IS enabled when both toggles exist."""
            # With toggle present, collector should accept
            body = json.dumps(FRONTEND_PROBE_PAYLOAD).encode("utf-8")
            req = urllib.request.Request(
                f"{BASE_URL}/__codepoint__",
                data=body,
                headers={"Content-Type": "application/json"},
                method="POST",
            )
            with urllib.request.urlopen(req, timeout=5) as resp:
                assert resp.status == 204

        def test_multi_flow_stack_differentiation(self):
            """Same codepoint produces different flow_ids across 3 business flows."""
            # Flow 1: API calculate
            resp, status = self._post_json("/api/calculate", {"expression": "2+3"})
            assert status == 200
            assert resp.get("result") == "5.0"

            # Flow 2: History query (record 1)
            resp, status = self._get_json("/api/history/1")
            assert status == 200
            assert "recomputed" in resp

            # Flow 3: Batch
            resp, status = self._post_json("/api/batch", {"expressions": ["1+1", "2*3", "10/2"]})
            assert status == 200
            assert len(resp.get("results", [])) == 3

            # Verify cp-python log has all 3 flow_ids
            python_log = self._read_python_log()
            assert "flow-api-calculate" in python_log
            assert "flow-history-query" in python_log
            assert "flow-batch-process" in python_log

            # Verify same codepoint (cp-calc-compute) appears with different flow_ids
            assert "cp-calc-compute" in python_log
            # Count occurrences of cp-calc-compute with different flow_ids
            compute_lines = [
                line for line in python_log.splitlines()
                if "cp-calc-compute" in line
            ]
            # Should have at least 3 entries (one per flow)
            assert len(compute_lines) >= 3, (
                f"Expected 3+ cp-calc-compute entries, got {len(compute_lines)}.\n"
                f"Log content:\n{python_log}"
            )

        def test_flow_id_correlation(self):
            """Frontend flow_id matches backend flow_id in logs."""
            # Send frontend probe with flow-api-calculate
            body = json.dumps(FRONTEND_PROBE_PAYLOAD).encode("utf-8")
            req = urllib.request.Request(
                f"{BASE_URL}/__codepoint__",
                data=body,
                headers={"Content-Type": "application/json"},
                method="POST",
            )
            urllib.request.urlopen(req, timeout=5)

            # Also trigger backend calculate with same flow_id
            self._post_json("/api/calculate", {"expression": "2+3"})

            # Verify cp-ts log has flow-api-calculate
            ts_log = self._read_ts_log()
            assert "flow-api-calculate" in ts_log, f"flow_id not in ts log.\n{ts_log}"

            # Verify cp-python log also has flow-api-calculate
            python_log = self._read_python_log()
            assert "flow-api-calculate" in python_log
    ```

    **Part C: Run the linkage tests manually to verify**

    Execute the verification steps BEFORE writing the test file, then confirm the test file passes:

    1. Ensure both toggle files exist: `mkdir -p ~/.codepoint && touch ~/.codepoint/.codepoint-python ~/.codepoint/.codepoint-ts`
    2. Clean previous logs: `rm -f ~/.codepoint/pyts-calculator/cp-*.log`
    3. Start server: `cd tmp/pyts-calculator && python main.py &` -- capture PID
    4. Poll for readiness: `for i in $(seq 1 15); do curl -s http://localhost:18091/api/history >/dev/null 2>&1 && break; sleep 1; done`
    5. Test calculate: `curl -s -X POST http://localhost:18091/api/calculate -H "Content-Type: application/json" -d '{"expression":"2+3"}'` -- expect `"result": "5.0"`
    6. Test history: `curl -s http://localhost:18091/api/history/1` -- expect `"recomputed": "5.0"`
    7. Test batch: `curl -s -X POST http://localhost:18091/api/batch -H "Content-Type: application/json" -d '{"expressions":["1+1","2*3","10/2"]}'` -- expect 3 results
    8. Test collector with EXACT payload:
       ```bash
       curl -s -X POST http://localhost:18091/__codepoint__ -H "Content-Type: application/json" -d '{"name":"cp-fe-calc-submit","stack":"Error: probe\n    at pointWithMeta (codepoint.ts:42:15)\n    at Calculator (Calculator.tsx:28:7)","meta":{"point_id":"cp-fe-calc-submit","flow_id":"flow-api-calculate","expr":"2+3"}}'
       ```
       Expect: 204 No Content
    9. Verify cp-python log: `cat ~/.codepoint/pyts-calculator/cp-python-*.log | grep -c "cp-calc-compute"` -- expect 3+
    10. Verify cp-ts log: `cat ~/.codepoint/pyts-calculator/cp-ts-*.log | grep "cp-fe-calc-submit"` -- expect match
    11. Kill server: `taskkill /F /PID {pid}`
    12. Write the test files (conftest.py and test_linkage.py) with the content above
    13. Verify tests can be collected: `cd tmp/pyts-calculator && python -m pytest tests/test_linkage.py --collect-only -q`
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/work-skills/tmp/pyts-calculator" && python -m pytest tests/test_linkage.py --collect-only -q 2>&1 | tail -10 && grep -c "def start_server" tests/conftest.py && grep -c "def wait_for_server" tests/conftest.py && grep -c "def stop_server" tests/conftest.py</automated>
  </verify>
  <done>
    - conftest.py contains start_server(), stop_server(), wait_for_server() with PID tracking and taskkill cleanup
    - test_linkage.py contains test_collector_receives_probe, test_multi_flow_stack_differentiation, test_flow_id_correlation
    - Collector probe payload is exactly specified (name, stack, meta with point_id/flow_id/expr)
    - Server readiness polling polls /api/history until 200 response
    - Windows cleanup uses taskkill /F /T /PID for reliable process tree termination
  </done>
  <acceptance_criteria>
    - tmp/pyts-calculator/tests/conftest.py contains `def wait_for_server(url: str = BASE_URL, timeout: float = 15.0, interval: float = 0.5)`
    - tmp/pyts-calculator/tests/conftest.py contains `def start_server(toggle_python: bool = True, toggle_ts: bool = True) -> subprocess.Popen`
    - tmp/pyts-calculator/tests/conftest.py contains `def stop_server(proc: subprocess.Popen) -> None`
    - tmp/pyts-calculator/tests/conftest.py contains `taskkill`
    - tmp/pyts-calculator/tests/conftest.py contains `CREATE_NEW_PROCESS_GROUP`
    - tmp/pyts-calculator/tests/test_linkage.py contains `FRONTEND_PROBE_PAYLOAD`
    - tmp/pyts-calculator/tests/test_linkage.py contains `"name": "cp-fe-calc-submit"`
    - tmp/pyts-calculator/tests/test_linkage.py contains `"point_id": "cp-fe-calc-submit"`
    - tmp/pyts-calculator/tests/test_linkage.py contains `"flow_id": "flow-api-calculate"`
    - tmp/pyts-calculator/tests/test_linkage.py contains `def test_collector_receives_probe`
    - tmp/pyts-calculator/tests/test_linkage.py contains `def test_multi_flow_stack_differentiation`
    - tmp/pyts-calculator/tests/test_linkage.py contains `def test_flow_id_correlation`
    - tmp/pyts-calculator/tests/test_linkage.py contains `@pytest.mark.integration`
    - `python -m pytest tests/test_linkage.py --collect-only -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Toggle 四组合独立控制验证</name>
  <files>
    tmp/pyts-calculator/tests/test_toggle.py
  </files>
  <read_first>
    tmp/pyts-calculator/tests/conftest.py
    tmp/pyts-calculator/codepoint/__init__.py
    tmp/pyts-calculator/codepoint/collector.py
    tmp/pyts-calculator/api/server.py
    tmp/pyts-calculator/main.py
    .planning/phases/36-pyts-fullstack-calc/36-CONTEXT.md (D-05)
    .planning/phases/36-pyts-fullstack-calc/36-RESEARCH.md (Pattern 3, Pitfall 2)
  </read_first>
  <action>
    This task verifies FULL-07 (Toggle 四组合独立控制). Uses the conftest.py server management fixtures from Task 1 for reliable process lifecycle. Addresses review concerns: Windows process management (HIGH), cleanup on failure (LOW), and test file separation (LOW).

    **Part A: Execute the 4-combination verification manually**

    For each combination, the server is started with specific toggle files set via start_server(toggle_python=..., toggle_ts=...), which handles PID tracking, readiness polling, and cleanup. The try/finally pattern in the test ensures the server is always stopped even if verification fails (addresses review LOW concern).

    Execute these steps manually to confirm behavior before writing tests:

    **Combination 1: Both enabled**
    ```bash
    touch ~/.codepoint/.codepoint-python ~/.codepoint/.codepoint-ts
    rm -f ~/.codepoint/pyts-calculator/cp-*.log
    cd tmp/pyts-calculator && python main.py &
    SERVER_PID=$!
    # Wait for ready
    for i in $(seq 1 15); do curl -s http://localhost:18091/api/history >/dev/null 2>&1 && break; sleep 1; done
    # Run flows
    curl -s -X POST http://localhost:18091/api/calculate -H "Content-Type: application/json" -d '{"expression":"2+3"}'
    curl -s -X POST http://localhost:18091/__codepoint__ -H "Content-Type: application/json" -d '{"name":"cp-fe-test","stack":"Error\n  at test","meta":{"point_id":"cp-fe-test","flow_id":"flow-api-calculate"}}'
    # Kill
    taskkill /F /T /PID $SERVER_PID
    # Verify: BOTH logs exist
    ls ~/.codepoint/pyts-calculator/cp-python-*.log && echo "PASS: python log exists"
    ls ~/.codepoint/pyts-calculator/cp-ts-*.log && echo "PASS: ts log exists"
    ```

    **Combination 2: Only Python enabled**
    ```bash
    touch ~/.codepoint/.codepoint-python && rm -f ~/.codepoint/.codepoint-ts
    rm -f ~/.codepoint/pyts-calculator/cp-*.log
    cd tmp/pyts-calculator && python main.py &
    SERVER_PID=$!
    for i in $(seq 1 15); do curl -s http://localhost:18091/api/history >/dev/null 2>&1 && break; sleep 1; done
    curl -s -X POST http://localhost:18091/api/calculate -H "Content-Type: application/json" -d '{"expression":"3+4"}'
    curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:18091/__codepoint__ -H "Content-Type: application/json" -d '{"name":"cp-fe-test","stack":"Error"}'
    # Should print 404
    taskkill /F /T /PID $SERVER_PID
    # Verify: ONLY python log exists
    ls ~/.codepoint/pyts-calculator/cp-python-*.log && echo "PASS: python log exists"
    ! ls ~/.codepoint/pyts-calculator/cp-ts-*.log 2>/dev/null && echo "PASS: no ts log"
    ```

    **Combination 3: Only TS enabled**
    ```bash
    rm -f ~/.codepoint/.codepoint-python && touch ~/.codepoint/.codepoint-ts
    rm -f ~/.codepoint/pyts-calculator/cp-*.log
    cd tmp/pyts-calculator && python main.py &
    SERVER_PID=$!
    for i in $(seq 1 15); do curl -s http://localhost:18091/api/history >/dev/null 2>&1 && break; sleep 1; done
    curl -s -X POST http://localhost:18091/api/calculate -H "Content-Type: application/json" -d '{"expression":"5+5"}'
    curl -s -X POST http://localhost:18091/__codepoint__ -H "Content-Type: application/json" -d '{"name":"cp-fe-test","stack":"Error\n  at test","meta":{"point_id":"cp-fe-test","flow_id":"flow-test"}}'
    taskkill /F /T /PID $SERVER_PID
    # Verify: ONLY ts log exists
    ! ls ~/.codepoint/pyts-calculator/cp-python-*.log 2>/dev/null && echo "PASS: no python log"
    ls ~/.codepoint/pyts-calculator/cp-ts-*.log && echo "PASS: ts log exists"
    ```

    **Combination 4: Both disabled**
    ```bash
    rm -f ~/.codepoint/.codepoint-python ~/.codepoint/.codepoint-ts
    rm -f ~/.codepoint/pyts-calculator/cp-*.log
    cd tmp/pyts-calculator && python main.py &
    SERVER_PID=$!
    for i in $(seq 1 15); do curl -s http://localhost:18091/api/history >/dev/null 2>&1 && break; sleep 1; done
    curl -s -X POST http://localhost:18091/api/calculate -H "Content-Type: application/json" -d '{"expression":"7+8"}'
    curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:18091/__codepoint__ -H "Content-Type: application/json" -d '{"name":"cp-fe-test","stack":"Error"}'
    # Should print 404
    taskkill /F /T /PID $SERVER_PID
    # Verify: NO logs exist
    ! ls ~/.codepoint/pyts-calculator/cp-python-*.log 2>/dev/null && echo "PASS: no python log"
    ! ls ~/.codepoint/pyts-calculator/cp-ts-*.log 2>/dev/null && echo "PASS: no ts log"
    ```

    **Restore toggles after all combinations:**
    ```bash
    touch ~/.codepoint/.codepoint-python ~/.codepoint/.codepoint-ts
    ```

    **Part B: Create tests/test_toggle.py**

    Write the toggle verification as a pytest test file using conftest.py fixtures:

    ```python
    """Toggle independence verification tests (FULL-07).
    
    Verifies the 2x2 toggle combination matrix:
    - .codepoint-python controls cp-python-*.log output
    - .codepoint-ts controls cp-ts-*.log output (via collector)
    - Each toggle is independent: changing one does not affect the other
    - Toggle requires server restart (module-level toggle check at import time)
    
    Addresses review concerns:
    - Windows process management: uses conftest.py start_server/stop_server with PID tracking
    - Cleanup on failure: try/finally in each combination ensures server is always stopped
    - Test file separation: dedicated file, not mixed with linkage tests
    """
    import json
    import pytest
    import urllib.request
    import urllib.error
    from pathlib import Path

    from conftest import (
        BASE_URL, LOG_DIR, start_server, stop_server,
        PYTHON_TOGGLE, TS_TOGGLE,
    )

    # Collector probe payload for toggle testing
    TOGGLE_TEST_PROBE = {
        "name": "cp-fe-toggle-test",
        "stack": "Error: toggle-test\n    at test_toggle",
        "meta": {"point_id": "cp-fe-toggle-test", "flow_id": "flow-toggle-test"},
    }


    @pytest.mark.integration
    class TestToggleCombinations:
        """Test all 4 toggle combinations with reliable server lifecycle."""

        def _run_calculate(self):
            """Trigger a backend calculate call."""
            body = json.dumps({"expression": "1+1"}).encode("utf-8")
            req = urllib.request.Request(
                f"{BASE_URL}/api/calculate",
                data=body,
                headers={"Content-Type": "application/json"},
                method="POST",
            )
            with urllib.request.urlopen(req, timeout=5) as resp:
                return json.loads(resp.read().decode("utf-8")), resp.status

        def _run_collector_probe(self) -> int:
            """POST a frontend probe to collector, return HTTP status."""
            body = json.dumps(TOGGLE_TEST_PROBE).encode("utf-8")
            req = urllib.request.Request(
                f"{BASE_URL}/__codepoint__",
                data=body,
                headers={"Content-Type": "application/json"},
                method="POST",
            )
            try:
                with urllib.request.urlopen(req, timeout=5) as resp:
                    return resp.status
            except urllib.error.HTTPError as e:
                return e.code

        def _has_python_log(self) -> bool:
            return bool(list(LOG_DIR.glob("cp-python-*.log")))

        def _has_ts_log(self) -> bool:
            return bool(list(LOG_DIR.glob("cp-ts-*.log")))

        def test_combination_1_both_enabled(self):
            """Both toggles on -> cp-python and cp-ts logs both present."""
            proc = None
            try:
                proc = start_server(toggle_python=True, toggle_ts=True)
                self._run_calculate()
                self._run_collector_probe()
            finally:
                stop_server(proc)

            assert self._has_python_log(), "Expected cp-python-*.log when .codepoint-python exists"
            assert self._has_ts_log(), "Expected cp-ts-*.log when .codepoint-ts exists"

        def test_combination_2_only_python(self):
            """Only .codepoint-python -> only cp-python log, no cp-ts."""
            proc = None
            try:
                proc = start_server(toggle_python=True, toggle_ts=False)
                self._run_calculate()
                status = self._run_collector_probe()
                assert status == 404, f"Expected 404 from disabled collector, got {status}"
            finally:
                stop_server(proc)

            assert self._has_python_log(), "Expected cp-python-*.log when .codepoint-python exists"
            assert not self._has_ts_log(), "Expected NO cp-ts-*.log when .codepoint-ts absent"

        def test_combination_3_only_ts(self):
            """Only .codepoint-ts -> only cp-ts log, no cp-python."""
            proc = None
            try:
                proc = start_server(toggle_python=False, toggle_ts=True)
                self._run_calculate()
                self._run_collector_probe()
            finally:
                stop_server(proc)

            assert not self._has_python_log(), "Expected NO cp-python-*.log when .codepoint-python absent"
            assert self._has_ts_log(), "Expected cp-ts-*.log when .codepoint-ts exists"

        def test_combination_4_both_disabled(self):
            """Neither toggle -> no logs at all."""
            proc = None
            try:
                proc = start_server(toggle_python=False, toggle_ts=False)
                self._run_calculate()
                status = self._run_collector_probe()
                assert status == 404, f"Expected 404 from disabled collector, got {status}"
            finally:
                stop_server(proc)

            assert not self._has_python_log(), "Expected NO cp-python-*.log when both toggles absent"
            assert not self._has_ts_log(), "Expected NO cp-ts-*.log when both toggles absent"
    ```

    **Part C: Verify test collection**

    Run: `cd tmp/pyts-calculator && python -m pytest tests/test_toggle.py --collect-only -q`
    All 4 test methods should be collected.
  </action>
  <verify>
    <automated>cd "C:/WorkSpace/agent/work-skills/tmp/pyts-calculator" && python -m pytest tests/test_toggle.py --collect-only -q 2>&1 | tail -10 && echo "---" && grep -c "test_combination" tests/test_toggle.py && ls ~/.codepoint/.codepoint-python ~/.codepoint/.codepoint-ts 2>&1</automated>
  </verify>
  <done>
    - test_toggle.py 包含 4 个测试方法（test_combination_1_both_enabled, test_combination_2_only_python, test_combination_3_only_ts, test_combination_4_both_disabled）
    - 每个组合使用 start_server/stop_server 管理，包含 try/finally 确保 cleanup
    - 四组合全部验证通过：
      - Both on: cp-python + cp-ts 都有输出
      - Only Python: 只有 cp-python，无 cp-ts
      - Only TS: 只有 cp-ts，无 cp-python
      - Both off: 无任何日志输出
    - 两个 toggle 文件互不干扰
    - 验证完成后恢复两个 toggle 文件
  </done>
  <acceptance_criteria>
    - tmp/pyts-calculator/tests/test_toggle.py contains `def test_combination_1_both_enabled`
    - tmp/pyts-calculator/tests/test_toggle.py contains `def test_combination_2_only_python`
    - tmp/pyts-calculator/tests/test_toggle.py contains `def test_combination_3_only_ts`
    - tmp/pyts-calculator/tests/test_toggle.py contains `def test_combination_4_both_disabled`
    - tmp/pyts-calculator/tests/test_toggle.py contains `try:` and `finally:` and `stop_server(proc)`
    - tmp/pyts-calculator/tests/test_toggle.py contains `start_server(toggle_python=` for each combination
    - tmp/pyts-calculator/tests/test_toggle.py contains `@pytest.mark.integration`
    - tmp/pyts-calculator/tests/test_toggle.py does NOT contain a monolithic test_integration.py reference
    - ~/.codepoint/.codepoint-python exists (restored after test)
    - ~/.codepoint/.codepoint-ts exists (restored after test)
    - `python -m pytest tests/test_toggle.py --collect-only -q` exits 0 with 4 tests collected
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toggle filesystem -> Python process | File existence check at module load time |
| Toggle filesystem -> Collector | File existence check at module load time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | Toggle file manipulation | accept | Test project, local filesystem only |
</threat_model>

<verification>
1. test_linkage.py: collector 接收前端探针并写入 cp-ts-*.log
2. test_linkage.py: 多流程堆栈验证 -- cp-calc-compute 在 3 个 flow_id 下有不同调用栈
3. test_toggle.py: 四组合全部验证通过（4 个独立测试方法）
4. conftest.py: 服务器管理可靠 -- PID 跟踪、readiness 轮询、taskkill 清理
5. 所有 toggle 文件恢复到 "both on" 状态
6. 无孤儿进程占用端口 18091
</verification>

<success_criteria>
- 跨语言联动：cp-python-*.log 和 cp-ts-*.log 都存在且包含正确的 flow_id
- 多流程堆栈：同一代码点 cp-calc-compute 在三个不同 flow_id 的调用中可区分
- Toggle 四组合：4 个独立测试方法，每个使用 try/finally 确保清理
- 服务器管理：conftest.py 提供 PID 跟踪、readiness 轮询、Windows taskkill 清理
- 测试文件分离：test_linkage.py 处理 FULL-06，test_toggle.py 处理 FULL-07
- flow_id 关联：前端设置的 flow_id 在后端日志中可见
</success_criteria>

<output>
After completion, create `.planning/phases/36-pyts-fullstack-calc/36-02-SUMMARY.md`
</output>
